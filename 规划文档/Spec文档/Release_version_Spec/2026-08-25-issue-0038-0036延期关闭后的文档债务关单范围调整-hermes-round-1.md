# Hermes Document Review — Round 1/3

## Metadata
- Document: `2026-08-25-issue-0038-0036延期关闭后的文档债务关单范围调整-spec-addendum.md` (sanitized copy)
- Document type: `ISSUE_SCOPE_ADJUSTMENT_ADDENDUM` (ISSUE-0038)
- Review round: 1 / 3
- Invocation model: `deepseek-v4-pro`
- Review scope: read-only, single sanitized copy only (no sibling files present; external hashes asserted, not verifiable in this sandbox)
- Date: 2026-08-25

## Verdict
**REWORK_REQUIRED** — 2 serious findings (evidence-binding / acceptance-testability gaps). No style-only reopens; fixes below are confined to the serious items and their direct regressions.

## Serious Findings

### S-1 — Sole evidence hash for all 7 B-items is not enumerated in the dependency table (unbound reference)
- Severity: SERIOUS
- Location: Section 3, matrix rows N-001 / N-005 / N-007 / NS-001 / NS-002 / NS-004 / NS-005 (lines 63, 65, 67, 69) vs Section 1 dependency table (lines 24–30)
- Evidence: All seven B rows cite the same source as `既有 0036 Spec \`005EA5F2490DC2E43A134BA0421EFBD357179C90E29A6F2AB560F6F61A97B437\``. That document is the original ISSUE-0036 spec, but the Section 1 dependency table lists only: 0038 原 V6 Spec (`7248241D…`), 0038 Hermes R1/R2/R3, ISSUE-0044 (`C0C2A36D…`), V5 product ruling (`FA56F5D1…`), and the 0036 close canonical (`4243F742…`). The `005EA5F2…` hash is a distinct document from both the 0038 spec and the 0036 close canonical, and it has **no path binding and no table entry**.
- Impact: The evidence source underpinning 7 of 13 disposition items cannot be located or independently re-read. This directly defeats the document's own stated purpose ("精确路径/hash/段落的既有文档引用", line 85) and triggers its own `REVIEW_BLOCKED` condition ("引用路径/hash 漂移", line 110), and makes the "证据索引完整" gate (line 88) unsatisfiable as written.
- Correction: Add a dependency-table row for the original ISSUE-0036 spec (exact file path + SHA-256 `005EA5F2…`), or — if it is intended to be one of the already-listed documents — correct the hash/title so the reference resolves. Freeze and re-hash the addendum after the fix.

### S-2 — B-item evidence lacks the precise path/段落 references the document itself requires; "证据索引" is never enumerated
- Severity: SERIOUS
- Location: Section 4.1 (line 85) vs Section 3 matrix evidence column (lines 63–69); also line 88
- Evidence: Section 4.1 states the B=7 adoption gate requires "精确路径/hash/段落的既有文档引用". The matrix provides only a hash plus a prose description (e.g. "高风险自动拒绝边界与合成 golden-case 文档引用"), with no file path and no paragraph/section pointer. Separately, line 88 gates closure on "证据索引完整", but no "证据索引" section or enumeration exists anywhere in the document — the closest artifact is the Section 1 table, which is labeled "依赖证据" and is itself incomplete per S-1.
- Impact: The ISSUE 管理员 cannot perform the required "独立复读" (line 43, 88) against a specific paragraph; the acceptance criterion cannot be verified, so the B-item adoption and the ISSUE-0038 closure gate are not testable as written.
- Correction: For each of the 7 B rows, add an exact file path + paragraph/section/line reference to the cited 0036 Spec. Either enumerate a real "证据索引" section (with path + hash per item) or reword line 88 to reference the Section 1 dependency table.

## Non-Serious Findings

- N-1: "B 不要求业务确认" (lines 40, 85) vs "在业务方确认本附录…前，ISSUE-0038 保持 open" (line 88). Since the addendum contains the B dispositions, business confirmation of the addendum appears to cover B content indirectly. Clarify who confirms what and in what order. (Also recorded as contradiction C-1.)
- N-2: Combined disposition token `N/A_FOR_CURRENT_CLOSURE + TRANSFER_EXISTING_TRACKER` (lines 71–75) is used in the matrix but not defined in Section 3's disposition list (lines 54–59, which defines four single tokens). Define the composite token or split into two columns.
- N-3: Section 4.1 line 86 enumerates C/D dispositions as only `V5_BOUNDED_SUFFICIENT` or `N/A_FOR_CURRENT_CLOSURE`, omitting the `TRANSFER_EXISTING_TRACKER` label actually used in the matrix. Minor enumeration mismatch.
- N-4: Owner labels ("0038 Spec owner", "0038/V5 owner", "0038 owner/ISSUE-0044", lines 63–75) are not mapped to the single-write owner (产品经理 Agent v2.3.2, line 8) or to concrete identities. Confirm role mapping.
- N-5: "N-08" (line 28) is an unexplained cross-reference to ISSUE-0044's own numbering and is unverifiable within this document; confirm it is not a typo.

## Contradictions
- C-1 (= N-1): Tension between "B 不要求业务确认" and "业务方确认本附录" as a closure precondition. Not fatal, but ambiguous for the admin gate.

## Missing Acceptance Criteria
1. Exit/PASS criteria for this addendum's own review beyond "no SERIOUS" (line 118) — e.g. what marks the addendum `DOCUMENT_GATE_PASSED`.
2. An enumerated "证据索引" (referenced at line 88 but never provided).
3. Precise path + 段落 references per B item (required by line 85; see S-2).
4. Binding of the `005EA5F2…` hash to a path/canonical (see S-1).
5. Definition of the composite disposition token (see N-2).

## Remediation Checklist
- [ ] Add original ISSUE-0036 Spec (`005EA5F2…`) to Section 1 with exact path + SHA-256 (S-1).
- [ ] Add file path + paragraph/section refs for all 7 B rows (S-2).
- [ ] Enumerate a real "证据索引" or reword line 88 to point at Section 1 (S-2).
- [ ] Clarify "业务方确认本附录" vs "B 不要求业务确认" scope/order (C-1/N-1).
- [ ] Define composite disposition token or split columns (N-2).
- [ ] Align line 86 disposition enumeration with the matrix (N-3).
- [ ] Freeze bytes and re-run Round 2 with model `deepseek-v4-pro`.

## Open-Issue List
- OI-1: Confirm `005EA5F2…` = original ISSUE-0036 spec; supply path (S-1).
- OI-2: Supply paragraph-level references for the 7 B items (S-2).
- OI-3: Define/enumerate "证据索引" (S-2).
- OI-4: Resolve "N-08" cross-reference provenance (N-5).

Round 1 complete. Two serious findings remain; Document QA should receive this unchanged per the addendum's own escalation rule (line 118).