# V5 / ISSUE-0036 产品验收与生产接线准备裁决

日期：2026-08-24  
角色：产品经理 Agent v2.3.2 / `019fefa7-9883-7af2-bdb5-acc5c8513781`  
项目 workflow：`WORKFLOW_ACTIVE`  
Issue：`ISSUE-0036`，仍 open；本报告不修改 Issue 状态。

## 1. 结论

`BOUNDED_PRODUCT_ACCEPTANCE=PRODUCT_ACCEPTANCE_PASS`

`DEPLOYMENT=ALLOWED_FLAG_OFF_ONLY`

`PRODUCTION_FLAG_ON_GATE=BLOCKED`

结论范围仅覆盖确定提交中的 ISSUE-0036 bounded 产品/代码/UI 合同，不代表生产通过、业务最终验收或 Issue 关闭。可以由平台/运维责任方部署候选提交，但 `CONTACT_REVIEW_ENABLED` 必须保持 `false`。当前不具备打开 flag 的最小生产证据条件。

## 2. 只读证据

- branch：`V5-issue-0036-contact-review-closure`
- commit：`f8ad5d009c5483d6791699d2c2394765a23fb2f2`
- parent：`03da0015be0d2ee403d848f149814039759cfcd1`
- tree：`19b903a8a4e6e2ece653c2c175cbcbbdfadae352`
- 提交范围：32 files；用户提供的独立代码复核 `TECH_REVIEW_PASS`（P0/P1/P2=`0/0/0`）、`UI_REVIEW_PASS`（P0/P1/P2=`0/0/0`）、full `659 passed / 1 existing skipped`、build `18/18`。
- 运维说明：`Code文档/docs/2026-08-23-v5-issue-0036-s3-local-operations.md`。其状态明确为仅本地实现与合成验证，不是平台配置、部署、生产启用、回滚演练、产品验收或 Issue 关闭证明。
- 提交内定向复审 `Code文档/docs/2026-08-24-v5-issue-0036-s3-targeted-rereview-3.md` 的结论为 `TECH_REVIEW_PASS`；代码/UI门与生产门分层保留。
- `.env.example` 与运维说明均显示 `CONTACT_REVIEW_ENABLED=false`、`CONTACT_REVIEW_SCHEMA_READY=false` 的默认关闭边界，真实 Secret/账号引用只能进入平台配置/Secret 存储。
- 本次仅做静态读取，未运行 npm、测试、构建、Git mutation、部署或平台操作。

## 3. 精确生产接线配置清单（不含 Secret 值）

### 3.1 必需应用配置

| 配置 | 当前安全值/要求 | flag-on 门 |
| --- | --- | --- |
| `CONTACT_REVIEW_ENABLED` | `false` | 仅在所有下游证据通过后才可改为 `true` |
| `CONTACT_REVIEW_SCHEMA_READY` | `false` | 只有真实 schema/index/权限/transaction/preflight 证据齐全才可为 `true` |
| `CONTACT_REVIEW_KEY_SECRET` | 平台 Secret 存储中的非空值；不入仓库、日志或聊天 | 必须完成 Secret 注入和轮换责任确认 |
| `CONTACT_REVIEW_PRIMARY_REVIEWER_REFS` | 受控 primary 账号引用 | 必须真实绑定且不能与 owner/其他不当角色重叠 |
| `CONTACT_REVIEW_BACKUP_REVIEWER_REFS` | 受控 backup 账号引用 | 必须真实绑定、具备接管权限 |
| `CONTACT_REVIEW_SECOND_REVIEWER_REFS` | 受控 second-review 账号引用 | 必须真实绑定，且与 triage/owner 分离 |
| `CLOUDBASE_ENV_ID` | 生产 CloudBase 环境引用 | 需平台 owner 证明目标环境和权限 |

不得启用 AI/OCR/provider/额外出域；联系方式默认隐藏，未知和故障转人工。`APP_ENV=production`、真实环境引用和所有真实 Secret 只能在平台配置中核验，不写入本报告。

### 3.2 Collection、索引和持久化门

必须在目标 CloudBase 环境中核验以下 collection、唯一约束、权限和事务能力：

- `contact_review_tasks`
- `contact_review_entity_versions`
- `contact_review_audit_events`

冻结的索引合同至少包括：

- `tasks_owner_entity_version_field_unique`
- `tasks_idempotency_unique`
- `tasks_status_dueAt`
- `tasks_content_rule_version_field`
- `entity_versions_owner_entity_version_unique`
- `entity_versions_owner_entity_pending_unique`
- `audit_task_occurredAt`
- `audit_owner_entity_version_time`

必须证明：字段 task/实体 aggregate 双向引用、N/N 公开门、CAS revision、幂等 receipt、审计追加、30 天 task 清理、180 天 audit 清理、legal hold 和失败回滚均在真实持久层成立。仅有变量名称、代码测试或本地合成 repository 不足以通过 flag-on 门。

### 3.3 健康、角色与观察

- 已登记 reviewer 才能读取 `/api/contact-review?scope=health`。
- reviewer 队列使用 `/api/contact-review?scope=queue`；owner 和未登记账号必须 403。
- health 必须核验 repository 可达性、队列/人工/超时数量、audit 数量、aggregate 完整性失败数和公开指针失败数；不返回正文、联系方式、Secret、token 或账号清单。
- `publicPointerFailures` 非零、配置缺失、repository/audit/transaction 不可用时，必须 503、关闭 flag、保持 hidden 或旧 approved snapshot。
- 必须提供监控告警 owner、备援、工作时段/SLA、停止条件、观察窗口和平台外运维事件记录方式。

## 4. 部署与回滚裁决

当前允许的最小动作是：平台/运维 owner 以该确定提交部署候选版本，保持 `CONTACT_REVIEW_ENABLED=false`，执行 flag-off smoke 和配置缺失 503 验证。此动作不等于生产启用。

flag-on 前置条件全部满足前，不得将 `CONTACT_REVIEW_ENABLED` 改为 `true`。生产启用还需预生产验证、小范围灰度和目标观察窗口；任何跨账号泄露、owner 自审、second/triage/owner 重叠、错误公开、双 active、审计缺失、queue 不可用、schema/index/transaction 失败或公开指针异常，立即停止并关闭 flag。

回滚顺序：关闭 flag、停止新的审核副作用、保留 task/aggregate/audit、核对公共端仍 hidden 或完整旧 approved snapshot，再使用部署平台记录的上一稳定 revision 回滚。不得使用本地 commit、分支名或本报告猜测生产 revision；真实反向回滚演练尚未证明。

## 5. 最小生产验证矩阵

| 层级 | 最小验证 | 当前状态 |
| --- | --- | --- |
| flag-off 部署 | 页面/列表/详情 smoke、配置缺失 503、无自动公开、旧路径无回归 | 可作为部署后首步；需平台实证 |
| 平台接线 | 环境、collection、索引、权限、事务、Secret 注入、reviewer refs | 未证明，阻塞 flag-on |
| reviewer 权限 | primary/backup/second 分离；owner 自审拒绝；非法账号 403 | 代码有本地证据，生产账号未证明 |
| 公开完整性 | N/N 才公开；missing/duplicate/wrong ref/digest drift hidden/503；approved snapshot 同源 | 代码/合成证据通过，生产持久层未证明 |
| 申诉 | 一版本一次申诉；triage 与 second 不同账号；错误向量 fail-closed；逐字段终审 | 代码/UI门通过，生产角色与审计未证明 |
| 幂等 | 同 key 同 payload 延迟重放首次结果；异 payload 409；零新增 revision/audit/receipt | 代码/合成证据通过，生产持久层未证明 |
| 生命周期 | create/edit/delete/restore、旧快照保持、恢复全量重审、并发冲突 | 代码/合成证据通过，生产事务未证明 |
| health/监控 | health 受控访问、pointer failure、队列/审计/超时指标、告警 owner | 端点合同明确，生产监控未证明 |
| 观察/回滚 | 预生产、小灰度、目标观察窗口、停止条件、上一稳定 revision、回滚演练 | 未执行，阻塞 flag-on |
| 产品/业务验收 | 用户文案、联系方式默认隐藏、人工 SLA/申诉路径、残余风险接受 | 本裁决仅通过 bounded scope，不是最终业务验收 |

## 6. 仍需外部提供或核验的信息

能由代码/提交核对的配置名称已核对；仍需平台、运维或业务责任方提供可复核证据：

1. 生产 CloudBase 环境 ID、三类 collection 的实际存在性、唯一索引、权限、事务和清理任务证据。
2. primary、backup、second-review 真实受控账号引用、权限清单、owner 分离、备援和审计身份。
3. `CONTACT_REVIEW_KEY_SECRET` 的平台 Secret 注入、轮换责任和泄露处理流程；不得通过聊天传值。
4. 监控/告警 owner、备援、工作时段/SLA、停止阈值和健康端点访问授权。
5. 部署批次、预生产/灰度安排、观察窗口、生产版本标识及平台记录的上一稳定 revision。
6. 真实回滚入口与回滚演练证据 owner。

无需再向业务方索取 AI/OCR/provider、额外出域、ISSUE-0031、支付或首次访问 503 信息；这些已明确排除。

## 7. 当前门禁与唯一下一步

`PRODUCT_ACCEPTANCE_PASS` 仅适用于 ISSUE-0036 bounded 产品/代码/UI交付。`PRODUCTION_GATE_BLOCKED_FOR_FLAG_ON`；可部署但必须默认 flag off。

ISSUE-0036 仍未关闭，项目 workflow 仍 `WORKFLOW_ACTIVE`。本报告不修改 Spec、Issue、代码、UI、平台或 worktree，不新增/重开 Issue。

唯一下一步：项目总负责人向既有平台/运维责任方路由 bounded production-preparation packet，先核验上述平台配置、人工角色、health、观察和回滚证据并保持 flag off；证据齐全后再进入 flag-on 生产验证与产品/业务最终验收。
