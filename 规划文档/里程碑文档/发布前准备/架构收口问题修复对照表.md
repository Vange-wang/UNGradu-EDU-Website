# 架构收口问题修复对照表

日期：2026-06-24
阶段：MVP 发布前准备

| 编号 | 等级 | 问题 | 当前处理 | 测试覆盖 | 托管复验状态 |
| --- | --- | --- | --- | --- | --- |
| 10.1 | P1 | 聊天会话查询全集合扫描与旧会话索引兼容 | 已改为写入 `conversationUniqKey`、`participantKeys`、`sourceKey`，创建复用和列表读取均走条件查询；旧会话重复发起时按 `sourceId`、`sourceType`、双方 `participantUserIds` 兼容复用并补写索引；已在测试 CloudBase 环境执行 `npm run m5:backfill:conversations`，脱敏结果为 Scanned 313 / Updated 235 / Already indexed 78 / Skipped 0；会话写入路径核对确认普通会话创建只由 `server/conversations.ts` 写入，且新建路径统一写入三类索引字段 | `tests/conversation-server.test.ts` 覆盖大量历史会话、重复创建复用、旧会话缺索引复用和迁移后列表可见；全量 `npm test` 通过 115 个测试；`node --check scripts/backfill-conversation-indexes.mjs` 通过 | 已通过。`M5_BASE_URL=https://ungradu-edu-m5-test-273732-8-1445807473.sh.run.tcloudbase.com npm run m5:hosted:verify`：flow 15 请求成功率 100.00%，load 375 请求成功率 100.00%，错误率 0.00%。 |
| 10.2 | P1 | 登录 `next` 参数未校验 | 已新增 `sanitizeNextPath()`，仅允许站内相对路径 | `tests/auth.test.ts` 覆盖站内路径、外部 URL、协议相对 URL、空值 | 已通过。`m5:hosted:verify` flow/load 成功率 100.00%，错误率 0.00%。 |
| 10.3 | P1 | 反馈入口不可执行 | 已将反馈页改为可点击 GitHub issue 提交入口，并保留 3 个工作日人工查看说明；页面文案已统一为“反馈提交页”口径 | Chrome headless DOM 冒烟确认 `/feedback` 可见“反馈提交页” | 已通过。`m5:hosted:verify` flow/load 成功率 100.00%，错误率 0.00%。 |
| 10.4 | P2 | Cookie 缺少服务端硬过期校验 | 已在读取签名 Cookie 时校验 `createdAt`，拒绝过期、非法和未来时间 | `tests/auth-session-api.test.ts` 覆盖有效、过期、非法和未来时间 | 已通过。`m5:hosted:verify` flow/load 成功率 100.00%，错误率 0.00%。 |
| 10.5 | P2 | API client 非 JSON 响应会抛错 | 已抽取 `features/api/api-client.ts` 统一解析非 JSON、错误状态和异常响应 | `tests/api-client.test.ts` 覆盖 HTML 和 500 JSON 错误 | 已通过。`m5:hosted:verify` flow/load 成功率 100.00%，错误率 0.00%。 |
| 10.6 | P2 | 缺少可见登出入口 | 已在个人页增加“退出登录”按钮，调用 `/api/auth/logout` 后回首页 | Chrome headless DOM 冒烟确认 `/profile` 可见“退出登录”，且 `/api/auth/logout` 返回 200 | 已通过。`m5:hosted:verify` flow/load 成功率 100.00%，错误率 0.00%。 |
| 10.7 | P2 | 证明图片能力边界不清 | 发布页和详情页均明确当前仅保存元信息，不提供正式上传、查看或审核 | 页面级代码审查；产品说明同步说明 | 已通过。`m5:hosted:verify` flow/load 成功率 100.00%，错误率 0.00%。 |

## 本次提交信息

本表中的提交 SHA 待本阶段变更提交后补充。提交前不得写入真实密钥或生产环境真实变量值。

## 补充说明

- backfill 统计解释：`Scanned 313` 为本次扫描到的会话总数；`Updated 235` 为缺少或不一致索引字段并已补写的旧会话；`Already indexed 78` 为扫描时已经具备 `conversationUniqKey`、`participantKeys`、`sourceKey` 且无需更新的会话；`Skipped 0` 表示没有因缺少 doc id、来源字段、参与者字段或来源类型异常而跳过的脏数据。本次未记录真实会话内容。
- 第二次验收中提到的同一托管地址前后结果差异，操作记录为：用户于 2026-06-24 晚间在 CloudBase Run 控制台重新保存测试环境变量，并对原 M5 测试托管服务 `ungradu-edu-m5-test` 执行重新部署/恢复；未将控制台日志或版本截图纳入仓库。重新部署/恢复后，开发员在同一托管地址执行 `m5:hosted:verify`，flow/load 均 100.00% 成功。
- 根目录未跟踪 `nul` 条目已通过根 `.gitignore` 的 `/nul` 规则排除，不再作为本阶段提交候选。
