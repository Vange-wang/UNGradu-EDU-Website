已完成 Round 2/3 独立只读审查。以下为报告。

# 审查报告 — ISSUE-0032 邮箱人机验证关闭 Spec（Round 2/3）

## 元数据
- 审查轮次：2 / 3
- 调用模型：deepseek-v4-pro（按要求）
- 审查对象：净化副本 `tmpbfwmzj.tmp`（35,408 字节，366 行），含 Round 2 指令、当前 canonical 候选（第 5–195 行）、内嵌 Round 1 报告、内嵌 Document QA 整改记录
- 审查性质：独立只读；不编辑文件、不背书用户已批准、不推断实现/部署/Issue 关闭
- 本轮范围：仅核验 Round 1 SERIOUS 发现 S1、S2 的修复到位情况，及其修复直接引起的回归；不重开 NON-SERIOUS、风格或纯措辞项
- 前置限制：本副本无法独立核验外部事实（源文件 SHA-256、Issue canonical 状态、整改记录中第 350 行声明的修订后 hash `F7939E3B…`）。这些作为审查前提保留，不判为文档缺陷

## 结论
**PASS_WITH_NONBLOCKING_OPEN_ISSUES**

Round 1 的两项 SERIOUS 发现（S1、S2）均已修复到位，复核通过；未发现修复引入的严重回归。其余未决项均为 NON-SERIOUS 或已单独跟踪项，不在本轮范围，不构成阻断。

## SERIOUS 发现（本轮核验结果）

### S1 — 第 9 节「可测试验收标准」名不符实 → 已修复 ✓
- 原问题：第 9 节标题自称「可测试」，但 token_ttl、verify_timeout、host/action 白名单、限流阈值等参数在 2.3/10 节明确「另行登记」「未决」，验收断言无法执行。
- 修复证据：
  - 第 163 行标题已改为「## 9. 验收骨架（参数待冻结）」——不再声称当前可测试；
  - 第 165 行新增门禁：「存在完整且经批准的 `V4_PARAMETER_RECEIPT`…缺失或冲突时本节不可执行，也不得给阶段 PASS」；
  - 第 127 行定义 `V4_PARAMETER_RECEIPT` 内容（token_ttl、verify_timeout、expected_hostnames/actions、消费标记保留/清理、组合限流阈值/窗口/顺序，及环境/边界值/provider 映射/owner/批准人/hash/冻结时间）与冻结时序（provider-neutral 参数须在 V4-S1 测试前冻结，provider-specific 参数在 V4-S2/S3 前冻结）；
  - 第 153 行（8.1 停止条件）加入「`V4_PARAMETER_RECEIPT` 缺失/冲突」为立即停止触发。
- 判定：标题/内容矛盾已消除，参数冻结门禁在第 6、8.1、9 节三处一致落地，无虚设 gate。S1 关闭。

### S2 — 「批准的无障碍路径」硬性引用未定义产物 → 已修复 ✓
- 原问题：2.1 目标 5、3.2 失败流程、第 7 节把「已批准无障碍路径」当硬要求，但 10 节声明其未决、未定义。
- 修复证据：
  - 第 48 行（2.1 目标 5）改为「经业务方批准并冻结后方可使用的无障碍替代路径当前未定义，保持 USER_CONFIRMATION_PENDING」；
  - 第 86 行（3.2）改为「若后续批准无障碍替代路径…在批准 receipt 出现前，不得把替代路径描述为可用」；
  - 第 129 行（第 6 节）明确「无障碍替代路径不属于已批准硬要求」，并指定产出链路：产品经理 V4-S0 提出候选 → 业务方精确 receipt 批准 → 实现 owner V4-S1 实现 → 独立产品/UI/安全复核证明仍走 verify → consume → limit → send 链；
  - 第 146 行（第 7 节）改为「当前未批准替代路径时，只验证常规挑战的适用基础无障碍要求，不得虚构替代方案」；
  - 第 170 行（9 节第 6 条）改为「未批准时明确 N/A/PENDING 而非通过」；
  - 第 153 行（8.1）停止条件覆盖「已批准无障碍路径绕过、未批准路径被当作可用」。
- 判定：对不存在产物的硬性引用已全部转为条件式/待定表述，产出 owner 与阶段归属已明确，与第 10 节未决清单不再冲突。S2 关闭。

### 回归检查 — 无严重回归
核验受影响段落（§2.1 / §3.2 / §6 及矩阵 / §7 / §9 / §10）：
- 第 9 节第 1 条门禁与第 6 节冻结时序一致（先冻结 receipt，再执行 9 节第 2–5 条边界断言），无鸡生蛋悖论；
- 无障碍路径在第 6 节矩阵（V4-S0「待决候选」→ V4-S1「仅实现已批准路径」）、第 9 节第 6 条、第 10 节「已通过无障碍」禁令之间表述一致，无新矛盾；
- 未发现因修复而删除必要门禁、引入新的不可执行断言或内部冲突。

## NON-SERIOUS 发现（本轮范围外，未重开）
N1（1.2 表截断 hash）、N2（版本编号多轴）、N3（独立复核无通过判据）、N4（「72 小时」硬编码与注释冲突）维持 Round 1 原状，属单独跟踪项，本轮不重开、不复核。

## 矛盾（Contradictions）
- Round 1 矛盾 1（无障碍路径 = S2）：已消解，见 S2 核验。
- Round 1 矛盾 2（第 51 行「不改变既有发送业务语义」vs 第 81/127 行引入重试/冷却）：**仍未解决，但该条不属于 SERIOUS 发现（Round 1 判定 REWORK 仅因 S1/S2），故不在本轮范围，维持单独跟踪**。后续轮次若需要处理业务语义变更，须由用户显式决定。

## 缺失的验收标准（Missing Acceptance Criteria）
Round 1 的 M1–M5（日志/PII 脱敏、无障碍防绕过、消费标记保留期、回滚已演练取证、send 时序断言）均非 SERIOUS 发现，本轮范围外，维持单独跟踪，未重开。

## 修复清单（Remediation Checklist）
1. [已完成] S1：第 9 节更名「验收骨架（参数待冻结）」+ 参数冻结门禁 + 停止条件（第 163/165/153 行）。
2. [已完成] S2：无障碍路径改为待定候选 + 指定 owner/阶段 + N/A/PENDING + 停止条件（第 48/86/129/146/170/153 行）。
3. [本轮不适用] 其余 N1–N4、矛盾 2、M1–M5 均不在 Round 2 核验范围，未作修改。

## 待决问题（Open-Issue List，承继不重开）
1. provider/等价方案、目标网络/域名/地域、DPA、成本、TTL、超时、IP 传输/保留、限流、轮换、观察窗口、无障碍路径、文案、误拒阈值、base commit —— 仍为 USER_CONFIRMATION_PENDING（第 179–185 行），按设计保留。
2. V3 关闭证据、精确 base commit 未知 → UPSTREAM_GATE_BLOCKED 是否已触发（第 111–123 行），本副本无法独立核验。
3. 冷却/重试与「既有发送业务语义」的关系（Round 1 矛盾 2）—— 单独跟踪，非 SERIOUS。
4. N1–N4、M1–M5 —— 单独跟踪，非 SERIOUS。

摘要：S1、S2 均已正确修复，无严重回归，治理设计（fail-closed、职责分离、provider-neutral、分支完成≠Issue 关闭）保持健全。本轮判定 PASS_WITH_NONBLOCKING_OPEN_ISSUES；遗留项均为已跟踪的非阻断问题。