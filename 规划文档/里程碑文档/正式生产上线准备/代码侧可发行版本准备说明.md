# 代码侧可发行版本准备说明

日期：2026-06-25

任务 ID：`RELEASE-RUN-2026-06-25-001-CODE`

执行 Agent：代码开发员 Agent

当前代码分支：`codex/m5-security-preflight`

GitHub 仓库：`https://github.com/Vange-wang/UNGradu-EDU-Website`

## 1. 代码侧必须完成项

依据 `规划文档/Spec文档/Release_version_Spec/2026-06-25-mvp-正式生产上线-spec.md`，代码侧在仓库内需要确认或完成以下事项：

| 项目 | 当前状态 | 说明 |
| --- | --- | --- |
| `APP_ENV=production` 生产主闸 | 已有代码保护 | `isTestLoginAllowed()` 在 `APP_ENV=production` 时强制返回 `false` |
| `/api/auth/test-login` 生产拒绝 | 已有代码保护，已补预检脚本 | `createAuthApiHandlers().POST_TEST_LOGIN` 在生产环境返回拒绝 |
| `M5_ENABLE_HOSTED_TEST_LOGIN` 不影响生产 | 已有代码保护，已补预检脚本 | 该变量只允许 `APP_ENV=test` 的隔离托管测试环境使用 |
| `NEXT_PUBLIC_ALLOW_TEST_LOGIN` 不影响生产 | 已有代码保护，已补预检脚本 | 生产构建语义下前端测试登录入口不展示 |
| `x-ungradu-test-user-phone` 生产拒绝 | 已有代码保护，已补预检脚本 | `readAuthenticatedUserId()` 生产环境拒绝临时测试身份头 |
| 正式生产预检命令 | 已新增 | `npm run release:production:preflight` |
| 真实密钥不写入 Git | 已遵守 | 本次仅写变量名、用途和占位符，不写真实值 |
| 代码验证 | 待本任务最终运行 | 至少运行 `typecheck`、`test`、`lint`、`build` 和生产预检 |

## 2. 文档与配置侧可由代码开发员补齐项

本轮代码开发员可在仓库内完成：

- 更新 `Code文档/.env.example`，区分本地/测试模板与正式生产模板。
- 更新 `Code文档/README.md`，补充 `npm run release:production:preflight` 用法、边界和生产误配测试说明。
- 新增生产上线预检脚本：`Code文档/scripts/production-readiness-check.mjs`。
- 新增预检脚本回归测试：`Code文档/tests/production-readiness-script.test.ts`。
- 更新 `Code文档/开发员工作记录.md`，记录本任务改动、验证结果和外部阻塞项。
- 在本目录补充代码侧可发行版本准备说明，但不输出产品验收最终结论。

## 3. 必须由项目总控制人处理项

以下事项需要外部账号、密钥、云权限、运营决策或手动部署，代码开发员不能在仓库内自行完成：

| 事项 | 原因 | 建议处理人 |
| --- | --- | --- |
| 创建或确认生产 CloudBase 环境 | 需要云账号和环境权限 | 项目总控制人 |
| 创建或确认生产 CloudBase Run 服务 | 需要云账号和部署权限 | 项目总控制人 |
| 配置生产 `AUTH_SESSION_SECRET` | 必须使用生产专用强随机密钥，不能写入仓库 | 项目总控制人 |
| 配置生产 `TENCENTCLOUD_SECRETID` / `TENCENTCLOUD_SECRETKEY` | 真实密钥不得交给代码文档或聊天记录 | 项目总控制人 |
| 确认正式登录或受控试运行登录方案 | 涉及运营、白名单、短信或人工确认机制 | 项目总控制人 / 产品经理 Agent |
| 维护试运行账号名单 | 涉及真实用户和运营审批 | 项目总控制人 / 产品经理 Agent |
| 部署正式生产版本 | 需要 CloudBase Run 权限 | 项目总控制人 |
| 在正式生产 URL 执行冒烟验收 | 需要生产 URL、账号和外部环境 | 项目总控制人统筹 |
| 回滚桌面演练或真实演练 | 需要部署控制台和版本信息 | 项目总控制人 |
| 基础隐私说明对受邀用户展示或发送 | 涉及用户告知口径 | 产品经理 Agent / 项目总控制人 |
| 上线验收报告最终结论 | 属于产品验收职责 | 产品经理 Agent |

## 4. 生产预检命令

该命令用于仓库内验证生产安全边界，不连接 CloudBase，不访问真实生产环境，不打印密钥值。

```powershell
$env:APP_ENV='production'
$env:AUTH_SESSION_SECRET='replace-with-production-strong-random-secret'
$env:CLOUDBASE_ENV_ID='replace-with-production-cloudbase-env-id'
$env:TENCENTCLOUD_SECRETID='replace-with-production-secret-id'
$env:TENCENTCLOUD_SECRETKEY='replace-with-production-secret-key'
$env:M5_ENABLE_HOSTED_TEST_LOGIN='true'
$env:NEXT_PUBLIC_ALLOW_TEST_LOGIN='true'
npm run release:production:preflight
Remove-Item Env:\APP_ENV
Remove-Item Env:\AUTH_SESSION_SECRET
Remove-Item Env:\CLOUDBASE_ENV_ID
Remove-Item Env:\TENCENTCLOUD_SECRETID
Remove-Item Env:\TENCENTCLOUD_SECRETKEY
Remove-Item Env:\M5_ENABLE_HOSTED_TEST_LOGIN
Remove-Item Env:\NEXT_PUBLIC_ALLOW_TEST_LOGIN
```

预期结果：

- 命令通过。
- 输出 `temporary test login rejected in production`。
- 输出 `x-ungradu-test-user-phone rejected in production`。
- 输出 `front-end test login entry hidden in production`。
- 若模拟误配了测试开关，输出 warning；真实生产配置中必须移除这些测试开关。

## 5. 不替代的事项

该预检命令不替代：

- 正式生产 URL 下的 `/api/auth/test-login` 拒绝验证。
- 正式生产 URL 下的 `x-ungradu-test-user-phone` 拒绝验证。
- 正式生产核心流程冒烟。
- 正式生产登录方案确认。
- CloudBase Run 部署、回滚和日志验证。
- 产品经理上线验收报告。

## 6. 与执行打包说明第 4、5、8 节核对

依据 `规划文档/里程碑文档/正式生产上线准备/正式生产上线阶段执行打包说明.md`，代码侧核对如下。

### 6.1 第 4 节“代码侧待办拆解”

| 执行包要求 | 覆盖位置 | 核对结果 |
| --- | --- | --- |
| `/api/auth/test-login` 在 `APP_ENV=production` 下不可用 | `scripts/production-readiness-check.mjs`、`tests/production-readiness-script.test.ts`、既有 `tests/auth-session-api.test.ts` | 已覆盖 |
| `x-ungradu-test-user-phone` 在 `APP_ENV=production` 下不可用 | `server/api-utils.ts` 既有逻辑、`scripts/production-readiness-check.mjs`、预检输出 | 已覆盖 |
| 误配 `M5_ENABLE_HOSTED_TEST_LOGIN=true` 仍不能打开生产测试登录 | `scripts/production-readiness-check.mjs`、`tests/production-readiness-script.test.ts` | 已覆盖；脚本会输出 warning，真实生产必须移除该变量 |
| 误配 `NEXT_PUBLIC_ALLOW_TEST_LOGIN=true` 仍不能打开生产测试登录 | `scripts/production-readiness-check.mjs`、`tests/production-readiness-script.test.ts` | 已覆盖；脚本会输出 warning，真实生产必须移除该变量 |
| 前端生产构建不展示测试验证码、测试账号登录入口或测试用户提示 | `features/auth/login-form.tsx` 既有 `allowTestLogin` 判断、`scripts/production-readiness-check.mjs` | 已覆盖生产构建语义；真实生产页面截图仍需生产 URL 可用后补证据 |
| 运行 `typecheck`、`test`、`lint`、`build` | 本任务最终验证记录、`Code文档/开发员工作记录.md` | 待最终记录 |
| 运行或补充生产预检 | `npm run release:production:preflight` | 已新增并通过本地模拟验证 |
| 生产 URL 冒烟协助 | 本文第 3、5 节 | 需要生产 URL、正式登录方案和账号，列为总控制人处理项 |
| 正式登录方案相关代码工作 | 本文第 3 节 | 当前未确认正式登录渠道；不得把 M5 测试登录用于生产 |

### 6.2 第 5 节“文档 / 配置 / 部署待办”

| 执行包要求 | 代码开发员覆盖方式 | 当前状态 |
| --- | --- | --- |
| 生产环境变量清单 | `Code文档/.env.example`、`Code文档/README.md`、本文第 3、4 节 | 已覆盖变量名、用途、禁止项和无密钥边界；真实配置状态需总控制人补证据 |
| 测试账号与试运行账号管理说明 | 本文第 3 节列为项目总控制人 / 产品经理 Agent 事项 | 代码侧只确认 M5 测试账号不可用于生产；名单、审批和数据处理需产品/运营确认 |
| 正式生产禁用测试登录确认 | `scripts/production-readiness-check.mjs`、`tests/production-readiness-script.test.ts`、本文第 4、5 节 | 仓库内模拟证据已覆盖；生产 URL 下证据需总控制人提供环境后补齐 |
| 生产上线回滚预案 | 本文第 3、5 节列出外部依赖 | 代码侧可提供提交和验证命令；镜像、CloudBase Run 回滚和执行人需总控制人确认 |
| MVP 生产试运行产品说明 | 不属于代码开发员最终输出 | 需产品经理 Agent 输出 |
| 基础隐私说明 | 不属于代码开发员最终输出 | 需产品经理 Agent / 业务方输出；代码侧不写用户告知口径结论 |

### 6.3 第 8 节“必须暂停确认项”

执行包第 8 节暂停确认项与本文第 3 节“必须由项目总控制人处理项”一致，均包括：

- 生产 CloudBase 环境与服务。
- 生产腾讯云密钥配置方式。
- 生产独立强随机 `AUTH_SESSION_SECRET`。
- 正式登录渠道。
- 白名单手机号维护主体与存储位置。
- 试运行账号新增 / 移除审批流程。
- 首轮试运行时间窗口和种子用户规模。
- 值守人、兜底负责人、回滚执行人。
- 生产域名或正式公网 URL。

结论：上述事项未确认前，代码开发员可以完成本地工程验证、预检脚本验证和技术说明，但不得宣布生产上线完成。
