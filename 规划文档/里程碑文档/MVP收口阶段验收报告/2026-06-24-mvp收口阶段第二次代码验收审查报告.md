# MVP 正式生产上线第一次 Claude 复核报告

日期：2026-06-25

任务 ID：`RELEASE-RUN-2026-06-25-001-CLAUDE`

报告身份：Claude Code 复核会话

审查对象：

- GitHub 仓库：`https://github.com/Vange-wang/UNGradu-EDU-Website`
- 分支：`codex/m5-security-preflight`
- 复核分支 HEAD：`8721e43a6c4766df4f183eb2fe3ec440f9c377ea`（`docs: update code developer handoff after M5`，提交时间 2026-06-23 19:21:29 +0800）
- 复核方式：直接 `git clone` 指定分支并逐文件查看，未依赖产品经理报告中的文本转述

---

## 1. 最终结论

**需代码开发员修复 / 需产品经理重新核实。本轮不允许进入最终报告整理。**

理由：产品经理第一次验收报告中描述的本轮代码侧核心证据——`release:production:preflight` 脚本、`Code文档/scripts/production-readiness-check.mjs`、`Code文档/scripts/clean-next-build.mjs`、`Code文档/tests/production-readiness-script.test.ts`、`规划文档/里程碑文档/正式生产上线准备/代码侧可发行版本准备说明.md`——**在 `codex/m5-security-preflight` 分支（以及 `main` 分支）的实际仓库内容中均不存在**。这不是“外部上线事项”，而是仓库内代码/ 文档与验收报告不一致，必须先由代码开发员或产品经理确认真实状态，再决定是否可以进入下一轮整理。

本结论与产品经理报告的判断**不一致**：产品经理报告判定“仓库内代码侧可发行候选允许进入 Claude Code 复核”，但 Claude 复核未能在仓库中找到支撑该判断的关键文件。

---

## 2. 复核方法

为避免仅依赖产品经理报告的文本描述，本轮 Claude 复核直接执行了以下操作：

1. `git clone --branch codex/m5-security-preflight` 拉取仓库，核对 HEAD 提交。
2. 在该分支工作区内对要求复核的全部文件逐一 `find` / `view` / `grep`：
   - `Code文档/.env.example`
   - `Code文档/README.md`
   - `Code文档/package.json`
   - `Code文档/scripts/production-readiness-check.mjs`
   - `Code文档/scripts/clean-next-build.mjs`
   - `Code文档/tests/production-readiness-script.test.ts`
   - `规划文档/里程碑文档/正式生产上线准备/代码侧可发行版本准备说明.md`
3. 额外检查 `main` 分支与远程全部分支列表，确认上述文件不是被放在了其他分支。
4. 检查 `Code文档/tests` 目录全量文件、`next.config.ts`、`Code文档/server` 与 `Code文档/lib` 中是否存在等价但命名不同的实现。

---

## 3. 逐项核对结果

### 3.1 `Code文档/package.json`

**实际内容（`scripts` 字段全文）：**

```json
"scripts": {
  "dev": "next dev",
  "build": "next build",
  "cloudbase:check": "node scripts/check-cloudbase-connection.mjs",
  "m5:cloudbase:collections": "node scripts/check-m5-cloudbase-collections.mjs",
  "m5:flow": "vitest run m5-server-flow-and-load",
  "m5:http:flow": "node scripts/m5-http-flow-and-load.mjs",
  "m5:http:load": "node scripts/m5-http-flow-and-load.mjs --load",
  "m5:hosted:verify": "node scripts/m5-hosted-verify.mjs",
  "m5:load": "vitest run m5-server-flow-and-load",
  "lint": "eslint . --max-warnings=0",
  "typecheck": "tsc --noEmit",
  "test": "vitest run"
}
```

核对结论：

- **不存在 `release:production:preflight` 脚本。** 产品经理报告中两次执行该命令（生产预检、误配测试登录验证）的关键证据，在当前分支无法复现，因为该 npm script 在 `package.json` 中根本不存在。
- **`build` 脚本仍为 `next build`，未加入任何前置清理步骤。** 产品经理报告称“代码开发员修复：将 `npm run build` 改为先安全清理本项目内可再生 `.next` 产物，再执行 `next build`”，但实际 `build` 脚本未做任何修改。
- `next.config.ts` 中确认存在 `output: "standalone"`（核对属实，该项与报告描述一致）。

### 3.2 `Code文档/scripts/clean-next-build.mjs`

**不存在。**

`Code文档/scripts/` 目录实际文件：

```
check-cloudbase-connection.mjs
check-m5-cloudbase-collections.mjs
m5-hosted-verify.mjs
m5-http-flow-and-load.mjs
```

全仓库 `find . -iname "*clean-next-build*"` 无任何匹配。产品经理报告中关于此文件“新增”并修复 P0 构建阻塞的描述，在本分支无法验证。

### 3.3 `Code文档/scripts/production-readiness-check.mjs`

**不存在。** 同上目录列表，全仓库搜索 `*production-readiness*` 无匹配。

报告 4.1 节描述的 `npm run release:production:preflight` 输出（如“required production variables checked without printing values”“temporary test login rejected in production”等）依赖该脚本运行，但脚本本身不存在于仓库。

### 3.4 `Code文档/tests/production-readiness-script.test.ts`

**不存在。**

`Code文档/tests/` 目录共 30 个测试文件（非报告中“32 个测试文件”），均为既有 M1–M5 阶段功能测试（认证、聊天、联系方式交换、家教/需求发布等），未见任何以 `production-readiness` 或 `preflight` 命名的测试文件。

### 3.5 `规划文档/里程碑文档/正式生产上线准备/代码侧可发行版本准备说明.md`

**目录与文件均不存在。**

`规划文档/里程碑文档/` 下实际只有 `M1阶段验收报告` 至 `M5阶段验收报告` 五个子目录及一个 `README.md`，没有 `正式生产上线准备` 子目录。产品经理报告引用的另外两份文档（`规划文档/Spec文档/Release_version_Spec/2026-06-25-mvp-正式生产上线-spec.md` 和 `正式生产上线阶段执行打包说明.md`）所在的 `Release_version_Spec` 目录也不存在；`规划文档/Spec文档/` 下实际只有 `2026-06-19-mvp-家教对接平台-spec.md` 和 `README.md`。

### 3.6 `Code文档/.env.example`（已存在，内容核对属实）

实际内容：

```
CLOUDBASE_ENV_ID=ungradu-edu-test-d0ed1mqeceb0ae1
TENCENTCLOUD_SECRETID=your-secret-id
TENCENTCLOUD_SECRETKEY=your-secret-key
AUTH_SESSION_SECRET=replace-with-a-long-random-server-session-secret
APP_ENV=test
M5_ENABLE_HOSTED_TEST_LOGIN=false
```

核对结论：未包含真实密钥，变量均为占位符或测试值，**该项与产品经理报告描述一致**。但该文件本身不区分“正式生产配置”小节，仅有测试环境默认值，生产取值需要外部另行配置，这一点报告描述基本准确。

### 3.7 `Code文档/README.md`（已存在，内容核对属实）

README 中确实记录了生产环境下测试登录边界的说明，例如：

> 正式生产环境必须配置 `APP_ENV=production`，此时即使误配 `M5_ENABLE_HOSTED_TEST_LOGIN=true` 或 `NEXT_PUBLIC_ALLOW_TEST_LOGIN=true`，服务端仍拒绝临时测试登录。

这段描述与 `server/auth-api.ts`、`server/cloudbase-server.ts` 中实际存在的 `APP_ENV` 判断逻辑一致——**这部分核心安全机制是真实存在于代码中的**，与脚本/测试文件缺失是两件独立的事。换言之：

- 代码中确实有"生产环境拒绝测试登录"的运行时保护（在 API 路由层面，通过 `APP_ENV` 判断）。
- 但报告中描述的"独立预检脚本 + 自动化集成测试"这一层包装（`release:production:preflight`、`production-readiness-check.mjs`、对应 test 文件）并不存在。

---

## 4. 与产品经理报告的差异汇总

| 产品经理报告声称 | Claude 复核在仓库中实际发现 | 差异性质 |
| --- | --- | --- |
| `package.json` 已包含 `release:production:preflight` 脚本 | 不存在该脚本 | 仓库内容与报告不符 |
| 新增 `Code文档/scripts/clean-next-build.mjs` | 文件不存在 | 仓库内容与报告不符 |
| `npm run build` 已改为先清理 `.next` 再 `next build` | `build` 脚本仍为纯 `next build` | 仓库内容与报告不符 |
| 新增/存在 `Code文档/scripts/production-readiness-check.mjs` | 文件不存在 | 仓库内容与报告不符 |
| `Code文档/tests/production-readiness-script.test.ts` 覆盖生产误配场景 | 文件不存在；测试目录共 30 个文件而非 32 个 | 仓库内容与报告不符 |
| `规划文档/里程碑文档/正式生产上线准备/代码侧可发行版本准备说明.md` 存在 | 目录与文件均不存在 | 仓库内容与报告不符 |
| 引用的正式 Spec 路径 `规划文档/Spec文档/Release_version_Spec/...` | 该子目录不存在 | 仓库内容与报告不符 |
| `.env.example` 区分本地/测试/生产配置，无真实密钥 | 属实（仅含测试默认值，无真实密钥） | 一致 |
| README 记录测试登录禁用边界 | 属实，且与服务端代码实现一致 | 一致 |
| `next.config.ts` 保留 `output: "standalone"` | 属实 | 一致 |

---

## 5. 区分代码缺陷与外部上线事项

按要求需求 4 的区分原则，本轮发现的问题**不属于**“外部上线事项”（生产 CloudBase 环境、生产密钥、登录方案、白名单、部署、生产 URL 冒烟、回滚演练、隐私告知等），因为：

- 这些缺失文件并非需要生产环境权限才能创建的内容；它们是仓库内本应提交的脚本、测试和文档文件。
- 它们的缺失直接导致产品经理报告第 4.1 节中两条关键复验记录（`release:production:preflight` 通过、`build` 修复后通过）**无法在当前分支复现**。

因此本轮问题归类为：**仓库内代码 / 文档与验收材料不一致**，需要先由代码开发员或产品经理解释清楚，而不是被允许作为"外部阻塞"放行。

---

## 6. Issues

### Issue 1

- **title**: `release:production:preflight` 脚本在仓库中不存在，但验收报告以其执行结果作为核心通过证据
- **type**: bug / 验收材料不一致
- **priority**: P0
- **source**: Claude Code 复核（直接克隆 `codex/m5-security-preflight` 分支核对 `Code文档/package.json`）
- **description**: 产品经理验收报告第 4.1 节描述本轮独立复跑 `npm run release:production:preflight` 通过，并给出详细命令行与输出日志。但当前分支 `Code文档/package.json` 的 `scripts` 字段中不存在名为 `release:production:preflight` 的脚本，`Code文档/scripts/` 目录中也没有对应实现文件（如 `production-readiness-check.mjs`）。该命令在当前分支无法执行，报告中的输出日志无法对应到仓库内任何脚本。
- **acceptance**: 1）确认该脚本应提交到哪个分支/提交，若已在本地开发但未推送，需推送并在 PR 中体现；2）若该脚本确实不应存在于本分支（例如属于另一规划阶段误写入报告），需要产品经理更正验收报告，明确去除该项证据或重新核实命令来源；3）`package.json` 的 `scripts` 字段、对应实现文件与验收报告三者必须一致。
- **owner_agent**: 代码开发员 / 产品经理
- **related_files**: `Code文档/package.json`, `Code文档/scripts/production-readiness-check.mjs`（缺失）

### Issue 2

- **title**: P0 构建阻塞修复方案（`clean-next-build.mjs` + 改写 `npm run build`）未实际提交到仓库
- **type**: bug / 验收材料不一致
- **priority**: P0
- **source**: Claude Code 复核（核对 `Code文档/scripts/` 目录与 `package.json` 中 `build` 脚本定义）
- **description**: 产品经理报告称代码开发员已新增 `Code文档/scripts/clean-next-build.mjs`，并将 `build` 脚本改为 `node scripts/clean-next-build.mjs && next build`，用以解决此前 `EPERM`/`ENOENT`/`.next/types/cache-life.d.ts` 相关的构建失败问题。实际仓库中 `clean-next-build.mjs` 文件不存在，`package.json` 的 `build` 脚本仍为纯 `next build`。若该修复未被合并，此前报告中描述的“P0 构建阻塞已修复”这一前提本身不成立，意味着 `npm run build` 在当前分支可能仍会遇到此前记录的相同失败（无法在当前只读复核环境中安装依赖并实际验证，因构建依赖 `node_modules` 与 Next.js 工具链，超出本次 Claude 复核的执行范围，但仓库内代码本身确认未变更）。
- **acceptance**: 1）确认修复提交是否已推送到 `codex/m5-security-preflight` 分支，若否需补推；2）代码开发员在 CI 或本地环境下重新执行 `npm run build` 并提供可复现的通过日志；3）Claude 或产品经理在修复确认推送后重新复核该文件与脚本是否落地。
- **owner_agent**: 代码开发员
- **related_files**: `Code文档/scripts/clean-next-build.mjs`（缺失）, `Code文档/package.json`, `Code文档/next.config.ts`

### Issue 3

- **title**: `production-readiness-script.test.ts` 不存在，测试文件总数与报告不符
- **type**: bug / 验收材料不一致
- **priority**: P1
- **source**: Claude Code 复核（`ls Code文档/tests`，共 30 个文件）
- **description**: 产品经理报告称 `Code文档/tests/production-readiness-script.test.ts` 覆盖“生产误配测试开关仍拒绝测试登录”及“`APP_ENV` 非生产时预检失败”等场景，且总负责人确认 32 个测试文件、117 个测试通过。当前分支 `Code文档/tests/` 目录实际只有 30 个测试文件，且没有任何与 production-readiness 相关命名的测试文件。无法确认 117 个测试是否仍然全部通过（部分用例可能依赖缺失的预检脚本逻辑）。
- **acceptance**: 1）确认缺失测试文件是否需要补推到本分支；2）补推后重新运行 `npm test` 并提供测试文件数量、用例数量、通过情况的可复现记录；3）若该测试文件原本不属于本分支范围，需更正验收报告中的测试文件数量描述。
- **owner_agent**: 代码开发员 / 产品经理
- **related_files**: `Code文档/tests/production-readiness-script.test.ts`（缺失）

### Issue 4

- **title**: `代码侧可发行版本准备说明.md` 及其所在目录、引用的 Release Spec 目录均不存在
- **type**: 验收材料不一致 / 文档缺失
- **priority**: P1
- **source**: Claude Code 复核（`find 规划文档 -type f`，逐一核对目录结构）
- **description**: 产品经理报告多处引用以下三份文档作为审查依据：`规划文档/Spec文档/Release_version_Spec/2026-06-25-mvp-正式生产上线-spec.md`、`规划文档/里程碑文档/正式生产上线准备/正式生产上线阶段执行打包说明.md`、`规划文档/里程碑文档/正式生产上线准备/代码侧可发行版本准备说明.md`。当前分支 `规划文档/` 下不存在 `Release_version_Spec` 子目录，也不存在 `正式生产上线准备` 子目录，三份文档均无法找到。报告第 3 节"正式 Spec 验收标准逐项核对"表格中的全部 22 项标准，其依据的 Spec 文档本身在本分支不可见，因此该表格的可验证性存疑。
- **acceptance**: 1）确认这三份文档是否已经创建但未推送/未合并到 `codex/m5-security-preflight` 分支；2）若已创建，补推到本分支或说明应在哪个分支查阅；3）若尚未创建，产品经理需更正报告，明确报告中第 3 节的 Spec 核对结果所依据的实际文档来源。
- **owner_agent**: 产品经理 / 项目总控制人
- **related_files**: `规划文档/里程碑文档/正式生产上线准备/代码侧可发行版本准备说明.md`（缺失）, `规划文档/Spec文档/Release_version_Spec/2026-06-25-mvp-正式生产上线-spec.md`（缺失）

---

## 7. 本轮确认无误、可直接采信的部分

为避免一概否定，以下经 Claude 直接核对、确认与产品经理报告描述一致的部分，**可以采信，无需重复核查**：

- `Code文档/.env.example` 不含真实密钥，仅含占位符与测试默认值。
- `Code文档/README.md` 中关于生产环境下拒绝测试登录的描述（`APP_ENV=production` 时拒绝 `M5_ENABLE_HOSTED_TEST_LOGIN`/`NEXT_PUBLIC_ALLOW_TEST_LOGIN`/`x-ungradu-test-user-phone`）与 `Code文档/server/auth-api.ts`、`Code文档/server/cloudbase-server.ts` 中的实际运行时判断逻辑一致，该核心安全机制确实存在于代码中。
- `next.config.ts` 中 `output: "standalone"` 配置确认存在。
- 报告中列出的"项目总控制人外部事项"清单（生产 CloudBase 环境、生产密钥、登录方案、白名单、部署、生产 URL 冒烟、回滚演练、隐私告知）本身的分类原则正确，这些确实不属于仓库内代码缺陷范畴——**但这一原则的适用前提是仓库内代码侧证据本身真实可验证，而本轮发现该前提不成立**。

---

## 8. 下一步建议

1. **不进入最终报告整理。** 在 Issue 1–4 澄清并解决之前，仓库内代码侧可发行候选的状态无法确认。
2. 请产品经理或项目总控制人确认：报告中描述的 4 个缺失文件/脚本/目录，是否存在于尚未推送的本地分支、尚未合并的 PR，或是否被误写入报告（例如混入了规划阶段尚未落地的设计稿内容）。
3. 若确认相关改动确实未推送，请代码开发员补推到 `codex/m5-security-preflight` 分支，随后发起第二次 Claude 复核，重新核对本报告 Issue 1–4 涉及的全部文件。
4. 若确认相关改动不应存在于本分支（例如属于不同的发布批次），请产品经理重新发起验收报告，移除不可验证的证据描述，并明确本分支当前实际可交付的范围。
5. 在仓库内代码侧证据一致性确认之前，维持产品经理报告第 5–7 节列出的外部事项清单作为后续上线前的待办参考，但不依赖其作为本轮代码侧通过的前提。

---

## 9. 结论字段汇总

- **是否允许验收 / 进入最终报告整理**：否
- **是否需代码开发员修复**：是（Issue 1、Issue 2 直接涉及代码/脚本/构建配置缺失）
- **是否存在不影响代码侧复核通过的外部事项阻塞**：是（产品经理报告第 5、7 节列出的生产环境、密钥、登录方案、白名单、部署、冒烟、回滚、隐私告知事项，分类原则正确，但与本轮代码侧复核结论无关，留待后续阶段）
- **本轮主要矛盾**：产品经理验收报告所依据的核心代码侧证据（预检脚本、构建修复脚本、对应测试、相关规划文档）在指定分支的仓库实际内容中未被发现，需先解决材料一致性问题，再决定是否进入下一轮最终报告整理。
