# vNext UI 文案精简与风险反馈 UI 验收记录

日期：2026-07-07

任务 ID：`VNEXT-UI-COPY-RISK-2026-07-07-001-UI-ACCEPT-1`

验收角色：UI 设计师验收执行代理

## 1. 验收范围

本次只做 UI 静态验收，不修改 `Code文档` 下业务代码，不提交不推送，不运行 npm。

已读取材料：

- `UI美术文档/小细节调整/2026-07-07-vNext-UI文案精简与风险边界清单.md`
- `协同工作文档/ISSUE/Open_Issue/ISSUE-0016-UI界面多余说明文字清理.md`
- `协同工作文档/ISSUE/Open_Issue/ISSUE-0017-风险反馈举报投诉最小记录能力缺口.md`
- `Code文档/开发员工作记录.md`
- `Code文档/app/page.tsx`
- `Code文档/app/login/page.tsx`
- `Code文档/app/rules/page.tsx`
- `Code文档/app/feedback/page.tsx`
- `Code文档/app/parent-needs/new/page.tsx`
- `Code文档/app/tutor-profiles/new/page.tsx`
- `Code文档/app/profile/page.tsx`
- `Code文档/app/profile/contact/page.tsx`
- `Code文档/app/chats/[id]/page.tsx`

## 2. 静态验收结论

结论：有条件通过。

通过依据：

- 首页、登录页、规则页、反馈页、发布需求页、发布家教信息页、个人中心、联系方式页和聊天页已按 UI 清单删除或压缩主要解释性文案。
- 本次指定页面范围内未发现 `MVP`、`任务分组`、`保留原有字段`、`文件元信息`、`自动轮询刷新`、`状态区`、`Step Form`、`Workspace`、`Dashboard`、`Contact Exchange` 等用户可见内部术语或实现说明。
- 必要风险边界仍保留：公开页不展示联系方式、双方确认后交换、不要提交敏感信息、平台不做支付 / 担保 / 认证 / 仲裁 / 退款、反馈仅用于记录排查。
- 页面动作仍清楚：登录 / 注册、返回、浏览、发布、保存、发送、请求交换联系方式、同意 / 拒绝 / 撤回、提交风险反馈等按钮和表单状态可理解。
- 反馈页已从单纯外部入口变成站内表单界面，包含反馈类型、对象、描述、证据说明、可联系信息、提交中、成功和失败反馈；UI 层可以支撑风险反馈最小闭环的用户操作。

条件与限制：

- 本次未运行 npm，也未重新浏览器截图；UI 侧接受代码开发员回报的桌面端和 390px 移动端 `/feedback` 冒烟结果作为移动端可接受依据。
- 开发员回执显示本地 CloudBase 缺少 `risk_feedback_records` 集合时提交会进入失败提示；这不阻断 UI 文案验收，但会影响 ISSUE-0017 的能力闭环关闭。
- 全局扫描时发现非本次必读范围的 `Code文档/app/tutor-profiles/[id]/page.tsx` 仍有“文件元信息数量”口径。该页不在本次指定主要页面验收清单中，建议后续单独纳入详情页文案扫尾，不作为本次 ISSUE-0016 指定范围阻断。

## 3. ISSUE-0016 意见

UI 侧允许 `ISSUE-0016` 进入关闭前流程。

说明：从 UI 文案和指定页面静态验收看，清单要求已基本满足；但 ISSUE 关闭条件还包含产品经理验收、完整验证命令、提交 / 推送等 Git 闭环证据，应由对应角色继续确认后再由 ISSUE 管理员归档。

## 4. ISSUE-0017 意见

UI 侧意见：反馈页的最小记录界面已具备可验收形态，但 `ISSUE-0017` 不建议仅凭 UI 验收关闭。

原因：

- UI 表单字段、边界提示、成功 / 失败状态和“不承诺客服介入、仲裁、退款或担保”的口径清楚。
- 最小记录能力还依赖产品经理确认字段 / 匿名策略 / 成功失败口径，以及 CloudBase 集合 `risk_feedback_records` 部署配置。
- 当前开发员回执已说明本地缺少集合时会失败并展示失败提示；因此 UI 通过不等于记录能力已完成生产闭环。

## 5. 移动端风险判断

基于代码结构与开发员冒烟回报，移动端风险可接受。

判断依据：

- 文案大幅压缩后，首屏说明负担降低。
- 发布页侧栏和表单仍为分区结构，字段与按钮没有因删文案失去上下文。
- 反馈页开发员已回报 390px 下 `scrollWidth` 等于 `clientWidth`，无横向溢出。
- 聊天页保留消息区、输入区和联系方式交换区，移动端仍需后续真实会话数据继续观察，但静态文案不构成阻断。

## 6. 本次未做事项

- 未修改 `Code文档` 下业务代码。
- 未运行 `npm run typecheck`、`npm run lint`、`npm test` 或 `npm run build`。
- 未提交、未推送。
