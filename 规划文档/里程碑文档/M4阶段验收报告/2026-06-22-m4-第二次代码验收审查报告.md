# M4 第二次代码验收审查报告

日期：2026-06-22

审查身份：产品经理

审查对象：

- GitHub 仓库：`https://github.com/Vange-wang/UNGradu-EDU-Website`
- 分支：`codex/m4-chat-contact-exchange`
- 本轮修复提交：`bdc8f79 fix: close M4 chat review blockers`
- 第一轮审查代码提交：`0fe4871 feat: start M4 chat and contact exchange`
- 第一轮最终报告：`规划文档/里程碑文档/M4阶段验收报告/2026-06-22-m4-第一次代码验收审查与复核确认报告.md`

本轮审查重点：

- 复验第一轮 P0：会话与联系方式交换请求在授权前返回双方手机号。
- 复验第一轮 P1：聊天页缺少准实时刷新，不能保证对方新消息 5 秒内出现。

审查依据：

- `规划文档/Spec文档/2026-06-19-mvp-家教对接平台-spec.md`
- `规划文档/里程碑文档/M4阶段验收报告/2026-06-22-m4-第一次代码验收审查与复核确认报告.md`
- `规划文档/钦定产品经理.md`
- `Code文档/开发员工作记录.md`

## 1. 验收结论

M4 第二次代码验收结论：**通过**。

本轮修复已关闭 M4 第一轮的两个阻塞项：

1. 会话、消息、联系方式交换请求对页面返回的对象已改为脱敏视图，不再在未授权前返回对方手机号。
2. 聊天页已增加 3 秒轮询刷新，满足 M4 对“新消息发送后 5 秒内被对方看到”的本地测试阶段要求。

工程验证命令全部通过，当前未发现新的 P0/P1 阻塞问题。M4“站内聊天与联系方式交换”阶段达到当前 Spec 的阶段验收要求，可以进入 M5：安全和发布前验证。

## 2. 审查范围

本轮代码修复相对第一轮审查后的主要改动：

```text
Code文档/app/chats/[id]/page.tsx
Code文档/features/chat/chat-polling.ts
Code文档/features/chat/chat-storage.ts
Code文档/tests/chat-polling.test.ts
Code文档/tests/chat-storage.test.ts
Code文档/开发员工作记录.md
```

未发现新增以下非 M4 能力：

- 支付、抽佣、退款。
- 评价体系。
- 人工审核后台。
- 自动推荐算法。
- 图片、语音、撤回、已读未读等复杂聊天能力。

## 3. 第一轮 P0 复验：未授权前不再返回手机号

第一轮问题：

- `SavedConversation` 直接暴露 `participantPhones`。
- `SavedContactExchangeRequest` 直接暴露 `requesterPhone`、`receiverPhone`。
- 聊天页和我的聊天页把这些完整对象放入 React state，用户在授权前即可通过浏览器开发者工具看到对方手机号。

本轮修复确认：

- `Code文档/features/chat/chat-storage.ts` 将内部存储类型与对外返回类型拆分。
- 内部仍使用 `StoredConversation`、`StoredConversationMessage`、`StoredContactExchangeRequest` 保存参与者手机号，用于当前本地测试阶段的权限判断。
- 对页面和测试暴露的类型改为：

```ts
export type SavedConversation = {
  id: string;
  sourceId: string;
  sourceType: ConversationSourceType;
  createdAt: string;
};

export type SavedConversationMessage = {
  id: string;
  conversationId: string;
  direction: "sent" | "received";
  text: string;
  createdAt: string;
};

export type SavedContactExchangeRequest = {
  id: string;
  conversationId: string;
  direction: "sent" | "received";
  status: ContactExchangeRequestStatus;
  secondConfirmedAt: string | null;
  createdAt: string;
  updatedAt: string;
};
```

关键实现：

- `toConversationView` 只返回会话 ID、来源 ID、来源类型和创建时间。
- `toMessageView` 将 `senderPhone` 转换为 `direction`，不返回发送方手机号。
- `toContactExchangeRequestView` 将 `requesterPhone` / `receiverPhone` 转换为 `direction`，不返回双方手机号。
- `createOrReadConversation`、`readConversationForUser`、`readConversationsForUser`、`sendConversationMessage`、`listConversationMessages`、`createContactExchangeRequest`、`approveContactExchangeRequest`、`rejectContactExchangeRequest`、`withdrawContactExchangeRequest`、`listContactExchangeRequestsForConversation` 均返回脱敏视图。
- `readAuthorizedContactProfiles` 仍只在存在已同意且完成二次确认的请求后返回双方存档联系方式。

测试确认：

- `tests/chat-storage.test.ts` 已补充断言：创建/读取会话返回对象不包含双方手机号。
- 消息列表返回对象不包含对方手机号。
- 联系方式交换请求列表返回对象不包含发起方手机号。

复验结论：第一轮 P0 已关闭。

## 4. 第一轮 P1 复验：聊天页已增加 3 秒轮询

第一轮问题：

- 聊天页只在页面加载和当前用户发送/处理请求后手动刷新。
- 对方在另一个页面发送新消息后，当前页面不会在 5 秒内自动看到新消息。

本轮修复确认：

- 新增 `Code文档/features/chat/chat-polling.ts`：

```ts
export const CHAT_POLLING_INTERVAL_MS = 3000;
```

- `Code文档/app/chats/[id]/page.tsx` 中新增轮询：

```ts
useEffect(() => {
  const intervalId = window.setInterval(refresh, CHAT_POLLING_INTERVAL_MS);

  return () => window.clearInterval(intervalId);
}, [refresh]);
```

这意味着聊天页在加载后每 3 秒重新读取当前会话、消息、交换请求和授权联系方式。对方写入新消息后，在当前本地测试模型下，当前聊天页最多约 3 秒会刷新展示，满足 Spec 中 5 秒内看到新消息的 M4 验收要求。

测试确认：

- `tests/chat-polling.test.ts` 已断言 `CHAT_POLLING_INTERVAL_MS` 大于 0 且不超过 5000。

复验结论：第一轮 P1 已关闭。

## 5. M4 阶段能力复核

当前 M4 已覆盖：

- 从需求详情和家教信息详情发起站内聊天。
- 我的聊天列表只展示当前测试账号参与的会话。
- 会话参与者可以发送和读取文字消息。
- 非会话参与者不能读取会话、消息或交换请求。
- 消息按时间顺序展示。
- 未完成联系方式交换授权前，页面返回对象不含对方手机号。
- 任意一方可以请求交换联系方式。
- 接收方同意前必须二次确认。
- 请求可以拒绝、撤回、过期。
- 7 天过期从创建时间起算。
- 已同意且完成二次确认后，双方可看到彼此存档联系方式。

当前仍未实现的内容属于后续 M5 或更后续范围：

- 生产级后端可信登录态。
- CloudBase 数据归属校验和安全规则。
- 接口级越权测试。
- 50 人并发基础压测。
- 账号不可用、内容删除后的完整异常闭环。

## 6. 非阻塞观察

### 6.1 本地存储内部仍使用手机号作为参与者标识

本轮修复已经确保页面可见返回对象不含对方手机号，但当前本地测试存储内部仍使用 `participantPhones`、`requesterPhone`、`receiverPhone` 来完成参与者判断和联系方式授权判断。

在当前 M4 本地测试阶段，这可以接受；但 M5 或接入 CloudBase 时，必须进一步将该策略升级为后端可信用户 ID、数据归属校验和接口过滤，不能把本地存储内部模型原样带入生产。

### 6.2 轮询测试覆盖的是时间常量，不是组件级自动刷新

`tests/chat-polling.test.ts` 目前验证了轮询间隔不超过 5 秒；实际自动刷新行为通过 `app/chats/[id]/page.tsx` 中的 `setInterval(refresh, CHAT_POLLING_INTERVAL_MS)` 代码审查确认。

这不阻塞 M4 第二轮验收，但建议 M5 或端到端测试阶段补充浏览器级验证：一方写入消息后，另一方停留在聊天页无需手动刷新即可看到新消息。

## 7. 验证结果

在 `Code文档` 目录下执行：

```powershell
npm run typecheck
npm test
npm run lint
npm run build
```

结果：

| 命令 | 结果 |
| --- | --- |
| `npm run typecheck` | 通过 |
| `npm test` | 通过，11 个测试文件，47 个测试用例 |
| `npm run lint` | 通过 |
| `npm run build` | 通过，生成 16 条路由，其中 `/chats/[id]`、`/parent-needs/[id]`、`/tutor-profiles/[id]` 为动态路由 |

## 8. 阶段判定

M4 第二次代码验收：**通过**。

M4 当前满足“基础实时/准实时文字聊天和双向授权联系方式交换”的阶段验收要求。

下一步建议进入 M5：安全和发布前验证。M5 必须重点验证：

- 核心流程全部走通。
- 非会话参与者不能读取消息、交换请求或授权联系方式。
- 未授权联系方式不能通过接口、ID 枚举、抓包或数据接口读取。
- CloudBase 后端可信登录态、数据归属校验和安全规则可用。
- 生产环境不得启用临时测试登录。
- 完成 50 并发核心流程基础压测。
