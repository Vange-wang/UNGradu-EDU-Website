# V4｜ISSUE-0032 用户确认与阶段边界冻结记录

任务 ID：`V4-ISSUE-0032-STEP1-SCOPE-CONFIRMATION-20260818`  
执行角色：产品经理 Agent v2.3.2（`019fefa7-9883-7af2-bdb5-acc5c8513781`）  
记录日期：2026-08-18  
用户授权类型：严格单步授权  

## 1. 结论

**V4_STEP1_SPEC_SCOPE_CONFIRMED_AND_FROZEN**

本结论只表示用户确认并冻结 ISSUE-0032 的 provider-neutral 关闭 Spec 目标、非目标、服务端 fail-closed 信任链、证据分离和阶段边界。它**不是** `V4_STARTED`、`BASE_ACCEPTED`、`IMPLEMENTATION_AUTHORIZED`、`PROVIDER_AUTHORIZED`、`DEPLOY_AUTHORIZED`、`PRODUCTION_ACCEPTED` 或 `ISSUE_CLOSED`。

项目 workflow 仍为 `WORKFLOW_ACTIVE`。`ISSUE-0032` 仍为 `open / USER_CONFIRMATION_PENDING`；`ISSUE-0042` 仍为 `open / NON_BLOCKING_DOCUMENT_REVIEW`。`ISSUE-0034` 已为 `closed / WORKFLOW_COMPLETE`，但仅代表 ISSUE-0034 自身，不改变项目总 workflow 或 V4 的入口门禁。

## 2. 精确用户确认绑定

### 2.1 本次授权输入

用户先明确“一步一步，不完全授权，授权一步做一步”。项目总负责人将第一步限定为：确认并冻结 ISSUE-0032 的 V4 关闭 Spec 与本阶段边界，不创建 V4 分支、不运行 npm、不改代码、不接 Turnstile、不配置密钥、不部署。用户随后仅回复：`授权`。

该 `授权` 是本任务的单步授权，不是持续授权，不延伸到任何后续阶段或外部系统动作。

### 2.2 绑定的规范源与审查结论

用户确认关系绑定到以下精确字节：

| 输入 | 精确指纹 |
| --- | --- |
| `规划文档/Spec文档/Release_version_Spec/2026-08-15-issue-0032-邮箱人机验证关闭-spec.md` | SHA-256 `F7939E3BD8769B9BE4CB18335A71B1BC624FD32182827F099F219F8DD36B9073`；16,889 bytes；191 lines |
| Hermes Round 2 报告 `规划文档/Spec文档/Release_version_Spec/2026-08-15-issue-0032-邮箱人机验证关闭-hermes-round-2.md` | SHA-256 `C56E4B980AAF26D4876AEA94A6C50CC4B77CB0F8AE1072DEC49FF00FFE517666`；6,781 bytes；66 lines；`PASS_WITH_NONBLOCKING_OPEN_ISSUES`；S1/S2 closed；0 unresolved SERIOUS |
| Hermes Round 2 metadata | SHA-256 `31CAEDFF6299901A967D7A1814E9F51C44AF6E80BA19DFEE4BCF72482353A5AC`；826 bytes；15 lines；`deepseek-v4-pro`；`exit_code=0`；`canonical_source_unchanged=true` |

原 Spec 正文的 `DRAFT_NON_CANONICAL / AUTHOR_DRAFT / USER_CONFIRMATION_PENDING` 表头保持不变。本记录只建立“用户确认 → 精确 Spec hash → Hermes R2 结论”的独立证据关系，不改写、不升级原 Spec 的文档状态。

Hermes Round 1 的 2 项 SERIOUS（S1 参数可测试性、S2 无障碍路径前向引用）已由 Document QA 批次修订并由 Round 2 复核关闭；Round 2 仍保留 NON_SERIOUS 与未决业务参数，不构成 provider、实现、生产或 Issue 关闭授权。`ISSUE-0042` 继续承载 Round 1 的非阻塞文档债务。

## 3. 本阶段已确认并冻结的范围

以下内容属于本次用户确认的 provider-neutral 阶段边界：

1. 目标是建立邮箱验证码发送前不可由客户端单独绕过的服务端人机验证信任链：服务端在允许发送前，验证挑战 token 的状态、一次性消费、时效、hostname/action、会话和请求上下文，并执行适用的组合限流。
2. 任何 token 缺失、伪造、过期、重复、上下文不符、服务端超时、provider 不可达、配置引用缺失、限流命中或结果不确定，都必须 fail-closed，邮箱发送次数为 0。
3. 用户只获得不泄露 provider、风控字段、Secret、账号存在性或内部 request ID 的安全、可操作反馈；不得把“稍后重试”降级为前端勾选即可发送。
4. 生产实现前的 provider-neutral、provider-specific、部署/生产、产品、业务和 ISSUE 管理员证据必须分层取证；本阶段确认不合并这些证据层级。
5. 分支完成、provider-neutral 本地测试通过、provider-specific 集成通过、生产观察稳定或配置回滚成功，均不自动等于 ISSUE-0032 关闭；关闭仍需完整适用证据、独立复核、产品验收、业务方确认和 ISSUE 管理员 canonical 操作。
6. 涉及未成年人信息、邮箱、会话凭据、token、Cookie、Secret 的测试必须使用合成/脱敏数据和最小化日志；任何真实 Secret、真实用户数据或可重放 token 不进入本仓库记录。

## 4. 明确不在本次确认内的事项

本次用户没有确认、批准或选择以下事项，均继续保持 `USER_CONFIRMATION_PENDING` 或适用的上游阻塞状态：

- `token_ttl`、`verify_timeout`、`expected_hostnames`、`expected_actions`；
- 一次性消费标记的保留/清理、账号/IP/设备/会话/动作限流阈值、窗口、冷却、重试和组合顺序；
- IP 处理与保留、轮换规则、生产观察窗口、误拒阈值；
- 无障碍替代路径、失败文案、人工支持边界和用户可见提示细节；
- 真实 provider、等价方案、widget、endpoint、响应映射、目标域名/网络/地域、DPA、采购、价格/计费；
- 任何测试或生产 site key、Secret、API key、平台配置或凭据引用的实际值；
- 严格 Issue 关闭串行中是否必须等待上一 Issue 的 `ISSUE_CLOSURE_RECEIPT`；用户未作该项选择。

以上事项必须在后续独立授权包中形成精确参数/决策 receipt；缺失或冲突时不得给相应阶段 PASS。

## 5. 阶段门禁与权限边界

本记录仅冻结 Step 1 的范围，不把任何后续阶段记为完成：

| 阶段 | 当前口径 | 本次是否授权 |
| --- | --- | --- |
| Step 1：Spec 与 provider-neutral 阶段边界冻结 | 本记录确认；原 Spec hash 和 Hermes R2 关系已绑定 | 是，仅限本记录与产品经理工作记录写入 |
| V4 base receipt | 必须引用上一已验收 V3 的精确 commit、ref/parent/tree、工作树/index/staged/unstaged/untracked 与验收关系 | 否 |
| V4 分支创建 | 只能从独立核对通过的 `BRANCH_BASE_RECEIPT` 创建 | 否 |
| V4-S0/S1 实现、代码、测试、npm、Git mutation | 需另行明确授权；参数 receipt 也尚未冻结 | 否 |
| provider/widget/Secret/网络/平台 | provider-neutral 方向不等于 provider-specific 或平台授权 | 否 |
| 部署、生产发送、生产观察、回滚 | 必须分别取得平台、生产和业务证据 | 否 |
| ISSUE-0032 状态修改/关闭 | 由 ISSUE 管理员依据完整证据独立操作 | 否 |

本次未创建任务或 subagent，未派发其他角色；产品经理只维护本记录和自己的工作记录。

## 6. V3 上游事实与 base receipt 边界

V3 上一阶段的精确提交 `ee41c3f30770be6f7a9a0e548975464268b911d2` 仅作为下一步 `BRANCH_BASE_RECEIPT` 的候选输入。它不因本记录而变成 `BASE_ACCEPTED`，也不因 ISSUE-0034 的 `closed / WORKFLOW_COMPLETE` 自动生成 V4 base receipt。

本记录不制作、不复核、不批准 `BRANCH_BASE_RECEIPT`。在用户另行授权“第二步：只读制作并独立核对 V4 base receipt”前，不读取/拼接新的 base 结论，不创建或切换 V4 分支，不将当前主工作树 HEAD、部署版本或短 SHA 当作 V4 base。

## 7. 安全停止、回滚和重开边界

- 缺少精确 base receipt 时，V4 入口保持 `UPSTREAM_GATE_BLOCKED`，不得创建分支或实现。
- 缺少或冲突的 `V4_PARAMETER_RECEIPT` 时，保持 `USER_CONFIRMATION_PENDING`，不得执行相应验收或给阶段 PASS。
- provider、Secret、目标网络、DPA、采购/计费或生产授权任一缺失时，不得接入真实服务；不得以 provider-neutral 准备替代 provider-specific 证据。
- 未来若出现客户端绕过、重放重复发送、错误 host/action 通过、服务不可达仍发送、Secret 入日志、限流失效或无障碍路径绕过，必须停止并由原责任角色复核/重开；本记录不预先接受这些风险。
- 真实回滚须由有权限的平台 owner 取得独立回执；保留旧入口或文档描述不能写成真实回滚已演练。

## 8. 完成定义与唯一下一步

本 Step 完成只意味着用户确认门已对“Spec 精确字节与 provider-neutral 阶段范围”建立绑定。它不改变 `ISSUE-0032` 的 `open / USER_CONFIRMATION_PENDING`，不关闭 `ISSUE-0042`，不改变项目 `WORKFLOW_ACTIVE`。

**唯一下一步：等待用户另行授权“第二步：只读制作并独立核对 V4 base receipt”。**

在该授权到达前，本产品经理不得自行派发、创建任务/subagent、制作 base receipt、创建分支或进入实现。

## 9. 写入与保护声明

本轮只允许并将只写入：

1. 本文件：`规划文档/Spec文档/Release_version_Spec/2026-08-18-v4-issue-0032-用户确认与阶段边界冻结记录.md`；
2. 追加：`规划文档/产品经理工作记录.md`。

未修改 0032 Spec 本体、Hermes/metadata/Document QA 报告、Issue canonical/state/总表、总版本索引、其他 Spec、代码、UI、角色注册/锚点、中央注册/协同总览、平台、Secret、数据库或付费配置；未运行 npm、未执行 Git mutation、未部署、未操作平台。

写入前主工作树保护基线为：branch=`V2-unified-navigation-responsive-profile-20260729`；HEAD=`33314857da0f2d72066443965454d23fc70a16d3`；staged=`23`；Code staged=`2`；unstaged=`18`；untracked=`270`；既有 cached OID=`d00aa22eb314e5c82710388d656a2250ff482ee8`。这些既有 dirty/staged/untracked 状态不属于本次确认范围，写入后必须保持不被清理、回退、暂存或覆盖。

