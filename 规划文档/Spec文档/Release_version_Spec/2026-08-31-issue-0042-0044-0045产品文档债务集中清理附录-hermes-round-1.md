Review complete. I read the full document (142 lines) and cross-checked every disposition table against its summary counts, the identifier usage across sections, and the scope/boundary framing.

---

# 独立只读审查报告 — Round 1/3

**Metadata**
- Document: `2026-08-31-issue-0042-0044-0045产品文档债务集中清理附录.md`
- Author/owner: 产品经理 Agent v2.3.2 (`019fefa7...`)
- Status at review: `DRAFT_NON_CANONICAL / AUTHOR_DRAFT / HERMES_REVIEW_PENDING`
- Round: 1 / 3
- Reviewer model: deepseek-v4-pro
- Scope: read-only review of the sanitized copy only; no file edits, no approval claimed

**Verdict: REWORK_REQUIRED**

Two serious findings: (1) the ISSUE-0045 summary miscounts `REQUIRES_IMPLEMENTATION_OR_INDEPENDENT_REVIEW`, and (2) the identifier `N-003` is overloaded between a ledger item marked `RESOLVED_BY_DOC` and a V7 transfer/deferral item marked "not resolved", creating a real ambiguity that can flip a transfer item into "resolved" during downstream read-back.

---

## Serious Findings

### S1 — ISSUE-0045 小结 miscounts REQUIRES (3 vs 4); summary arithmetic does not sum to table rows
- **Location:** §5 小结, line 122.
- **Evidence:** Line 122 states `RESOLVED_BY_DOC=16，REQUIRES_IMPLEMENTATION_OR_INDEPENDENT_REVIEW=3，EXTERNAL_OR_USER_BLOCKED=1`. The §5 table contains four `REQUIRES_IMPLEMENTATION_OR_INDEPENDENT_REVIEW` rows: `MAC-3` (line 108), `V7-R1-O-001` (line 113), `V7-R1-O-002` (line 114), `V7-R2-OI-001` (line 120). `EXTERNAL_OR_USER_BLOCKED` = 1 (`V7-R1-O-003`, line 115) is correct.
- **Impact:** The disposition ledger's summary count is a factual error. It also fails internal consistency: 16 + 3 + 1 = 20, but the table has 21 rows. This count is the exact number downstream Issue-admin read-back relies on to size remaining receipt/review work; a wrong count mis-states the outstanding gate load for ISSUE-0045.
- **Correction:** Change `REQUIRES_IMPLEMENTATION_OR_INDEPENDENT_REVIEW=3` to `=4`; verify 16 + 4 + 1 = 21.

### S2 — Identifier `N-003` overloaded: ledger item resolved vs V7 transfer item "not resolved"
- **Location:** line 112 vs lines 96, 115, 122.
- **Evidence:** Line 112 marks `V7-R1-N-003` as `RESOLVED_BY_DOC`. Line 96 states "V7 的 N-003、N-006、N-010、N-013 继续按 transfer/延期边界处理"; line 115 states "N-003/006/010/013 transfer 不写 resolved"; line 122 states "N-003/010/013 专项证据". The token `N-003` is used both for the resolved ledger item and for a V7 transfer/deferral item, with no explicit disambiguation of the two numbering systems.
- **Impact:** The document's own core safety rule is "transfer 不写 resolved" / "不用来源 Issue 的关闭替代专项证据". A downstream reader (Issue admin doing the independent read-back) can mis-associate the resolved `V7-R1-N-003` with the deferral item `N-003`, or vice-versa, directly undermining the transfer boundary the appendix is meant to protect. Either the same item is being marked both RESOLVED and deferred (a contradiction), or two distinct items share a colliding identifier (an unaddressed ambiguity). Both require correction before the ledger is trustworthy.
- **Correction / closure trigger:** Explicitly disambiguate the numbering (e.g., prefix V7-spec items as `V7-N-003` vs ledger items as `LEDGER-R1-N-003`), and add a mapping note asserting `V7-R1-N-003 ≠ V7 的 N-003`. If they are in fact the same item, correct line 112's disposition to the transfer/deferral boundary.

---

## Non-Serious Findings

### N1 — §3 intro qualifier does not match actual coverage
- **Location:** line 38.
- **Evidence:** Says "Round 2 中属于产品/文档 owner 的 N1–N9", but the table covers all nine R2 items regardless of owner, several with `EXTERNAL_OR_USER_BLOCKED` / `REQUIRES...` (non-product-owner) dispositions.
- **Impact:** Local wording imprecision; the table itself is clear, so no material ambiguity. **Correction:** reword to "Round 2 的 N1–N9（含转交/外部项）".

### N2 — Evidence column header over-promises hash/bytes/lines
- **Location:** lines 40, 68, 98.
- **Evidence:** Header reads `证据 path:section:hash:bytes/lines`, but cells contain only `path:section` (e.g., line 42 "A42-ISSUE §3 N1；A42-REC §B；A42-CAND §3"). Hash/bytes/lines exist only at file level in §1.
- **Impact:** Per-item evidence is pinned only to file-level hash; a section could drift undetected. Weakens the appendix's own evidence-binding discipline and testability. **Correction:** either fix the header to `path:section` or add per-item hash/bytes/lines.

### N3 — Inconsistent evidence reference formatting
- **Location:** lines 69–90 (e.g., line 69 "§NON_SERIOUS N-01" vs line 70 "N-02" without `§`, and mixed subsection qualifiers across §3/§4/§5).
- **Impact:** Slightly reduces precise locatability of cited sections; no material ambiguity. **Correction:** normalize `§<subsection> <id>` format.

### N4 — §1/§3 scope framing vs mixed dispositions
- **Location:** line 5 ("仅清理产品/Spec owner 可独立处理的本地文档债务") and line 38.
- **Evidence:** Many rows are `REQUIRES...` or `EXTERNAL...`, i.e., explicitly not independently handled. The doc elsewhere clarifies these are re-classified/handed-off, so this is framing imprecision rather than a contradiction.
- **Impact:** Cosmetic. **Correction:** state the appendix "re-indexes and hands off" rather than "仅清理…可独立处理".

---

## Contradictions

1. **S1 (count):** §5 小结 `REQUIRES=3` vs four REQUIRES rows in the §5 table (16+3+1=20 ≠ 21 rows).
2. **S2 (identifier):** `V7-R1-N-003` = `RESOLVED_BY_DOC` (line 112) vs `N-003` = transfer/deferral "不写 resolved" (lines 96/115/122). Same-token, opposite disposition, unresolved.

No other material contradictions found. §3 and §4 summary counts reconcile with their tables (18 rows = 5/8/5; 22 rows = 19/3/0).

---

## Missing Acceptance Criteria

1. No measurable criterion for `RESOLVED_BY_DOC` items — i.e., what confirms the cited section actually contains the claimed definition/mapping (verification currently depends on unbound cross-reading of source files).
2. No concrete receipt artifact per `REQUIRES...` / `EXTERNAL...` item. §7 names role packages but not the specific evidence (hash, test receipt, sample, target date) that satisfies each gate.
3. No target date or sequencing for the independent read-back ("唯一下一步" names the action but no completion evidence).
4. Per-item evidence is not hash-pinned (only file-level), so section-level drift is undetectable without re-opening sources.

---

## Remediation Checklist

1. Fix §5 小结: `REQUIRES...=3` → `=4` (confirm 16+4+1=21). **[S1]**
2. Disambiguate `N-003` (and confirm `N-006/N-010/N-013`): assert distinct prefixes, or correct line 112 if same item. **[S2]**
3. Reword §3 intro qualifier to match actual R2 coverage. **[N1]**
4. Fix evidence header or add per-item hash/bytes/lines. **[N2]**
5. Normalize evidence reference formatting. **[N3]**
6. Add measurable acceptance criteria + target date for read-back. **[Missing AC]**

---

## Open-Issue List (deferred to Issue admin / owner, not blocking this doc's own fix)

- OI-1: All SHA-256 / bytes / lines claims (§1, line 27) are unverifiable in this round — referenced source files are not present in the review directory; require the Issue admin to independently re-hash.
- OI-2: §3 covers `MAC-1/3/4/5` but not `MAC-2`; confirm `MAC-2` is genuinely out of 0042 scope (or closed previously) and not silently dropped.
- OI-3: Central-table state claims (line 27: Active Open set, central SHA) need independent confirmation from the Issue 管理员.
- OI-4: Each `REQUIRES...` / `EXTERNAL...` item still lacks a named receipt owner with a target date; assign during read-back.
- OI-5: Cross-section reference integrity (`path:section` resolving to a real section) is asserted but not evidenced in this copy.

---

Verdict stands at **REWORK_REQUIRED** pending S1 and S2. These are factual/structural, not stylistic; round 2 should verify the two fixes and check for regressions in the §5 table and the V7 numbering scheme, without reopening non-serious polish items.