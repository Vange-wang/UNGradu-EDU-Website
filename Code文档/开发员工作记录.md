# 开发员工作记录

用途：持续记录程序开发员在本项目中的代码更新、代码文档更新、目录结构调整、实现功能、验证结果、遗留风险和后续优化预测。

本文件只记录开发侧工作，不替代产品 Spec、里程碑或产品经理工作记录。任何业务范围、验收标准和优先级仍以 `规划文档/Spec文档` 和 `规划文档/里程碑文档` 为准。

## 记录规则

- 每次新增、修改或删除业务代码后，必须追加记录。
- 每次调整代码目录、开发命令、测试命令、部署方式或环境变量说明后，必须追加记录。
- 每次根据 Spec 完成一个功能点后，必须写明对应的 Spec 范围和验收方式。
- 如果某项验证无法运行，必须写明原因、影响和后续补救方式。
- 不修改产品经理工作记录；如开发发现会影响产品决策的问题，应先向业务方或产品经理确认。

## 2026-06-20

### 初始化开发侧文档工作区

类型：代码文档初始化

涉及文件：

- `Code文档/README.md`
- `Code文档/开发员工作记录.md`

内容摘要：

- 确认当前仓库已有 Git 初始化，但尚未配置 GitHub remote。
- 读取当前 MVP Spec，确认本阶段代码开发必须以 Spec 为边界。
- 建立 `Code文档` 的用途、开发边界、未来代码类别规划和 Git/GitHub 管理要求。
- 建立开发员工作记录，用于后续每次代码更新后追加说明。

当前代码状态：

- 尚未新增业务代码。
- 尚未初始化 Next.js、TypeScript、React、Tailwind CSS 或 CloudBase 相关代码。
- 尚未配置本地开发、构建、测试和部署命令。

后续预测：

- GitHub remote 确认后，应配置远程仓库并建立代码分支工作流。
- 技术验证完成后，应初始化 Next.js + TypeScript 项目结构。
- 项目初始化后，应补充安装、启动、构建、类型检查、测试和环境变量说明。
- 第一批代码应优先围绕 M1：登录、主页身份入口、个人页和联系方式管理。

验证结果：

- 本次只新增代码文档，无业务代码可运行。
- 未运行构建、类型检查或测试命令，因为当前尚未创建代码项目和 package 配置。

### 初始化 M1 代码骨架

类型：代码初始化

涉及文件：

- `Code文档/package.json`
- `Code文档/package-lock.json`
- `Code文档/app`
- `Code文档/features/profile/contact-profile.ts`
- `Code文档/tests/contact-profile.test.ts`
- `Code文档/tsconfig.json`
- `Code文档/vitest.config.ts`
- `Code文档/next.config.ts`
- `Code文档/.eslintrc.json`
- `Code文档/.gitignore`
- `AGENTS.md`

内容摘要：

- 初始化 Next.js + TypeScript + React 代码项目。
- 建立首页、登录页、个人页、联系方式管理页、规则页、反馈页、需求广场、家教信息广场和发布入口占位页。
- 使用 TDD 先写联系方式管理校验测试，再实现 `validateContactProfileInput`。
- 联系方式管理当前只完成前端本地校验；真实保存、登录态和授权展示后续必须接 CloudBase 后端 API。
- 更新本地开发命令和代码目录说明。

当前实现范围：

- 对应 Spec M1 的产品骨架、身份入口、个人页面和联系方式管理入口。
- 联系方式手机号必填、微信号选填，并校验中国大陆 11 位手机号格式。
- 未实现真实手机号验证码登录、CloudBase 登录态、数据库保存和后端权限校验。

验证结果：

- `npm test`：通过，1 个测试文件，3 个测试。
- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm run build`：通过，生成 13 个静态页面。

已知风险：

- `npm audit --audit-level=moderate` 当前报告 Next.js 内部 `postcss` 相关 moderate advisory；npm 建议的自动修复会破坏性降级 Next，不采用。后续应跟进 Next.js 可用补丁版本。
- 开发环境安装时 `eslint-config-next` 的间接依赖提示 Node `20.19+`，本机为 `20.18.0`；当前 lint/build/test 未受阻，后续建议升级 Node 到 20.19+ 或 22 LTS。

### 修补 M1 代码验收阻塞项

类型：代码审查修复

依据文档：

- `规划文档/里程碑文档/2026-06-20-m1-代码验收审查-复核版.md`

涉及文件：

- `Code文档/app/login/page.tsx`
- `Code文档/app/page.tsx`
- `Code文档/app/profile/contact/page.tsx`
- `Code文档/app/profile/parent-needs/page.tsx`
- `Code文档/app/profile/tutor-profiles/page.tsx`
- `Code文档/app/profile/chats/page.tsx`
- `Code文档/features/auth/test-auth.ts`
- `Code文档/features/auth/use-test-session.ts`
- `Code文档/features/auth/guarded-entry-link.tsx`
- `Code文档/features/profile/contact-profile-storage.ts`
- `Code文档/lib/storage.ts`
- `Code文档/lib/memory-storage.ts`
- `Code文档/tests/auth.test.ts`
- `Code文档/tests/contact-profile-storage.test.ts`

内容摘要：

- 修复 P0：登录页从静态表单改为可交互测试登录页。
- 在开发环境默认启用临时测试登录；生产环境必须显式配置 `NEXT_PUBLIC_ALLOW_TEST_LOGIN=true` 才允许测试登录。
- 本地测试验证码固定为 `000000`，仅用于 M1 验收和本地开发。
- 登录成功后写入本地测试登录态，并支持 `next` 参数跳转。
- 修复 P0：联系方式管理增加本地测试存储的保存与读取能力，刷新后可从浏览器存储读回。
- 修复 P2：首页“我要找家教”“我要做家教”入口增加登录态守卫；未登录时进入登录页，登录后回到目标入口。
- 修复 P2：补齐个人页下 `/profile/parent-needs`、`/profile/tutor-profiles`、`/profile/chats` 三个占位页，避免 404。

当前实现边界：

- 登录与联系方式持久化仍是 M1 本地/测试环境方案，不是正式 CloudBase 手机号短信登录。
- 所有正式权限、数据归属、联系方式授权展示仍必须在后续 CloudBase 后端 API 中实现。

验证结果：

- `npm test`：通过，3 个测试文件，10 个测试。
- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm run build`：通过，生成 16 个静态页面。

### 修复 M1 再次审查安全闸门

类型：代码审查修复

依据文档：

- `规划文档/里程碑文档/2026-06-20-m1-代码再次审查-复核版.md`

涉及文件：

- `Code文档/features/auth/test-auth.ts`
- `Code文档/features/auth/require-test-session.tsx`
- `Code文档/features/profile/contact-profile-storage.ts`
- `Code文档/app/page.tsx`
- `Code文档/app/profile/page.tsx`
- `Code文档/app/profile/contact/page.tsx`
- `Code文档/tests/auth.test.ts`
- `Code文档/tests/contact-profile-storage.test.ts`

内容摘要：

- 修复 P0：生产环境强制禁用临时测试登录，不再允许 `NEXT_PUBLIC_ALLOW_TEST_LOGIN=true` 覆盖生产限制。
- 更新测试：覆盖 `production + allowTestLogin=true` 仍返回 `false` 的安全要求。
- 收口 P1：个人页和联系方式管理页均要求存在测试登录态；未登录访问会跳转到登录页并携带 `next` 参数。
- 收口 P1：联系方式本地测试存储 key 增加当前测试手机号，避免同一浏览器中不同测试账号互相覆盖联系方式。

当前实现边界：

- 临时测试登录仍仅服务于 M1 本地/测试验收。
- 正式上线前仍必须替换为合规手机号短信登录和后端可信登录态。

验证结果：

- `npm test`：通过，3 个测试文件，12 个测试。
- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm run build`：通过，生成 16 个静态页面。
- `NODE_ENV=production NEXT_PUBLIC_ALLOW_TEST_LOGIN=true npm run build`：通过，生产模拟构建不会被该环境变量重新启用临时测试登录。
- 产物复核：未发现旧的 `return"development"...||"true"` 风险分支。

### 初始化开发侧身份交接文档

类型：代码文档更新

涉及文件：

- `Code文档/钦定代码开发员.md`
- `Code文档/开发员工作记录.md`

内容摘要：

- 参考 `规划文档/钦定产品经理.md` 的交接功能，新增开发侧新会话衔接文档。
- 明确代码开发员身份、职责边界、必须读取的核心文档、当前代码状态、代码目录分类、本地开发命令、GitHub 状态和验证要求。
- 明确开发员不得擅自修改产品经理工作记录、Spec、里程碑、产品迭代和技术验证文档。
- 补充 M2 双边发布的下一步开发建议，强调不得提前扩展到聊天、联系方式交换、支付、评价或审核后台。

当前实现边界：

- 本次只新增代码侧交接文档，不改变产品 Spec、里程碑或业务功能。
- 未修改 `规划文档/产品经理工作记录.md` 或 `规划文档/钦定产品经理.md`。

验证结果：

- 文档内容已用 UTF-8 读取确认。
- 本次为 Markdown 文档更新，无业务代码、类型检查、测试或构建变更。

## 2026-06-21

### 启动 M2 双边发布开发

类型：业务代码开发

依据文档：

- `规划文档/Spec文档/2026-06-19-mvp-家教对接平台-spec.md`
- `规划文档/里程碑文档/M1阶段验收报告/2026-06-21-m1-阶段代码复核审查报告.md`
- `Code文档/钦定代码开发员.md`

涉及文件：

- `Code文档/features/parent-needs/parent-need.ts`
- `Code文档/features/parent-needs/parent-need-storage.ts`
- `Code文档/features/tutor-profiles/tutor-profile.ts`
- `Code文档/features/tutor-profiles/tutor-profile-storage.ts`
- `Code文档/app/parent-needs/new/page.tsx`
- `Code文档/app/tutor-profiles/new/page.tsx`
- `Code文档/app/profile/parent-needs/page.tsx`
- `Code文档/app/profile/tutor-profiles/page.tsx`
- `Code文档/app/globals.css`
- `Code文档/tests/parent-need.test.ts`
- `Code文档/tests/parent-need-storage.test.ts`
- `Code文档/tests/tutor-profile.test.ts`
- `Code文档/tests/tutor-profile-storage.test.ts`
- `Code文档/开发员工作记录.md`

内容摘要：

- 按 TDD 增加 M2 家长需求发布与大学生家教信息发布的校验测试和本地存储测试。
- 新增家长需求校验：必填字段、科目、学段、预算区间、时间段、地址层级、小区或村、孩子简介 100 字以内、孩子简介不得包含手机号或微信号、具体位置不得填写门牌号。
- 新增大学生家教信息校验：性别、学校、专业、可教科目、可教学段、可上课时间段、课时费区间、能力说明、可选证明图片类型和大小。
- 新增按测试手机号隔离的本地测试存储：`parent_needs` 和 `tutor_profiles` 对应的 M2 数据不会在不同测试账号之间互相覆盖。
- 将 `/parent-needs/new` 和 `/tutor-profiles/new` 从占位页替换为可交互发布表单。
- 将 `/profile/parent-needs` 和 `/profile/tutor-profiles` 从占位页替换为当前测试账号的本地发布记录列表。
- 补充表单、复选项、课时费区间和发布记录列表的基础样式。

当前实现范围：

- 对应 Spec M2：家长可发布结构化需求；大学生可发布家教信息；大学生可填写课时费区间和能力说明；证明图片入口可选。
- 当前仍沿用 M1 本地测试登录和浏览器本地存储，只用于开发与验收演示。
- 证明图片当前仅保存文件名、类型和大小，未上传到 CloudBase 存储。

当前实现边界：

- 未接入 CloudBase 数据库、云函数、云托管 API 或 CloudBase 存储。
- 未实现正式手机号短信登录、后端可信登录态、后端数据归属校验和越权防护。
- 未实现 M3 双边广场筛选、详情页、M4 聊天、联系方式交换、支付、评价、人工审核后台或自动推荐。
- 联系方式仍不得在需求、家教信息或列表中展示。

验证结果：

- `npm test`：通过，7 个测试文件，26 个测试。
- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm run build`：通过，生成 16 个静态页面。

已知风险与后续建议：

- M2 本地存储只能验证前端发布闭环，不能替代后端权限、数据归属和持久化能力；后续进入 CloudBase 接入时必须补接口和权限测试。
- 大学生证明图片入口当前是本地文件选择入口，未完成真实上传和访问权限控制；后续接 CloudBase 存储时必须限制文件类型、大小、所属用户和访问授权。
- 反馈页仍未补真实可点击反馈渠道；M1 复核报告已建议在 M2/M3 期间跟踪。

### 修复 M2 阶段代码验收 P1 阻塞项

类型：代码审查修复

依据文档：

- `规划文档/里程碑文档/M2阶段验收报告/2026-06-21-m2-阶段代码验收审查报告.md`

涉及文件：

- `Code文档/features/parent-needs/parent-need.ts`
- `Code文档/app/parent-needs/new/page.tsx`
- `Code文档/tests/parent-need.test.ts`
- `Code文档/开发员工作记录.md`

内容摘要：

- 修复 P1-1：家长需求发布的孩子简介为空时不再允许发布。
- 修复 P1-2：孩子简介中出现类似 `3 栋 1201 室` 的详细门牌地址时不再允许发布。
- 为以上两个阻塞项补充单元测试，先确认测试失败，再修复校验逻辑并确认测试通过。
- 发布表单的孩子简介文本框同步增加 `required`，但最终校验仍以 `validateParentNeedInput` 为准。

当前实现边界：

- 本次只修复 M2 家长需求发布校验，不新增 M3 双边广场、详情页、聊天、联系方式交换、支付、评价或审核后台能力。
- `Code文档/app/profile/parent-needs/page.tsx` 的空值条件渲染保留为防御性展示；空孩子简介已在源头校验层被拦截。

验证结果：

- `npm test -- parent-need`：通过，2 个测试文件，9 个测试。
- `npm test`：通过，7 个测试文件，28 个测试。
- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm run build`：首次运行因本项目此前残留的 `next dev` 和超时残留 `next build` 进程占用 `.next` 导致 124 秒超时；停止本项目相关 Node 进程后复跑通过，生成 16 个静态页面。
