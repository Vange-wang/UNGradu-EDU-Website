# 后 0033 分阶段执行草案：数据安全与自主内容管理

> 文档状态：DRAFT_NON_CANONICAL / QA_DOCUMENT_REWORK_COMPLETE（Round 1 SERIOUS 已修订，待 Round 2）  
> 任务 ID：POST-0033-STAGED-SPEC-AUDIT-20260809  
> 责任角色：产品经理 v2.3.0（019fad1b-0006-74f3-9b38-ae71e6464ad4）  
> 形成日期：2026-08-09；当前项目状态：WORKFLOW_ACTIVE  
> 实施授权：未授权；本稿不授权代码、测试、迁移、部署、平台配置或 Issue 状态修改
> 覆盖范围：ISSUE-0031、ISSUE-0032、ISSUE-0034、ISSUE-0035、ISSUE-0036、ISSUE-0020；
> ISSUE-0033 仅作为已完成的前置基线

本稿在 ISSUE-0033 已完成并关闭后，提出 0031、0032、0034、0035、0036 与
0020 的分阶段执行顺序。它不是联合 Spec 的替代品，不覆盖或删除既有 canonical
文件，也不把推荐方案写成已确认的供应商、预算、地域、凭据、法律口径或生产事实。
只有在基线冻结、业务方确认受影响决策并完成适用文档门禁后，才可将本稿冻结为实施依据。

## 1. 审计结论与合法基线

### 1.1 11CB、5B 与 Round 3 的证据关系

| 项目 | 可核对事实 | 判断 |
| --- | --- | --- |
| 11CBF1E4CA2523153136C92EB3567B81FAB3175EDA6B4A2EFE5127921D3C3004 | Document QA、总负责人连续性、工作总览和 Round 2 source SHA 均引用该值；记录将其描述为 Round 1 修订后的 canonical | D1-D8 决策更新前的既有基线摘要。当前工作树没有找到其逐字节正文，不能在本轮重建 |
| 5B59796EA52A55F5F23E9C46A029A04F4E250A6A92A5BCA4F0D7C5D7BE58344E | 当前联合 Spec 实测 SHA-256=该值，38619 字节、361 行；正文包含 D1-D8 决策更新 | 当前可读取的决策后 Spec，也是 Round 3 的实际输入 |
| Round 3 sidecar | metadata 实际存在：round=3/3、model=deepseek-v4-pro、source_sha256=5B、report_sha256=73B2、canonical_source_unchanged=true、exit_code=0 | 证明 Round 3 已对 5B 执行并正常退出，不应重跑第四轮 |
| Round 3 report | report SHA=73B2BE5BDB4192A866F0F7B36EE20314B50E105EB3665E0100A5622E8C026E3B，verdict=PASS_ZERO_ISSUES，0 SERIOUS/0 NON_SERIOUS | 只读原件；正文仍写“当前轮输入 SHA 待总负责人冻结”，与 sidecar 不一致，不能单独作为 binding 来源 |

结论：现有证据足以把 5B 识别为“D1-D8 修改后、Round 3/3 已审查通过的
reviewed canonical candidate”。它不是对 11CB 字节的重建，而是有实际文件、
sidecar、报告摘要和 canonical_source_unchanged 证据支持的后续版本。binding 来源
固定为 sidecar 加总负责人/联合 Spec owner 的 append-only freeze 记录；Round 3
报告正文与 sidecar 均为只读原件，不能回写或以新 hash 覆盖。总负责人/联合 Spec
owner 仍需记录 11CB 为前基线、5B 为决策后基线，登记 5B 路径/字节数/SHA、
Round 3 3/3、report/metadata SHA、冻结时间与责任角色，并以 append-only 勘误
说明正文 pending 字段的历史瑕疵、权威取值和不重跑理由。无法形成该 binding 记录
时，G0 保持 `PROVENANCE_PENDING`，不得进入后续实现冻结；这不需要也不允许重新审查。

Round 3 已完成，不能重跑、不能改写为 2/3、不能增加 Round 4。若本独立草案将来
接受审查，它是新文档周期，不能冒充旧联合 Spec 的第四轮；旧计数不得静默归零。

### 1.2 当前可合法启动的最小输入

在同步前允许：只读核对 5B/11CB provenance、建立差距清单、形成决策矩阵和本草案。
本稿已进入独立 Hermes/Document QA 周期；Round 1 的完整 SERIOUS 批次已修订，仍需
业务方确认本稿范围以及 D4/D6/D7 的实施前决策，并由总负责人发起 Round 2/3。此前
禁止后续 Issue 的代码、迁移、生产写入、Cloudflare/CloudBase 配置和 Issue 状态修改。

### 1.3 已解决、未解决、外部阻塞

| 范围 | 状态与已解决项 | 可阶段化工作 | 仍不可假定 |
| --- | --- | --- | --- |
| 0033 | closed / WORKFLOW_COMPLETE；052 发布/编辑/列表/聊天/联系方式正常；D2 lifecycle 13/13；cleanup exact4；audits=5 retained、targets=0、mutation=0 | 仅作为后续不可破坏的回归基线 | 不重新改写 0033 或把其证据扩大到后续 Issue |
| 联合 Spec | R1 两个 SERIOUS 已 QA 修订；R2=0 SERIOUS；R3 对 5B=PASS_ZERO_ISSUES | owner freeze/连续性对账 | 无需第四轮，不可将未同步当作生产授权 |
| 0031 | USER_CONFIRMATION_PENDING，0033 前置已清除 | 存储盘点、候选矩阵、备份/恢复演练设计 | MySQL 供应商/地域/预算/RPO/RTO 未定 |
| 0032 | USER_CONFIRMATION_PENDING | 服务端信任边界、官方测试 key 隔离、限流设计 | 最终供应商、hostname/action/TTL/IP 保留、成本未定 |
| 0034 | USER_CONFIRMATION_PENDING；不承诺绝对安全 | 威胁/资产/控制差距盘点 | D7 owner/频率/保留期/阈值未量化 |
| 0035 | NON_BLOCKING_DOCUMENT_REVIEW，N-001..N-015 待台账对账 | 对账和引用修复建议 | 不自动开启新的旧 Spec Round 3 |
| 0036 | USER_CONFIRMATION_PENDING、独立范围 | 选项、字段分类、隐私影响稿 | 供应商/模型/OCR/阈值/拦截语义未定 |
| 0020 | EXTERNAL_BLOCKED | 外部 owner 只读核对和风险/回滚材料 | 不并入 0034；凭据轮换、专用账号、回滚和业务风险接受仍缺 |

## 2. 目标、非目标和角色

### 2.1 目标

逐阶段提升数据持久化、验证码入口和全站安全控制的可验证性；让每个结论分别拥有
本地、集成、预生产、生产和业务方证据；对删除、联系方式、未成年人数据、备份、
密钥和不可逆操作设置审批、观察和回滚门。

### 2.2 非目标

不实现代码、SQL、CloudBase/Cloudflare、邮箱、AI/OCR 或生产迁移；不改变 0033
关闭结论，不修改 Issue canonical，不收口 0020；不承诺绝对安全、固定供应商/成本/
地域/法律结果，不并入支付、积分、担保交易或资质审核。

### 2.3 责任

业务方确认供应商、成本、地域、RPO/RTO、保留、阈值、生产窗口和风险接受；PM 维护
Spec/规则/记录并作独立产品验收；开发/迁移负责人只在冻结后实现；技术/UI/安全
角色独立复核；运维负责备份、密钥、部署、监控和回滚；总负责人/Issue 管理员负责
路由和收口。任何角色不得代替另一角色的正式通过。

## 3. 方案比较与推荐顺序

### 3.1 方案 A：严格串行

5B freeze → 0031 → 0032 → 0034；0035 独立非阻塞维护，0036 独立门禁；0020 独立。追溯和回滚最清晰，
但只读调研也排队，交付最慢。

### 3.2 方案 B：只读并行、写入串行（推荐）

完成 5B freeze 后，并行做 0031 数据盘点、0034 威胁盘点和 0032 供应商中立信任
边界；D4/D6/D7 冻结后按 0031 → 0034 → 0032 一次只开一个有副作用
窗口。0036 等业务确认；0020 由外部 owner 独立推进。这样提前取得决策证据，又不
让迁移、密钥和全站策略写入互相污染；必须严格标注“只读调研”与“实施授权”。

### 3.3 方案 C：安全控制优先

5B freeze → 0034 → 0032 → 0031。风险控制更早，但数据流/日志/备份会受迁移影响，
可能重复验证；D7 未量化时容易反复。当前不推荐。

推荐图：G0（5B freeze，不重跑 R3）→ R0 并行（0031/0032/0034 只读）→ 业务冻结
D4/D6/D7 → S1 0031 → S2 0034 → S3 0032。0035 移出关键路径并独立维护，0036 另行门禁，
0020 保持 EXTERNAL_BLOCKED。0033 是已完成前置，不授权任何后续代码。

## 4. 通用门禁、环境、分支和证据

### 4.1 不可并行和回滚

canonical freeze、业务确认和适用文档审查不可被实现绕过。生产迁移、删除、密钥
切换、验证码供应商切换和全站策略收紧各自需要审批窗口、备份 receipt、停止条件、
回滚入口和审计；0031、0032、0034 的生产写入不得同窗。只读调研可并行，但必须
标注环境、时间、责任人、输入 hash 和零写入声明。

### 4.2 环境与配置

开发、测试、预生产、生产使用不同项目/数据库、认证主体、密钥引用、日志访问和
备份目标；测试只用合成或脱敏数据。变量只列名、用途、owner，真实值不入仓库：

| 变量名 | 用途 | owner |
| --- | --- | --- |
| DEV_DB_URL、TEST_DB_URL、PREPROD_DB_URL、PROD_DB_URL | 环境数据库连接引用 | 平台/迁移 |
| DB_CA_CERT_REF、DB_POOL_MAX、DB_CONNECT_TIMEOUT_MS | TLS、池和超时 | 平台/迁移 |
| MIGRATION_LOCK_TIMEOUT_MS、WRITE_FREEZE_WINDOW_ID | 迁移锁和短写窗口 | 迁移 |
| BACKUP_TARGET_REF、RESTORE_ROLE_REF、BACKUP_RETENTION_POLICY | 备份/恢复/保留 | 运维 |
| TURNSTILE_SITE_KEY、TURNSTILE_SECRET_REF、CHALLENGE_EXPECTED_HOSTNAMES、CHALLENGE_EXPECTED_ACTIONS、CHALLENGE_TTL_SECONDS | 挑战参数和密钥引用 | 认证/平台 |
| CHALLENGE_IP_RETENTION_MODE、EMAIL_PROVIDER_ENDPOINT、EMAIL_PROVIDER_KEY_REF | IP 最小化和邮件引用 | 认证 |
| EMAIL_RATE_LIMIT_POLICY、LOGIN_RATE_LIMIT_POLICY、CONTACT_RATE_LIMIT_POLICY | 组合限流 | 风控 |
| SESSION_COOKIE_POLICY、SESSION_SECRET_REF、CSRF_ORIGIN_ALLOWLIST、CSP_POLICY_REF | 会话/CSRF/CSP | 安全 |
| SECURITY_ALERT_SINK_REF、AUDIT_LOG_REDACTION_POLICY、OBSERVATION_DASHBOARD_REF | 告警/脱敏/观察 | 安全/运维 |
| CLOUDFLARE_ZONE_REF、WORKER_ROUTE_REF、ORIGIN_ALLOWLIST_REF | 0020 边界引用 | Cloudflare owner |

### 4.3 分支和版本

沿用用户冻结的 Vx-分支正名-日期；只有代码大类切换才提出新分支并先询问业务方。
本稿阶段不创建或切换分支。版本号由业务方部署时给出，不能预填虚构生产版本；
证据同时记录部署标识、候选 commit、构建时间和 provenance。

### 4.4 统一验收证据

每条标准记录责任角色、环境、时间、输入 hash、步骤、预期/实际、截图或 DOM/API/
日志/审计/备份 receipt、失败和清理结果。产品通过不等于技术或业务通过，缺证据
保持 PENDING/BLOCKED。

### 4.5 D4/D6/D7 实施前决策门

D4、D6、D7 是独立的实施前门，不能用“倾向”“优先评估”或业务沉默代替冻结。
下表的值域是可测试的最小安全骨架或保守默认，不是供应商、预算、地域、凭据、
法律或业务已确认值；业务方和技术 owner 可以收紧，不能放宽后仍声称满足本稿。
具体个人尚未登记时只写责任角色，由总负责人补登记，不虚构姓名。

| 决策门 | 决策 owner / 证据 owner | 必须冻结的变量 | 建议值域或保守默认（非已确认） | 冻结证据与最迟点 | 无法冻结时 |
| --- | --- | --- | --- | --- | --- |
| D4 数据存储 | ISSUE-0031 实施/迁移负责人；平台/备份负责人提供证据 | provider、region、预算上限、RPO、RTO、容量、短写冻结时长、备份保留、单源切换、退出方案 | MySQL/PostgreSQL/保留现状候选；默认不采购、不迁移；建议 RPO ≤15 分钟、RTO ≤60 分钟、短写目标 5 分钟且硬上限 15 分钟、对账 100%、数据丢失 0、迁移后至少 72 小时观察 | 候选矩阵、业务确认、脱敏备份恢复 receipt、迁移计划与输入 hash；预生产演练或采购/生产写入前 | 保持现有存储和行为，停止迁移/采购；若已部分切换回滚旧单源，不得猜测填值 |
| D6 人机验证 | ISSUE-0032 认证/邮箱验证负责人；安全/平台复核负责人提供证据 | provider、hostname/action allowlist、token TTL、一次性消费、验证超时、IP 传输/保留、限流、无障碍、成本、轮换 | Turnstile/等价服务候选；TTL 建议 30–300 秒、默认 120 秒；服务端超时建议 1–5 秒、默认 2 秒；hostname/action 精确匹配；建议账号 3 次/15 分钟、10 次/24 小时、IP 10 次/15 分钟，均待确认；无效/超时/不可达一律不发送 | 服务端契约、供应商选择、隔离 key/域名、失败注入、限流矩阵、无障碍/隐私确认、配置 hash；绑定供应商或生产配置前 | 不启用新集成；缺任一值时 fail-closed、不发验证码、不得前端勾选绕过；超过期限终止本轮接入 |
| D7 安全运营 | ISSUE-0034 安全/平台负责人；运维监控负责人提供证据 | control register owner、审查/演练频率、日志/审计/备份保留、P0/P1 阈值、告警升级、降级/停止语义、未成年人和联系方式最小字段 | owner 必须是实名角色；建议每月权限/日志复核、每季度恢复/事件演练；法律/业务确认前仅建议 operational log 30–90 天、audit 180–365 天、backup 30 天值域；认证绕过、对象越权、联系方式/secret 泄露零容忍；建议检测 ≤5 分钟、确认 ≤15 分钟、缓解 ≤60 分钟 | 控制差距矩阵、owner/频率登记、保留/最小化确认、告警测试、恢复/回滚 receipt、生产观察报告；预生产策略收紧或安全配置写入前 | 保持既有控制，不上线新收紧规则；缺 owner/阈值/保留不得标记安全通过，进入延期或终止 |

每个决策门的冻结记录必须包含确认者、日期、输入 hash、取值及可复现证据。不能
冻结不等于自动放宽：只读调研和回滚准备可以保留，生产写入、采购、密钥切换和
策略收紧必须停止。D4/D6/D7 任一未冻结时，G0 之外的 S1/S2/S3 只能保持准备态。

## 5. G0：5B canonical freeze/连续性同步

**目标**：登记 5B 为 D1-D8 决策后的 reviewed candidate，保留 11CB 前基线，并以
sidecar + 总负责人/联合 Spec owner 的 append-only freeze 记录建立唯一 binding；
Round 3 报告正文和 sidecar 均保持只读，不回写、不覆盖 hash、不重新调用 Hermes。

**依赖/不可并行**：总负责人或联合 Spec owner 完成一次 append-only freeze/勘误记录；
PM 不代改其连续性文件。勘误至少引用 11CB 前基线、5B 路径/字节数/SHA、Round 3
report/metadata SHA、冻结时间、责任角色、权威取值和不重跑声明。
**失败/回滚**：来源或 5B 字节变化时保持候选、保留 R1/R2/R3/sidecar，不覆盖旧证据。

| 层级 | 可观察标准 | 责任与证据 |
| --- | --- | --- |
| 本地 | 5B 路径、38619 字节、361 行、SHA 与 R3 sidecar/report SHA 可复读；原报告/sidecar 未被改写 | 文档 owner；文件、hash、metadata、只读原件 |
| 集成 | R1/R2/R3 计数一致；R3=3/3、PASS_ZERO_ISSUES、0/0；append-only freeze 明确 sidecar 为 binding，并解释正文 pending 字段 | QA/总负责人；ledger、原报告、sidecar、勘误记录 |
| 预生产 | 后续草案绑定冻结 5B，无未授权代码/配置 | 总负责人/独立 QA；门禁清单 |
| 生产 | 本阶段零生产变更 | 运维；零写入声明 |
| 业务方 | 确认 5B 是后续基线，D4/D6/D7 仍是实施前门 | 业务方；日期确认 |

交付物为 canonical provenance 记录、append-only 勘误/绑定说明、门禁清单和零写入
声明；观察期不适用，因为本阶段不改运行系统。完成定义：唯一入口、来源、字节数、
3/3、报告/sidecar hash、权威取值、勘误 owner 和下一动作可审计；不新增或重跑轮次。
若 binding 记录缺失，状态固定为 `PROVENANCE_PENDING`，不得把候选当基线。最迟
业务确认点：任何后续实现授权前。

## 6. ISSUE-0035 独立非阻塞文档维护

0035 保持 `NON_BLOCKING_DOCUMENT_REVIEW`，不属于 G0、R0 或 D4/D6/D7 的关键路径，
不阻塞 0031/0032/0034 的只读调查、决策门冻结、实施、验收或收口。逐项对齐 N-001
至 N-015 与联合 Spec、R1/R2/R3、QA ledger 和引用时，标注原位置、影响、owner、
证据和状态；它不自动开启旧 Spec 第四轮、不改 0033 状态、不授权实现。可以独立
只读维护，但不得把其未完成或对账结果当作 S1/S2/S3 的完成条件。

发现实质矛盾时保留原件并记录差异，不删除历史；只有另行形成明确的严重发现和
授权包时才进入新的文档周期，不能因 0035 对账自动重开旧 Spec Round 3。

本地验收为 ledger 可重放；集成验收为可选地引用 G0 hash 且不把非阻塞项当完成事实；
预生产和生产均零变更；业务方确认不扩大范围或明确新的文档周期。交付物为 N 项
对账表、受影响章节索引、非阻塞清单和 owner 签名；观察期不适用。责任人为 PM/
ISSUE 管理员/Document QA（各自只维护授权文件），证据为 ledger、hash 和带日期确认。
误标记可回退上一 ledger 版本；最迟业务确认点不早于相关后续实现 Spec 冻结前，
但不得成为该冻结的硬前置。

## 7. S1：ISSUE-0031 SQL 数据库迁移

### 7.1 范围和决策门

目标是安全迁移支持 0033 所有权、版本、软删除、聊天和联系方式门控的数据。非目标
是不重做 0033、不并行实施 0032/0034、不删除历史。候选矩阵如下：

| 候选 | 收益 | 代价/风险 | 必须确认 |
| --- | --- | --- | --- |
| MySQL | 关系约束、事务、团队倾向、托管选择多 | 供应商/地域/适配/停机/成本未知 | 供应商、地域、预算、RPO/RTO、停机容忍、备份责任 |
| PostgreSQL | 约束、事务、查询和扩展能力强 | 运行时适配、运维和托管成本未知 | 同上及兼容性/人员证据 |
| 保留 CloudBase/等价存储 | 迁移风险和初期运维低 | 约束、事务、长期成本和退出方案需证据 | 数据规模、并发、事务边界、供应商承诺 |

D4 仅“倾向 MySQL”，不是最终选型。D5 已确认“经演练的短写冻结 + 单源切换”，
不默认双写。最终供应商/地域/预算/RPO/RTO/容量和恢复目标是实施前决策门；在 D4
未冻结前只允许只读盘点、候选矩阵和恢复演练设计，不采购、不迁移、不写生产。

### 7.2 数据、步骤、失败和回滚

盘点 users、parent_needs、tutor_profiles、conversations、messages、contact_requests、
audit records 及引用；逐字段标注所有者、公开/私密/未成年人敏感级别、deletedAt、
version、updatedAt、UTC 时区、保留期。主键不可枚举，外键/唯一/状态/版本条件更新/
审计关联必须可证明；字符集支持中文和精确比较。

只读盘点 → 结构/约束映射 → 脱敏抽样对账 → 备份/恢复演练 → 集成迁移/回滚 →
预生产短写冻结/单源切换 → 审批生产窗口 → 逐集合计数/摘要对账 → 观察期。锁等待、
池耗尽、版本冲突、摘要不一致、权限改变、恢复失败或延迟超阈值即停止，维持旧单源，
按演练恢复，不能人工补写掩盖差异。日志只写脱敏 ID/集合/版本/错误类别；迁移账号
最小权限；0033 的 48 小时恢复与 D2 聊天/联系方式门控不变。

### 7.3 分层验收、责任和最迟确认

* 本地：schema、主键/外键/唯一、版本更新、UTC、中文、失败回滚；开发/迁移负责人，
  测试和 schema hash。
* 集成：脱敏数据逐集合数量/摘要/权限/聊天交换关联一致，restore 成功；技术复核/
  运维，migration 对账和 restore receipt。
* 预生产：冻结、单源切换、连接池/延迟、备份恢复和回滚可重复；迁移负责人，窗口/
  监控/回滚日志。
* 生产：审批窗口完成备份、对账、观察和有效回滚；运维/独立技术，生产日志/告警。
* 业务：确认供应商、地域、预算、RPO/RTO、停机、保留并验收本人管理/聊天/隐私；
  业务方/PM，日期确认和生产操作记录。

生产迁移不得与 0032 密钥切换或 0034 全站收紧同窗。交付物为选型矩阵、schema/
约束映射、备份与恢复 receipt、迁移/对账报告、监控面板、回滚脚本和生产观察报告。

**S1 最小定量技术骨架（建议安全默认，非业务已确认）**：迁移前可恢复备份通过
`1/1`；逐集合计数、摘要、主键/版本/所有权/聊天和交换关联一致率 `100%`；数据
丢失、未授权写入、重复记录均为 `0`；RPO `≤15` 分钟、RTO `≤60` 分钟；短写冻结
目标 `≤5` 分钟且硬上限 `15` 分钟；迁移相关 5xx 建议 `<0.5%`、p95 延迟回归
`≤20%`；幂等重试不得重复记录。生产切换后至少观察 `72` 小时，期间 Sev1 数据
事件 `0`、权限回归 `0`。D4 只能收紧这些建议值，不能放宽后仍标记通过；任一零
容忍项失败或无法恢复时为 `TECH_REWORK_REQUIRED`，停止切换并回滚旧单源。

完成定义拆为两层：技术 owner 和独立技术复核依据上述骨架及证据标记
`TECH_VERIFIED`；业务方另行确认供应商、地域、预算、RPO/RTO、停机、保留及本人
管理/聊天/隐私路径后才可标记 `BUSINESS_ACCEPTED`。业务未签收不自动通过，具体
超时升级和失败传播见 §13。

## 8. S2：ISSUE-0034 全站安全基线

目标是可复现威胁模型和控制证据；非目标是绝对安全、重写 0033、吸收 0020 或先假定
法律结论。必须覆盖认证/回调、IDOR/对象权限、输入输出、CSRF/XSS/CSP、SQL/命令/
模板注入、SSRF、secret、Cookie/会话、速率/滥用、依赖供应链、日志脱敏/告警、
备份恢复、Cloudflare/Worker/CloudBase/源站隔离、隐私/未成年人、删除保留、DoS/
容量。0020 保持独立。

D7 方向为明确 owner、演练频率、数据最小化，但 owner、频率、保留期、阈值、告警
升级和降级语义必须量化。建议先做控制差距矩阵，再由业务确认高风险拒绝、会话
异常、验证码绕过、速率异常阈值，日志/备份/联系方式最小字段与保留期，以及演练
频率和响应时限。误拒、告警风暴、容量下降或错误率升高时回退配置/功能开关并保留
审计，不关闭全部防护。

**S2 最小定量技术骨架（建议安全默认，非业务已确认）**：P0/P1 安全发现、认证
绕过、IDOR、对象越权、联系方式或 secret 明文泄露均为 `0`；高风险负向场景执行率
`100%` 且全部 fail-closed；告警建议检测 `≤5` 分钟、确认 `≤15` 分钟、缓解
`≤60` 分钟；备份恢复演练通过 `1/1`；安全策略导致的 5xx 建议 `<0.5%`。策略
切换后至少观察 `72` 小时真实流量，期间 P0/P1 和未授权披露均为 `0`。D7 只能
收紧这些建议值；零容忍项失败时为 `TECH_REWORK_REQUIRED`，回滚安全配置/功能
开关并保留审计。

验收：本地负向认证/401/404/403、注入/SSRF、Cookie/CSP、脱敏和依赖；集成多角色/
删除恢复/过期/限流/告警 fail-closed；预生产边界、备份恢复、容量/DoS 和回滚；
生产窗口、观察期、owner、停止条件、无 secret/联系方式泄露；业务确认 D7 精确值、
失败文案、隐私最小化、误拒和响应。交付物为威胁模型、控制差距矩阵、配置快照、
负向测试、告警/备份/回滚报告和业务风险记录；观察期至少为上述 72 小时，具体门槛
由 D7 确认时只能收紧。责任分别为开发/安全、独立安全、平台/运维、业务方；证据为
测试/扫描、API/日志、流量/恢复、生产告警和确认记录。全站收紧不得与 0031 迁移同窗；
最迟业务确认点为预生产收紧前。技术证据齐全后标记 `TECH_VERIFIED`，业务方明确
确认 D7 后才标记 `BUSINESS_ACCEPTED`；业务未响应不自动通过。

## 9. S3：ISSUE-0032 人机验证服务端强制

前置输入为 G0 冻结、D6 供应商中立边界、邮箱发送现状和限流/无障碍需求；非目标
是不以客户端勾选代替服务端校验、不提前绑定生产供应商、不改变 0033。只读信任
边界设计可并行，生产 key 切换与 0031 迁移、0034 全站收紧不可并行。

浏览器 challenge 仅产生待验证 token；服务端验证签名/状态、一次性、时效、预期
hostname、action 和上下文成功后才可发送邮箱验证码。前端勾选或客户端状态不授权。
缺失、伪造、过期、重复、hostname/action 不符、超时、服务商不可达、密钥缺失或
风险不确定均 fail-closed，用统一不泄露风控细节的文案；账号/IP/设备/会话/动作
仍组合限流。IP 采用业务确认的最小化模式；无障碍替代也必须服务端验证和限流。

D6 采用 Turnstile/等价服务优先评估、服务端强制、fail-closed、最小 IP 方向；最终
供应商、hostname/action、TTL、IP 保留、无障碍、成本和轮换频率仍由 D6 冻结。官方
测试 key、生产 key、域名和环境隔离。误判/延迟/不可达/发送失败超阈值时停止切换并
回退上一配置，不能绕过挑战；轮换采用双钥短窗、撤销和审计。

**S3 最小定量技术骨架（建议安全默认，非业务已确认）**：缺 token、伪造、过期、
重复、错误 hostname/action、验证超时或服务不可达时邮箱发送为 `0`；有效 token
至多发送 `1` 次；绕过率为 `0`；业务方批准的合成语料误拒建议 `≤2%`；服务端
verify p95 建议 `≤2` 秒且硬超时 `≤5` 秒；限流负向用例执行率 `100%` 且全部阻断。
至少观察 `72` 小时发送稳定窗口；流量不足时，须完成批准的合成/集成样本和全部
零容忍负例后仍保持生产观察。D6 只能缩短 TTL/超时、收紧限流或降低误拒上限；
任一零容忍项失败时为 `TECH_REWORK_REQUIRED`，新 gate fail-closed，不发验证码，
不能回退为前端勾选绕过。

验收：本地所有伪造/过期/重复/不符/超时均不发送且成功只消费一次；集成真实测试
服务验证 challenge→server verify→send、限流、文案、无障碍、IP 最小化；预生产
独立 key/域名、邮箱 sink、故障注入、轮换、回滚、告警；生产审批窗口观察通过/
拒绝/发送/误拒，故障 fail-closed；业务确认供应商、TTL/IP、无障碍、成本、文案、
到达体验。交付物为服务端验证契约、供应商/test key 记录、负向测试、限流矩阵、
密钥轮换、监控和回滚报告；观察期至少为上述 72 小时，具体门槛由 D6 确认时只能
收紧。责任为开发/测试、技术/安全、平台/运维、独立复核、业务方；证据为服务端
测试、服务商响应、邮件 sink、配置/回滚 receipt、指标和确认。生产密钥切换不得与
0031/0034 同窗；最迟业务确认点为绑定供应商或写生产配置前。技术证据齐全后标记
`TECH_VERIFIED`，业务方明确确认 D6 后才标记 `BUSINESS_ACCEPTED`；业务未响应不
自动通过。

## 10. ISSUE-0036 联系方式快速智能审核（独立未来门禁，未编入 S 序列）

前置输入为业务方对检测目的、字段范围、人工介入和隐私保留的确认；非目标是把
推荐直接变成供应商或上线行为。与 0031/0032/0034 的生产写入不可并行，也不得
读取生产正文作为未授权试验数据。

目标是为需求与老师资料中的联系方式建立独立决策和后续 Spec 入口，保护 0033
所有权、聊天和删除门控。非目标是并入 0031/0032/0034/0035、选择供应商/模型/OCR/
阈值或直接上线拦截。0036 不构成 S1→S2→S3 的第四阶段，不阻塞其收口；任何未来
实现须另行形成范围、业务确认、技术门和独立验收包。

推荐选项：电话、邮箱、微信、QQ、URL 及变体的本地确定性规则作为可解释第一层；
不确定项进入人工复核；AI、OCR、自动隐藏/拒绝、申诉和误判补救由业务方确认。原文、
图片和未成年人数据最小化、隔离、脱敏、限制第三方训练和保留/删除。服务不可用、
误判或隐私未知时不静默修改原文，或按业务已确认的待审核/不可发布语义 fail-closed；
不能临时自动删除。回滚只撤销队列/检测开关。

验收：本地合成中文/英文/数字/变体可解释且不进外部日志；集成权限/队列/人工结果/
申诉/超时/审计可重放；预生产合成数据、清理、成本/延迟、回滚；生产仅在供应商、
阈值、文案、人工 SLA、保留和监控确认后小流量；业务书面确认自动/人工/误判/未成年
数据语义。当前最小动作是整理选项供确认，不实现、不部署、不改变 0033。交付物为
字段分类、规则样例、人工队列语义、隐私影响说明、成本/延迟假设和回滚草案；观察期
仅在业务方确认供应商和小流量方案后定义。最迟业务确认点为任何模型/服务采购、
生产数据发送或用户可见拦截前。

## 11. ISSUE-0020 独立外部阻塞

目标是补齐该 Issue 自身的外部证据，非目标是用 0034 或本稿代替其风险接受。外部
执行 owner 当前身份、联系窗口和可替代账号均 `UNKNOWN`；不得填写虚构姓名，也不
得由 PM、总负责人或其他内部角色冒充平台执行侧。平台变更与 0031/0032/0034 的
生产写入不可并行，读-only 资料核对可以并行。

0020 保持 open / `EXTERNAL_BLOCKED`，不由本稿或 0034 代关闭。最小剩余证据：生产
凭据轮换/吊销/审计；专用非敏感账号登录态反馈；保留/回滚入口实际可用和停止条件；
业务方残余风险接受。总负责人只负责接收、登记和转交证据，不代执行平台操作；
ISSUE 管理员继续维护状态，直到原 owner 的证据和业务风险接受齐全。

升级是计划节奏而非外部 SLA：首次请求后每 1 个工作日由总负责人复核；第 3 个工作日
仍无响应时，向已登记的业务 sponsor/owner manager 升级；三次请求或第 5 个工作日仍
无可用外部 owner 时，保持 `EXTERNAL_BLOCKED`，之后每周只读更新一次。若 sponsor、
manager 或联系渠道也未登记，只记录“无可路由输入”并暂停；不得发明替代人。外部
不可达不阻塞 0031/0032/0034 的只读准备，但不允许 0020 进入通过或借用 0034 证据
关闭。交付物为凭据轮换 receipt、专用账号反馈、回滚/停止条件报告和风险接受记录；
验收仍分本地、集成、预生产、生产和业务方五层；最迟确认点为任何再次扩大外部流量
或声明 0020 关闭前。

## 12. 数据、删除、审批、审计与回滚

迁移、删除、恢复、密钥轮换和策略收紧均需变更单、最小权限执行者、审批人、窗口、
备份 receipt、停止条件、前后计数/摘要和回滚入口。恢复演练用隔离合成/脱敏数据；
备份保留、访问、加密、销毁由业务/合规 owner 确认。0033 规则不变：deletedAt 后
48 小时可恢复；删除期间保留历史聊天但只读，禁止新消息、联系方式创建/确认/继续
或查看；恢复后重新校验，不自动披露。日志只保留最小字段、版本和结果，不记录联系
方式正文、challenge secret 或完整 token。清理必须有 exact targets、audits retained、
profiles/legacy 摘要及 mutation 证据。幂等重试不得重复发布、发送、迁移或审计。

## 13. 总验收、完成定义和门禁

S1→S2→S3 的顺序为：Spec/决策冻结 → 本地 → 集成 → 预生产 → 生产 → 独立
技术/安全/产品复核 → 业务方验收 → Issue 管理员收口。G0 是 S1/S2/S3 的 provenance
前置；0035 是独立非阻塞文档维护；0036 是独立未来门禁，不编入 S 序列，也不阻塞
S1→S2→S3。任一层级缺证据保持 `PENDING/BLOCKED`，并写责任人、最小解除条件、
恢复触发和唯一下一步。

### 13.1 TECH_VERIFIED 与 BUSINESS_ACCEPTED 双层收口

技术门和业务签收不可互相替代。下表的建议阈值是可测试骨架，非业务已确认值；D4、
D6、D7 只能收紧。技术证据齐全才能标记 `TECH_VERIFIED`，业务方明确、带日期确认
后才能标记 `BUSINESS_ACCEPTED`。

| 阶段 | `TECH_VERIFIED`（责任与证据） | `BUSINESS_ACCEPTED`（责任与证据） |
| --- | --- | --- |
| G0 provenance | Document QA/总负责人核对 source、report、sidecar hash、append-only freeze/勘误、零写入和不重跑；证据为只读原件、metadata、冻结记录 | 业务方确认 5B/本稿作为后续基线，确认 binding 规则与不重跑 Round 3；带日期确认 |
| S1 / 0031 | 迁移负责人和独立技术复核完成备份恢复、100% 对账、RPO/RTO、性能、72 小时观察、回滚和零容忍负例；证据符合 §7 最小骨架 | 业务方确认 provider/region/budget/RPO/RTO、停机、保留、本人管理/聊天/隐私路径 |
| S2 / 0034 | 独立安全/技术复核完成 P0/P1=0、负向 fail-closed、监控、恢复、72 小时观察和回滚；证据符合 §8 最小骨架 | 业务方确认 D7 owner/frequency/retention/threshold、失败文案、隐私和误拒风险 |
| S3 / 0032 | 认证/安全复核完成 token 信任边界、零绕过、限流、无障碍、轮换、72 小时观察和回滚；证据符合 §9 最小骨架 | 业务方确认 provider、hostname/action、TTL/IP、无障碍、成本、文案和到达体验 |
| 0020 | 只有外部执行 owner 提供凭据轮换、专用账号、回滚和隔离证据才可技术核验；否则保持 `EXTERNAL_BLOCKED` | 业务方明确残余风险接受；沉默不构成接受 |

业务方不响应的统一处理：T+1 个工作日提醒一次，T+3 个工作日升级到已登记的总
负责人/业务 sponsor，T+5 个工作日或三次请求后固定为 `ACCEPTANCE_PENDING`，之后
每周只读记录一次。升级频率不能代替确认，不能把 `BUSINESS_ACCEPTED` 自动写成通过，
也不能授权下一阶段生产写入。`TECH_VERIFIED` 通过而业务未签收时，只允许已批准的
只读调研、回滚准备和证据整理。

### 13.2 跨阶段失败传播

- G0 provenance 或 D4/D6/D7 任一决策门未冻结：S1/S2/S3 均不得生产实施，只保留 R0 只读准备。
- S1 技术失败：立即停止切换并回滚旧单源；冻结 S2/S3 生产写入，重新核对数据、权限和备份输入后再评估，不能自动宣称后续阶段仍有效。
- S2 技术失败：回滚安全配置或功能开关并保留审计；冻结 S3 新认证生产切换；S1 已有数据不自动回滚，除非权限或泄露影响要求 owner 决定。
- S3 技术失败：新 gate fail-closed，验证码不发送，不得以旧前端路径绕过；S1/S2 已有技术通过不被改写，但 S3 生产关闭和业务接受保持 pending。
- 业务方未签收：只产生 `ACCEPTANCE_PENDING`，不向后传播为“已通过”；0020 继续 `EXTERNAL_BLOCKED`；0035 非阻塞不传播。
- 0036 始终是独立未来决策，不成为 S1→S2→S3 的隐含 S4 或前置。

完成还须满足：无超范围代码/UI/平台变化；部署版本、commit 和 provenance 可核对；
监控/告警/观察期/回滚实际演练；隐私、未成年人、联系方式、审计、备份最小化有证据；
业务方确认影响行为、成本、停机、保留、误拒和风险；各 owner 更新自己的 canonical/
工作记录，PM 不代写。

当前本稿为 `DRAFT_NON_CANONICAL / QA_DOCUMENT_REWORK_COMPLETE`（Round 1/3 的完整
SERIOUS 批次已由 Document QA 修订，非通过结论）。没有调用 Hermes、npm、Git mutation、
部署或平台操作；不绕过联合 Spec 已完成的 Round 3/3，也不构成旧 Spec 第四轮。未通过
门禁为：G0 append-only provenance freeze 尚未落盘；D4/D6/D7 尚未由业务方冻结；S1/S2/S3
尚未实现、技术核验或业务签收；0035 仍为独立非阻塞维护；0036 仍待独立业务确认；
0020 仍 `EXTERNAL_BLOCKED`；本稿尚待 Hermes Round 2。

**唯一下一步**：总负责人核对本次修订后的 source hash，登记/确认 append-only provenance
freeze 后，发起本新草案的 Hermes Round 2/3；此前不得进入 0031/0032/0034/0036 实现
或生产变更。
