# ISSUE-0023：智能客服页增加标准返回首页箭头

## 基本信息

- Issue ID：`ISSUE-0023`
- 任务名称：智能客服标准返回首页箭头
- 类型：improvement / navigation consistency / accessibility
- 状态：`closed`
- 工作流状态：`WORKFLOW_COMPLETE`
- 优先级：P2
- 登记日期：2026-07-28
- 关闭日期：2026-07-28
- 来源：业务方在 `ISSUE-0022` 生产复测收口后的新增明确要求
- 责任角色：代码开发员（实现与开发验证） / UI 设计师（独立视觉与交互复核） / 项目总负责人（任务路由、部署与生产证据收口） / 业务方（最终验收） / ISSUE 管理员（状态维护）
- 目标页面：`/customer-service`
- resolution：生产版本 034 已部署标准返回首页箭头，生产复测与业务方最终关单确认通过

## 问题与目标

`ISSUE-0022` 已完成生产复测并关闭。业务方随后明确要求在智能客服页增加一个与登录、规则、家教信息广场、需求广场一致的标准左上角返回箭头。

本 Issue 仅追踪 `/customer-service` 的返回首页导航一致性，不重开 `ISSUE-0022`，也不复用已关闭的历史 Issue。

## 冻结实现范围

- 仅在 `/customer-service` 增加标准左上角返回箭头。
- 链接语义必须为：
  - `href="/"`
  - `aria-label="返回首页"`
- 必须复用现有 `page-back-arrow` 视觉标准及既有组件 / 样式，不新造第二套返回样式。
- 桌面端与 390px 手机端均须可见、可聚焦、可点击。

## 已通过的开发与独立 UI 门禁

- 分支：`codex/customer-service-back-arrow-20260728`。
- 提交：`644710446808a08bd80d84ba4ae452f826a360e9`；已推送，远端同 SHA，开发工作树 clean。
- 变更范围仅包含智能客服页标准 Link、定向测试、开发员记录和证据索引；未修改 CSS、其他页面、截图、构建产物或密钥。
- typecheck、lint、build 均 exit 0；相关测试 242/242 通过。
- 完整测试唯一未通过项为隔离工作树缺少运维夹具；原工作树只读复核 2/2 通过，证据证明该项与本 Issue 代码无关。
- Chrome 1920×974 与 390×844 本地复核通过：页面只有一个标准返回箭头，无水平溢出，无浅色虚框或重复边框；点击与 Enter 均返回 `/`。
- 智能客服既有快捷问题交互回归通过：`messageCount` 从 1 增至 3，`chatState=active`，`overlapPairs=0`。
- 独立 UI 线程给出正式结论 `UI_PASS`。
- 证据边界：以上均为本地实现、开发验证和本地独立 UI 复核，不代表生产已部署、生产环境复测通过或业务方最终验收通过。

## 生产版本 034 复测与业务方验收

- 生产版本：`034`。
- 生产 URL：`https://ungradeedu.eu.cc/customer-service`。
- 对应分支：`codex/customer-service-back-arrow-20260728`。
- 对应提交：`644710446808a08bd80d84ba4ae452f826a360e9`。
- 生产 Chrome 页面标题为 `UNGradu EDU`，`readyState=complete`。
- `a.page-back-arrow[href="/"][aria-label="返回首页"]` 唯一，count=1，且在生产页面可见。
- 返回箭头文本为标准 `←`；样式为浅底、黑色实线边框和黑色硬阴影，outline 为 none，未见浅色虚框或双框。
- DOM 目标 `href` 为 `/`，与同一提交在本地已通过的点击和 Enter 返回首页回归一致。
- 生产桌面记录为 `documentElement.scrollWidth=clientWidth=1583`，无水平溢出。
- 生产桌面截图人工复核通过；客服布局和快捷提问区域未见回归，控制台未发现本次功能相关错误。
- 390×844 手机端证据来自部署前同一提交的本地 Chrome 与独立 UI `UI_PASS`；生产关单包未单列新的 390px 生产截图，不将其误写为生产移动端实测。
- 业务方当前明确要求正式关闭 `ISSUE-0023`，并要求“确认 issue 关闭后”再迁移新线程；该指令作为本 Issue 的最终验收与关单授权。

## 明确非目标

- 不修改智能客服顶部说明。
- 不修改左右布局。
- 不修改聊天框尺寸。
- 不修改快捷问题。
- 不修改输入、发送、对话逻辑或文案。
- 不修改其他页面。
- 不把生产部署或业务方最终验收误计为本地实现阶段已经完成。

## 验收标准

### 本地实现与独立复核

1. `/customer-service` 左上角出现与登录、规则、家教信息广场、需求广场一致的标准返回箭头。
2. 返回箭头使用现有 `page-back-arrow` 组件 / 样式，不出现浅色虚框、外浅色底板、重复边框或第二套视觉。
3. 链接为 `href="/"`，无障碍名称为 `aria-label="返回首页"`；桌面与 390px 手机端均可见、可聚焦、可点击，点击后返回首页。
4. 智能客服顶部说明、左右布局、聊天框尺寸、快捷问题、输入发送、对话逻辑和文案保持不变。
5. 智能客服既有交互回归通过：快捷问题激活后 `messageCount` 从 1 增至 3，`overlapPairs=0`。
6. 桌面端与 390px 手机端均无水平溢出。
7. typecheck、lint、相关测试、受影响回归测试和 build 通过。
8. 独立 UI 复核明确覆盖返回箭头视觉、焦点 / 点击行为、客服冻结范围和桌面 / 手机布局，并给出通过结论。

### 最终关闭门禁

1. 代码修复具备提交与推送证据。
2. 本地实现与独立 UI 复核全部通过。
3. 修复版本完成生产部署，并在生产环境复测桌面端与 390px 手机端的返回箭头、首页跳转、无水平溢出和客服交互无回归。
4. 业务方基于生产结果明确验收通过。

## 关闭结论

- `ISSUE-0023` 已完成 `open -> in-progress -> review -> closed` 闭环，当前状态为 `closed`，工作流状态为 `WORKFLOW_COMPLETE`。
- Git 闭环、开发验证、独立 UI `UI_PASS`、同一提交本地桌面 / 390px 回归、生产版本 034 桌面复测和业务方最终关单授权均已具备。
- 返回箭头在生产页唯一、可见、语义正确、视觉符合既有标准；无横向溢出或本次功能相关控制台错误；客服布局和快捷提问区域无回归。
- 本 Issue 不再存在未通过门禁，canonical 文件归档至 `Close_Issue`。

## 关单后的唯一下一步

- 项目总负责人向业务方确认 `ISSUE-0023` 已关闭，然后按业务方指令继续新线程迁移；ISSUE 管理员不创建或迁移线程。

## 边界

- `ISSUE-0020` 为独立安全范围，本 Issue 不触碰其状态或内容。
- `ISSUE-0022` 已 `closed / WORKFLOW_COMPLETE`，本 Issue 不修改或重开其内容。
- ISSUE 管理员仅负责编号、Open / Close、总表、状态流转和关闭证据，不实现代码、不部署、不替 UI 设计师或业务方验收。
- 本次关单仅维护 Issue canonical 文件、总表和 ISSUE 管理员工作记录；不执行 Git mutation，不修改代码、不部署，不创建任务 / 线程 / subagent。
