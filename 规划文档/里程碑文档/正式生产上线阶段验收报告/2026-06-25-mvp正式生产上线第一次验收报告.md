# MVP 正式生产上线第一次验收报告

日期：2026-06-25

任务 ID：`RELEASE-RUN-2026-06-25-001-PM`

报告身份：产品经理 Agent（`019efc62-584b-79a2-90df-808f28ecefce`）

审查对象：

- GitHub 仓库：`https://github.com/Vange-wang/UNGradu-EDU-Website`
- 分支：`codex/m5-security-preflight`
- 正式 Spec：`规划文档/Spec文档/Release_version_Spec/2026-06-25-mvp-正式生产上线-spec.md`
- 执行打包说明：`规划文档/里程碑文档/正式生产上线准备/正式生产上线阶段执行打包说明.md`
- 代码侧准备说明：`规划文档/里程碑文档/正式生产上线准备/代码侧可发行版本准备说明.md`

## 1. 最终结论

本轮正式生产上线阶段第一次验收结论：**仓库内代码侧可发行候选允许进入 Claude Code 复核；正式生产上线完成仍因外部生产事项暂停。**

判断依据：

- 仓库内生产预检能力已经具备，产品经理本轮独立复跑 `npm run release:production:preflight` 通过。
- 总负责人已独立确认 `npm run typecheck`、`npm test`、`npm run lint` 通过，其中测试结果为 32 个测试文件、117 个测试通过。
- 此前 P0 构建阻塞已由代码开发员修复：保留 `next.config.ts` 中 `output: "standalone"`，新增 `Code文档/scripts/clean-next-build.mjs`，并将 `npm run build` 改为先安全清理本项目内可再生 `.next` 产物，再执行 `next build`。
- 产品经理本轮独立复跑 `npm run build` 通过，输出包含 `cleaned .next build artifacts`，Next.js 15.5.19 编译、类型检查、24 条 App 路由生成和 build traces 均完成。
- 产品经理本轮独立复跑 `npm run release:production:preflight` 通过，模拟误配 `M5_ENABLE_HOSTED_TEST_LOGIN=true` 与 `NEXT_PUBLIC_ALLOW_TEST_LOGIN=true` 时，仍确认生产拒绝测试登录、拒绝 `x-ungradu-test-user-phone`、前端测试登录入口隐藏。
- 因仓库内构建阻塞已关闭，代码侧可发行候选可以进入 Claude Code 复核。

本轮不把以下外部事项误判为代码返工：

- 生产 CloudBase 环境尚未确认。
- 生产密钥尚未配置。
- 正式登录或试运行登录方案尚未拍板。
- 白名单账号管理尚未落地。
- 生产部署、生产 URL 冒烟、回滚演练和隐私告知触达尚未完成。

这些属于项目总控制人、业务方或运维负责人必须处理的外部上线事项，不属于仓库内代码缺陷，也不影响本轮“仓库内代码侧可发行候选允许进入 Claude Code 复核”的判断。

## 2. 读取与核对材料

本轮已读取或核对：

- `规划文档/Spec文档/Release_version_Spec/2026-06-25-mvp-正式生产上线-spec.md`
- `规划文档/里程碑文档/正式生产上线准备/正式生产上线阶段执行打包说明.md`
- `规划文档/里程碑文档/正式生产上线准备/代码侧可发行版本准备说明.md`
- `Code文档/README.md`
- `Code文档/.env.example`
- `Code文档/package.json`
- `Code文档/scripts/production-readiness-check.mjs`
- `Code文档/tests/production-readiness-script.test.ts`

核对结论：

- `package.json` 已包含 `release:production:preflight` 脚本。
- `.env.example` 区分本地 / 隔离 M5 测试环境与正式生产配置，生产变量只用占位符，不包含真实密钥。
- `README.md` 已记录正式生产上线预检命令、测试登录禁用边界和真实密钥保护规则。
- `production-readiness-check.mjs` 检查生产必需变量，并验证生产环境下测试登录、临时身份头和前端测试入口保持拒绝或隐藏。
- `production-readiness-script.test.ts` 覆盖生产误配测试开关仍拒绝测试登录，以及 `APP_ENV` 非生产时预检失败。

## 3. 正式 Spec 验收标准逐项核对

| Spec 验收项 | 当前状态 | 核对说明 |
| --- | --- | --- |
| 生产环境部署完成 | 未完成 | 尚未提供正式生产 CloudBase Run 或等价生产部署证据。 |
| 生产 URL 可访问 | 未完成 | 尚未提供正式生产 URL。 |
| 环境变量清单完成并逐项确认 | 部分准备 | `.env.example` 与代码侧说明已给出变量要求，但真实生产配置需项目总控制人确认。 |
| 测试登录禁用验证通过 | 仓库内预检通过，生产 URL 未验证 | 产品经理本轮独立复跑 `release:production:preflight` 通过；正式生产 URL 下仍需复验。 |
| 生产专用 `AUTH_SESSION_SECRET` 已配置 | 未完成 | 必须由项目总控制人在生产环境配置独立强随机密钥，不得写入文档。 |
| 生产登录方案已确认 | 未完成 | 正式短信登录或受控试运行登录方案仍需项目总控制人确认。 |
| 试运行时间窗口和退出条件已确认 | 未完成 | Spec 默认建议首轮 30 天，但实际窗口与退出条件需项目总控制人确认并写入上线材料。 |
| 种子用户名单或识别方式已确认 | 未完成 | 需业务方或项目总控制人确认名单、维护主体和审批流程。 |
| 生产核心流程冒烟通过 | 未完成 | 依赖生产 URL、生产账号和真实运行环境。 |
| 联系方式授权保护验证通过 | 测试阶段已有依据，生产 URL 未验证 | M5 和测试托管阶段已有验证；正式生产仍需 URL 下复验。 |
| 非参与者越权访问验证通过 | 测试阶段已有依据，生产 URL 未验证 | M5 和测试托管阶段已有验证；正式生产仍需 URL 下复验。 |
| 反馈入口可用 | 测试阶段已有依据，生产 URL 未验证 | 正式生产 URL 下仍需确认。 |
| 登出入口可用 | 测试阶段已有依据，生产 URL 未验证 | 正式生产 URL 下仍需确认。 |
| 基础隐私说明已展示或已向受邀用户提供 | 未完成 | 需产品 / 业务方形成并触达受邀用户。 |
| 回滚预案完成 | 未完成 | 需结合生产提交、镜像、CloudBase Run 版本和回滚执行人补齐。 |
| 回滚路径至少完成桌面演练或真实演练 | 未完成 | 依赖生产部署和回滚负责人。 |
| 上线说明完成 | 未完成 | 需基于正式试运行口径输出。 |
| 测试账号管理说明完成 | 未完成 | 需说明 M5 测试账号、本地开发账号、试运行账号边界。 |
| 技术债清单更新，包含公开列表 1000 条上限 | 已在 Spec 和执行打包说明中明确 | 后续仍需在正式上线准备文档或 vNext 清单中保留。 |
| 上线验收报告归档 | 本报告为第一次审查，不是上线完成报告 | 仓库内代码侧允许复核，但生产上线完成验收仍待外部事项完成。 |
| 产品经理确认进入生产试运行 | 未确认 | 代码侧可进入 Claude Code 复核；生产试运行仍需生产环境、密钥、登录方案、生产冒烟、回滚和隐私告知等外部事项完成。 |

## 4. 代码侧验证结果

### 4.1 产品经理本轮独立复验

执行目录：

```text
D:\codex_project\家教对接website\Code文档
```

#### `npm run release:production:preflight`

执行命令：

```powershell
$env:APP_ENV='production'
$env:AUTH_SESSION_SECRET='replace-with-production-strong-random-secret'
$env:CLOUDBASE_ENV_ID='replace-with-production-cloudbase-env-id'
$env:TENCENTCLOUD_SECRETID='replace-with-production-secret-id'
$env:TENCENTCLOUD_SECRETKEY='replace-with-production-secret-key'
$env:M5_ENABLE_HOSTED_TEST_LOGIN='true'
$env:NEXT_PUBLIC_ALLOW_TEST_LOGIN='true'
npm run release:production:preflight
```

结果：通过。

关键输出：

```text
release production preflight
- required production variables checked without printing values
- warning: M5_ENABLE_HOSTED_TEST_LOGIN=true must be removed from real production configuration.
- warning: NEXT_PUBLIC_ALLOW_TEST_LOGIN=true must be removed from real production configuration.
- temporary test login rejected in production
- x-ungradu-test-user-phone rejected in production
- front-end test login entry hidden in production
release production preflight passed
```

产品判断：

- 仓库内生产预检脚本符合正式 Spec 的测试登录禁用主线。
- 该命令不替代正式生产 URL 冒烟和真实生产环境变量检查。

#### `npm run build`

历史失败记录：

- 产品经理此前第一次执行失败，输出停在：

```text
Generating static pages (0/24) ...
```

- 产品经理此前第二次执行失败，明确错误为：

```text
uncaughtException [Error: EPERM: operation not permitted, open 'D:\codex_project\家教对接website\Code文档\.next\trace']
```

- 总负责人后续独立验证也确认 build 失败，exit 1；第一次 build 在 standalone trace copy 阶段出现多处 `ENOENT`，删除可再生构建产物 `.next` 后干净重跑仍失败；干净构建关键错误为 `.next/types/cache-life.d.ts` 在类型检查阶段被报告找不到，随后文件又可见。

修复方式：

- 代码开发员保留 `next.config.ts` 中 `output: "standalone"`。
- 新增 `Code文档/scripts/clean-next-build.mjs`。
- 将 `npm run build` 改为先安全清理本项目内可再生 `.next` 产物，再执行 `next build`。

产品经理本轮复验结果：通过。

关键输出：

```text
> ungradu-edu-website@0.1.0 build
> node scripts/clean-next-build.mjs && next build

cleaned .next build artifacts
Compiled successfully
Generating static pages (24/24)
Collecting build traces ...
```

产品判断：

- P0 构建阻塞已修复并通过产品经理复验。
- 构建修复未改变正式 Spec 的业务范围。
- 代码侧构建门槛已满足，可以进入 Claude Code 复核。

### 4.2 总负责人提供的独立验证状态

根据总负责人重新送达任务中的输入：

| 命令 | 总负责人提供状态 | 本报告处理方式 |
| --- | --- | --- |
| `npm run release:production:preflight` | 通过 | 产品经理已独立复跑并确认通过 |
| `npm run typecheck` | 通过 | 作为总负责人独立验证证据引用 |
| `npm test` | 32 个测试文件、117 个测试通过 | 作为总负责人独立验证证据引用 |
| `npm run lint` | 通过 | 作为总负责人独立验证证据引用 |
| `npm run build` | 修复后通过；总负责人和产品经理均已取得通过证据 | P0 构建阻塞已关闭 |

## 5. 必须项目总控制人处理项

以下事项不是代码开发员可单独完成的仓库内任务，但都是正式生产上线完成前的阻塞项：

| 事项 | 当前状态 | 下一步 |
| --- | --- | --- |
| 生产 CloudBase 环境 | 未确认 | 项目总控制人创建或确认生产环境，并记录环境 ID 的脱敏信息。 |
| 生产 CloudBase Run 服务 | 未确认 | 项目总控制人或运维负责人创建服务并确认部署权限。 |
| 生产密钥 | 未配置到本报告可验证范围 | 在云控制台或受控环境变量中配置，不写入 Git 或文档正文。 |
| 生产 `AUTH_SESSION_SECRET` | 未确认 | 使用生产专用强随机密钥，不得复用测试密钥。 |
| 正式登录 / 试运行登录方案 | 未确认 | 明确正式短信登录，或受邀手机号白名单 + 运营确认机制。 |
| 白名单账号管理 | 未完成 | 确认维护主体、存储位置、新增 / 移除审批流程。 |
| 生产部署 | 未完成 | 部署正式生产版本，记录提交、镜像和运行环境。 |
| 生产 URL 冒烟 | 未完成 | 在生产 URL 下验证首页、登录、发布、筛选、聊天、联系方式交换、权限拒绝、反馈、登出。 |
| 回滚演练 | 未完成 | 完成桌面演练或真实演练，明确回滚执行人和验收步骤。 |
| 隐私告知触达 | 未完成 | 基础隐私说明需展示给受邀用户，或通过试运行说明触达并记录。 |

## 6. 不判为代码返工的外部缺口

以下缺口属于正式生产上线外部准备事项，本轮不判为代码开发员返工：

- 生产 CloudBase 环境尚未创建或确认。
- 生产密钥尚未配置。
- 正式登录方案尚未确定。
- 白名单账号管理尚未确定。
- 生产 URL 尚未提供。
- 生产冒烟尚未执行。
- 回滚演练尚未执行。
- 隐私告知尚未触达。

这些事项会阻止“生产试运行上线完成”结论，但不等同于仓库内代码实现偏差。

## 7. 仍需项目总控制人处理项

仓库内代码侧可发行候选已允许进入 Claude Code 复核。正式生产试运行上线完成前，仍需项目总控制人、业务方或运维负责人补齐：

1. 生产 CloudBase 环境与 CloudBase Run 服务。
2. 生产独立强随机 `AUTH_SESSION_SECRET`。
3. 生产 `TENCENTCLOUD_SECRETID` / `TENCENTCLOUD_SECRETKEY` 的配置方式。
4. 正式登录或受控试运行登录方案。
5. 白名单手机号维护主体、存储位置、新增 / 移除审批流程。
6. 正式生产部署版本、镜像和生产 URL。
7. 生产 URL 下核心流程冒烟、联系方式保护验证和非参与者越权验证。
8. 回滚桌面演练或真实演练。
9. 基础隐私说明对受邀用户展示或触达记录。

这些事项不属于仓库内代码返工项，但它们仍阻止“正式生产试运行上线完成”结论。

## 8. 阶段结论

本轮结论：**仓库内代码侧可发行候选允许进入 Claude Code 复核；正式生产上线完成仍因外部生产事项暂停。**

代码侧判断：

- P0 构建阻塞已修复并通过复验。
- 生产预检通过。
- 总负责人已验证 typecheck、test、lint 通过。
- 仓库内代码侧可发行候选允许进入 Claude Code 复核。

外部上线判断：

- 生产部署、生产 URL 冒烟、生产密钥、正式登录方案、白名单、回滚演练和隐私告知触达尚未完成。
- 这些事项应由项目总控制人统筹，不误判为代码开发员返工。
- 在上述外部事项完成前，不能宣布正式生产试运行上线完成。

下一步：

1. 进入 Claude Code 复核，复核代码侧可发行候选与本报告判断。
2. 项目总控制人继续推进生产环境、密钥、登录方案、白名单、生产部署、生产 URL 冒烟、回滚演练和隐私告知触达。
3. 生产 URL 可用后，再发起正式生产上线完成验收。
