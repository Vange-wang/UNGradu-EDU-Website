# AGENTS.md

本项目是“家教对接 website”的产品与研发协作仓库。当前阶段以产品规划、需求澄清、MVP 定义和交付验收标准为第一优先级；在 Spec 文档确认前，不应贸然进入业务代码开发。

## Subagent 创建限制（最高优先级）

1. 默认在当前主线程内完成任务。不得自行创建、启动、派发或调用临时 subagent、并行 agent 或后台 agent。
2. 只有以下任一条件成立时才可使用 subagent：
   - 用户在当前任务中明确要求使用 subagent；
   - 任务属于特别庞大的项目架构、系统拆分或同等级复杂工作，当前负责人先说明使用必要性、拟创建数量、职责边界和影响范围，并向用户申请，且用户明确批准。
3. 仅提到角色、分工、并行、负责人、专业线程或验收链路，不等于授权创建 subagent。
4. 已有固定角色线程、旧线程或历史 subagent 不自动获得调用授权；需要使用时仍须满足上述条件。
5. 未获授权时，即使并行可能更快，也必须由当前主线程直接完成，或如实报告规模和限制。

本节高于本文件其他关于多线程协作、角色路由和任务分发的默认规则。

## Vange_workflow 强制执行（最高优先级）

1. 项目总负责人启动、接续或恢复任何正式 workflow 前，必须调用 `$vange-workflow`，按其状态机、注册线程路由、职责隔离、验收门禁、Issue 闭环和会话连续性规则执行。
2. workflow 一经启动即进入 `WORKFLOW_ACTIVE`。负责人必须持续推进 Spec、实现、验证、产品/技术/UI 复核、返工、Issue 闭环、部署或生产证据和业务方验收，直到全部必需门禁通过。
3. Spec、代码、提交、本地测试、构建、阶段报告或一次复核完成，均只是中间状态；`有条件通过`、线程空闲、工具失败、上下文压缩、业务方反馈和追加要求也不是停止指令。
4. 只有业务方明确说“停下来”“暂停”“停止跟踪”“取消任务”或同等含义时，才允许进入 `USER_PAUSED` 或取消；不得从沉默、状态询问或阶段完成推断停止许可。
5. 阻塞不是完成。必须保持 `WAITING_ROLE`、`UPSTREAM_GATE_BLOCKED` 或 `EXTERNAL_BLOCKED`，严格区分本地、集成、生产和业务方验收，记录责任人、最小解除条件、恢复触发条件和唯一下一步。
6. 总负责人只负责调度、持续跟进、证据检查、门禁和收口，不得越权替产品经理、代码开发员、UI、ISSUE 或独立复核角色完成正式产物；返工必须回到原注册线程。
7. 固定角色工作必须使用已核验的注册线程 ID；不得把临时 subagent 当固定角色，不得因持续推进而绕过本文件的 Subagent 创建限制、只读线程规则、隐私或生产权限边界。
8. 会话迁移前必须落盘已完成事项、不可变约束、活动线程 ID、未通过门禁、Open Issue、外部阻塞和唯一下一步，进入 `SESSION_RELAY_REQUIRED` 后继续。
9. 只有全部产物、验证、独立验收、阻塞 Issue、生产证据和业务方门禁通过后才可报告 `WORKFLOW_COMPLETE`。

## 默认工程流程：Matt Pocock Skills

- 需求不清用 `grill-with-docs`；多阶段实现走 `to-spec` → `to-tickets` → `implement`。
- 小型明确改动直接用 `implement`，测试驱动用 `tdd`，Bug 用 `diagnosing-bugs`。
- 代码审查用 `code-review`，默认在当前线程串行完成。
- 架构治理用 `improve-codebase-architecture` 与 `codebase-design`；特别庞大且路径不清时用 `wayfinder`。
- 不确定入口时用 `ask-matt`。上述流程不得绕过 Subagent 创建限制。

## 角色分工

- 产品经理：负责追问业务目标、拆解用户角色、定义 MVP、编写 Spec、里程碑、产品迭代和验收标准。
- 客户/业务方：负责确认业务规则、服务范围、交易模式、人工介入边界、合规要求和优先级。
- 开发者：仅在 Spec 和里程碑明确后进入实现，不自行扩展未经确认的业务范围。

## 项目当前状态

- 当前仓库尚未包含业务代码。
- 当前已存在规划目录：`规划文档/Spec文档`、`规划文档/里程碑文档`、`规划文档/技术验证`、`规划文档/产品迭代`。
- 当前已存在代码文档目录：`Code文档`。
- 当前技术栈尚未确认，后续应由 Spec 或技术验证文档明确。
- 当前 Git 仓库尚未初始化，GitHub 仓库信息尚未确认。

## 核心业务

本项目暂定为家教对接平台，用于连接有补课需求的学生/家长与可提供教学服务的老师。最终业务范围必须以 `规划文档/Spec文档` 中的正式 Spec 为准。

在 Spec 确认前，以下问题不得默认：

- 平台是否负责担保交易、在线支付或只负责线索撮合。
- 老师是否需要资质审核、实名认证、试讲或平台人工审核。
- 学生/家长是否可以直接联系老师。
- 平台是否提供排课、课时记录、评价、退款、投诉处理。
- 首个版本是否面向单城市、全国、线上课或线下课。

## 文档优先级

当文档之间出现冲突时，按以下顺序判断：

1. `规划文档/Spec文档`：定义要做什么、为什么做、验收标准是什么。
2. `规划文档/里程碑文档`：定义分阶段交付范围、MVP 边界、每阶段验收标准。
3. `规划文档/产品迭代`：记录后续版本、暂缓需求、优化方向。
4. `规划文档/技术验证`：记录技术选型、可行性验证、风险结论。
5. `Code文档`：记录代码结构、接口、部署、运维等实现层说明。
6. `AGENTS.md`：定义协作规则和仓库工作方式。

## 重要目录职责

- `规划文档/Spec文档`：放置产品 Spec。每个重大功能或版本应有独立 Spec，文件名建议使用 `YYYY-MM-DD-功能名-spec.md`。
- `规划文档/里程碑文档`：放置 MVP 和阶段性交付计划，说明每个节点交付什么、不交付什么、如何验收。
- `规划文档/技术验证`：放置技术可行性调研、第三方服务验证、风险验证和结论。
- `规划文档/产品迭代`：放置版本规划、待办池、后续优化、用户反馈和优先级调整。
- `Code文档`：业务代码产生后，用于说明代码结构、模块职责、本地开发、部署和接口约定。

## 需求推进流程

1. 先确认产品目标：谁用、解决什么问题、为什么现在要做。
2. 再确认最小 MVP：只保留打通核心闭环所必需的功能。
3. 然后输出 Spec：包括背景、用户角色、核心流程、功能范围、非功能要求、验收标准。
4. 再输出里程碑：先做最小闭环，再逐步补运营、审核、支付、评价等能力。
5. 最后进入技术验证和开发：技术选择必须服务于已确认的 Spec。

## MVP 原则

MVP 必须先打通完整业务链路，而不是做大量精致但孤立的页面。

推荐的最小闭环是：

1. 家长/学生提交找老师需求。
2. 老师提交个人资料和可教科目。
3. 平台能看到需求和老师资料。
4. 平台能进行初步匹配或人工推荐。
5. 家长/学生能表达意向或留下联系方式。
6. 平台能记录一次成功撮合的状态。

在未确认前，支付、复杂排课、合同、自动推荐算法、积分会员、复杂后台权限都应视为后续迭代，不进入最小 MVP。

## 本地开发步骤

当前业务代码位于 `Code文档` 目录。代码框架为 Next.js + TypeScript + React。

## Codex 权限运行规则

本项目使用多个 Codex 线程协作时，必须先确认当前线程的运行权限。

- 如果当前线程显示 `approval_policy: on-request`、`sandbox_policy: read-only`，说明该线程是受限/只读运行环境。
- 受限线程不得继续执行写文件、`git fetch`、`git push`、`npm install`、`npm run build` 等会触发审批或网络/写入的任务。
- 需要修改代码、更新文档、提交、推送或运行完整验证时，优先切换到用户当前已选择“完全访问”的前台线程执行。
- 如果必须由某个角色线程继续执行，先让用户打开该线程并重新选择“完全访问”，再继续任务。
- 不要主动用提升权限请求绕过该限制；避免让用户在同一任务中反复批准。

进入代码目录：

```powershell
Set-Location Code文档
```

依赖安装命令：

```powershell
npm install
```

本地启动命令：

```powershell
npm run dev
```

构建命令：

```powershell
npm run build
```

测试命令：

```powershell
npm test
```

类型检查命令：

```powershell
npm run typecheck
```

代码检查命令：

```powershell
npm run lint
```

环境变量配置方式：

- 当前尚未接入 CloudBase 和短信服务，暂不需要真实环境变量。
- 后续新增 `.env.example` 说明必需变量。
- 任何真实密钥不得提交到 Git 仓库。

常见故障处理：

- 如果 npm 安装提示 Node 版本警告，优先升级到 Node 20.19+ 或 22 LTS。
- 如果 npm audit 提示 Next.js 内部依赖风险，不要直接执行会降级框架的 `npm audit fix --force`，应先确认 Next.js 是否已有兼容补丁版本。

## 改完代码后的验证要求

当前已引入 Next.js + TypeScript 技术栈。开发者每次改动后至少应运行：

- 类型检查。
- 单元测试或组件测试。
- 构建检查。
- 与改动相关的端到端流程验证。

当前对应命令：

```powershell
Set-Location Code文档
npm run typecheck
npm test
npm run build
```

如涉及页面、样式或路由，也应运行：

```powershell
npm run lint
```

如果某项检查无法运行，必须在交付说明中写明原因和风险。

## 编码偏好

代码尚未创建前，先遵守以下项目偏好：

- 业务术语必须与 Spec 保持一致，不要在代码里创造新的同义词。
- 页面、接口、数据模型命名要围绕真实业务对象，例如老师、学生、家长、需求、匹配、咨询、课时。
- 优先实现清晰、可验证的业务闭环，不优先追求复杂抽象。
- 不把后续迭代能力提前硬塞进 MVP。
- 任何涉及钱、身份、联系方式、未成年人信息、审核、投诉的逻辑都必须在 Spec 中先写清楚。

## 测试约定

当前测试框架尚未确定。未来选择技术栈后应补充：

- 单元测试框架。
- 组件或页面测试框架。
- 端到端测试框架。
- 测试文件放置位置。
- 哪些改动必须补测试。

原则上，以下场景必须加测试：

- 用户注册、登录、资料提交。
- 需求发布和老师资料发布。
- 匹配状态变化。
- 表单校验。
- 权限控制。
- 支付、退款、联系方式展示等高风险逻辑。

## 重要设计约定

- Spec 是验收依据，不以口头描述替代正式文档。
- 每个里程碑都必须写清楚“做什么”和“不做什么”。
- MVP 阶段优先人工可运营，不急于自动化。
- 涉及未成年人、联系方式和交易的功能必须谨慎设计，默认需要审核、隐私保护和风控说明。
- 产品流程应先追求可用和可信，再追求复杂推荐和增长玩法。

## 环境变量

当前尚无代码和运行环境，必要环境变量待技术验证后确认。未来可能包括：

- 数据库连接字符串。
- 身份认证密钥。
- 文件存储配置。
- 短信或邮件服务密钥。
- 支付服务密钥。
- 地图或地理位置服务密钥。
- 管理后台初始账号配置。

任何真实密钥不得提交到 Git 仓库。

## Git 与 GitHub

仓库初始化后应遵守：

- 主分支建议使用 `main`。
- 每个明确任务使用独立分支。
- 提交信息应说明用户可见变化或文档变化。
- Spec、里程碑、技术验证等规划文档应与代码一起纳入版本管理。
- 在 GitHub 仓库创建前，需要确认仓库名称、可见性以及是否需要 README、license、issue 模板。
- 产品经理负责的文件和文档完成修改后，应及时提交并推送到 GitHub；提交前只暂存产品经理本次负责的文档，不暂存无关代码、构建产物、缓存、环境变量或他人改动。

## 给后续 Agent 的工作规则

- 不要在需求未确认时直接写业务代码。
- 如果用户表达模糊，先追问，且每次只问一个关键问题。
- 能从仓库文件确认的信息不要重复问用户。
- 修改规划文档时，要保留历史决策依据，不要只留下结论。
- 产品经理每次确认需求、变更决策、修订 Spec/里程碑/技术验证/产品迭代/上下文文档后，必须同步更新 `规划文档/产品经理工作记录.md`。
- 产品经理必须遵守 `规划文档/钦定产品经理.md` 中的“产品经理 v1.1 标准操作”：先读上下文文档、明确角色边界、执行后同步工作记录、必要时更新钦定文档和 AGENTS、只暂存负责文件、及时提交并推送 GitHub。
- 新增代码或技术栈后，必须同步更新本文件的开发、测试、验证和环境变量部分。

## Vange_workflow vNext 强制增补（最高优先级，2026-07-22）

1. 正式 workflow 启动、接续或恢复前，必须重新读取当前全局 Skill：`C:\Users\86166\.codex\skills\vange-workflow\SKILL.md`，不得只凭旧会话记忆执行。
2. 强制执行 `THINK_BEFORE_ACTING`。充分思考是结合当前问题和证据作出的定性判断，严禁用百分比、分数、思考时长或勾选数量作为行动阈值。
3. 在修改、派发、正式结论或高成本审查前，必须充分考虑目标与非目标、事实来源、约束、依赖、方案取舍、长期影响、失败路径、安全/隐私、回滚和验收证据。仍有可能实质改变决策的未知项时不得行动。
4. workflow 一经启动即保持非终态，持续推进至全部适用产物、验证、独立验收、阻塞 Issue 和用户门禁通过。只有用户明确要求暂停、停止或取消时才能停止。
5. 一个职责对应一个稳定登记任务和一个写入 owner。总负责人只调度、跟踪、检查证据、控制门禁和收口，不得代做专业角色产物；不得未经授权创建新任务或 subagent。
6. 关键文档必须先形成完整连贯草案，再使用临时脱敏副本进行 Hermes CLI `deepseek-v4-pro` 审查；不得修改 Hermes 默认模型。
7. 同一任务、规范文档和冻结范围最多三轮 Hermes 审查，首轮计入且任何修改或换任务均不重置。首轮整体审查，后续仅复核严重修订及受影响回归，禁止逐轮提出低价值新问题。
8. `SERIOUS` 问题交独立文档 QA 一次性批量整改；措辞、风格、可选增强等 `NON_SERIOUS` 项登记为非阻塞 Open Issue。严重问题为零即可 `PASS_ZERO_ISSUES` 或 `PASS_WITH_NONBLOCKING_OPEN_ISSUES`。
9. 第三轮后仍有严重问题必须进入 `DOCUMENT_REVIEW_LIMIT_REACHED`，禁止自动第四轮，由用户决定风险接受、范围调整或新周期。
10. 当前用户明确指令和更严格的项目规则优先；阻塞不是完成，持续推进不得扩大权限。
