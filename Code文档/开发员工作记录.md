# 开发员工作记录

用途：持续记录程序开发员在本项目中的代码更新、代码文档更新、目录结构调整、实现功能、验证结果、遗留风险和后续优化预测。

本文件只记录开发侧工作，不替代产品 Spec、里程碑或产品经理工作记录。任何业务范围、验收标准和优先级仍以 `规划文档/Spec文档` 和 `规划文档/里程碑文档` 为准。

## 记录规则

- 每次新增、修改或删除业务代码后，必须追加记录。
- 每次调整代码目录、开发命令、测试命令、部署方式或环境变量说明后，必须追加记录。
- 每次根据 Spec 完成一个功能点后，必须写明对应的 Spec 范围和验收方式。
- 如果某项验证无法运行，必须写明原因、影响和后续补救方式。
- 不修改产品经理工作记录；如开发发现会影响产品决策的问题，应先向业务方或产品经理确认。

## 2026-06-20

### 初始化开发侧文档工作区

类型：代码文档初始化

涉及文件：

- `Code文档/README.md`
- `Code文档/开发员工作记录.md`

内容摘要：

- 确认当前仓库已有 Git 初始化，但尚未配置 GitHub remote。
- 读取当前 MVP Spec，确认本阶段代码开发必须以 Spec 为边界。
- 建立 `Code文档` 的用途、开发边界、未来代码类别规划和 Git/GitHub 管理要求。
- 建立开发员工作记录，用于后续每次代码更新后追加说明。

当前代码状态：

- 尚未新增业务代码。
- 尚未初始化 Next.js、TypeScript、React、Tailwind CSS 或 CloudBase 相关代码。
- 尚未配置本地开发、构建、测试和部署命令。

后续预测：

- GitHub remote 确认后，应配置远程仓库并建立代码分支工作流。
- 技术验证完成后，应初始化 Next.js + TypeScript 项目结构。
- 项目初始化后，应补充安装、启动、构建、类型检查、测试和环境变量说明。
- 第一批代码应优先围绕 M1：登录、主页身份入口、个人页和联系方式管理。

验证结果：

- 本次只新增代码文档，无业务代码可运行。
- 未运行构建、类型检查或测试命令，因为当前尚未创建代码项目和 package 配置。

### 初始化 M1 代码骨架

类型：代码初始化

涉及文件：

- `Code文档/package.json`
- `Code文档/package-lock.json`
- `Code文档/app`
- `Code文档/features/profile/contact-profile.ts`
- `Code文档/tests/contact-profile.test.ts`
- `Code文档/tsconfig.json`
- `Code文档/vitest.config.ts`
- `Code文档/next.config.ts`
- `Code文档/.eslintrc.json`
- `Code文档/.gitignore`
- `AGENTS.md`

内容摘要：

- 初始化 Next.js + TypeScript + React 代码项目。
- 建立首页、登录页、个人页、联系方式管理页、规则页、反馈页、需求广场、家教信息广场和发布入口占位页。
- 使用 TDD 先写联系方式管理校验测试，再实现 `validateContactProfileInput`。
- 联系方式管理当前只完成前端本地校验；真实保存、登录态和授权展示后续必须接 CloudBase 后端 API。
- 更新本地开发命令和代码目录说明。

当前实现范围：

- 对应 Spec M1 的产品骨架、身份入口、个人页面和联系方式管理入口。
- 联系方式手机号必填、微信号选填，并校验中国大陆 11 位手机号格式。
- 未实现真实手机号验证码登录、CloudBase 登录态、数据库保存和后端权限校验。

验证结果：

- `npm test`：通过，1 个测试文件，3 个测试。
- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm run build`：通过，生成 13 个静态页面。

已知风险：

- `npm audit --audit-level=moderate` 当前报告 Next.js 内部 `postcss` 相关 moderate advisory；npm 建议的自动修复会破坏性降级 Next，不采用。后续应跟进 Next.js 可用补丁版本。
- 开发环境安装时 `eslint-config-next` 的间接依赖提示 Node `20.19+`，本机为 `20.18.0`；当前 lint/build/test 未受阻，后续建议升级 Node 到 20.19+ 或 22 LTS。

### 修补 M1 代码验收阻塞项

类型：代码审查修复

依据文档：

- `规划文档/里程碑文档/2026-06-20-m1-代码验收审查-复核版.md`

涉及文件：

- `Code文档/app/login/page.tsx`
- `Code文档/app/page.tsx`
- `Code文档/app/profile/contact/page.tsx`
- `Code文档/app/profile/parent-needs/page.tsx`
- `Code文档/app/profile/tutor-profiles/page.tsx`
- `Code文档/app/profile/chats/page.tsx`
- `Code文档/features/auth/test-auth.ts`
- `Code文档/features/auth/use-test-session.ts`
- `Code文档/features/auth/guarded-entry-link.tsx`
- `Code文档/features/profile/contact-profile-storage.ts`
- `Code文档/lib/storage.ts`
- `Code文档/lib/memory-storage.ts`
- `Code文档/tests/auth.test.ts`
- `Code文档/tests/contact-profile-storage.test.ts`

内容摘要：

- 修复 P0：登录页从静态表单改为可交互测试登录页。
- 在开发环境默认启用临时测试登录；生产环境必须显式配置 `NEXT_PUBLIC_ALLOW_TEST_LOGIN=true` 才允许测试登录。
- 本地测试验证码固定为 `000000`，仅用于 M1 验收和本地开发。
- 登录成功后写入本地测试登录态，并支持 `next` 参数跳转。
- 修复 P0：联系方式管理增加本地测试存储的保存与读取能力，刷新后可从浏览器存储读回。
- 修复 P2：首页“我要找家教”“我要做家教”入口增加登录态守卫；未登录时进入登录页，登录后回到目标入口。
- 修复 P2：补齐个人页下 `/profile/parent-needs`、`/profile/tutor-profiles`、`/profile/chats` 三个占位页，避免 404。

当前实现边界：

- 登录与联系方式持久化仍是 M1 本地/测试环境方案，不是正式 CloudBase 手机号短信登录。
- 所有正式权限、数据归属、联系方式授权展示仍必须在后续 CloudBase 后端 API 中实现。

验证结果：

- `npm test`：通过，3 个测试文件，10 个测试。
- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm run build`：通过，生成 16 个静态页面。

### 修复 M1 再次审查安全闸门

类型：代码审查修复

依据文档：

- `规划文档/里程碑文档/2026-06-20-m1-代码再次审查-复核版.md`

涉及文件：

- `Code文档/features/auth/test-auth.ts`
- `Code文档/features/auth/require-test-session.tsx`
- `Code文档/features/profile/contact-profile-storage.ts`
- `Code文档/app/page.tsx`
- `Code文档/app/profile/page.tsx`
- `Code文档/app/profile/contact/page.tsx`
- `Code文档/tests/auth.test.ts`
- `Code文档/tests/contact-profile-storage.test.ts`

内容摘要：

- 修复 P0：生产环境强制禁用临时测试登录，不再允许 `NEXT_PUBLIC_ALLOW_TEST_LOGIN=true` 覆盖生产限制。
- 更新测试：覆盖 `production + allowTestLogin=true` 仍返回 `false` 的安全要求。
- 收口 P1：个人页和联系方式管理页均要求存在测试登录态；未登录访问会跳转到登录页并携带 `next` 参数。
- 收口 P1：联系方式本地测试存储 key 增加当前测试手机号，避免同一浏览器中不同测试账号互相覆盖联系方式。

当前实现边界：

- 临时测试登录仍仅服务于 M1 本地/测试验收。
- 正式上线前仍必须替换为合规手机号短信登录和后端可信登录态。

验证结果：

- `npm test`：通过，3 个测试文件，12 个测试。
- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm run build`：通过，生成 16 个静态页面。
- `NODE_ENV=production NEXT_PUBLIC_ALLOW_TEST_LOGIN=true npm run build`：通过，生产模拟构建不会被该环境变量重新启用临时测试登录。
- 产物复核：未发现旧的 `return"development"...||"true"` 风险分支。

### 初始化开发侧身份交接文档

类型：代码文档更新

涉及文件：

- `Code文档/钦定代码开发员.md`
- `Code文档/开发员工作记录.md`

内容摘要：

- 参考 `规划文档/钦定产品经理.md` 的交接功能，新增开发侧新会话衔接文档。
- 明确代码开发员身份、职责边界、必须读取的核心文档、当前代码状态、代码目录分类、本地开发命令、GitHub 状态和验证要求。
- 明确开发员不得擅自修改产品经理工作记录、Spec、里程碑、产品迭代和技术验证文档。
- 补充 M2 双边发布的下一步开发建议，强调不得提前扩展到聊天、联系方式交换、支付、评价或审核后台。

当前实现边界：

- 本次只新增代码侧交接文档，不改变产品 Spec、里程碑或业务功能。
- 未修改 `规划文档/产品经理工作记录.md` 或 `规划文档/钦定产品经理.md`。

验证结果：

- 文档内容已用 UTF-8 读取确认。
- 本次为 Markdown 文档更新，无业务代码、类型检查、测试或构建变更。

## 2026-06-21

### 启动 M2 双边发布开发

类型：业务代码开发

依据文档：

- `规划文档/Spec文档/2026-06-19-mvp-家教对接平台-spec.md`
- `规划文档/里程碑文档/M1阶段验收报告/2026-06-21-m1-阶段代码复核审查报告.md`
- `Code文档/钦定代码开发员.md`

涉及文件：

- `Code文档/features/parent-needs/parent-need.ts`
- `Code文档/features/parent-needs/parent-need-storage.ts`
- `Code文档/features/tutor-profiles/tutor-profile.ts`
- `Code文档/features/tutor-profiles/tutor-profile-storage.ts`
- `Code文档/app/parent-needs/new/page.tsx`
- `Code文档/app/tutor-profiles/new/page.tsx`
- `Code文档/app/profile/parent-needs/page.tsx`
- `Code文档/app/profile/tutor-profiles/page.tsx`
- `Code文档/app/globals.css`
- `Code文档/tests/parent-need.test.ts`
- `Code文档/tests/parent-need-storage.test.ts`
- `Code文档/tests/tutor-profile.test.ts`
- `Code文档/tests/tutor-profile-storage.test.ts`
- `Code文档/开发员工作记录.md`

内容摘要：

- 按 TDD 增加 M2 家长需求发布与大学生家教信息发布的校验测试和本地存储测试。
- 新增家长需求校验：必填字段、科目、学段、预算区间、时间段、地址层级、小区或村、孩子简介 100 字以内、孩子简介不得包含手机号或微信号、具体位置不得填写门牌号。
- 新增大学生家教信息校验：性别、学校、专业、可教科目、可教学段、可上课时间段、课时费区间、能力说明、可选证明图片类型和大小。
- 新增按测试手机号隔离的本地测试存储：`parent_needs` 和 `tutor_profiles` 对应的 M2 数据不会在不同测试账号之间互相覆盖。
- 将 `/parent-needs/new` 和 `/tutor-profiles/new` 从占位页替换为可交互发布表单。
- 将 `/profile/parent-needs` 和 `/profile/tutor-profiles` 从占位页替换为当前测试账号的本地发布记录列表。
- 补充表单、复选项、课时费区间和发布记录列表的基础样式。

当前实现范围：

- 对应 Spec M2：家长可发布结构化需求；大学生可发布家教信息；大学生可填写课时费区间和能力说明；证明图片入口可选。
- 当前仍沿用 M1 本地测试登录和浏览器本地存储，只用于开发与验收演示。
- 证明图片当前仅保存文件名、类型和大小，未上传到 CloudBase 存储。

当前实现边界：

- 未接入 CloudBase 数据库、云函数、云托管 API 或 CloudBase 存储。
- 未实现正式手机号短信登录、后端可信登录态、后端数据归属校验和越权防护。
- 未实现 M3 双边广场筛选、详情页、M4 聊天、联系方式交换、支付、评价、人工审核后台或自动推荐。
- 联系方式仍不得在需求、家教信息或列表中展示。

验证结果：

- `npm test`：通过，7 个测试文件，26 个测试。
- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm run build`：通过，生成 16 个静态页面。

已知风险与后续建议：

- M2 本地存储只能验证前端发布闭环，不能替代后端权限、数据归属和持久化能力；后续进入 CloudBase 接入时必须补接口和权限测试。
- 大学生证明图片入口当前是本地文件选择入口，未完成真实上传和访问权限控制；后续接 CloudBase 存储时必须限制文件类型、大小、所属用户和访问授权。
- 反馈页仍未补真实可点击反馈渠道；M1 复核报告已建议在 M2/M3 期间跟踪。

### 修复 M2 阶段代码验收 P1 阻塞项

类型：代码审查修复

依据文档：

- `规划文档/里程碑文档/M2阶段验收报告/2026-06-21-m2-阶段代码验收审查报告.md`

涉及文件：

- `Code文档/features/parent-needs/parent-need.ts`
- `Code文档/app/parent-needs/new/page.tsx`
- `Code文档/tests/parent-need.test.ts`
- `Code文档/开发员工作记录.md`

内容摘要：

- 修复 P1-1：家长需求发布的孩子简介为空时不再允许发布。
- 修复 P1-2：孩子简介中出现类似 `3 栋 1201 室` 的详细门牌地址时不再允许发布。
- 为以上两个阻塞项补充单元测试，先确认测试失败，再修复校验逻辑并确认测试通过。
- 发布表单的孩子简介文本框同步增加 `required`，但最终校验仍以 `validateParentNeedInput` 为准。

当前实现边界：

- 本次只修复 M2 家长需求发布校验，不新增 M3 双边广场、详情页、聊天、联系方式交换、支付、评价或审核后台能力。
- `Code文档/app/profile/parent-needs/page.tsx` 的空值条件渲染保留为防御性展示；空孩子简介已在源头校验层被拦截。

验证结果：

- `npm test -- parent-need`：通过，2 个测试文件，9 个测试。
- `npm test`：通过，7 个测试文件，28 个测试。
- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm run build`：首次运行因本项目此前残留的 `next dev` 和超时残留 `next build` 进程占用 `.next` 导致 124 秒超时；停止本项目相关 Node 进程后复跑通过，生成 16 个静态页面。

### 启动 M3 双边广场和筛选开发

类型：业务代码开发

依据文档：

- `规划文档/里程碑文档/M2阶段验收报告/2026-06-21-m2-p1修复最终复验报告.md`
- `规划文档/Spec文档/2026-06-19-mvp-家教对接平台-spec.md`

涉及文件：

- `Code文档/features/parent-needs/parent-need-storage.ts`
- `Code文档/features/tutor-profiles/tutor-profile-storage.ts`
- `Code文档/app/parent-needs/page.tsx`
- `Code文档/app/tutor-profiles/page.tsx`
- `Code文档/app/parent-needs/[id]/page.tsx`
- `Code文档/app/tutor-profiles/[id]/page.tsx`
- `Code文档/app/globals.css`
- `Code文档/tests/parent-need-marketplace.test.ts`
- `Code文档/tests/tutor-profile-marketplace.test.ts`
- `Code文档/开发员工作记录.md`

内容摘要：

- 按 TDD 新增 M3 广场读取、筛选和详情查询测试，先确认新增 API 缺失导致测试失败，再实现对应能力。
- 家长需求广场支持跨测试账号读取本地发布需求，并按科目、学段、预算区间、老师性别偏好筛选。
- 家教信息广场支持跨测试账号读取本地发布资料，并按科目、学段、课时费区间、大学生性别筛选。
- 筛选条件写入 URL query，便于刷新和回溯当前筛选状态。
- 新增家长需求详情页和大学生家教信息详情页，详情页只展示结构化发布信息，不展示手机号、微信号或存档联系方式。
- M2 本地存储增加发布者索引，用于 M3 在本地测试环境读取所有测试账号发布内容。

当前实现范围：

- 对应 Spec M3：双方可以浏览对方信息、支持科目/学段/价格/性别筛选、可进入详情页、详情页不展示联系方式。
- 当前仍使用 M1/M2 的本地测试登录和浏览器本地存储，只用于阶段验收演示。

当前实现边界：

- 未实现 M4 聊天、请求交换联系方式、二次确认或联系方式展示。
- 未实现支付、评价、人工审核后台、自动推荐排序或地图模式。
- 未接入 CloudBase 数据库、云函数、正式登录态、后端数据归属校验或文件存储权限控制。

验证结果：

- `npm test -- marketplace`：通过，2 个测试文件，6 个测试。
- `npm test`：通过，9 个测试文件，34 个测试。
- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm run build`：通过，生成 16 个静态/动态路由条目，其中 `/parent-needs/[id]` 和 `/tutor-profiles/[id]` 为动态详情路由。

## 2026-06-22

### 启动 M4 站内聊天和联系方式交换开发

类型：业务代码开发

依据文档：

- `规划文档/Spec文档/2026-06-19-mvp-家教对接平台-spec.md`
- `规划文档/里程碑文档/M3阶段验收报告/2026-06-21-m3-第三次代码验收复核确认报告.md`
- `Code文档/钦定代码开发员.md`

涉及文件：

- `Code文档/features/chat/chat-storage.ts`
- `Code文档/app/chats/[id]/page.tsx`
- `Code文档/app/profile/chats/page.tsx`
- `Code文档/app/parent-needs/[id]/page.tsx`
- `Code文档/app/tutor-profiles/[id]/page.tsx`
- `Code文档/app/globals.css`
- `Code文档/tests/chat-storage.test.ts`
- `Code文档/开发员工作记录.md`

内容摘要：

- 按 TDD 新增 M4 聊天存储测试，先确认缺少 `features/chat/chat-storage` 导致测试失败，再实现会话、消息、联系方式交换请求和授权读取能力。
- 新增站内聊天会话能力：同一来源对象、同一双方用户只创建一个会话；只有会话参与者可以读取会话。
- 新增基础文字消息能力：只有会话参与者可以发送和读取消息，消息按创建时间升序展示。
- 新增联系方式交换请求能力：支持待处理、同意、拒绝、撤回、过期状态；待处理请求从创建时间起 7 天过期。
- 新增同意交换联系方式的二次确认要求；未完成二次确认前，授权读取接口返回 `null`。
- 新增授权后联系方式读取能力：只有会话参与者、且存在已同意并完成二次确认的请求时，才返回双方存档联系方式。
- 将需求详情页和家教信息详情页的沟通入口从 M4 占位改为创建或进入站内聊天会话。
- 将 `/profile/chats` 从占位页改为当前测试账号参与的聊天会话列表。
- 新增 `/chats/[id]` 聊天页，支持文字消息发送、请求交换联系方式、处理对方请求、二次确认同意、拒绝、撤回和授权后展示双方联系方式。

当前实现范围：

- 对应 Spec M4：双方可发起聊天、收发文字消息、请求交换联系方式、同意时进行二次确认、同意后在聊天页展示双方存档联系方式。
- 当前沿用 M1-M3 的本地测试登录和浏览器本地存储，只用于阶段验收演示。
- 联系方式交换前，聊天页、会话列表、详情页和聊天存储读取路径均不展示或返回对方存档联系方式。

当前实现边界：

- 未接入 CloudBase 数据库、云函数、云托管 API 或正式后端权限校验。
- 未实现真实手机号短信登录、后端可信登录态、服务端会话权限校验或接口级 IDOR 防护。
- 未实现准实时轮询、50 人并发压测、账号不可用自动失效、需求或家教信息删除后的会话限制。
- 未实现支付、评价、人工审核后台、举报封禁后台、自动推荐算法、图片/语音聊天、已读未读、撤回或推送通知。
- 当前联系方式交换请求状态仅保存在本地测试存储；后续接入 CloudBase 时必须迁移到后端可信接口，并补充接口权限测试和端到端验证。

验证结果：

- `npm test -- chat-storage`：先失败，失败原因为缺少 M4 聊天存储模块；实现后通过，1 个测试文件，6 个测试。
- `npm test`：通过，10 个测试文件，46 个测试。
- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm run build`：通过，生成 16 个静态/动态路由条目，其中 `/chats/[id]`、`/parent-needs/[id]` 和 `/tutor-profiles/[id]` 为动态路由。

已知风险与后续建议：

- M4 当前仍是本地测试存储版本，只能验证前端闭环和规则意图，不能替代后端可信权限控制。
- 后续进入 M5 或 CloudBase 接入时，必须重点补齐：非参与者读取聊天、越权发送消息、越权处理交换请求、未授权读取联系方式、请求过期和账号不可用失效的服务端测试。
- 如果要满足“新消息 5 秒内被对方看到”和 50 并发验收，需要在后端方案确定后补轮询或准实时机制与轻量压测。

### 修复 M4 第一次代码验收阻塞项

类型：代码验收修复

依据文档：

- `规划文档/里程碑文档/M4阶段验收报告/2026-06-22-m4-第一次代码验收审查与复核确认报告.md`

涉及文件：

- `Code文档/features/chat/chat-storage.ts`
- `Code文档/features/chat/chat-polling.ts`
- `Code文档/app/chats/[id]/page.tsx`
- `Code文档/tests/chat-storage.test.ts`
- `Code文档/tests/chat-polling.test.ts`
- `Code文档/开发员工作记录.md`

内容摘要：

- 修复 P0：将聊天会话、聊天消息、联系方式交换请求的内部存储对象与页面可读 View Model 分离。
- 会话内部仍使用手机号做本地测试权限判断，但 `createOrReadConversation`、`readConversationForUser`、`readConversationsForUser` 对页面返回时不再包含 `participantPhones`。
- 消息内部仍保存 `senderPhone` 用于权限和方向判断，但 `listConversationMessages` 对页面返回时只暴露 `direction: "sent" | "received"`，不再返回 `senderPhone`。
- 联系方式交换请求内部仍保存 `requesterPhone` 和 `receiverPhone` 用于处理权限，但 `createContactExchangeRequest`、`listContactExchangeRequestsForConversation`、同意、拒绝、撤回返回时只暴露 `direction`、状态和时间，不再返回双方手机号。
- 保留 `readAuthorizedContactProfiles` 作为唯一授权后读取双方存档联系方式的路径，且仍要求会话参与者、请求已同意、二次确认已完成。
- 修复 P1：新增 `CHAT_POLLING_INTERVAL_MS = 3000`，聊天页进入后每 3 秒自动刷新会话、消息、交换请求和授权联系方式，并在组件卸载时清理定时器。
- 补充回归测试：未授权前会话、消息、交换请求返回值均不包含双方手机号；聊天轮询间隔不超过 5 秒。

当前实现边界：

- P0 本轮修复只处理 M4 聊天数据返回路径；M1-M3 本地测试登录态、发布记录 ownerPhone 和联系方式本地存储 key 仍属于本地测试方案的身份实现，后续接入 CloudBase 时必须替换为后端可信用户 ID。
- P1 当前为浏览器本地测试轮询，不代表正式 CloudBase 准实时方案；后续应迁移到后端 API 轮询或经安全规则验证的实时能力。

验证结果：

- `npm test -- chat-storage`：先失败，失败原因为会话返回 `participantPhones`、消息缺少 `direction`、交换请求缺少 `direction`；修复后通过，1 个测试文件，6 个测试。
- `npm test -- chat-polling`：先失败，失败原因为缺少聊天轮询间隔模块；修复后通过，1 个测试文件，1 个测试。
- `npm test -- chat-storage chat-polling`：通过，2 个测试文件，7 个测试。
- `npm run typecheck`：通过。
- `npm run lint`：通过。

### 更新代码开发员新会话接续文档

类型：代码文档更新

依据：

- 业务方要求开启新会话前先更新生成 `Code文档/钦定代码开发员.md`
- `Code文档/README.md` 中“每次代码或代码文档更新后，同步更新开发员工作记录”的约定

涉及文件：

- `Code文档/钦定代码开发员.md`
- `Code文档/开发员工作记录.md`

内容摘要：

- 将 `Code文档/钦定代码开发员.md` 从 M1/M2 接续状态更新为当前真实状态：M1、M2、M3 均已通过，下一步进入 M4。
- 补充当前代码能力：M2 双边发布、M3 双边广场和筛选、详情页、公开 ID 脱敏、M2 旧本地记录 ID 脱敏迁移。
- 补充当前 Git 状态：当前开发分支为 `codex/m3-marketplace-filtering`，M3 第三轮代码修复提交为 `c0db2f3`，M3 第三轮验收文档提交为 `d928ff4`。
- 明确代码开发员边界：不处理产品经理验收文档归档，除非用户再次明确要求且内容限定清楚。
- 明确 M4 下一步建议：站内聊天和联系方式交换必须先补测试，并继续遵守未完成双方同意和二次确认前不得展示对方联系方式的安全主线。

当前实现边界：

- 本次只更新代码侧交接文档和开发员工作记录，不改业务代码。
- 不修改产品经理工作记录、钦定产品经理、Spec、里程碑验收报告或其他产品侧文档。

验证结果：

- 本次为 Markdown 文档更新，无业务代码、类型、测试或构建变更。

### 修复 M3 第二次代码验收旧记录 ID 脱敏阻塞项

类型：代码验收修复

依据文档：

- `规划文档/里程碑文档/M3阶段验收报告/2026-06-21-m3-第二次代码验收复核确认报告.md`
- `规划文档/里程碑文档/M3阶段验收报告/2026-06-21-m3-第一次代码验收审查报告-定稿.md`
- `规划文档/Spec文档/2026-06-19-mvp-家教对接平台-spec.md`

涉及文件：

- `Code文档/features/parent-needs/parent-need-storage.ts`
- `Code文档/features/tutor-profiles/tutor-profile-storage.ts`
- `Code文档/tests/parent-need-marketplace.test.ts`
- `Code文档/tests/tutor-profile-marketplace.test.ts`
- `Code文档/开发员工作记录.md`

内容摘要：

- 按 TDD 补充旧数据兼容回归测试，先确认 `npm test -- marketplace` 失败，失败点为旧 M2 记录恢复后 `id` 仍包含 `ownerPhone`。
- 家长需求旧记录读取时，如果记录 `id` 包含对应 `ownerPhone`，立即生成新的不透明 `parent-need-*` ID，并写回该 owner 的本地记录列表。
- 大学生家教信息旧记录读取时执行同等迁移，生成新的不透明 `tutor-profile-*` ID，并写回该 owner 的本地记录列表。
- 补充两侧断言：带手机号旧 ID 的 M2 记录恢复到广场后，公开 `id` 和详情链接均不包含手机号。
- 补充两侧稳定性断言：同一条旧记录首次读取脱敏后，第二次读取不再重复生成新 ID，也不产生重复记录。

当前实现范围：

- 本次只修复 M3 第二次验收报告指出的旧数据恢复路径 P0 阻塞项。
- 修复范围限于本地测试存储的读取迁移，不新增 M4 站内聊天、联系方式交换、支付、评价或审核后台能力。

当前实现边界：

- 当前迁移只处理本地测试存储中的 M2 历史记录；后续接入 CloudBase 时，应由后端可信迁移或接口层统一保证公开 ID 不包含联系方式。
- 迁移触发条件为旧记录 `id` 包含当前 owner 手机号；不改写已经安全的不透明 ID。

验证结果：

- `npm test -- marketplace`：先失败，失败原因为旧记录 `id` 包含 `13800138000`；修复后通过，2 个测试文件，12 个测试。
- `npm test`：通过，9 个测试文件，40 个测试。
- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm run build`：通过，生成 16 个静态/动态路由条目，其中 `/parent-needs/[id]` 和 `/tutor-profiles/[id]` 为动态详情路由。

已知风险与后续建议：

- M3 广场依赖本地存储发布者索引；旧版本本地已保存但未进入索引的数据需要重新发布后才会进入广场。
- 后续接入 CloudBase 时，广场读取、筛选、详情查询必须迁移到后端可信接口或经过安全规则验证的数据库查询。
- 详情页当前只提供 M4 聊天入口占位，不创建会话，不交换联系方式。

### 修复 M3 第一次代码验收 P0 阻塞项

类型：代码验收修复

依据文档：

- `规划文档/里程碑文档/M3阶段验收报告/2026-06-21-m3-第一次代码验收审查报告.md`
- `规划文档/里程碑文档/M3阶段验收报告/2026-06-21-m3-第一次代码验收复核报告.md`
- `规划文档/Spec文档/2026-06-19-mvp-家教对接平台-spec.md`

涉及文件：

- `Code文档/features/parent-needs/parent-need-storage.ts`
- `Code文档/features/tutor-profiles/tutor-profile-storage.ts`
- `Code文档/lib/storage.ts`
- `Code文档/lib/memory-storage.ts`
- `Code文档/tests/parent-need-marketplace.test.ts`
- `Code文档/tests/tutor-profile-marketplace.test.ts`
- `Code文档/开发员工作记录.md`

内容摘要：

- 修复 P0：家长需求和大学生家教信息的公开详情 ID 不再使用登录手机号拼接，改为 `crypto.randomUUID()` 优先的不透明 ID，避免手机号出现在详情页 URL、浏览器历史、截图、日志或分享链接中。
- 补充家长需求与家教信息两侧测试，断言保存后的 `id` 和详情链接均不包含 `ownerPhone`。
- 处理 P2：为本地测试存储增加可选 `key`/`length` 枚举能力，广场读取时可从 `ungradu.parentNeeds.{ownerPhone}` 与 `ungradu.tutorProfiles.{ownerPhone}` 旧数据 key 中补建发布者索引。
- 补充 M2 遗留本地数据兼容测试，覆盖没有 owners 索引时仍能进入 M3 广场的读取场景。

当前实现范围：

- 本次只修复 M3 代码验收报告指出的 P0 阻塞项和 P2 本地测试数据兼容风险。
- 详情页仍只展示结构化发布信息，不展示手机号、微信号或存档联系方式。

当前实现边界：

- 未实现 M4 站内聊天、请求交换联系方式、二次确认或联系方式展示。
- 未实现支付、评价、人工审核后台、自动推荐排序或地图模式。
- 当前不透明 ID 仍属于本地测试存储方案；后续接入 CloudBase 时，应由后端可信接口生成和校验记录 ID。

验证结果：

- `npm test -- marketplace`：通过，2 个测试文件，10 个测试。
- `npm test`：通过，9 个测试文件，38 个测试。
- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm run build`：通过，生成 16 个静态/动态路由条目，其中 `/parent-needs/[id]` 和 `/tutor-profiles/[id]` 为动态详情路由。

### 更新代码开发员新会话接续文档至 M5 前状态

类型：代码文档更新

依据：

- 业务方要求更新 `Code文档/钦定代码开发员.md`，准备开启新会话线程。
- `规划文档/里程碑文档/M4阶段验收报告/2026-06-22-m4-第二次代码验收审查与复核确认报告.md` 已确认 M4 第二次代码验收通过。
- `Code文档/README.md` 中“每次代码或代码文档更新后，同步更新开发员工作记录”的约定。

涉及文件：

- `Code文档/钦定代码开发员.md`
- `Code文档/开发员工作记录.md`

内容摘要：

- 将 `Code文档/钦定代码开发员.md` 从“M3 已通过，下一步进入 M4”的旧状态更新为当前真实状态：M1、M2、M3、M4 均已通过，下一步进入 M5。
- 补充 M4 当前代码能力：站内聊天页、会话创建与复用、基础文字消息、联系方式交换请求、拒绝/撤回/过期、同意二次确认、授权后展示联系方式、授权前脱敏 View Model、3 秒轮询。
- 补充当前 Git 状态：当前开发分支为 `codex/m4-chat-contact-exchange`，M4 第一轮功能提交为 `0fe4871`，M4 第一轮阻塞项修复提交为 `bdc8f79`，M4 第二次验收相关文档提交为 `90826eb` 和 `1a94e27`。
- 明确 M5 下一步建议：安全和发布前验证应重点围绕生产禁用临时测试登录、CloudBase 或后端可信权限、数据归属校验、未授权联系方式不可读取、接口级越权防护和基础压测。
- 更新新会话启动提示，要求后续代码开发员先读取 M4 第二次验收报告，并围绕 M5 安全和发布前验证工作，不处理产品经理验收文档归档。

当前实现边界：

- 本次只更新代码侧交接文档和开发员工作记录，不改业务代码。
- 不修改产品经理工作记录、钦定产品经理、Spec、里程碑验收报告或其他产品侧文档。
- 当前工作区存在产品侧文档变更和未跟踪文件，非本次代码开发员职责范围，未处理也未暂存。

验证结果：

- 本次为 Markdown 文档更新，无业务代码、类型、测试或构建变更。

### 启动 M5 安全闭环第一轮代码验证

类型：安全验收修复与验证

依据文档：

- `规划文档/Spec文档/2026-06-19-mvp-家教对接平台-spec.md`
- `规划文档/里程碑文档/M4阶段验收报告/2026-06-22-m4-第二次代码验收审查与复核确认报告.md`
- `Code文档/钦定代码开发员.md`

涉及文件：

- `Code文档/features/parent-needs/parent-need-storage.ts`
- `Code文档/features/tutor-profiles/tutor-profile-storage.ts`
- `Code文档/features/chat/chat-storage.ts`
- `Code文档/app/parent-needs/page.tsx`
- `Code文档/app/parent-needs/[id]/page.tsx`
- `Code文档/app/tutor-profiles/page.tsx`
- `Code文档/app/tutor-profiles/[id]/page.tsx`
- `Code文档/tests/parent-need-marketplace.test.ts`
- `Code文档/tests/tutor-profile-marketplace.test.ts`
- `Code文档/tests/chat-storage.test.ts`
- `Code文档/tests/m5-load-baseline.test.ts`
- `Code文档/开发员工作记录.md`

内容摘要：

- 修复 M5 安全风险：公开广场和公开详情读取路径不再向页面 state 返回发布者 `ownerPhone`。
- 为家长需求和大学生家教信息新增 public view 读取函数，采用白名单字段输出，避免未来新增内部字段时自动泄露到公开视图。
- 将需求详情页和家教信息详情页的聊天入口改为 `createOrReadConversationFromSource`，页面只传当前用户和来源对象 ID，不再直接读取或传递对方手机号。
- 保留内部本地测试存储中的 `ownerPhone`，仅用于当前 M1-M4 本地方案的权限判断和会话创建；该内部模型仍不得直接带入生产。
- 补充公开读取脱敏测试，断言 `readAllPublicParentNeeds`、`findPublicParentNeedById`、`readAllPublicTutorProfiles`、`findPublicTutorProfileById` 返回值不含 owner 手机号。
- 补充按来源创建聊天测试，确认外部只依赖来源 ID 也能创建会话，返回的会话视图不包含对方手机号。
- 新增 M5 本地 50 用户基线测试，模拟 25 组家长/大学生完成存档联系方式、发布家教信息、创建聊天、双方发消息、请求并二次确认交换联系方式；该测试只作为本地内存模型基线，不代表生产压测结论。

当前实现范围：

- 本次只围绕 M5 的安全和发布前验证主线，收口公开读取路径的联系方式泄露风险，并补充本地可执行验证。
- 未新增支付、评价、人工审核后台、举报封禁后台、自动推荐算法、图片/语音聊天或第三方 IM SDK。
- 未处理产品经理验收文档归档。

当前实现边界：

- 当前仍未接入 CloudBase、短信登录或生产后端可信用户 ID。
- 当前 50 用户基线基于浏览器本地/内存存储模型，只能证明本地核心流程可执行，不能替代 CloudBase 或生产环境压测。
- 后续 M5 仍需继续补齐 CloudBase 或后端可信权限方案、接口级 IDOR 验证、数据归属校验和端到端浏览器级验证。

验证结果：

- `npm test -- marketplace chat-storage`：改动前通过，3 个测试文件，18 个测试。
- `npm test -- marketplace chat-storage`：公开视图和来源创建会话修复后通过，3 个测试文件，20 个测试。
- `npm test -- marketplace chat-storage m5-load-baseline`：通过，4 个测试文件，22 个测试。
- `npm run typecheck`：通过。
- `npm run lint`：首次因脱敏转换中的未使用解构变量产生 2 个 warning；改为白名单字段输出后通过。
- `npm test`：通过，12 个测试文件，51 个测试。
- `npm run build`：通过，生成 16 条路由，其中 `/chats/[id]`、`/parent-needs/[id]`、`/tutor-profiles/[id]` 为动态路由。
- `$env:NODE_ENV='production'; $env:NEXT_PUBLIC_ALLOW_TEST_LOGIN='true'; npm run build; Remove-Item Env:\NODE_ENV; Remove-Item Env:\NEXT_PUBLIC_ALLOW_TEST_LOGIN`：通过，生产模拟构建未重新启用临时测试登录。

### 接入 CloudBase 服务端连接底座

类型：M5 技术接入与连接验证

依据文档：

- `规划文档/Spec文档/2026-06-19-mvp-家教对接平台-spec.md`
- `Code文档/钦定代码开发员.md`
- 业务方已确认 CloudBase 测试环境、文档型数据库集合和 ADMINONLY 集合权限已准备完成。

涉及文件：

- `Code文档/package.json`
- `Code文档/package-lock.json`
- `Code文档/.env.example`
- `Code文档/server/cloudbase-server.ts`
- `Code文档/scripts/check-cloudbase-connection.mjs`
- `Code文档/tests/cloudbase-server.test.ts`
- `Code文档/README.md`
- `Code文档/开发员工作记录.md`

内容摘要：

- 安装 `@cloudbase/node-sdk` 作为 CloudBase 服务端 SDK。
- 安装 `ws`，消除 CloudBase Node 环境连接检查时提示缺少 WebSocket 依赖的问题；该依赖不等同于第三方 IM SDK。
- 新增 `server/cloudbase-server.ts`，提供服务端环境变量解析、真实密钥占位值拦截、配置脱敏和 CloudBase SDK 初始化包装。
- 新增 `.env.example`，说明本地 `.env.local` 需要的变量名，不包含真实密钥。
- 新增 `scripts/check-cloudbase-connection.mjs` 和 `npm run cloudbase:check`，用于在本地读取 `.env.local` 并验证 CloudBase 服务端连接，输出中只显示脱敏后的 `SecretId` 前缀，不输出 `SecretKey`。
- 更新 `README.md`，补充 CloudBase 本地连接配置和检查命令。
- 按 TDD 增加 `tests/cloudbase-server.test.ts`，先确认模块缺失导致测试失败，再实现服务端连接模块并通过测试。

当前实现范围：

- 本次只完成 CloudBase 服务端连接底座和本地连接检查，不迁移业务数据读写。
- 未把登录、联系方式、需求、家教信息或聊天切换到 CloudBase 生产接口。
- 未新增支付、评价、人工审核后台、举报封禁后台、自动推荐算法、图片/语音聊天或第三方 IM SDK。

当前实现边界：

- `.env.local` 为本地密钥文件，已被 `.gitignore` 忽略，不得提交。
- `npm run cloudbase:check` 默认读取 `users` 集合 1 条数据，只证明服务端 SDK 能访问当前 CloudBase 环境和集合，不代表业务权限模型已经完成。
- 后续 M5 仍需继续将本地测试存储逐步迁移到后端可信接口，并补充数据归属、接口越权和未授权联系方式不可读取的服务端测试。

验证结果：

- `npm test -- cloudbase-server`：先失败，失败原因为 `@/server/cloudbase-server` 模块不存在；实现后通过，1 个测试文件，4 个测试。
- `npm run cloudbase:check`：通过，成功连接 `ungradu-edu-test-d0ed1mqeceb0ae1` 并读取 `users` 集合，读取条数为 0；输出已脱敏。
- `npm run typecheck`：首次发现 `process.env` 和泛型 initializer 类型过窄；修复后通过。
- `npm run lint`：通过。
- `npm test`：通过，13 个测试文件，55 个测试。
- `npm run build`：通过，生成 16 条路由；构建识别到 `.env.local`，但未输出真实密钥。
- `npm install @cloudbase/node-sdk` 和 `npm install ws` 仍提示本机 Node `20.18.0` 低于部分间接依赖建议的 `20.19+`，并报告 7 个 npm audit 风险；未执行 `npm audit fix --force`，避免破坏 Next.js 版本。

### 迁移第一个 CloudBase 业务接口：联系方式存档 contact_profiles

类型：M5 安全闭环接口迁移

依据文档：

- `规划文档/Spec文档/2026-06-19-mvp-家教对接平台-spec.md`
- `规划文档/里程碑文档/M4阶段验收报告/2026-06-22-m4-第二次代码验收审查与复核确认报告.md`
- `Code文档/钦定代码开发员.md`

涉及文件：

- `Code文档/app/api/contact-profile/route.ts`
- `Code文档/app/profile/contact/page.tsx`
- `Code文档/features/profile/contact-profile-api-client.ts`
- `Code文档/server/contact-profile-api.ts`
- `Code文档/server/contact-profiles.ts`
- `Code文档/tests/contact-profile-api-client.test.ts`
- `Code文档/tests/contact-profile-api.test.ts`
- `Code文档/tests/contact-profile-server.test.ts`
- `Code文档/README.md`
- `Code文档/开发员工作记录.md`

内容摘要：

- 新增 CloudBase 集合名 `contact_profiles`，作为 M5 第一个后端可信业务读写集合。
- 新增服务端联系方式存档模块，保存时只写入 `doc(currentUserId)`，字段包含 `ownerUserId`、`phone`、`wechat`、`updatedAt`。
- 新增服务端读取逻辑，只读取当前认证用户自己的文档；文档不存在时返回空联系方式；如历史数据 owner 不匹配则按不可读处理。
- 新增 `/api/contact-profile` 的 `GET` 和 `PUT` 路由，页面不再直接把联系方式保存作为唯一持久化路径。
- 非生产环境临时沿用 `x-ungradu-test-user-phone` 请求头承接 M1-M4 本地测试登录态；生产环境即使配置 `NEXT_PUBLIC_ALLOW_TEST_LOGIN=true` 也拒绝临时测试登录身份。
- 更新联系方式管理页，保存和读取优先走服务端 API；成功保存后临时同步本地存储，避免聊天和联系方式交换模块尚未迁移前断开 M4 本地闭环。
- 按 TDD 补充服务端存档、接口权限和前端 API client 测试，覆盖缺少登录态、生产禁用临时登录、只读写自己文档和请求字段约定。
- 更新 `README.md`，补充当前已接入服务端接口、CloudBase 集合、临时认证边界和未迁移范围。

当前实现范围：

- 本次只迁移联系方式存档 `contact_profiles`，不迁移聊天、联系方式交换请求、需求、家教信息或正式短信登录。
- 本次属于 M5 安全和发布前验证主线，重点推进后端可信读写、数据归属校验和生产禁用临时测试登录。
- 未新增支付、评价、人工审核后台、举报封禁后台、自动推荐算法、图片/语音聊天或第三方 IM SDK。
- 未处理产品经理验收文档归档。

当前实现边界：

- 当前接口的用户身份仍是 M5 过渡方案：非生产环境使用临时测试登录请求头，生产环境拒绝该请求头；正式登录态仍待后续接入。
- 联系方式管理页保留本地存储镜像，只用于未迁移的 M4 聊天授权读取链路；后续迁移聊天和联系方式交换后应移除该过渡镜像。
- `contact_profiles` 只解决“当前用户自己的联系方式存档”读写，不代表“授权后读取对方联系方式”接口已经迁移完成。

验证结果：

- `npm test -- contact-profile-server`：先失败，失败原因为 `@/server/contact-profiles` 模块不存在；实现后通过，覆盖保存、读取和越权文档不可读。
- `npm test -- contact-profile-api`：先失败，失败原因为 `@/server/contact-profile-api` 模块不存在；实现后通过，覆盖未登录拒绝、测试登录读取、生产拒绝临时测试登录和保存。
- `npm test -- contact-profile-api-client`：先失败，失败原因为前端 API client 模块不存在；实现后通过，覆盖请求头、HTTP 方法和错误处理。
- `npm test -- contact-profile`：通过，5 个测试文件，13 个测试。
- `npm run typecheck`：通过。
- `npm test`：通过，16 个测试文件，62 个测试。
- `npm run lint`：通过。
- `npm run build`：通过，生成 17 条路由，新增 `/api/contact-profile` 动态路由。
- `$env:NODE_ENV='production'; $env:NEXT_PUBLIC_ALLOW_TEST_LOGIN='true'; npm run build; Remove-Item Env:\NODE_ENV; Remove-Item Env:\NEXT_PUBLIC_ALLOW_TEST_LOGIN`：通过，生产模拟构建未重新启用临时测试登录。
- `npm run typecheck` 曾在与 `npm run build` 并行执行时因 `.next/types` 被构建过程重建而出现 TS6053 噪声；改为顺序执行后通过。

### 迁移联系方式交换授权读取接口 contact_exchange_requests

类型：M5 安全闭环接口迁移

依据文档：

- `规划文档/Spec文档/2026-06-19-mvp-家教对接平台-spec.md`
- `规划文档/里程碑文档/M4阶段验收报告/2026-06-22-m4-第二次代码验收审查与复核确认报告.md`
- `Code文档/钦定代码开发员.md`

涉及文件：

- `Code文档/app/api/contact-exchange/route.ts`
- `Code文档/server/contact-exchange.ts`
- `Code文档/server/contact-exchange-api.ts`
- `Code文档/tests/contact-exchange-server.test.ts`
- `Code文档/tests/contact-exchange-api.test.ts`
- `Code文档/docs/superpowers/plans/2026-06-22-m5-api-migration.md`
- `Code文档/README.md`
- `Code文档/开发员工作记录.md`

内容摘要：

- 新增 M5 接口迁移计划，明确后续接口迁移顺序：联系方式交换授权、会话消息、家长需求、家教信息和安全回归。
- 新增 CloudBase 集合名 `contact_exchange_requests` 和 `conversations`，用于服务端联系方式交换权限判断。
- 新增服务端联系方式交换模块，支持创建请求、同意、拒绝、撤回、过期、按会话列出请求和授权后读取双方联系方式。
- 服务端校验会话参与者身份：非参与者不能发起请求、不能查看请求、不能读取授权联系方式。
- 服务端校验操作归属：只有接收方可以同意或拒绝，只有发起方可以撤回。
- 服务端校验授权条件：只有请求已同意且完成二次确认，并且双方都已有 `contact_profiles` 存档时，才返回联系方式。
- 新增 `/api/contact-exchange`，通过 `GET` 支持请求列表和授权联系方式读取，通过 `POST` 支持 `create`、`approve`、`reject`、`withdraw`。
- 生产环境继续拒绝 `x-ungradu-test-user-phone` 临时测试登录身份。
- 更新 `README.md`，补充接口约定、集合依赖、权限规则和生产边界。

当前实现范围：

- 本次只迁移联系方式交换请求和授权后读取联系方式的服务端可信判断。
- 本次不迁移聊天消息页面调用，也不迁移需求、家教信息或正式短信登录。
- 未新增支付、评价、人工审核后台、举报封禁后台、自动推荐算法、图片/语音聊天或第三方 IM SDK。
- 未处理产品经理验收文档归档。

当前实现边界：

- `/api/contact-exchange` 当前仍使用 M5 过渡临时身份请求头，生产环境会拒绝该身份；正式登录态仍待后续接入。
- 会话 `conversations` 集合目前由服务端交换接口读取，但完整会话创建和消息收发尚未迁移到服务端 API。
- 聊天页面仍暂时使用本地 M4 存储链路；后续 Task 2 需要迁移会话和消息 API，并接入该交换接口。

验证结果：

- `npm test -- contact-exchange-server`：先失败，失败原因为 `@/server/contact-exchange` 模块不存在；实现后通过，1 个测试文件，4 个测试。
- `npm test -- contact-exchange-api`：先失败，失败原因为 `@/server/contact-exchange-api` 模块不存在；实现后通过，1 个测试文件，3 个测试。
- `npm test -- contact-exchange`：通过，2 个测试文件，7 个测试。
- `npm run typecheck`：通过。

### 迁移会话与文字消息接口 conversations / conversation_messages

类型：M5 安全闭环接口迁移

依据文档：

- `规划文档/Spec文档/2026-06-19-mvp-家教对接平台-spec.md`
- `规划文档/里程碑文档/M4阶段验收报告/2026-06-22-m4-第二次代码验收审查与复核确认报告.md`
- `Code文档/钦定代码开发员.md`

涉及文件：

- `Code文档/app/api/conversations/route.ts`
- `Code文档/app/api/conversations/[id]/route.ts`
- `Code文档/app/api/conversations/[id]/messages/route.ts`
- `Code文档/server/conversations.ts`
- `Code文档/server/conversation-api.ts`
- `Code文档/tests/conversation-server.test.ts`
- `Code文档/tests/conversation-api.test.ts`
- `Code文档/README.md`
- `Code文档/开发员工作记录.md`

内容摘要：

- 新增 CloudBase 集合名 `conversations` 和 `conversation_messages`。
- 新增服务端会话模块，支持按来源对象创建或复用会话、读取当前用户会话、列出当前用户会话、发送文字消息和读取消息列表。
- 服务端按 `parent_needs` 或 `tutor_profiles` 来源对象的 `ownerUserId` 建立参与者关系，禁止用户和自己发布的信息创建会话。
- 服务端对会话和消息统一做参与者校验：非参与者不能读取单个会话、不能读取消息、不能发送消息。
- 会话和消息接口返回脱敏 View Model，不返回参与者用户 ID 或联系方式。
- 新增 `/api/conversations`、`/api/conversations/[id]` 和 `/api/conversations/[id]/messages`。
- 生产环境继续拒绝 `x-ungradu-test-user-phone` 临时测试登录身份。
- 更新 `README.md`，补充会话和消息 API 的路由、集合、权限规则和隐私规则。

当前实现范围：

- 本次只迁移会话创建、会话读取和基础文字消息读写接口。
- 本次不接入第三方 IM SDK，不新增图片/语音聊天，不新增自动推荐算法。
- 家长需求和家教信息来源集合仍待后续 Task 3/4 正式迁移；当前会话接口已按目标集合名 `parent_needs`、`tutor_profiles` 读取。
- 未处理产品经理验收文档归档。

当前实现边界：

- 聊天页面尚未改为调用新 API，仍保留本地 M4 页面链路；后续应在接口全部迁移后统一切换页面调用，降低中间态破坏风险。
- 当前接口仍使用 M5 过渡临时身份请求头，生产环境会拒绝该身份；正式登录态仍待后续接入。

验证结果：

- `npm test -- conversation-server`：先失败，失败原因为 `@/server/conversations` 模块不存在；实现后通过，1 个测试文件，4 个测试。
- `npm test -- conversation-api`：先失败，失败原因为 `@/server/conversation-api` 模块不存在；实现后通过，1 个测试文件，3 个测试。
- `npm test -- conversation`：通过，2 个测试文件，7 个测试。
- `npm run typecheck`：通过。

### 迁移家长需求接口 parent_needs

类型：M5 安全闭环接口迁移

依据文档：

- `规划文档/Spec文档/2026-06-19-mvp-家教对接平台-spec.md`
- `规划文档/里程碑文档/M4阶段验收报告/2026-06-22-m4-第二次代码验收审查与复核确认报告.md`
- `Code文档/钦定代码开发员.md`

涉及文件：

- `Code文档/app/api/parent-needs/route.ts`
- `Code文档/app/api/parent-needs/[id]/route.ts`
- `Code文档/server/parent-needs.ts`
- `Code文档/server/parent-need-api.ts`
- `Code文档/tests/parent-need-server.test.ts`
- `Code文档/tests/parent-need-api.test.ts`
- `Code文档/README.md`
- `Code文档/开发员工作记录.md`

内容摘要：

- 新增 CloudBase 集合名 `parent_needs`。
- 新增服务端家长需求模块，支持当前用户发布、读取我的需求、公开列表、公开详情和公开筛选。
- 服务端保存时强制使用当前认证用户作为 `ownerUserId`，客户端不能指定 owner。
- 我的需求列表只按当前认证用户 `ownerUserId` 查询，避免读取他人记录。
- 公开列表和公开详情使用白名单 View Model，不返回 `ownerUserId` 或任何联系方式。
- 新增 `/api/parent-needs` 和 `/api/parent-needs/[id]`。
- 私有读写继续在生产环境拒绝 `x-ungradu-test-user-phone` 临时测试登录身份。
- 更新 `README.md`，补充家长需求 API 的路由、集合、数据归属和公开读取规则。

当前实现范围：

- 本次只迁移家长需求的服务端接口，不切换页面调用。
- 本次不新增支付、评价、人工审核后台、举报封禁后台、自动推荐算法、图片/语音聊天或第三方 IM SDK。
- 未处理产品经理验收文档归档。

当前实现边界：

- 页面仍暂时保留 M1-M4 本地存储链路；后续应在接口全部迁移后统一切换页面调用。
- 当前接口仍使用 M5 过渡临时身份请求头，生产环境会拒绝该身份；正式登录态仍待后续接入。

验证结果：

- `npm test -- parent-need-server`：先失败，失败原因为 `@/server/parent-needs` 模块不存在；实现后通过，1 个测试文件，5 个测试。
- `npm test -- parent-need-api`：先失败，失败原因为 `@/server/parent-need-api` 模块不存在；实现后通过，1 个测试文件，3 个测试。
- `npm test -- parent-need`：通过，5 个测试文件，24 个测试。
- `npm run typecheck`：通过。

### 迁移大学生家教信息接口 tutor_profiles

类型：M5 安全闭环接口迁移

依据文档：

- `规划文档/Spec文档/2026-06-19-mvp-家教对接平台-spec.md`
- `规划文档/里程碑文档/M4阶段验收报告/2026-06-22-m4-第二次代码验收审查与复核确认报告.md`
- `Code文档/钦定代码开发员.md`

涉及文件：

- `Code文档/app/api/tutor-profiles/route.ts`
- `Code文档/app/api/tutor-profiles/[id]/route.ts`
- `Code文档/server/tutor-profiles.ts`
- `Code文档/server/tutor-profile-api.ts`
- `Code文档/tests/tutor-profile-server.test.ts`
- `Code文档/tests/tutor-profile-api.test.ts`
- `Code文档/README.md`
- `Code文档/开发员工作记录.md`

内容摘要：

- 新增 CloudBase 集合名 `tutor_profiles`。
- 新增服务端家教信息模块，支持当前用户发布、读取我的家教信息、公开列表、公开详情和公开筛选。
- 服务端保存时强制使用当前认证用户作为 `ownerUserId`，客户端不能指定 owner。
- 我的家教信息列表只按当前认证用户 `ownerUserId` 查询，避免读取他人记录。
- 公开列表和公开详情使用白名单 View Model，不返回 `ownerUserId` 或任何联系方式。
- 保存时保留证明图片元数据，但不新增真实文件上传或存储权限能力。
- 新增 `/api/tutor-profiles` 和 `/api/tutor-profiles/[id]`。
- 私有读写继续在生产环境拒绝 `x-ungradu-test-user-phone` 临时测试登录身份。
- 更新 `README.md`，补充家教信息 API 的路由、集合、数据归属和公开读取规则。

当前实现范围：

- 本次只迁移大学生家教信息的服务端接口，不切换页面调用。
- 本次不新增真实图片上传、支付、评价、人工审核后台、举报封禁后台、自动推荐算法、图片/语音聊天或第三方 IM SDK。
- 未处理产品经理验收文档归档。

当前实现边界：

- 页面仍暂时保留 M1-M4 本地存储链路；后续应在接口全部迁移后统一切换页面调用。
- 当前接口仍使用 M5 过渡临时身份请求头，生产环境会拒绝该身份；正式登录态仍待后续接入。

验证结果：

- `npm test -- tutor-profile-server`：先失败，失败原因为 `@/server/tutor-profiles` 模块不存在；实现后通过，1 个测试文件，5 个测试。
- `npm test -- tutor-profile-api`：先失败，失败原因为 `@/server/tutor-profile-api` 模块不存在；实现后通过，1 个测试文件，3 个测试。
- `npm test -- tutor-profile`：通过，5 个测试文件，22 个测试。
- `npm run typecheck`：通过。

### M5 接口迁移安全回归验证

类型：M5 安全闭环回归验证

覆盖接口：

- `/api/contact-profile`
- `/api/contact-exchange`
- `/api/conversations`
- `/api/conversations/[id]`
- `/api/conversations/[id]/messages`
- `/api/parent-needs`
- `/api/parent-needs/[id]`
- `/api/tutor-profiles`
- `/api/tutor-profiles/[id]`

安全点确认：

- 生产环境拒绝 `x-ungradu-test-user-phone` 临时测试登录身份。
- `contact_profiles` 只读写当前用户自己的联系方式存档。
- `contact_exchange_requests` 只有会话参与者可发起和查看，只有接收方可同意/拒绝，只有发起方可撤回。
- 未同意、未二次确认、已拒绝、已撤回、已过期或非参与者场景均不可读取对方联系方式。
- `conversations` 和 `conversation_messages` 只允许会话参与者读取和发送。
- `parent_needs` 和 `tutor_profiles` 私有列表只按当前用户 `ownerUserId` 查询。
- 家长需求和家教信息公开列表/详情均使用白名单 View Model，不返回 `ownerUserId` 或联系方式。

当前实现边界：

- 服务端 API 已完成 M5 第一轮迁移；页面层除联系方式页外仍保留 M1-M4 本地测试链路，后续如要进入联调，应统一切换页面调用新 API。
- 正式短信登录、真实生产用户 ID 解析、CloudBase 安全规则细化和生产压测仍属于 M5 后续发布前工作。
- 本次未新增支付、评价、人工审核后台、举报封禁后台、自动推荐算法、图片/语音聊天或第三方 IM SDK。
- 本次未处理产品经理验收文档归档。

验证结果：

- `npm run typecheck`：通过。
- `npm test`：通过，24 个测试文件，92 个测试。
- `npm run lint`：通过。
- `npm run build`：通过，生成 21 条 App 路由，新增 API 均进入动态路由清单。
- `$env:NODE_ENV='production'; $env:NEXT_PUBLIC_ALLOW_TEST_LOGIN='true'; npm run build; Remove-Item Env:\NODE_ENV; Remove-Item Env:\NEXT_PUBLIC_ALLOW_TEST_LOGIN`：通过，生产模拟构建未重新启用临时测试登录。
- `npm run cloudbase:check`：通过，成功连接 `ungradu-edu-test-d0ed1mqeceb0ae1` 并读取 `users` 集合，输出已脱敏。

### M5 页面层切换到服务端 API

类型：M5 安全闭环页面联调

涉及文件：

- `Code文档/features/chat/chat-api-client.ts`
- `Code文档/features/parent-needs/parent-need-api-client.ts`
- `Code文档/features/tutor-profiles/tutor-profile-api-client.ts`
- `Code文档/app/chats/[id]/page.tsx`
- `Code文档/app/profile/chats/page.tsx`
- `Code文档/app/parent-needs/page.tsx`
- `Code文档/app/parent-needs/[id]/page.tsx`
- `Code文档/app/parent-needs/new/page.tsx`
- `Code文档/app/profile/parent-needs/page.tsx`
- `Code文档/app/tutor-profiles/page.tsx`
- `Code文档/app/tutor-profiles/[id]/page.tsx`
- `Code文档/app/tutor-profiles/new/page.tsx`
- `Code文档/app/profile/tutor-profiles/page.tsx`
- `Code文档/tests/chat-api-client.test.ts`
- `Code文档/tests/parent-need-api-client.test.ts`
- `Code文档/tests/tutor-profile-api-client.test.ts`
- `Code文档/README.md`
- `Code文档/开发员工作记录.md`

内容摘要：

- 新增聊天 API client，覆盖服务端会话创建、会话列表、会话详情、消息读写、联系方式交换请求处理和授权联系方式读取。
- 新增家长需求 API client，覆盖公开列表/详情、我的需求列表和发布需求。
- 新增家教信息 API client，覆盖公开列表/详情、我的家教信息列表和发布家教信息。
- 将我的聊天页、聊天详情页、需求详情页和家教信息详情页的会话相关操作切换为服务端 API。
- 将需求广场、需求详情、发布需求和我发布的需求切换为 `parent_needs` 服务端 API。
- 将家教信息广场、家教信息详情、发布家教信息和我的家教信息切换为 `tutor_profiles` 服务端 API。
- 重写部分原先存在乱码的页面文案，保留原有页面结构和 MVP 功能范围。

当前实现范围：

- 页面层已开始使用本轮 M5 迁移后的服务端 API，关键用户路径不再直接读写本地测试存储。
- 联系方式管理页保存成功后仍保留本地镜像，仅用于兼容旧测试数据；后续正式登录态完成后可移除。
- 未新增支付、评价、人工审核后台、举报封禁后台、自动推荐算法、图片/语音聊天或第三方 IM SDK。
- 未处理产品经理验收文档归档。

当前实现边界：

- 当前页面仍依赖 M5 过渡临时测试登录会话来发送 `x-ungradu-test-user-phone`，生产环境服务端会拒绝该身份。
- 真实短信登录、真实用户 ID 解析和 CloudBase 安全规则精细化仍待后续 M5 发布前工作。

验证结果：

- `npm test -- chat-api-client`：先失败，失败原因为 `@/features/chat/chat-api-client` 模块不存在；实现后通过。
- `npm test -- parent-need-api-client`：先失败，失败原因为 `@/features/parent-needs/parent-need-api-client` 模块不存在；实现后通过。
- `npm test -- tutor-profile-api-client`：先失败，失败原因为 `@/features/tutor-profiles/tutor-profile-api-client` 模块不存在；实现后通过。
- `npm test -- api-client`：通过，4 个测试文件，4 个测试。
- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm test`：通过，27 个测试文件，95 个测试。
- `npm run build`：通过，生成 21 条 App 路由。
- `$env:NODE_ENV='production'; $env:NEXT_PUBLIC_ALLOW_TEST_LOGIN='true'; npm run build; Remove-Item Env:\NODE_ENV; Remove-Item Env:\NEXT_PUBLIC_ALLOW_TEST_LOGIN`：通过，生产模拟构建未重新启用临时测试登录。

### M5 后端可信登录态、越权矩阵和基础压测

类型：M5 安全闭环发布前验证

涉及文件：
- `Code文档/server/auth-session.ts`
- `Code文档/server/auth-api.ts`
- `Code文档/server/api-utils.ts`
- `Code文档/app/api/auth/test-login/route.ts`
- `Code文档/app/api/auth/session/route.ts`
- `Code文档/app/api/auth/logout/route.ts`
- `Code文档/features/auth/login-form.tsx`
- `Code文档/features/auth/use-test-session.ts`
- `Code文档/features/chat/chat-api-client.ts`
- `Code文档/features/profile/contact-profile-api-client.ts`
- `Code文档/features/parent-needs/parent-need-api-client.ts`
- `Code文档/features/tutor-profiles/tutor-profile-api-client.ts`
- `Code文档/server/parent-needs.ts`
- `Code文档/server/parent-need-api.ts`
- `Code文档/app/api/parent-needs/[id]/route.ts`
- `Code文档/server/tutor-profiles.ts`
- `Code文档/server/tutor-profile-api.ts`
- `Code文档/app/api/tutor-profiles/[id]/route.ts`
- `Code文档/scripts/check-m5-cloudbase-collections.mjs`
- `Code文档/tests/auth-session-api.test.ts`
- `Code文档/tests/m5-server-flow-and-load.test.ts`
- `Code文档/package.json`
- `Code文档/.env.example`
- `Code文档/README.md`
- `Code文档/开发员工作记录.md`

内容摘要：
- 新增后端签名会话 Cookie：`/api/auth/test-login` 在非生产环境写入 HttpOnly `ungradu_auth_session`，`/api/auth/session` 从服务端签名 Cookie 读取身份，`/api/auth/logout` 清除 Cookie。
- 业务 API 的认证入口改为优先读取服务端签名 Cookie；`x-ungradu-test-user-phone` 仅保留为非生产测试兼容路径，生产环境继续拒绝。
- 前端 API client 不再发送 `x-ungradu-test-user-phone`，私有接口请求改为依赖同源 HttpOnly Cookie。
- 登录表单不再写浏览器 localStorage，会调用 `/api/auth/test-login`，再由后端写入登录态。
- 新增家长需求和家教信息 `[id]` PATCH 接口，只允许 owner 更新自己的记录；客户端伪造 ID 或改别人记录会返回 403。
- 新增 `npm run m5:cloudbase:collections`，检查 M5 目标 CloudBase 集合可读：`contact_profiles`、`parent_needs`、`tutor_profiles`、`conversations`、`messages`、`contact_exchange_requests`。
- 新增 `npm run m5:flow` 和 `npm run m5:load`，使用服务端模块跑核心流程和 50 并发基础压测，覆盖发布、筛选、聊天、联系方式交换、二次确认、授权展示、授权前不可读和非参与者不可读。

当前实现范围：
- 本次只完成 M5 安全和发布前验证相关代码，不新增支付、评价、人工审核后台、举报封禁后台、自动推荐算法、图片/语音聊天或第三方 IM SDK。
- 本次未处理产品经理验收文档归档。

验证结果：
- `npm test -- auth-session-api`：先失败，失败原因为 `@/server/auth-api` 不存在；实现后通过，3 个测试。
- `npm test -- contact-profile-api-client parent-need-api-client tutor-profile-api-client chat-api-client`：先失败，失败原因为前端 API client 仍发送 `x-ungradu-test-user-phone`；改为 Cookie 后通过，4 个测试。
- `npm test -- parent-need-api tutor-profile-api`：先失败，失败原因为 `PATCH_ITEM` 不存在；实现 owner 校验更新后通过，4 个测试文件，10 个测试。
- `npm test -- m5-server-flow-and-load`：通过，服务端核心流程 50 并发基础压测完成。
- `npm run m5:flow`：通过。
- `npm run m5:load`：通过。
- `npm run m5:cloudbase:collections`：首次发现 `conversation_messages` 不符合本次图示集合名；统一为 `messages` 后通过，`contact_profiles`、`parent_needs`、`tutor_profiles`、`conversations`、`messages`、`contact_exchange_requests` 均可读，输出已脱敏。
- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm test`：通过，30 个测试文件，102 个测试。
- `npm run build`：通过，生成 24 条 App 路由。
- `$env:NODE_ENV='production'; $env:NEXT_PUBLIC_ALLOW_TEST_LOGIN='true'; npm run build; Remove-Item Env:\NODE_ENV; Remove-Item Env:\NEXT_PUBLIC_ALLOW_TEST_LOGIN`：通过，生产模拟构建未重新启用临时测试登录。

### M5 真实 HTTP API 闭环与 50 并发脚本

类型：M5 第一轮验收整改

涉及文件：
- `Code文档/scripts/m5-http-flow-and-load.mjs`
- `Code文档/package.json`
- `Code文档/README.md`
- `Code文档/开发员工作记录.md`

内容摘要：
- 新增 `npm run m5:http:flow`，通过 HTTP 调用真实 Next API route 跑核心闭环，覆盖测试登录 Cookie、联系方式存档、发布需求、发布家教信息、公开筛选、创建会话、发送消息、授权前联系方式不可读、发起联系方式交换、二次确认和授权后读取联系方式。
- 新增 `npm run m5:http:load`，通过 HTTP 对真实 API route 发起 25 对用户 / 50 虚拟用户基础压测。
- 脚本默认目标为 `http://127.0.0.1:3000`，可通过 `M5_BASE_URL` 指向 CloudBase 托管或其他部署地址。

当前实现范围：
- 本次补齐真实 HTTP API route 层验证脚本，并已在本地 Next API + 真实 CloudBase 测试环境跑通。
- 仓库当前仍未包含 CloudBase 托管部署配置或部署 URL，因此尚不能在代码侧直接完成“部署地址下的闭环验证”；需要获得部署地址后使用 `M5_BASE_URL` 复跑。
- 未新增支付、评价、人工审核后台、举报封禁后台、自动推荐算法、图片/语音聊天或第三方 IM SDK。
- 未处理产品经理验收文档归档。

验证结果：
- `npm run m5:http:flow`：通过。目标 `http://127.0.0.1:3000`，成功 1 对用户 / 2 虚拟用户，耗时约 10592ms。
- `npm run m5:http:load`：通过。目标 `http://127.0.0.1:3000`，成功 25 对用户 / 50 虚拟用户，耗时约 9904ms。
- `npm run cloudbase:check`：通过，成功连接 CloudBase 测试环境，输出已脱敏。

### M5 服务端 API 统一硬化

类型：M5 安全闭环发布前验证

涉及文件：
- `Code文档/server/api-utils.ts`
- `Code文档/server/contact-profile-api.ts`
- `Code文档/server/contact-exchange-api.ts`
- `Code文档/server/conversation-api.ts`
- `Code文档/server/parent-need-api.ts`
- `Code文档/server/tutor-profile-api.ts`
- `Code文档/tests/m5-api-hardening.test.ts`
- `Code文档/tests/contact-profile-api.test.ts`
- `Code文档/tests/contact-exchange-api.test.ts`
- `Code文档/tests/conversation-api.test.ts`
- `Code文档/tests/parent-need-api.test.ts`
- `Code文档/tests/tutor-profile-api.test.ts`
- `Code文档/README.md`
- `Code文档/开发员工作记录.md`

内容摘要：
- 新增 `server/api-utils.ts`，统一服务端 JSON 响应、临时测试身份读取、生产环境拒绝临时身份、JSON 请求体解析和结果状态码映射。
- 已迁移写接口统一处理 malformed JSON，请求体不是合法 JSON 时返回 400 和 `Invalid JSON body.`，避免发布前验证中坏请求变成 500。
- 服务端 API 状态码不再依赖中文错误文案判断；会话和联系方式交换越权继续返回 403，表单输入错误继续返回 400。
- 生产环境拒绝 `x-ungradu-test-user-phone` 的错误文案改为稳定 ASCII 文案，减少编码差异对测试和发布日志的影响。

当前实现范围：
- 仅硬化 M5 已迁移的联系方式存档、联系方式交换、会话、家长需求和家教信息 API。
- 未新增支付、评价、人工审核后台、举报封禁后台、自动推荐算法、图片/语音聊天或第三方 IM SDK。
- 未处理产品经理验收文档归档。

验证结果：
- `npm test -- m5-api-hardening`：先失败，失败原因为坏 JSON 会从 `request.json()` 抛出异常；实现统一解析后通过。
- `npm test -- contact-profile-api conversation-api contact-exchange-api parent-need-api tutor-profile-api m5-api-hardening`：通过，9 个测试文件，19 个测试。
- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm test`：通过，28 个测试文件，96 个测试。
- `npm run build`：通过，生成 21 条 App 路由。
- `$env:NODE_ENV='production'; $env:NEXT_PUBLIC_ALLOW_TEST_LOGIN='true'; npm run build; Remove-Item Env:\NODE_ENV; Remove-Item Env:\NEXT_PUBLIC_ALLOW_TEST_LOGIN`：通过，生产模拟构建未重新启用临时测试登录。

### M5 托管测试登录隔离与 HTTP 压测指标补强

类型：M5 第二次代码验收复核整改

涉及文件：
- `Code文档/features/auth/test-auth.ts`
- `Code文档/server/auth-api.ts`
- `Code文档/server/api-utils.ts`
- `Code文档/scripts/m5-http-flow-and-load.mjs`
- `Code文档/tests/auth.test.ts`
- `Code文档/tests/auth-session-api.test.ts`
- `Code文档/.env.example`
- `Code文档/README.md`
- `Code文档/开发员工作记录.md`

内容摘要：
- 针对 M5 第二次代码验收报告指出的“托管部署环境通常以 `NODE_ENV=production` 运行，现有 HTTP 验收脚本无法创建测试登录”的阻塞点，新增隔离测试托管环境开关。
- `APP_ENV=production` 为最高优先级生产边界，即使误配 `NEXT_PUBLIC_ALLOW_TEST_LOGIN=true` 或 `M5_ENABLE_HOSTED_TEST_LOGIN=true`，也拒绝临时测试登录。
- 当部署平台使用 `NODE_ENV=production` 运行隔离 M5 测试环境时，只有同时配置 `APP_ENV=test` 和 `M5_ENABLE_HOSTED_TEST_LOGIN=true`，才允许 `/api/auth/test-login` 写入后端签名 Cookie，供 `m5:http:flow` 和 `m5:http:load` 验证部署 URL。
- 业务 API 的临时测试身份兜底入口同步使用同一判断，避免登录接口和业务接口边界不一致。
- `m5-http-flow-and-load.mjs` 增加请求数、成功率、错误率、avg/p95/max 延迟和失败分布输出，补齐压测证据粒度。
- `.env.example` 增加 `M5_ENABLE_HOSTED_TEST_LOGIN=false`，README 补充隔离托管测试环境变量组合和正式生产禁止规则。

当前实现范围：
- 本次仅补齐 M5 安全和发布前验证链路，不新增支付、评价、人工审核后台、举报封禁后台、自动推荐算法、图片/语音聊天或第三方 IM SDK。
- 本次未处理产品经理验收文档归档。
- 仓库仍没有实际 CloudBase 托管部署 URL；部署后的真实 URL 复测仍需获得 URL 后执行 `M5_BASE_URL=<部署地址> npm run m5:http:flow` 和 `M5_BASE_URL=<部署地址> npm run m5:http:load`。

验证结果：
- `npm test -- auth auth-session-api`：通过，2 个测试文件，11 个测试。
- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm test`：通过，30 个测试文件，104 个测试。
- `npm run m5:http:flow`：通过。目标 `http://127.0.0.1:3000`，成功 1 对用户 / 2 虚拟用户，15 个请求，成功率 100.00%，错误率 0.00%，avg/p95/max 延迟 1242ms / 3421ms / 3421ms，耗时 13994ms。
- `npm run m5:http:load`：通过。目标 `http://127.0.0.1:3000`，成功 25 对用户 / 50 虚拟用户，375 个请求，成功率 100.00%，错误率 0.00%，avg/p95/max 延迟 1039ms / 1643ms / 2697ms，耗时 12666ms。
- `npm run build`：通过，生成 24 条 App 路由。
- `$env:APP_ENV='production'; $env:M5_ENABLE_HOSTED_TEST_LOGIN='true'; $env:NEXT_PUBLIC_ALLOW_TEST_LOGIN='true'; npm run build; Remove-Item Env:\APP_ENV; Remove-Item Env:\M5_ENABLE_HOSTED_TEST_LOGIN; Remove-Item Env:\NEXT_PUBLIC_ALLOW_TEST_LOGIN`：通过，误配测试开关的生产模拟构建未失败，服务端生产硬拒绝规则由单测覆盖。

### M5 公开列表查询稳定性与托管复验入口补强

类型：M5 第三次代码验收复核整改

涉及文件：
- `Code文档/server/parent-needs.ts`
- `Code文档/server/tutor-profiles.ts`
- `Code文档/scripts/m5-http-flow-and-load.mjs`
- `Code文档/scripts/m5-hosted-verify.mjs`
- `Code文档/tests/parent-need-server.test.ts`
- `Code文档/tests/tutor-profile-server.test.ts`
- `Code文档/package.json`
- `Code文档/README.md`
- `Code文档/开发员工作记录.md`

内容摘要：
- 修复第三次 M5 复核报告中的 P1：公开家长需求列表和公开家教信息列表不再依赖 `collection.where({}).get()` 后内存筛选完整集合。
- 公开列表改为按 `status=published` 查询，并优先使用 CloudBase 查询链 `orderBy("createdAt", "desc")`、`skip`、`limit` 分页读取近期公开记录，避免测试集合文档数超过单次 `.get()` 默认返回上限后，新发布内容在筛选列表不可见。
- 为家长需求和家教信息各新增一条回归测试：模拟集合内已有 120 条旧数据时，刚创建的新记录仍必须能通过科目筛选出现在公开列表。
- `m5-http-flow-and-load.mjs` 将业务断言失败纳入失败分布和错误率统计，避免再次出现压测失败但 `Error rate: 0.00%` 的误导性输出。
- 新增 `npm run m5:hosted:verify`，强制要求非 localhost 的 `M5_BASE_URL`，用于 CloudBase 或等价托管环境部署后一次性复跑 HTTP flow 和 50 虚拟用户 load。

当前实现范围：
- 本次关闭第三次复核报告中“本地真实 HTTP API route + CloudBase 测试环境下 50 并发压测失败”的代码阻塞。
- 本次未新增支付、评价、人工审核后台、举报封禁后台、自动推荐算法、图片/语音聊天或第三方 IM SDK。
- 本次未处理产品经理验收文档归档。
- 仓库当前仍没有实际 CloudBase 托管 URL；代码侧已提供 `m5:hosted:verify` 防误用入口，但真实部署地址与部署后复跑证据必须在完成外部托管部署后补齐。

验证结果：
- `npm test -- parent-need-server tutor-profile-server`：先失败，失败原因是 fake collection 模拟默认 100 条返回上限后，新建记录无法通过旧实现进入公开筛选列表；实现查询层按发布时间倒序分页读取后通过，2 个测试文件，12 个测试。
- `npm run typecheck`：通过。
- `node --check scripts/m5-http-flow-and-load.mjs`：通过。
- `node --check scripts/m5-hosted-verify.mjs`：通过。
- `npm run m5:hosted:verify`：按预期拒绝空 `M5_BASE_URL`，返回 `M5_BASE_URL is required for hosted verification.`，确认不会把本地 localhost 误当托管复验证据。
- `npm run m5:http:flow`：通过。目标 `http://127.0.0.1:3000`，成功 1 对用户 / 2 虚拟用户，15 个请求，成功率 100.00%，错误率 0.00%，avg/p95/max 延迟 1518ms / 5768ms / 5768ms，耗时 19070ms。
- `npm run m5:http:load`：通过。目标 `http://127.0.0.1:3000`，成功 25 对用户 / 50 虚拟用户，375 个请求，成功率 100.00%，错误率 0.00%，avg/p95/max 延迟 887ms / 2061ms / 4597ms，耗时 13953ms。
- `npm test`：通过，30 个测试文件，106 个测试。
- `npm run lint`：通过。
- `npm run build`：通过，生成 24 条 App 路由。
- `$env:APP_ENV='production'; $env:M5_ENABLE_HOSTED_TEST_LOGIN='true'; $env:NEXT_PUBLIC_ALLOW_TEST_LOGIN='true'; npm run build; Remove-Item Env:\APP_ENV; Remove-Item Env:\M5_ENABLE_HOSTED_TEST_LOGIN; Remove-Item Env:\NEXT_PUBLIC_ALLOW_TEST_LOGIN`：通过，生产误配测试开关模拟构建未破坏生产硬拒绝规则。
- `npm run cloudbase:check`：通过，成功连接 CloudBase 测试环境，输出已脱敏。
- `npm run m5:cloudbase:collections`：通过，`contact_profiles`、`parent_needs`、`tutor_profiles`、`conversations`、`messages`、`contact_exchange_requests` 均可读，输出已脱敏。
- `npm run m5:flow`：通过。
- `npm run m5:load`：通过。

### M5 通过后钦定代码开发员接续文档更新

类型：代码协作文档更新

涉及文件：
- `Code文档/钦定代码开发员.md`
- `Code文档/开发员工作记录.md`

内容摘要：
- 将 `Code文档/钦定代码开发员.md` 从 M4 已通过、准备进入 M5 的旧状态，更新为 M1-M5 均已复核通过后的新会话接续状态。
- 补充 M5 托管环境最终验收复核确认报告为后续新会话必读文档。
- 记录 M5 测试托管服务 `ungradu-edu-m5-test`、托管地址 `https://ungradu-edu-m5-test-273732-8-1445807473.sh.run.tcloudbase.com` 和 `npm run m5:hosted:verify` 已通过。
- 更新后续开发边界：不要继续扩大 MVP 功能范围；如用户未明确进入 vNext，只围绕正式生产发布准备、生产环境变量清单、测试账号管理、回滚预案和上线安全边界工作。
- 强调正式生产必须配置 `APP_ENV=production`，不得启用 `M5_ENABLE_HOSTED_TEST_LOGIN=true` 或 `NEXT_PUBLIC_ALLOW_TEST_LOGIN=true`，不得把真实密钥写入 Git、报告正文或聊天记录。

当前实现范围：
- 本次只更新代码开发员接续文档和开发员工作记录，不改业务代码、不改产品经理验收文档归档状态。

验证结果：
- 文档更新，无需运行 `npm test`、`npm run typecheck`、`npm run lint` 或 `npm run build`。

## 2026-06-24

### MVP 发布前收口与架构治理

类型：MVP 发布前代码收口与发布准备文档

依据文档：
- `规划文档/Spec文档/MVP_version_Spec/2026-06-24-mvp-发布前收口与架构治理-spec.md`

涉及文件：
- `Code文档/server/conversations.ts`
- `Code文档/server/auth-session.ts`
- `Code文档/features/auth/test-auth.ts`
- `Code文档/features/auth/login-form.tsx`
- `Code文档/features/auth/logout-button.tsx`
- `Code文档/features/api/api-client.ts`
- `Code文档/features/chat/chat-api-client.ts`
- `Code文档/features/profile/contact-profile-api-client.ts`
- `Code文档/features/parent-needs/parent-need-api-client.ts`
- `Code文档/features/tutor-profiles/tutor-profile-api-client.ts`
- `Code文档/app/profile/page.tsx`
- `Code文档/app/feedback/page.tsx`
- `Code文档/app/tutor-profiles/new/page.tsx`
- `Code文档/app/tutor-profiles/[id]/page.tsx`
- `Code文档/tests/conversation-server.test.ts`
- `Code文档/tests/auth.test.ts`
- `Code文档/tests/auth-session-api.test.ts`
- `Code文档/tests/api-client.test.ts`
- `Code文档/README.md`
- `规划文档/里程碑文档/发布前准备/MVP产品说明.md`
- `规划文档/里程碑文档/发布前准备/生产环境变量清单.md`
- `规划文档/里程碑文档/发布前准备/测试账号管理说明.md`
- `规划文档/里程碑文档/发布前准备/上线回滚预案.md`
- `规划文档/里程碑文档/发布前准备/正式生产禁用测试登录确认.md`
- `规划文档/里程碑文档/发布前准备/架构收口问题修复对照表.md`
- `规划文档/里程碑文档/发布前准备/发布前浏览器冒烟验收清单.md`
- `规划文档/产品迭代/2026-06-24-vnext-性能优化与技术债清单.md`

内容摘要：
- 修复 P1：聊天会话创建与列表读取不再依赖 `where({})` 全集合扫描，新增 `conversationUniqKey`、`participantKeys` 和 `sourceKey`，按稳定唯一键复用会话，按参与者索引查询聊天列表。
- 修复 P1：新增 `sanitizeNextPath()`，登录成功后只允许跳转到站内相对路径，外部 URL、协议相对 URL、空值和异常输入统一回到首页。
- 修复 P1：反馈页从纯文案改为可点击 GitHub issue 反馈入口，并保留 3 个工作日人工查看说明。
- 修复 P2：读取签名 Cookie 时增加服务端硬过期校验，拒绝超过有效期、`createdAt` 非法或未来异常时间的会话。
- 修复 P2：新增 `features/api/api-client.ts`，前端 API client 统一处理非 JSON、HTML 错误页、非 2xx JSON 和解析异常，避免页面因 `response.json()` 直接抛错。
- 修复 P2：个人页新增可见“退出登录”按钮，调用 `/api/auth/logout` 后回到首页。
- 修复 P2：发布家教信息页和家教信息详情页明确证明图片当前仅保存元信息，不提供正式上传、查看或审核能力。
- 新增 MVP 产品说明、生产环境变量清单、测试账号管理说明、上线回滚预案、正式生产禁用测试登录确认、架构收口问题修复对照表、浏览器冒烟验收清单和 vNext 技术债清单。

当前实现边界：
- 本次不新增支付、评价、人工审核后台、举报封禁后台、自动推荐算法、图片/语音聊天或第三方 IM SDK。
- 正式生产环境仍必须配置 `APP_ENV=production`，不得启用 `M5_ENABLE_HOSTED_TEST_LOGIN=true` 或 `NEXT_PUBLIC_ALLOW_TEST_LOGIN=true`。
- 本次未写入任何真实 `AUTH_SESSION_SECRET`、`TENCENTCLOUD_SECRETID` 或 `TENCENTCLOUD_SECRETKEY`。
- 生产环境尚未重新部署本次代码，因此现有 M5 测试托管地址的复验结果不能代表本地新改动已在托管环境生效。

验证结果：
- `npm test -- conversation-server`：先失败，失败原因为旧实现调用 `where({})` 全集合扫描；改为索引化查询后通过，1 个测试文件，5 个测试。
- `npm test -- auth`：先失败，失败原因为 `sanitizeNextPath` 缺失且 Cookie 未做服务端硬过期校验；实现后通过，2 个测试文件，15 个测试。
- `npm test -- api-client`：先失败，失败原因为共享 API client 解析工具不存在；实现后通过，5 个测试文件，6 个测试。
- `npm run typecheck`：通过。
- `npm test`：通过，31 个测试文件，113 个测试。
- `npm run lint`：首次因未使用类型导入失败；移除多余导入后通过。
- `npm run build`：通过，生成 24 条 App 路由。
- `$env:APP_ENV='production'; $env:M5_ENABLE_HOSTED_TEST_LOGIN='true'; $env:NEXT_PUBLIC_ALLOW_TEST_LOGIN='true'; npm run build; Remove-Item Env:\APP_ENV; Remove-Item Env:\M5_ENABLE_HOSTED_TEST_LOGIN; Remove-Item Env:\NEXT_PUBLIC_ALLOW_TEST_LOGIN`：通过，生产误配测试开关模拟构建未破坏服务端生产硬拒绝规则。
- `npm run m5:flow`：通过。
- `npm run m5:load`：通过。
- 本地真实 HTTP 验证：启动 `npm run dev -- --hostname 127.0.0.1 --port 3000` 后，`npm run m5:http:flow` 通过，1 对用户 / 2 虚拟用户，15 个请求，成功率 100.00%，错误率 0.00%；`npm run m5:http:load` 通过，25 对用户 / 50 虚拟用户，375 个请求，成功率 100.00%，错误率 0.00%。
- `M5_BASE_URL=https://ungradu-edu-m5-test-273732-8-1445807473.sh.run.tcloudbase.com npm run m5:hosted:verify`：未通过。托管地址返回 HTML，脚本报 `POST /api/auth/test-login -> invalid-json`，错误率 100.00%。该结果记录为现有测试托管服务当前复验失败；需重新部署本次代码或恢复测试托管服务后再复跑。

遗留风险：
- 现有 M5 测试托管地址当前无法通过 `m5:hosted:verify`，不能作为本次本地收口代码的托管复验证据。
- `规划文档/里程碑文档/发布前准备/发布前浏览器冒烟验收清单.md` 目前是手工验收入口，尚未形成完整浏览器截图或脚本化 E2E 证据。
- 正式生产禁用测试登录确认仍标记为生产发布前阻塞项，必须在正式生产 URL 和环境变量可用后补充证据。

### 根据 MVP 收口阶段第一次代码验收报告完善

类型：代码验收 P1/P2 修复补强

依据文档：
- `规划文档/里程碑文档/MVP收口阶段验收报告/2026-06-24-mvp收口阶段第一次代码验收审查与复核确认报告.md`

涉及文件：
- `Code文档/server/conversations.ts`
- `Code文档/scripts/backfill-conversation-indexes.mjs`
- `Code文档/package.json`
- `Code文档/tests/conversation-server.test.ts`
- `Code文档/app/feedback/page.tsx`
- `Code文档/README.md`
- `规划文档/Spec文档/README.md`
- `规划文档/Spec文档/2026-06-19-mvp-家教对接平台-spec.md`
- `规划文档/Spec文档/MVP_version_Spec/2026-06-24-mvp-发布前收口与架构治理-spec.md`
- `规划文档/里程碑文档/发布前准备/架构收口问题修复对照表.md`
- `规划文档/里程碑文档/发布前准备/发布前浏览器冒烟验收清单.md`

内容摘要：
- 修复 P1-3：旧会话缺少 `conversationUniqKey` / `participantKeys` / `sourceKey` 时，重复发起聊天会按 `sourceId`、`sourceType` 和双方 `participantUserIds` 兼容复用旧会话，并补写索引字段，避免重复创建。
- 修复 P1-3：新增 `backfillServerConversationIndexes()` 和 `npm run m5:backfill:conversations`，用于发布前对 CloudBase `conversations` 旧数据一次性补齐索引字段。脚本只输出环境 ID、脱敏 SecretId 和扫描/更新数量，不输出会话内容或真实密钥。
- 修复 P1-4：恢复根目录固定 MVP Spec 入口 `规划文档/Spec文档/2026-06-19-mvp-家教对接平台-spec.md`，并在 `规划文档/Spec文档/README.md` 明确当前固定入口；同步修正发布前收口 Spec 内部对核验材料和自身交付路径的引用。
- 修复 P2：反馈页文案从“运营邮箱”改为“反馈提交页”，与实际 GitHub issue 反馈入口保持一致。
- 补 P1-2 验收证据：更新发布前浏览器冒烟验收清单，记录 Chrome headless DOM 冒烟和本地真实 HTTP flow/load 结果。

当前实现边界：
- 本次未新增支付、评价、人工审核后台、举报封禁后台、自动推荐算法、图片/语音聊天或第三方 IM SDK。
- `npm run m5:backfill:conversations` 需要本地 `.env.local` 中存在 CloudBase 测试环境凭据；不得在报告或聊天中记录真实密钥。
- 旧 M5 托管地址仍未部署本轮本地改动，不能作为本轮托管通过证据。

验证结果：
- `npm test -- conversation-server`：先失败，失败原因为旧会话缺少新索引字段时会重复创建，且缺少迁移函数；实现后通过，1 个测试文件，7 个测试。
- `node --check scripts/backfill-conversation-indexes.mjs`：通过。
- `npm run typecheck`：通过。
- `npm test`：通过，31 个测试文件，115 个测试。
- `npm run lint`：通过。
- `npm run build`：首次因残留本地 Next dev 进程占用 `.next/trace` 出现 Windows `EPERM`；确认并停止本项目残留 Node 进程后复跑通过，生成 24 条 App 路由。
- `npm run m5:http:flow`：通过，1 对用户 / 2 虚拟用户，15 个请求，成功率 100.00%，错误率 0.00%。
- `npm run m5:http:load`：通过，25 对用户 / 50 虚拟用户，375 个请求，成功率 100.00%，错误率 0.00%。
- Chrome headless DOM 冒烟：通过，确认受控测试登录、家长发布页、家教信息发布页、需求广场、家教信息广场、我的聊天、反馈提交页、个人页退出登录入口均可见，并确认 `/api/auth/logout` 返回 200。
- `$env:APP_ENV='production'; $env:M5_ENABLE_HOSTED_TEST_LOGIN='true'; $env:NEXT_PUBLIC_ALLOW_TEST_LOGIN='true'; npm run build; Remove-Item Env:\APP_ENV; Remove-Item Env:\M5_ENABLE_HOSTED_TEST_LOGIN; Remove-Item Env:\NEXT_PUBLIC_ALLOW_TEST_LOGIN`：通过。
- `npm run m5:flow`：通过。
- `npm run m5:load`：通过。
- `M5_BASE_URL=https://ungradu-edu-m5-test-273732-8-1445807473.sh.run.tcloudbase.com npm run m5:hosted:verify`：未通过。旧托管地址仍返回 HTML，脚本报 `POST /api/auth/test-login -> invalid-json`，错误率 100.00%。需重新部署本轮代码或恢复测试托管服务后再复验。

遗留风险：
- P1-1 仍需有效 CloudBase Run 测试托管地址完成 `npm run m5:hosted:verify`，当前本地修复不能替代托管复验。
- 工作区仍存在未跟踪 `nul` 条目，本轮未纳入提交范围。

### MVP 收口托管环境复验通过

类型：MVP 收口 P1-1 托管复验证据补充

托管地址：
- `https://ungradu-edu-m5-test-273732-8-1445807473.sh.run.tcloudbase.com`

验证命令：
- `$env:M5_BASE_URL='https://ungradu-edu-m5-test-273732-8-1445807473.sh.run.tcloudbase.com'; npm run m5:hosted:verify; Remove-Item Env:\M5_BASE_URL`

验证结果：
- `npm run m5:hosted:verify`：通过。
- HTTP flow：1 对用户 / 2 虚拟用户，15 个请求，成功率 100.00%，错误率 0.00%，延迟 avg/p95/max 为 268ms / 449ms / 449ms。
- HTTP load：25 对用户 / 50 虚拟用户，375 个请求，成功率 100.00%，错误率 0.00%，延迟 avg/p95/max 为 450ms / 1010ms / 11678ms。

结论：
- P1-1 “服务端业务逻辑已改动但托管环境复验未完成”已补齐证据。
- 该结果仅代表 M5 测试托管环境复验通过，不代表正式生产已发布。

### MVP 收口旧会话索引迁移执行与收尾

类型：MVP 收口 P1-3 旧数据迁移证据补充

执行命令：
- `npm run m5:backfill:conversations`

脱敏执行结果：
- CloudBase 测试环境：`ungradu-edu-test-d0ed1mqeceb0ae1`
- SecretId 输出：仅显示 `AKID***`
- Scanned：313
- Updated：235
- Already indexed：78
- Skipped：0

统计解释：
- `Scanned 313` 为本次扫描到的会话总数。
- `Updated 235` 为缺少或不一致索引字段并已补写的旧会话。
- `Already indexed 78` 为扫描时已经具备 `conversationUniqKey`、`participantKeys`、`sourceKey` 且无需更新的会话；脚本当前不会把这类记录计入 `Skipped`。
- `Skipped 0` 表示没有因缺少 doc id、来源字段、参与者字段或来源类型异常而跳过的脏数据。

写入路径确认：
- `conversations` 普通会话创建路径集中在 `Code文档/server/conversations.ts`。
- `createOrReadServerConversationFromSource()` 新建会话时统一写入 `conversationUniqKey`、`participantKeys`、`sourceKey`。
- `writeBackfilledConversationIndexes()` 只用于旧会话索引补齐。
- `Code文档/server/contact-exchange.ts` 只读取会话用于联系方式交换权限判断，不创建会话。
- `Code文档/scripts/backfill-conversation-indexes.mjs` 只更新旧会话索引字段，不写入消息、联系方式或会话内容。

其他收尾：
- 根目录未跟踪 `nul` 条目已通过根 `.gitignore` 的 `/nul` 规则排除，避免纳入本阶段提交候选。
- 第二次验收中记录的同一托管地址前后差异，操作记录为：用户于 2026-06-24 晚间在 CloudBase Run 控制台重新保存测试环境变量，并对原 M5 测试托管服务 `ungradu-edu-m5-test` 执行重新部署/恢复；未将控制台日志或版本截图纳入仓库。重新部署/恢复后，同一地址 `m5:hosted:verify` 已通过。

当前边界：
- 本次 backfill 只针对 M5 测试 CloudBase 环境，不代表正式生产环境已执行迁移。
- 未记录真实 `TENCENTCLOUD_SECRETKEY`、完整 `TENCENTCLOUD_SECRETID`、`AUTH_SESSION_SECRET` 或真实会话内容。

## 2026-06-25

### 正式生产上线阶段代码侧可发行版本准备

类型：正式生产上线阶段代码侧收口与发布准备

任务 ID：`RELEASE-RUN-2026-06-25-001-CODE`

依据文档：
- `规划文档/Spec文档/Release_version_Spec/2026-06-25-mvp-正式生产上线-spec.md`
- `规划文档/里程碑文档/正式生产上线准备/正式生产上线阶段执行打包说明.md`

涉及文件：
- `Code文档/package.json`
- `Code文档/scripts/production-readiness-check.mjs`
- `Code文档/scripts/clean-next-build.mjs`
- `Code文档/tests/production-readiness-script.test.ts`
- `Code文档/.env.example`
- `Code文档/README.md`
- `规划文档/里程碑文档/正式生产上线准备/代码侧可发行版本准备说明.md`
- `Code文档/开发员工作记录.md`

内容摘要：
- 新增 `npm run release:production:preflight`，用于仓库内无密钥模拟检查正式生产安全边界。
- 预检脚本检查生产必填变量是否存在，但不打印 `AUTH_SESSION_SECRET`、`TENCENTCLOUD_SECRETID` 或 `TENCENTCLOUD_SECRETKEY` 的真实值。
- 预检脚本验证 `APP_ENV=production` 下临时测试登录保持拒绝：`/api/auth/test-login`、`x-ungradu-test-user-phone` 和前端测试登录入口均不能被误配测试开关打开。
- 新增回归测试覆盖：生产误配 `M5_ENABLE_HOSTED_TEST_LOGIN=true` 与 `NEXT_PUBLIC_ALLOW_TEST_LOGIN=true` 时预检仍确认测试登录被拒绝；`APP_ENV` 不是 `production` 时预检失败。
- 更新 `.env.example`，区分本地/隔离 M5 测试模板与正式生产模板，并明确正式生产不得设置测试登录开关。
- 更新 `Code文档/README.md`，补充正式生产预检命令、使用边界和不替代生产 URL 冒烟的说明。
- 新增 `代码侧可发行版本准备说明.md`，对齐执行打包说明第 4、5、8 节，明确代码侧已覆盖项、协助项和必须由项目总控制人处理项。
- 针对正式上线阶段 P0 构建阻塞，新增 `scripts/clean-next-build.mjs`，并将 `npm run build` 调整为先清理可再生 `.next` 构建产物再执行 `next build`，避免 Windows / 中文路径 / standalone 输出场景下半残留 `.next` 导致 `trace`、manifest 或 cache-life 相关 `EPERM` / `ENOENT` 失败。
- 保留 `next.config.ts` 的 `output: "standalone"`，不移除 CloudBase Run 所需的 standalone 构建产物。

构建阻塞处理记录：
- 首次复跑 `npm run build` 失败：`.next/trace` 出现 Windows `EPERM`。
- 排查发现本项目残留 `npm run build` / `next build` / `jest-worker` 子进程曾占用 `.next` 产物。
- 构建进程退出后再次复跑，出现 `.next` 中间产物缺失导致的 manifest 读取失败。
- 删除本项目内可再生 `.next` 后干净构建通过。
- 为避免后续复现同类半残留构建产物问题，构建脚本现会在每次 `next build` 前安全清理 `Code文档/.next`。

验证结果：
- `npm run release:production:preflight`：通过。模拟误配 `M5_ENABLE_HOSTED_TEST_LOGIN=true` 和 `NEXT_PUBLIC_ALLOW_TEST_LOGIN=true` 时输出 warning，并确认 production 下测试登录、临时身份头和前端测试入口均被拒绝或隐藏。
- `npm run typecheck`：通过。
- `npm test`：通过，32 个测试文件，117 个测试。
- `npm run lint`：通过。
- `npm run build`：最终通过。当前构建命令先输出 `cleaned .next build artifacts`，随后 `next build` 成功生成 24 条 App 路由，并保留 standalone 输出配置。

当前边界：
- 本次未接入正式短信登录或新的生产登录渠道。
- 本次未创建、部署或验证真实生产 CloudBase / CloudBase Run 环境。
- 本次未读取、打印或写入真实生产密钥。
- 正式生产 URL 冒烟、生产密钥配置、生产账号白名单、回滚演练和上线验收报告仍需项目总控制人、运维负责人和产品经理 Agent 继续处理。

## 2026-06-29

### 生产登录状态导航 UI 同步修复

类型：正式生产试运行阶段登录状态导航体验缺陷修复

任务 ID：`PROD-AUTH-SESSION-UI-2026-06-29-001-DEV-1`

分支：`codex/prod-ux-auth-fixes-20260628`

基线 commit：`74c90c19d34dbfad0adcf99122474c8cc3bacf91`

目标 commit：本次提交；最终哈希以交付说明和 `git log --oneline -1` 为准。

根因确认：
- `useTestSession()` 原始机制只在客户端组件首次挂载时读取一次 `/api/auth/session`，登录或退出成功后没有统一通知已挂载的导航组件刷新本地 session state。
- `router.refresh()` 能刷新服务端组件数据，但不会自动重置已经挂载的客户端 hook 本地 state。
- 权限状态和个人信息可见性已由服务端 session 正确控制；本次问题限定为右上角导航客户端状态同步不及时。

涉及文件：
- `Code文档/features/auth/auth-session-events.ts`
- `Code文档/features/auth/use-test-session.ts`
- `Code文档/features/auth/login-form.tsx`
- `Code文档/features/auth/session-nav.tsx`
- `Code文档/features/auth/logout-button.tsx`
- `Code文档/tests/auth-session-events.test.ts`
- `Code文档/开发员工作记录.md`

内容摘要：
- 新增轻量统一登录态事件 `auth-session-changed`，并区分 `authenticated`、`anonymous`、`unknown` 三类状态变化。
- 验证码登录成功和密码登录成功后派发 `authenticated` 事件，并保留 `router.push()` 与 `router.refresh()`。
- 顶部导航退出和登录页内退出按钮在退出接口成功后派发 `anonymous` 事件；失败或取消退出不派发事件，不误清本地状态。
- `useTestSession()` 订阅统一事件：登录成功后重拉 `/api/auth/session`；退出成功后立即清空本地 session，同时重拉服务端 session 做最终一致性确认。
- 不新增数据库集合，不修改聊天、联系方式交换、密码存储或密码登录主逻辑。

验证结果：
- `npm test -- auth-session-events`：先按 TDD 红灯失败，暴露事件模块默认目标与事件名/通知行为未满足本轮测试；实现后通过，3 个测试。
- `npm run typecheck`：已通过。
- 完整验证待本次提交前继续运行：`npm test`、`npm run lint`、`npm run build`。

未覆盖项与边界：
- 当前 Vitest 配置为 Node 环境，未引入 jsdom / Testing Library；本次自动化测试覆盖统一事件机制本身，导航 DOM 切换将结合构建和本地冒烟验证。
- 本轮不关闭 Issue，不扩展支付、排课、实名认证、合同、审核后台、第三方登录、多因素认证、复杂账号合并或聊天高级能力。

## 2026-06-27

### 登录系统顺利落地代码实现

类型：正式手机号验证码登录代码实现

任务 ID：`LOGIN-RUN-2026-06-27-001-CODE`

依据文档：
- `规划文档/里程碑文档/登录系统顺利落地准备/登录系统顺利落地阶段执行打包说明.md`
- `规划文档/Spec文档/Release_version_Spec/2026-06-25-mvp-正式生产上线-spec.md`
- `规划文档/里程碑文档/正式生产上线阶段验收报告/2026-06-25-mvp正式生产上线第二次最终审查报告.md`

涉及文件：
- `Code文档/app/login/page.tsx`
- `Code文档/features/auth/login-form.tsx`
- `Code文档/features/auth/phone-auth.ts`
- `Code文档/server/sms-auth.ts`
- `Code文档/server/sms-auth-api.ts`
- `Code文档/server/sms-delivery.ts`
- `Code文档/app/api/auth/sms/send-code/route.ts`
- `Code文档/app/api/auth/sms/login/route.ts`
- `Code文档/tests/sms-auth.test.ts`
- `Code文档/.env.example`
- `Code文档/README.md`
- `规划文档/里程碑文档/登录系统顺利落地准备/代码侧登录系统落地说明.md`
- `Code文档/开发员工作记录.md`

内容摘要：
- 登录页移除 M1 测试登录说明，改为手机号验证码登录 / 注册流程。
- 新增 `POST /api/auth/sms/send-code`，服务端校验手机号、生成一次性验证码、保存验证码哈希、设置 5 分钟有效期和 60 秒重复发送频控。
- 新增 `POST /api/auth/sms/login`，校验验证码并登录；新手机号首次通过后创建 `sms_login_users` 账号，老手机号再次通过后复用原账号。
- 登录态继续使用 `ungradu_auth_session` HttpOnly 签名 Cookie；前端不能通过传入 `userId` 冒充身份。
- 生产环境继续禁用 `/api/auth/test-login`、固定测试验证码 `000000`、`x-ungradu-test-user-phone` 和前端测试登录入口。
- 新增验证码错误次数限制、过期拒绝、重复使用拒绝和响应脱敏；接口不返回验证码或完整手机号。
- 新增腾讯云短信服务端 TC3 签名调用适配和环境变量占位说明；真实短信账号、签名、模板、密钥、额度和告警仍由项目总控制人配置。

验证计划：
- `npm test -- sms-auth`：已通过，1 个测试文件，9 个测试。
- `npm run typecheck`：通过。
- `npm test`：通过，33 个测试文件，126 个测试。
- `npm run lint`：通过。
- `npm run build`：通过，生成 26 条 App 路由，包含 `/api/auth/sms/send-code` 与 `/api/auth/sms/login`。
- `npm run release:production:preflight`：通过。模拟误配 `M5_ENABLE_HOSTED_TEST_LOGIN=true` 和 `NEXT_PUBLIC_ALLOW_TEST_LOGIN=true` 时，仍确认生产测试登录、临时身份头和前端测试入口被拒绝或隐藏。

当前边界：
- 本次未写入真实短信密钥、真实验证码或真实手机号名单。
- 真实腾讯云短信发送需要项目总控制人提供生产短信配置并完成服务商侧审核。
- 如短信服务尚未就绪，不得恢复测试登录或使用 `000000` 作为生产验证码。

### 登录系统方案变更：邮箱验证码返工

类型：登录系统顺利落地阶段方案变更代码返工

任务 ID：`LOGIN-RUN-2026-06-27-001-CODE`

变更原因：
- 项目总控制人为个人认证用户，腾讯云国内短信签名 / 模板资质暂不可落地。
- 正式登录方案从“手机号短信验证码登录”变更为“邮箱验证码登录”。
- 本次属于需求方案变更，不属于上一轮代码实现偏差。

依据文档：
- `规划文档/里程碑文档/登录系统顺利落地准备/登录系统顺利落地方案变更说明-邮箱验证码.md`
- `规划文档/里程碑文档/登录系统顺利落地准备/登录系统顺利落地阶段执行打包说明.md`

涉及文件：
- `Code文档/app/login/page.tsx`
- `Code文档/features/auth/login-form.tsx`
- `Code文档/features/auth/email-auth.ts`
- `Code文档/server/email-auth.ts`
- `Code文档/server/email-auth-api.ts`
- `Code文档/server/email-delivery.ts`
- `Code文档/app/api/auth/email/send-code/route.ts`
- `Code文档/app/api/auth/email/login/route.ts`
- `Code文档/server/auth-session.ts`
- `Code文档/server/api-utils.ts`
- `Code文档/features/auth/test-auth.ts`
- `Code文档/tests/email-auth.test.ts`
- `Code文档/scripts/production-readiness-check.mjs`
- `Code文档/tests/production-readiness-script.test.ts`
- `Code文档/.env.example`
- `Code文档/README.md`
- `规划文档/里程碑文档/登录系统顺利落地准备/代码侧登录系统落地说明.md`

移除文件：
- `Code文档/features/auth/phone-auth.ts`
- `Code文档/server/sms-auth.ts`
- `Code文档/server/sms-auth-api.ts`
- `Code文档/server/sms-delivery.ts`
- `Code文档/app/api/auth/sms/send-code/route.ts`
- `Code文档/app/api/auth/sms/login/route.ts`
- `Code文档/tests/sms-auth.test.ts`

内容摘要：
- 登录页和登录表单改为邮箱验证码登录 / 注册，不再要求手机号短信。
- 新增 `POST /api/auth/email/send-code`，服务端校验邮箱、生成一次性 6 位验证码、保存验证码哈希、设置 5 分钟有效期和 60 秒重复发送频控。
- 新增 `POST /api/auth/email/login`，校验邮箱验证码并登录；新邮箱首次通过后创建 `email_login_users` 账号，老邮箱再次通过后复用原账号。
- 登录态继续使用 `ungradu_auth_session` HttpOnly 签名 Cookie；邮箱账号身份使用稳定 `email_<hash>` 形式，前端不能通过传入 `userId` 冒充身份。
- 保留旧 `phone` Cookie 字段读取能力，仅用于兼容历史 M1-M5 测试登录会话；正式邮箱登录不依赖前端手机号。
- 生产环境继续禁用 `/api/auth/test-login`、固定测试码 `000000`、`x-ungradu-test-user-phone` 和前端测试登录入口。
- 邮箱验证码实现发送频控、错误次数限制、过期拒绝、重复使用拒绝和响应脱敏；接口不返回验证码或完整邮箱。
- 新增 SMTP / console 邮件发送适配。`EMAIL_PROVIDER=console` 仅允许非生产环境使用，生产缺少 SMTP 配置时发送失败且不回传验证码。
- 生产预检新增 `EMAIL_CODE_SECRET`、`EMAIL_PROVIDER=smtp` 和 SMTP 发信配置检查，确保真实生产缺少邮件发送配置时提前失败。
- `.env.example`、README 和代码侧落地说明已从短信变量 / 短信接口口径调整为邮箱变量 / 邮箱接口口径。

验证结果：
- `npm test -- email-auth`：已通过，1 个测试文件，10 个测试。
- `npm run typecheck`：已通过。
- `npm test`：已通过，33 个测试文件，128 个测试。
- `npm run lint`：已通过。
- `npm run build`：已通过，生成 26 条 App 路由；构建产物包含 `/api/auth/email/send-code` 与 `/api/auth/email/login`，不再包含 `/api/auth/sms/*` 路由。
- `npm run release:production:preflight`：已通过。使用占位符生产环境变量模拟 `APP_ENV=production`，并故意设置 `M5_ENABLE_HOSTED_TEST_LOGIN=true` 与 `NEXT_PUBLIC_ALLOW_TEST_LOGIN=true`；预检输出 warning，同时确认生产测试登录、`x-ungradu-test-user-phone` 和前端测试入口仍被拒绝或隐藏。

当前边界：
- 本次未写入真实邮件密钥、真实验证码、完整真实邮箱名单或真实生产配置。
- 真实邮箱发送需要项目总控制人提供生产邮件服务商 / SMTP 配置、发件人、额度、退信告警和试运行邮箱范围。
- 正式生产仍必须配置 `APP_ENV=production`，不得启用 `M5_ENABLE_HOSTED_TEST_LOGIN=true` 或 `NEXT_PUBLIC_ALLOW_TEST_LOGIN=true`。
- 不得为了登录可用而恢复测试登录、固定验证码或短信兼容入口。

## 2026-06-28

### 登录与发布体验优化

类型：正式环境试用反馈体验优化 / 发布表单与登录入口收口

任务 ID：`UX-PUBLISH-2026-06-28-001`

依据文档：
- `规划文档/里程碑文档/登录与发布体验优化准备/登录与发布体验优化阶段执行打包说明.md`
- `规划文档/产品经理工作记录.md`

涉及文件：
- `Code文档/app/layout.tsx`
- `Code文档/app/login/page.tsx`
- `Code文档/features/auth/session-nav.tsx`
- `Code文档/features/auth/login-page-content.tsx`
- `Code文档/app/tutor-profiles/new/page.tsx`
- `Code文档/app/tutor-profiles/page.tsx`
- `Code文档/app/tutor-profiles/[id]/page.tsx`
- `Code文档/app/profile/tutor-profiles/page.tsx`
- `Code文档/app/parent-needs/new/page.tsx`
- `Code文档/app/profile/parent-needs/page.tsx`
- `Code文档/app/profile/contact/page.tsx`
- `Code文档/app/profile/chats/page.tsx`
- `Code文档/app/chats/[id]/page.tsx`
- `Code文档/features/tutor-profiles/tutor-profile.ts`
- `Code文档/tests/tutor-profile.test.ts`
- `Code文档/app/globals.css`
- `Code文档/README.md`
- `Code文档/开发员工作记录.md`

内容摘要：
- 发布家教信息表单支持多组“学段 + 科目 + 课时费”，提供“新增一组”和删除按钮，并保证至少保留 1 组。
- 家教信息列表、详情页和“我的家教信息”统一使用 `formatTutorFeeRange()` 展示多组收费规则，兼容旧数据中缺少学段或科目的收费记录。
- 顶部导航改为根据登录态展示入口：未登录显示“登录”，已登录显示“个人页 / 退出登录”。
- 已登录用户访问 `/login` 时不再展示邮箱验证码表单，而是显示已登录状态、进入个人页和退出登录入口。
- 发布需求、发布家教信息、联系方式管理、我的需求、我的家教信息、我的聊天和聊天详情补充返回入口。
- 普通用户页面清理“当前测试账号”“服务端集合名”“来源 ID”等内部技术字段，改为中文业务文案。
- README 同步修正 `auth` 模块说明为邮箱验证码注册登录，并补充本轮体验优化说明。

验证结果：
- `npm test -- tutor-profile.test.ts`：先失败，失败原因为 `formatTutorFeeRange` 不存在；实现 helper 后通过，1 个测试文件，7 个测试。
- `npm run typecheck`：已通过。
- `npm test`：已通过，33 个测试文件，130 个测试。
- `npm run lint`：已通过。
- `npm run build`：已通过，生成 26 条 App 路由。
- 本地 Chrome headless 冒烟：已通过。临时启动 `npm run dev -- --hostname 127.0.0.1 --port 3001`，验证未登录登录页显示邮箱表单、已登录后 `/login` 隐藏登录表单并显示已登录状态、导航隐藏“登录”并显示“退出登录”、发布家教信息页存在返回入口和“新增一组学段与课时费”按钮、点击后收费规则从 1 组增加到 2 组、我的聊天页存在返回个人页入口且不展示“来源 ID”或“当前测试账号”文案。验证后已停止临时 dev server。

当前边界：
- 本轮不扩展支付、排课、实名认证、审核后台、举报封禁后台、自动推荐或聊天高级功能。
- 聊天窗完整验收仍需要项目总控制人提供第二个可用邮箱账号，用于双账号模拟家长和老师；当前单账号条件下不判定聊天窗已坏。
- 本次未写入真实密钥、真实验证码或真实邮箱名单。

### 生产试运行登录与基础体验缺陷修复

类型：正式生产试运行阶段登录可靠性 / 导航体验 / 密码登录能力修复

任务 ID：`PROD-UX-AUTH-2026-06-28-001-DEV-1`

分支：`codex/prod-ux-auth-fixes-20260628`

基线 commit：`987d59aad58ca50e2194232692dfd43835abced7`（`feat: improve login and tutor publishing UX`）

目标 commit：本次提交；最终哈希以交付说明和 `git log --oneline -1` 为准。

依据文档：
- `规划文档/里程碑文档/生产试运行体验修复准备/生产试运行登录与基础体验缺陷修复执行包.md`
- `协同工作文档/ISSUE/Open_Issue/ISSUE-0008-表单详情页缺少返回按钮.md`
- `协同工作文档/ISSUE/Open_Issue/ISSUE-0011-未登录状态显示退出缺少登录入口.md`
- `协同工作文档/ISSUE/Open_Issue/ISSUE-0012-退出登录缺少二次确认.md`
- `协同工作文档/ISSUE/Open_Issue/ISSUE-0013-验证码服务不可用后仍标记已使用.md`
- `协同工作文档/ISSUE/Open_Issue/ISSUE-0014-首次注册后设置密码登录.md`

涉及文件：
- `Code文档/server/email-auth.ts`
- `Code文档/server/email-auth-api.ts`
- `Code文档/app/api/auth/password/login/route.ts`
- `Code文档/app/api/auth/password/set/route.ts`
- `Code文档/app/api/auth/password/reset/route.ts`
- `Code文档/features/auth/login-form.tsx`
- `Code文档/features/auth/password-settings-form.tsx`
- `Code文档/features/auth/logout-button.tsx`
- `Code文档/features/auth/session-nav.tsx`
- `Code文档/app/login/page.tsx`
- `Code文档/app/profile/page.tsx`
- `Code文档/app/parent-needs/page.tsx`
- `Code文档/app/tutor-profiles/page.tsx`
- `Code文档/app/rules/page.tsx`
- `Code文档/app/feedback/page.tsx`
- `Code文档/app/globals.css`
- `Code文档/tests/email-auth.test.ts`
- `Code文档/.env.example`
- `Code文档/README.md`
- `Code文档/docs/生产试运行返回入口清单.md`
- `Code文档/开发员工作记录.md`

内容摘要：
- 修复 `ISSUE-0013`：邮箱验证码不再在验证码校验通过后立即标记 `usedAt`；现在先校验验证码、创建或复用邮箱账号、成功生成会话 Cookie，再标记验证码已使用。若登录服务配置缺失、会话创建失败或登录阶段不可用，返回“登录服务暂时不可用，请稍后重试”，且同一未过期验证码可继续重试。
- 补充邮箱验证码生命周期回归测试：覆盖“登录失败不消耗验证码”和“成功登录后才消耗验证码”。
- 实现 `ISSUE-0014` 最小密码能力：已登录邮箱账号可在个人页设置密码；登录页新增“密码登录”和“忘记密码”入口；忘记密码通过邮箱验证码重置；密码使用服务端 `crypto.scrypt` 加唯一 salt 保存，不保存明文。
- 密码登录失败使用“邮箱或密码不正确”通用提示，并增加基础失败次数限制。
- 修复 `ISSUE-0011`：未登录导航显示“登录 / 注册”，不显示“退出登录”；已登录才显示个人页和退出登录。
- 修复 `ISSUE-0012`：顶部导航和个人页退出登录均增加二次确认；取消不调用退出接口，确认后才退出。
- 修复 `ISSUE-0008`：补齐登录页、个人页、需求广场、家教信息广场、规则页和反馈页首屏返回入口；保留详情页、发布页、个人子页和聊天页既有返回入口。
- 新增 `Code文档/docs/生产试运行返回入口清单.md`，列出主要页面返回入口文案和目标路径。

验证结果：
- `git fetch --all --prune`：已执行。
- `npm test -- email-auth`：先按 TDD 失败，失败点为会话创建失败后验证码已写入 `usedAt`；调整后通过，17 个测试。
- `npm run typecheck`：通过。
- `npm test`：通过，33 个测试文件，137 个测试。
- `npm run lint`：通过。
- `npm run build`：通过，生成 29 条 App 路由，包含 `/api/auth/password/login`、`/api/auth/password/set`、`/api/auth/password/reset`。
- 本地 Browser 冒烟：临时启动 `npm run dev -- --hostname 127.0.0.1 --port 3002`，验证未登录首页导航显示“登录 / 注册 / 规则”且不显示“退出登录”；登录页显示“返回首页”和“验证码登录 / 密码登录 / 忘记密码”，切换到密码登录后显示密码输入框，切换到忘记密码后显示确认新密码；需求广场、家教信息广场、规则页、反馈页显示“返回首页”；受保护页面未登录时跳转登录页并保留 `next`。

未覆盖项与边界：
- 本轮未连接真实生产 SMTP，不发送真实验证码，不写入真实密钥、真实验证码、完整真实邮箱名单或真实密码。
- 已登录状态下的退出二次确认已由源码、构建和浏览器旧会话导航状态核查覆盖；本地 Browser 未通过真实邮箱验证码完成完整登录，因为当前环境不使用真实 SMTP。
- 受保护页面在未登录状态会先跳转登录页；登录页提供返回首页和 `next` 回跳机制。
- 本轮不关闭 Issue；Issue 状态仍由 ISSUE 管理员处理。
- 本轮不扩展支付、复杂排课、实名认证、合同、审核后台、第三方登录、多因素认证、复杂账号合并或聊天高级能力。

### S2 生产运行观察与运维基线

类型：发布阶段 S2 / 生产运行观察 / 运维基线

任务 ID：`RELEASE-S2-OPS-2026-06-30-001-DEV-1`

依据文档：
- `规划文档/里程碑文档/生产运行观察与运维基线准备/S2生产运行观察与运维基线执行包.md`
- `规划文档/里程碑文档/生产运行观察与运维基线准备/生产运行观察记录模板.md`
- `规划文档/里程碑文档/生产运行观察与运维基线准备/生产问题分级与响应规则.md`
- `规划文档/里程碑文档/生产运行观察与运维基线准备/部署回滚与环境配置核对清单.md`
- `规划文档/里程碑文档/生产运行观察与运维基线准备/数据库集合与权限配置检查表.md`

涉及文件：
- `Code文档/scripts/production-ops-baseline-check.mjs`
- `Code文档/tests/production-ops-baseline-script.test.ts`
- `Code文档/package.json`
- `Code文档/README.md`
- `Code文档/开发员工作记录.md`

内容摘要：
- 新增 `npm run production:ops:baseline`。
- 新增无密钥 S2 运维基线检查脚本，检查 S2 运行观察文档、生产环境变量名称、禁用测试登录开关、必备 CloudBase 集合清单、当前发布修复 head 和回滚记录口径。
- 脚本不连接 CloudBase，不读取真实生产数据，不打印真实密钥、验证码、密码或生产用户信息。
- 如果当前 shell 误设 `M5_ENABLE_HOSTED_TEST_LOGIN=true` 或 `NEXT_PUBLIC_ALLOW_TEST_LOGIN=true`，脚本会失败，避免生产运行基线接受测试登录开关。
- 补充脚本测试，验证通过路径不泄露敏感环境变量值，并验证测试登录开关误设会失败。

验证结果：
- `npm run production:ops:baseline`：通过；输出 S2 运维基线检查通过，并提示回滚基线仍依赖部署平台记录。
- `npm test -- production-ops-baseline-script`：通过，1 个测试文件，2 个测试。
- `npm run typecheck`：通过。
- `npm test`：通过，35 个测试文件，145 个测试。
- `npm run lint`：通过。
- `npm run build`：通过，生成 29 条 App 路由。

边界说明：
- S2 不新增业务功能。
- S2 不实现客服与人工介入，也不实现智能 Agent 客服。
- 真实生产 URL 冒烟、真实 SMTP 验证、真实 CloudBase 权限检查和部署平台回滚演练仍需由项目总控制人在安全环境执行。

### S3 风控审核与可信度增强代码侧核查

类型：发布阶段 S3 / 风控审核 / 可信边界文案收口

任务 ID：`RELEASE-S3-TRUST-2026-06-30-001-DEV-1`

依据文档：
- `规划文档/里程碑文档/风控审核与可信度增强准备/S3风控审核与可信度增强执行包.md`
- `规划文档/里程碑文档/风控审核与可信度增强准备/风控与审核Spec.md`
- `规划文档/里程碑文档/风控审核与可信度增强准备/联系方式与隐私保护规则.md`
- `规划文档/里程碑文档/风控审核与可信度增强准备/举报入口与风险提示最小闭环说明.md`

涉及文件：
- `Code文档/app/page.tsx`
- `Code文档/app/feedback/page.tsx`
- `Code文档/开发员工作记录.md`

内容摘要：
- 只读核查确认：反馈入口存在；联系方式交换具备二次确认；公开需求 / 家教信息服务端 View Model 过滤 `ownerUserId`；公开页面不直接展示联系方式。
- 发现首页和反馈页原文案使用“反馈/联系客服”及“人工查看”口径，容易误导为当前已有客服 / 人工介入能力。
- 已将相关入口文案调整为“风险与功能反馈”，并明确当前阶段仅记录和排查反馈，不承诺即时客服或人工介入处理。

验证结果：
- `rg -n "联系客服|人工查看|即时客服|人工介入" Code文档/app Code文档/features -S`：未发现误导性“联系客服 / 人工查看”承诺文案；仅保留反馈页“当前阶段仅记录和排查反馈，不承诺即时客服或人工介入处理”。
- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm test`：通过，35 个测试文件，145 个测试。
- `npm run build`：通过，生成 29 条 App 路由。

### S4 公开推广前发布评审代码侧核查

类型：发布阶段 S4 / 公开推广前发布评审 / 代码承诺边界核查

任务 ID：`RELEASE-S4-PROMOTION-2026-06-30-001-DEV-1`

依据文档：
- `规划文档/里程碑文档/公开推广前发布评审准备/S4公开推广前发布评审执行包.md`
- `规划文档/里程碑文档/公开推广前发布评审准备/公开推广风险清单.md`
- `规划文档/里程碑文档/公开推广前发布评审准备/客服与人工介入缺口说明.md`
- `规划文档/里程碑文档/公开推广前发布评审阶段验收报告/2026-06-30-S4公开推广前发布评审报告.md`

涉及文件：
- `Code文档/开发员工作记录.md`

内容摘要：
- 本阶段不新增业务代码，不实现客服系统、人工介入、智能 Agent 客服、举报投诉后台、老师认证、实名认证、交易担保、退款仲裁或公开推广能力。
- 代码侧核查确认：当前实现没有发现阻塞小范围继续试运行的已知代码问题。
- S4 风险主要集中在运营承接、客服 / 人工介入、举报投诉闭环、审核认证能力等产品与运营边界。
- 反馈页保留边界文案：“当前阶段仅记录和排查反馈，不承诺即时客服或人工介入处理。”

验证结果：
- 代码文案风险扫描：未发现“允许公开推广”“平台认证”“已认证”“担保”“退款”“仲裁”等超出当前能力的用户承诺。
- `npm run production:ops:baseline`：通过。
- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm test`：通过，35 个测试文件，145 个测试用例。
- `npm run build`：通过，生成 29 条 App 路由。

结论：
- S4 代码侧核查通过。
- 当前决策不是公开推广，而是允许小范围继续试运行，不建议公开推广。
## 2026-07-02

操作类型：代码开发员 Agent v2.1.0 新会话线程迁移

涉及文档：

- `Code文档/钦定代码开发员.md`
- `协同工作文档/AGENT身份注册信息/代码开发员Agent-019f2318-ec47-7870-9097-ca7124837a26.md`
- `协同工作文档/AGENT身份注册信息/AGENT注册状态总览.md`

记录摘要：

- 根据业务方要求，代码开发员 Agent 已迁移到 v2.1.0 新会话线程。
- 当前代码开发员 Agent 会话 ID 更新为 `019f2318-ec47-7870-9097-ca7124837a26`。
- 历史会话 ID `019f0d73-572a-72e1-90ca-ab6a30ef8343` 仅作为旧线程归档参考，不再作为当前调度入口。
- 新线程已收到只读初始化指令，需读取 `AGENTS.md`、钦定代码开发员、开发员工作记录和注册信息，熟悉职责、边界和权限规则。
- 新规则：后台线程默认只读汇报；写代码、npm 验证、commit、push 仅在用户明确确认的前台完全访问线程执行，不再触发权限审批弹窗。

### D+ 高对比漫画风第 1 批入口页面落地

类型：UI 视觉系统第 1 批实现 / 入口页与低风险公开页改造

任务 ID：`UI-DPLUS-IMPLEMENT-2026-07-05-001`

关联设计任务：`UI-REDESIGN-MULTI-STYLE-2026-07-03-001`

参考设计文档：
- `UI美术文档/多风格重设计方案/2026-07-03-四套视觉方向完整重设计方案.md`
- `UI美术文档/多风格重设计方案/2026-07-05-D-plus视觉落地规范.md`
- `UI美术文档/多风格重设计方案/预览图/D+全界面/`

涉及文件：
- `Code文档/app/globals.css`
- `Code文档/app/page.tsx`
- `Code文档/app/login/page.tsx`
- `Code文档/features/auth/login-form.tsx`
- `Code文档/app/rules/page.tsx`
- `Code文档/app/feedback/page.tsx`
- `Code文档/开发员工作记录.md`

内容摘要：
- 新增 D+ 高对比漫画风全局 token 与基础组件视觉覆盖：纸白底、黑色粗描边、硬阴影、黄 / 橙 / 青绿强色块、按钮和标签溢出保护、移动端小阴影规则。
- 首页改为 D+ 业务闭环表达：家长 / 学生发布需求、大学生发布家教资料、站内沟通、双方同意后交换联系方式。
- 登录页保留当前真实邮箱验证码 / 邮箱密码登录口径，明确不提供手机号登录、实名认证或第三方认证入口；未修改登录请求、验证码、密码、session 或跳转逻辑。
- 规则页和反馈页改为 D+ 漫画卡片视觉，并继续保留“不支付、不担保、不认证、不人工仲裁、不即时客服”的业务边界。
- 本批未触碰广场、详情、发布页、聊天页、API、server、数据模型、CloudBase、环境变量或联系方式交换逻辑。

验证结果：
- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm test`：通过，35 个测试文件，145 个测试。
- `npm run build`：通过，生成 29 条 App 路由。

本地冒烟：
- 尝试使用临时 `npm run dev -- --hostname 127.0.0.1 --port 3003` 和 PowerShell Job 包裹的 `3004` 端口访问 `/`、`/login`、`/rules`、`/feedback`，两次均在 dev server 启动 / HTTP 访问阶段超时。
- 已检查并清理临时 Node 进程，未发现残留 Node 进程。
- 因冒烟未完成，当前仍建议后续在可稳定前台浏览器线程或完全访问线程中补一次四页视觉冒烟截图。

边界说明：
- 本批为纯 UI 改造；不新增支付、担保、认证、审核、仲裁、自动推荐、客服承诺或联系方式公开能力。
- 当前线程未执行 commit / push。

### D+ 第 1 批验收阶段登录页文案收口

类型：UI 验收反馈修正 / 登录页文案边界收口

任务 ID：`UI-DPLUS-IMPLEMENT-2026-07-05-001`

涉及文件：
- `Code文档/app/login/page.tsx`

内容摘要：
- UI 设计师复核指出 `/login` 中出现“手机号登录”字样，虽然语义是否定说明，但容易形成不存在的登录入口概念。
- 总负责人在前台完全访问线程内将登录页壳层说明调整为“当前仅支持邮箱验证码登录或注册；设置密码后，也可以使用邮箱和密码登录。这里不提供实名认证或第三方认证入口。”
- 本次仅改展示文案，不修改登录请求、验证码、密码、session、跳转或任何服务端逻辑。

补充验证结果：
- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm test`：通过，35 个测试文件，145 个测试。
- `npm run build`：通过，生成 29 条 App 路由。
- 前台浏览器 `/login`：邮箱验证码表单正常出现，`手机号登录` / `手机登录` 不再出现，无 console error/warn。

### D+ 高对比漫画风第 2 批核心业务页面落地

类型：UI 视觉系统第 2 批实现 / 广场、详情与发布页改造

任务 ID：`UI-DPLUS-IMPLEMENT-2026-07-05-002`

关联设计任务：`UI-REDESIGN-MULTI-STYLE-2026-07-03-001`

涉及文件：
- `Code文档/app/globals.css`
- `Code文档/app/parent-needs/page.tsx`
- `Code文档/app/tutor-profiles/page.tsx`
- `Code文档/app/parent-needs/[id]/page.tsx`
- `Code文档/app/tutor-profiles/[id]/page.tsx`
- `Code文档/app/parent-needs/new/page.tsx`
- `Code文档/app/tutor-profiles/new/page.tsx`

内容摘要：
- 需求广场和家教广场加入 D+ 业务安全条，强调公开信息、站内沟通、双方同意后交换联系方式。
- 广场列表卡加入“联系方式未公开”状态与站内沟通提示，保留原筛选、列表加载和详情跳转逻辑。
- 需求详情和家教详情加入 D+ 提示文案，明确下一步是站内沟通，不是支付、担保、认证、仲裁或自动推荐。
- 详情侧栏加入“联系方式未公开”状态卡，继续强调公开详情不展示手机号、微信号或内部账号标识。
- 发布需求和发布家教信息页面强化“公开说明不要填写联系方式”的提示，仍使用原有 `RequireTestSession`、提交 API、表单字段和校验逻辑。
- 补充第 2 批 D+ CSS：业务页外框、状态条、列表行动堆叠、详情状态卡、发布页隐私强提示和移动端堆叠收敛。
- 修复后台写入过程中产生的 UTF-8 BOM、字面量换行残留和文件尾多余空行。

验证结果：
- `git diff --check`：通过，仅有 CRLF 提示。
- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm test`：通过，35 个测试文件，145 个测试。
- `npm run build`：首次因 Windows 下 `.next` 短暂占用报 `ENOTEMPTY`，确认无残留 Next dev 进程后复跑通过，生成 29 条 App 路由。

前台浏览器冒烟：
- 本地 dev server：`http://127.0.0.1:3013`。
- 桌面端检查 `/parent-needs`、`/tutor-profiles`、`/parent-needs/__missing__`、`/tutor-profiles/__missing__`：页面可打开，无 framework overlay，无 console error/warn，无横向溢出，隐私边界文案可见。
- 发布页 `/parent-needs/new`、`/tutor-profiles/new` 未登录状态正确进入登录保护；发布表单完整视觉已由代码结构、类型检查和构建验证覆盖，未在浏览器中绕过 HttpOnly 会话强行查看。
- 移动端 390px 抽查 `/parent-needs`、`/tutor-profiles`：无横向溢出，安全条和联系方式未公开状态可见。
- 筛选交互：在 `/parent-needs` 输入预算下限 `80` 后点击筛选，URL 更新为 `/parent-needs?budgetMin=80`，页面无 console error/warn。

边界说明：
- 本批为纯 UI 改造；不修改 API、server、存储、登录、session、聊天、联系方式交换逻辑、数据模型、CloudBase 或环境变量。
- 公开列表和详情页仍不展示直接联系方式。
- 未新增支付、担保、认证、审核、退款、仲裁、自动推荐或评分排名能力暗示。

### D+ 高对比漫画风第 3 批个人中心与聊天页面落地

类型：UI 视觉系统第 3 批实现 / 个人中心、联系方式、我的列表与聊天页改造

任务 ID：`UI-DPLUS-IMPLEMENT-2026-07-05-003`

关联设计任务：`UI-REDESIGN-MULTI-STYLE-2026-07-03-001`

涉及文件：
- `Code文档/app/globals.css`
- `Code文档/app/profile/page.tsx`
- `Code文档/app/profile/contact/page.tsx`
- `Code文档/app/profile/parent-needs/page.tsx`
- `Code文档/app/profile/tutor-profiles/page.tsx`
- `Code文档/app/profile/chats/page.tsx`
- `Code文档/app/chats/[id]/page.tsx`

内容摘要：
- 个人中心增加 D+ 页面容器、账号工作台 banner、隐私规则胶囊和更明确的功能入口卡片。
- 联系方式页套用 D+ 个人中心视觉容器，继续强调“未授权前不展示”。
- 我的需求、我的家教信息、我的聊天增加统一分类标签，列表视觉改为整组行式面板，减少碎片感。
- 聊天详情页增加 D+ 页面容器和聊天工作区样式，强化联系方式交换状态区、消息区和输入区的视觉层级。
- 本批仅调整 JSX 结构和 CSS 样式，不修改 API、server、登录、发布、聊天、联系方式交换、数据模型、CloudBase 或环境变量。

验证结果：
- `git diff --check`：通过，仅有 CRLF 提示。
- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm test`：通过，35 个测试文件，145 个测试。
- `npm run build`：通过，生成 29 条 App 路由。

前台浏览器冒烟：
- 本地 dev server：`http://127.0.0.1:3014`。
- 未登录状态：`/profile`、`/profile/contact`、`/profile/parent-needs`、`/profile/tutor-profiles`、`/profile/chats`、`/chats/__missing__` 均正确进入登录守卫。
- 登录态测试账号：`13800138000`，测试验证码 `000000`。
- 登录态桌面检查 `/profile`、`/profile/contact`、`/profile/parent-needs`、`/profile/tutor-profiles`、`/profile/chats`、`/chats/__missing__` 均正常渲染，无 Next overlay、无横向溢出。
- 移动端 390px 抽查 `/profile`、`/profile/contact`、`/profile/chats`、`/chats/__missing__`：无横向溢出。

边界说明：
- 未新增支付、担保、认证、审核、退款、仲裁、自动推荐、评分排名或匹配百分比能力暗示。
- 联系方式仍仅作为私密资料保存，并在双方同意和二次确认后通过会话授权展示。
- dev 模式 React DevTools info、既有密码输入 autocomplete verbose 建议和 favicon 404 不属于本批 UI 阻断项。

## 2026-07-07

### vNext UI 文案精简与风险反馈最小闭环

类型：vNext UI 文案精简 / 风险反馈最小记录能力

任务 ID：`VNEXT-UI-COPY-RISK-2026-07-07-001-DEV-1`

关联 Issue：
- `ISSUE-0016`：UI 界面多余说明文字清理。
- `ISSUE-0017`：风险反馈 / 举报投诉最小记录能力缺口。

涉及文件：
- `Code文档/app/page.tsx`
- `Code文档/app/login/page.tsx`
- `Code文档/app/rules/page.tsx`
- `Code文档/app/feedback/page.tsx`
- `Code文档/app/parent-needs/new/page.tsx`
- `Code文档/app/tutor-profiles/new/page.tsx`
- `Code文档/app/profile/page.tsx`
- `Code文档/app/profile/contact/page.tsx`
- `Code文档/app/chats/[id]/page.tsx`
- `Code文档/app/layout.tsx`
- `Code文档/app/api/feedback/route.ts`
- `Code文档/features/feedback/risk-feedback.ts`
- `Code文档/features/feedback/risk-feedback-api-client.ts`
- `Code文档/server/risk-feedback.ts`
- `Code文档/server/risk-feedback-api.ts`
- `Code文档/tests/risk-feedback.test.ts`
- `Code文档/tests/risk-feedback-api.test.ts`
- `Code文档/tests/feedback-page-copy.test.ts`
- `Code文档/vitest.config.ts`
- `Code文档/开发员工作记录.md`

内容摘要：
- 按 UI 清单压缩首页、登录页、规则页、发布需求页、发布家教信息页、个人中心、联系方式页和聊天页用户可见文案。
- 删除或替换 `MVP`、任务分组、自动轮询、状态区、文件元信息、文件名 / 类型 / 大小等内部术语或实现说明。
- 保留并压缩公开页不展示联系方式、双方确认后交换、不要提交敏感信息、平台不做支付 / 担保 / 认证 / 仲裁 / 退款等必要风险边界。
- 将 `/feedback` 从外部 GitHub Issue 链接改为站内风险反馈表单，支持匿名提交、可选联系方式、反馈类型、反馈对象、相关链接或对象、问题描述、证据说明和提交来源。
- 新增 `/api/feedback`，按现有 Next.js + TypeScript + server/api handler 模式写入 CloudBase 集合 `risk_feedback_records`。
- 反馈提交成功文案明确“用于后续排查”，不承诺即时处理或人工仲裁；提交失败提示稍后重试并建议先复制保存已填写内容。
- 新增反馈输入校验、API handler 和反馈页关键文案测试；将 Vitest 单用例超时调整为 15 秒，避免既有生产脚本测试在全量并发时超过默认 5 秒。

验证结果：
- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm test`：通过，38 个测试文件，153 个测试。
- `npm run build`：通过，生成 30 条 App 路由，包含 `/api/feedback` 和 `/feedback`。
- 禁用词扫描：`rg -n "Step Form|Workspace|Dashboard|Contact Exchange|MVP|任务分组|状态区|自动轮询|文件名|类型和大小|客服会立即联系|工作日内处理|平台会介入裁决|必定退款|issues/new" Code文档/app --glob '!globals.css' -S` 未命中。

页面冒烟：
- 本地 dev server：`http://127.0.0.1:3015`。
- 桌面端 `/feedback`：页面返回 200，表单字段和 `提交风险反馈` 按钮可见，无 console error/warn，无横向溢出。
- 表单交互：可选择反馈类型 / 反馈对象并填写问题描述；当前本地 CloudBase 缺少 `risk_feedback_records` 集合，提交后显示“提交失败，请稍后重试；可先复制保存已填写内容。”，未出现控制台错误。
- 移动端 390px `/feedback`：`scrollWidth` 等于 `clientWidth`，无横向溢出；表单字段和风险边界文案可见。
- Browser DOM snapshot API 本次在 in-app Browser 中报运行时能力错误，冒烟改用只读页面状态、console logs、截图和真实表单交互完成。

配置与部署说明：
- 需要随部署新增 CloudBase 数据库集合 `risk_feedback_records`，否则 `/api/feedback` 会返回标准 JSON 失败并触发页面失败提示。
- 不需要新增前端环境变量或真实密钥。
- 本轮不新增运营查看后台，不新增客服、人工介入、仲裁、退款、担保、认证、支付或智能客服能力。

### 聊天页消息列表高度与内部滚动修复

类型：聊天页布局修复 / D+ UI 滚动约束

任务 ID：`CHAT-LAYOUT-FIX-2026-07-07-001-DEV-1`

涉及文件：
- `Code文档/app/globals.css`
- `Code文档/tests/chat-layout-css.test.ts`
- `Code文档/开发员工作记录.md`

根因：
- D+ 聊天页 `.message-list` 只有 `min-height`，没有 `max-height` 或 `overflow-y`，消息数量增加时会继续撑高聊天框。
- `.dplus-chat-page .conversation-main` 没有固定可视高度和内部网格行约束，无法把消息区限制在聊天窗口中间区域滚动。

内容摘要：
- 将 D+ 聊天主面板设为三行 grid：头部、消息列表、输入区。
- 用 `height: clamp(...)` 固定聊天窗口在视口内的可视高度，并给主面板补充 `min-height: 0`。
- 给消息列表增加 `max-height: 100%`、`overflow-y: auto` 和 `overscroll-behavior: contain`，使长聊天记录在消息区内部滚动。
- 给聊天工作区子项和消息气泡补充收缩 / 长文本断行保护，继续保护 390px 移动端无横向溢出。
- 新增 CSS 静态约束测试，覆盖聊天页必须具备固定高度 / 内部滚动约束。

验证结果：
- `npm test -- chat-layout-css`：先失败后通过，确认测试覆盖到缺失的滚动约束。
- 完整验证命令和页面冒烟结果见本任务最终交付说明。

配置与部署说明：
- 不需要新增环境变量、CloudBase 集合、真实密钥或部署配置。

### 智能客服首屏即时可用修复

类型：性能与首屏可用性修复 / 智能客服入口优化

任务 ID：`CUSTOMER-SERVICE-INSTANT-2026-07-07-001-DEV-1`

涉及文件：
- `Code文档/app/customer-service/page.tsx`
- `Code文档/features/customer-service/customer-service-chat.tsx`
- `Code文档/features/customer-service/tutor-customer-service-agent.ts`
- `Code文档/app/globals.css`
- `Code文档/tests/customer-service-page-copy.test.ts`
- `Code文档/tests/chat-layout-css.test.ts`
- `Code文档/开发员工作记录.md`

根因：
- `/customer-service` 页面显式设置 `export const dynamic = "force-dynamic"`，导致每次访问都走动态渲染，线上首次响应约 828ms。
- 配置 `NEXT_PUBLIC_DIFY_CUSTOMER_SERVICE_URL` 后，页面首屏直接渲染 Dify iframe，并用 iframe 替代站内离线客服，用户需要等待第三方 WebApp 加载后才能开始使用。
- 站内离线客服 `CustomerServiceChat` 和 `getTutorCustomerServiceReply` 本身是本地同步交互逻辑，不是首屏等待的根因。

内容摘要：
- 移除 `/customer-service` 的 `force-dynamic`，让页面恢复静态预渲染；构建结果中 `/customer-service` 显示为 `○ Static`。
- 页面主体验始终渲染站内离线客服 `CustomerServiceChat`，打开后即可看到快捷问题和输入框。
- Dify 不再作为首屏 iframe 渲染；如配置 URL，仅在侧栏显示“打开 Dify 客服”的可选外链入口，不阻塞主客服体验。
- 移动端将客服主体排到说明侧栏之前，并降低首屏消息区高度，保证 390px 视口内快捷问题和输入框可见。
- 更新页面文案测试和 CSS 约束测试，覆盖不强制动态、首屏站内客服存在、无 Dify iframe、移动端优先显示客服控制区。

验证结果：
- `npm test -- customer-service-page-copy`：先失败后通过，失败点命中旧的 `force-dynamic` 行为。
- `npm test -- chat-layout-css`：通过，覆盖客服移动端首屏控制区约束。
- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm test`：通过，42 个测试文件，167 个测试。
- `npm run build`：首次复跑因本轮冒烟 dev server 占用 `.next` 报 `ENOTEMPTY`；停止本轮 3016 端口 dev server 后复跑通过，生成 31 个静态页面，`/customer-service` 为 `○ Static`。

页面冒烟：
- 本地 dev server：`http://127.0.0.1:3016`。
- 桌面端 1440x900 `/customer-service`：快捷问题和输入框均在首屏内，输入框可输入并发送，点击快捷问题可立即追加回复；iframe 数量为 0，console 无 error/warn，无横向溢出。
- 移动端 390x844 `/customer-service`：快捷问题 top 约 220px，输入框 top 约 464px，均在首屏内；输入框可输入并发送，点击快捷问题可立即追加回复；iframe 数量为 0，console 无 error/warn，`scrollWidth` 等于 `clientWidth`。
- Browser DOM snapshot API 本次仍有运行时能力错误，冒烟改用只读 DOM 状态、唯一 CSS locator 交互和 console logs 完成。

配置与部署说明：
- 不需要新增依赖、环境变量、CloudBase 集合、真实密钥或部署配置。
- 如要启用 Dify 可选外链，继续使用既有 `NEXT_PUBLIC_DIFY_CUSTOMER_SERVICE_URL`；未配置时不影响站内离线客服首屏使用。

## 2026-07-17

### vNext 反馈状态最小闭环与公开访问安全提示

类型：vNext 反馈状态闭环 / 公开访问与安全提示体验优化

任务 ID：`VNEXT-WEB-ITERATION-2026-07-17-001-DEV-1`

分支：`codex/vnext-feedback-status-security-20260717`

基线 commit：`09ed3b937353ee3f37b04429763466d8e4e87cd5`

涉及文件：
- `Code文档/app/api/feedback/route.ts`
- `Code文档/app/feedback/page.tsx`
- `Code文档/app/globals.css`
- `Code文档/app/page.tsx`
- `Code文档/app/profile/page.tsx`
- `Code文档/app/rules/page.tsx`
- `Code文档/features/feedback/risk-feedback-api-client.ts`
- `Code文档/features/feedback/risk-feedback.ts`
- `Code文档/server/risk-feedback-api.ts`
- `Code文档/server/risk-feedback.ts`
- `Code文档/tests/feedback-page-copy.test.ts`
- `Code文档/tests/risk-feedback-api.test.ts`
- `Code文档/tests/risk-feedback.test.ts`
- `Code文档/tests/security-access-copy.test.ts`
- `Code文档/开发员工作记录.md`

实现摘要：
- 基于既有 `risk_feedback_records` 反馈能力补充轻量状态枚举与中文展示：`recorded`、`reviewing`、`closed`、`unable_to_process`。
- `/api/feedback` 增加 `GET`，只允许登录用户读取自己提交的反馈记录；匿名请求返回 401，不公开枚举匿名反馈。
- `/feedback` 提交成功后展示“已记录”、反馈编号、当前状态和“不承诺即时处理、人工仲裁、退款、担保或封禁”的边界文案。
- `/feedback` 页内增加“我的反馈记录”区块，展示分类、反馈对象、提交时间、状态和简短描述；未登录时提示匿名反馈后续可能无法查询。
- 首页、规则页、反馈页补充 HTTPS 推荐入口、隐私边界和 Cloudflare Worker 临时访问 / 基础安全加固提示。
- 个人中心增加“我的反馈”入口，指向 `/feedback` 页内记录区。
- 补充反馈状态、权限读取边界、匿名不可枚举和安全提示文案测试。

验证结果：
- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm test`：通过，45 个测试文件，175 个测试。
- `npm run build`：通过，生成 31 条 App 路由，`/api/feedback` 为动态路由，`/feedback` 为静态页面。

页面与 API 冒烟：
- 临时本地 dev server：`http://127.0.0.1:3017`。
- `/`：HTTP 200，安全提示可见。
- `/rules`：HTTP 200，安全提示可见。
- `/feedback`：HTTP 200，“我的反馈记录”和安全提示可见。
- 匿名 `GET /api/feedback`：HTTP 401，返回 `登录后才能查看自己的反馈记录。`，确认匿名反馈不可公开枚举。
- 冒烟结束后已停止 3017 端口临时 dev server 及残留 Next 子进程。

下游验收结论：
- 产品经理：通过，符合 Spec，允许继续下游验收 / 提交准备；`ISSUE-0020` 仍是生产门禁。
- UI 设计师：通过，无必须修复项。
- ISSUE 管理员：有条件通过，无需新开或重开 Issue；`ISSUE-0020` 继续保持 Open，生产验收最多有条件通过。
- Hermes deepseek-v4-pro：通过，无阻断问题 / 无必须返工项；非阻断建议为描述截断、分页和未来状态管理端。

配置与部署说明：
- 不需要新增环境变量、依赖或真实密钥。
- 继续依赖既有 CloudBase 集合 `risk_feedback_records`。
- 本轮不实现后台状态管理端，普通用户不能修改反馈状态。
- `ISSUE-0020` 不由本轮关闭，Cloudflare Worker、安全响应头、WAF / rate limit 或等价免费防护仍归生产安全门禁。

边界说明：
- 本轮未修改 PM Spec、PM 工作记录、UI 文档或 ISSUE 文档。
- 未关闭 `ISSUE-0020`，未创建 subagent。
- 未新增支付、担保、实名认证、人工仲裁、自动推荐、复杂后台、即时客服承诺、封禁承诺或退款处理。
- 本记录追加时，本轮代码准备提交；仓库仍存在大量既有脏文件和未跟踪文档 / ops 文件，提交时只暂存本轮白名单文件。

### ISSUE-0020 代码侧安全基线收口

类型：Cloudflare Worker 临时反代示例 / 安全响应头 / 生产运维基线脚本与测试证据

任务 ID：`ISSUE-0020-CODE-SECURITY-BASELINE-2026-07-17-001`

分支：`codex/vnext-feedback-status-security-20260717`

基线 commit：`f2b781f4`

涉及文件：
- `Code文档/cloudflare/README.md`
- `Code文档/cloudflare/worker.ts`
- `Code文档/cloudflare/wrangler.example.toml`
- `Code文档/next.config.ts`
- `Code文档/scripts/production-ops-baseline-check.mjs`
- `Code文档/tests/cloudflare-worker-proxy.test.ts`
- `Code文档/tests/production-ops-baseline-script.test.ts`
- `Code文档/tests/security-headers.test.ts`
- `Code文档/package.json`
- `Code文档/README.md`
- `Code文档/开发员工作记录.md`

实现摘要：
- 补充 `cloudflare/worker.ts` 临时反代示例，使用 `UPSTREAM_ORIGIN` 指向 CloudBase 默认生产域名，不写入真实密钥；转发路径和 query，并移除源站/框架识别类响应头。
- 补充 `cloudflare/wrangler.example.toml` 和 `cloudflare/README.md`，说明 Worker 示例部署方式、TLS / 安全设置边界和外部控制台证据不在代码开发员职责内。
- 在 `next.config.ts` 增加全站安全响应头：CSP、`X-Content-Type-Options`、`Referrer-Policy`、`Permissions-Policy`、`X-Frame-Options`、HSTS。
- 增加 `npm run production:ops:baseline` 仓库内无密钥检查脚本，覆盖 S2 运维文档、生产环境变量名称、禁用测试开关、CloudBase 必备集合清单和发布回滚基线口径。
- 补充 Worker、生产基线脚本和安全响应头测试。

验证结果：
- `npm run production:ops:baseline`：通过；输出 warning：回滚基线仍依赖部署平台真实发布记录。
- `npm test -- cloudflare-worker-proxy production-ops-baseline-script security-headers`：通过，3 个测试文件，6 个测试。
- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm test`：通过，45 个测试文件，175 个测试。
- `npm run build`：通过，生成 31 条 App 路由。

生产 / 外部入口只读探测：
- CloudBase 默认域名 `/`：HTTP 200。
- CloudBase 默认域名 `/rules`：HTTP 200。
- CloudBase 默认域名 `/feedback`：HTTP 200。
- CloudBase 默认域名匿名 `GET /api/feedback`：HTTP 405，不是当前仓库代码预期的 401，说明线上默认域名尚不能证明已部署本轮反馈状态 / 安全基线代码。
- 响应头抽检仍可见 `server: cbrgw` 和页面 `x-powered-by: Next.js`，未见本轮安全响应头落地证据；真实 Worker 域名、Cloudflare DNS / WAF / Bot / Rate Limit 和安全头线上验证仍需外部配置与 ISSUE 管理员复核。

边界说明：
- 本轮只处理代码开发员白名单文件，未修改 PM / UI / ISSUE 文档，未关闭 `ISSUE-0020`，未创建 subagent。
- 未写入任何真实 Cloudflare、CloudBase、腾讯云、SMTP、数据库密钥或外部账号配置。
- 代码侧基线可作为提交证据，但 `ISSUE-0020` 关闭仍缺 Cloudflare Worker 实际 URL、域名激活、WAF / rate limit 或等价免费防护、生产安全头和源站绕过风险处置等外部证据。

### ISSUE-0020 Cloudflare Dashboard 粘贴版 Worker 支持

类型：ISSUE-0020 最小解除支持 / Cloudflare Worker JavaScript 粘贴版

任务 ID：`ISSUE-0020-CLOUDFLARE-DASHBOARD-WORKER-JS-2026-07-18-001`

分支：`codex/vnext-feedback-status-security-20260717`

实现摘要：
- 新增 `Code文档/cloudflare/worker.js`，与 `worker.ts` 等价，但移除 TypeScript-only 语法，可直接粘贴到 Cloudflare Dashboard 的 `worker.js` 在线编辑器。
- `worker.js` 内置 CloudBase 默认生产域名作为无密钥 fallback，同时兼容 `UPSTREAM_ORIGIN` 环境变量。
- 更新 `Code文档/cloudflare/README.md`，补充最短 Dashboard 操作路径：编辑 Worker、粘贴 `worker.js`、Save and Deploy、访问四个路径验证。
- 扩展 `cloudflare-worker-proxy` 测试，覆盖 `worker.js` 语法检查、无 `type Env` / TS 标注、代理路径与 query、响应头硬化和无环境变量 fallback。

边界说明：
- 本轮只改代码侧 Cloudflare 示例、对应测试和开发员工作记录。
- 未操作用户 Cloudflare 账号，未写入真实密钥，未关闭 `ISSUE-0020`，未修改 PM / UI / ISSUE 文档，未创建 subagent。

### ISSUE-0020 Worker 线上 CloudBase 响应头去指纹返工

类型：REWORK_REQUIRED / Cloudflare Worker 线上去指纹

任务 ID：`ISSUE-0020-WORKER-CLOUDBASE-HEADER-STRIP-2026-07-18-001`

分支：`codex/vnext-feedback-status-security-20260717`

线上输入：
- 用户已部署 Worker：`https://ungradu-edu-proxy.vangewang0919.workers.dev/`。
- 总负责人只读核验：`/`、`/rules`、`/feedback` 均 200，匿名 `/api/feedback` 为 401，`CF-Ray` 存在，`x-powered-by` 已移除，安全头存在。
- 仍泄漏 `x-cloudbase-session-id`、`x-cloudbase-upstream-status-code`、`x-cloudbase-upstream-timecost`、`x-cloudbase-upstream-type`。

实现摘要：
- `worker.ts` 与 Dashboard 粘贴版 `worker.js` 的响应头清理逻辑改为固定黑名单加 `x-cloudbase-` 前缀通配，大小写不敏感删除所有 `X-CloudBase-*` 响应头。
- 保留安全响应头注入逻辑，不删除 Cloudflare 自身 `CF-Ray` 等边缘侧响应头。
- 更新 README，明确 Worker 会移除 `X-CloudBase-Session-Id` 和 `X-CloudBase-Upstream-*`。
- 扩展 `cloudflare-worker-proxy` 测试，覆盖大小写混合的 `X-CloudBase-*` 前缀去除，并确认 CSP、`Referrer-Policy`、`X-Content-Type-Options` 等安全头保留。

边界说明：
- 本轮只改代码侧 Cloudflare Worker、README、测试和开发员工作记录。
- 未操作用户 Cloudflare 账号，未修改 ISSUE / PM / UI 文档，未关闭 `ISSUE-0020`，未写入真实密钥，未创建 subagent。

### 代码开发员 Agent v2.2.0 会话迁移与正式绑定

类型：代码开发员角色连续性重新注册

任务 ID：`CODE-DEVELOPER-REBIND-2026-07-18-001`

新会话绑定：

- 会话 ID：`019f70ec-6331-7083-aecb-bb8484511518`
- 线程标题：`代码开发员v2.2.0`
- 角色版本：v2.2.0

历史绑定：

- `019f2318-ec47-7870-9097-ca7124837a26`：代码开发员 Agent v2.1.0，已归档，不再作为当前调度入口。
- `019f0d73-572a-72e1-90ca-ab6a30ef8343`：更早历史线程，仅作归档参考。

涉及文件：

- `Code文档/钦定代码开发员.md`
- `协同工作文档/AGENT身份注册信息/代码开发员Agent-019f70ec-6331-7083-aecb-bb8484511518.md`
- `Code文档/开发员工作记录.md`

连续性状态：

- 当前分支：`codex/vnext-feedback-status-security-20260717`。
- Cloudflare Dashboard JavaScript Worker 与全部 `X-CloudBase-*` 去指纹修复已提交并推送。
- 最新 Worker 修复 commit：`a141acf8af67ac4d94efea5735abe95a009c2ba8`。
- 线上 Worker `https://ungradu-edu-proxy.vangewang0919.workers.dev/` 已完成技术复测：核心页面返回 200，匿名反馈接口返回 401，`CF-Ray` 和关键安全响应头保留，`x-powered-by` 与全部 `X-CloudBase-*` 响应头已移除。

职责与写入边界：

- 新线程继承旧注册文件中的代码、测试、构建、脚本、代码文档和技术验证职责。
- 不作产品验收，不维护或关闭 Issue 状态，不操作用户 Cloudflare 账号。
- 本次仅维护上述三个代码开发员角色连续性文件；未修改中央 `AGENT注册状态总览.md`、协同总览、PM、UI 或 ISSUE 文件。
- 本次未运行 npm、git、部署或线上探测，未创建 subagent。

当前状态：

- 代码开发员 Agent v2.2.0 已完成自身角色文件接管与新会话绑定，等待职责范围内的下一项正式任务。

### 自定义公开域名切换技术检查与可执行配置

类型：Cloudflare Worker 自定义域名切换 / DNS 与线上技术验证

任务 ID：`DOMAIN-SWITCH-2026-07-18-001-DEV-1`

目标域名：

- 唯一公开主域名：`https://ungradeedu.eu.cc`
- `https://www.ungradeedu.eu.cc`：部署后以 308 重定向到根域并保留 path / query。
- 回退入口：`https://ungradu-edu-proxy.vangewang0919.workers.dev/`，保留资源，不删除。

涉及文件：

- `Code文档/cloudflare/worker.js`
- `Code文档/cloudflare/worker.ts`
- `Code文档/cloudflare/wrangler.example.toml`
- `Code文档/cloudflare/README.md`
- `Code文档/tests/cloudflare-worker-proxy.test.ts`
- `Code文档/开发员工作记录.md`

实现摘要：

- 将 Wrangler 示例的 Worker 名称对齐为现有 `ungradu-edu-proxy`，显式保留 `workers_dev = true`。
- 增加 `ungradeedu.eu.cc` 和 `www.ungradeedu.eu.cc` 两个 `custom_domain = true` 配置；根域为 canonical，`www` 由 Worker 返回 308 到根域。
- Worker 保留 CloudBase 生产地址作为上游，不把 CloudBase 地址改成公开主域名。
- 扩展上游响应头清理：除既有 `X-CloudBase-*` 外，继续移除 `X-CloudBaseRun-*`、`X-Upstream-*`、`X-Nextjs-*` 和上游 `X-Request-Id`。
- README 补充主域名、回退入口、Active zone 前置条件、Dashboard 最小配置步骤和不得从错误账户部署的边界。

TDD 与定向验证：

- 首轮 RED：`npm test -- cloudflare-worker-proxy` 失败，3 个用例命中缺少 Custom Domain 配置和 `www` canonical 重定向。
- 首轮 GREEN：实现配置和重定向后，同一命令通过，1 个测试文件、8 个测试。
- 线上抽检发现新指纹头后补第二轮 RED：同一命令失败，2 个用例命中 `x-cloudbaserun-scale-timecost` 未移除。
- 第二轮 GREEN：扩展清理规则后，同一命令通过，1 个测试文件、8 个测试。
- 根据总负责人收口指令，本轮未继续运行全量测试、lint、typecheck 或 build。

DNS / Cloudflare 检查结果：

- 公共 DNS `1.1.1.1`：`ungradeedu.eu.cc` 仅返回 `ns1.julydns.com`、`ns2.julydns.com` 委派，无 A / AAAA / CNAME / SOA；`www.ungradeedu.eu.cc` 无 A / AAAA / CNAME / NS / SOA。
- 对根域和 `www` 直接 HTTP / HTTPS 请求均因无法解析返回 curl exit 6；尚无可验证证书或重定向。
- 本机未安装 `wrangler`，未发现 `CLOUDFLARE_API_TOKEN`、账户 ID、API Key 或邮箱环境变量，因此没有有效 Cloudflare CLI 会话。
- 未执行任何 Cloudflare 外部变更，未添加、删除或覆盖现有路由。

现有 Worker 线上只读证据：

- 经现有网络代理成功抽检 `/feedback` 返回 200，匿名 `/api/feedback` 返回 401；`CF-Ray`、HSTS、CSP、Permissions-Policy、Referrer-Policy、`X-Content-Type-Options` 和 `X-Frame-Options` 存在。
- 当前已部署版本仍返回 `x-cloudbaserun-scale-timecost`、`x-upstream-status-code`、`x-nextjs-*` 和 `x-request-id`，说明本轮扩展去指纹代码尚未部署。
- `/`、`/rules` 在代理抽检中超时；随后绕过代理直连四个路径均 curl exit 28，属于当前执行环境到 `workers.dev:443` 的连接超时，不能据此宣称入口整体下线或本轮线上验证通过。

阻塞与最小解除动作：

- 阻塞责任方：域名 / DNS 与 Cloudflare 账户配置执行侧（项目总负责人或用户本人）。
- 最小解除动作：先把 `ungradeedu.eu.cc` 添加到拥有现有 Worker 的 Cloudflare 账户，将当前 `julydns` 权威 NS 改为 Cloudflare 分配的 NS，并等待 zone 变为 Active；随后在 `ungradu-edu-proxy` 的 Domains & Routes 添加根域和 `www` Custom Domain，保留 `workers.dev`，部署本轮 Worker 代码并等待证书 Active。
- 恢复触发条件：公共 DNS 可解析、Cloudflare zone / 两个证书均 Active，且有已验证的 Cloudflare CLI 会话或用户在 Dashboard 完成明确配置。

提交与验收边界：

- 本轮产生 Worker、Wrangler 配置、README、测试和开发记录变更，需要形成独立代码提交并推送后再部署；本轮未提交、未推送。
- DNS 尚未解析、证书尚未生效、代码尚未部署，因此本任务不能报告域名切换完成，也不作产品验收或关闭 `ISSUE-0020`。

### ISSUE-0020 生产返工部署与技术复测收口

类型：Cloudflare Worker 生产部署 / canonical redirect / 响应头去指纹

任务 ID：`DOMAIN-SWITCH-2026-07-18-001-DEV-1`

生产部署证据：

- Worker：`ungradu-edu-proxy`。
- 生产版本：`d8eff139`，Cloudflare Dashboard 状态为 Active。
- 证据接收时间：`2026-07-18 15:51:06 +08:00`；Dashboard 的精确部署时刻未单独记录。
- 由项目总负责人在代码开发员移交的同一编辑器页面执行 Deploy；未重复添加或删除域名路由。
- `ungradeedu.eu.cc`、`www.ungradeedu.eu.cc` 两个自定义域名与 `ungradu-edu-proxy.vangewang0919.workers.dev` 回退入口均保留。

代码与工程验证：

- `node --check cloudflare/worker.js`：通过。
- `npm test -- cloudflare-worker-proxy`：1 个测试文件、8 个测试全部通过。
- `npm run typecheck`：通过。
- `npm run lint`：通过，0 warning。
- `npm test`：45 个测试文件、181 个测试全部通过。
- `npm run build`：通过，Next.js 15.5.19 生产构建成功。

生产路由复测：

- `https://ungradeedu.eu.cc/`、`/rules`、`/feedback` 均返回 200。
- 匿名请求 `https://ungradeedu.eu.cc/api/feedback` 返回 401，符合认证边界。
- `https://www.ungradeedu.eu.cc/feedback?from=www&keep=1` 返回 308，`Location` 为 `https://ungradeedu.eu.cc/feedback?from=www&keep=1`，path 与 query 完整保留。
- `https://ungradu-edu-proxy.vangewang0919.workers.dev/` 返回 200，回退入口仍可用。
- 根域、`www` 和 `workers.dev` 均保留 `CF-Ray`、HSTS、CSP、Permissions-Policy、Referrer-Policy、`X-Content-Type-Options` 和 `X-Frame-Options`。
- 三入口未再出现 `x-nextjs-*`、`x-request-id`、`x-upstream-*` 或 `x-cloudbase*` 响应头。

剩余技术风险与边界：

- CloudBase 仍是单一上游；其可用性、限额或地域网络抖动会直接影响三个入口。
- `workers.dev` 仅作回退入口，不应再作为对外主域名推广。
- 当前证据为本次部署后的定点复测，不代替持续可用性监控。
- 本记录只确认代码、部署与技术验证状态；不作产品验收，不修改或关闭 `ISSUE-0020`。

### ISSUE-0020 Cloudflare zone 第一批安全加固

类型：Cloudflare zone 外部安全配置 / 生产技术复测

任务 ID：`ISSUE-0020-ZONE-HARDENING-BATCH-1`

执行时间：`2026-07-18 16:37:17 +08:00`

目标 zone：`ungradeedu.eu.cc`

变更项与前后状态：

- `Always Use HTTPS`：从 Off / checkbox 未勾选改为 On / checkbox `checked=true`。
- `Minimum TLS Version`：从 `TLS 1.0 (default)` 改为 `TLS 1.2`；变更后页面按钮文字为 `TLS 1.2`。
- 两项均在同一已登录 Cloudflare 会话的 `SSL/TLS -> Edge Certificates` 页面逐项执行并核对。

生产复测：

- `https://ungradeedu.eu.cc/`、`/rules`、`/feedback` 均返回 200。
- 匿名 `https://ungradeedu.eu.cc/api/feedback` 返回 401。
- `https://www.ungradeedu.eu.cc/feedback?from=www&keep=1` 返回 308，`Location` 精确为 `https://ungradeedu.eu.cc/feedback?from=www&keep=1`。
- `https://ungradu-edu-proxy.vangewang0919.workers.dev/` 返回 200。
- `http://ungradeedu.eu.cc/` 从变更前的 200 改为 301，`Location: https://ungradeedu.eu.cc/`，证明 zone 层 HTTPS 强制生效。
- HTTPS 核心路由与 `workers.dev` 保留 `CF-Ray`、HSTS、CSP、Permissions-Policy、Referrer-Policy、`X-Content-Type-Options` 和 `X-Frame-Options`。
- 复测响应中未出现 `x-nextjs-*`、`x-request-id`、`x-upstream-*` 或 `x-cloudbase*` 指纹头。

TLS 1.0 拒绝证据：

- `curl --tls-max 1.0` 在本机 Schannel 上返回 exit 35 / `SEC_E_UNSUPPORTED_FUNCTION`，单独不足以区分客户端和服务端限制。
- `openssl s_client -tls1` 对 `ungradeedu.eu.cc:443` 返回 exit 1 与 `tlsv1 alert protocol version` / alert 70，证明服务端不再接受 TLS 1.0。

明确未改范围：

- SSL/TLS mode 保持 `Full`，未改 `Full (strict)`。
- 未改 `Automatic HTTPS Rewrites`、TLS 1.3、WAF / Managed Rules、Custom Rules、Rate limiting、Bot / AI bot、Leaked credentials、DNS、Custom Domains、Worker 部署或代码。
- 本记录只属于代码开发员 / 技术验证职责，不修改或关闭 `ISSUE-0020`。

剩余技术风险：

- SSL/TLS mode 仍为 `Full`，未进入源站证书严格校验。
- zone 仍无自定义 rate-limit，也无可核验的已部署 Managed Rules / Bot Fight Mode。
- 本次是定点生产复测，不代替持续监控。

### ISSUE-0020 Cloudflare zone 第二批安全修复与源站绕过验证

类型：Cloudflare Worker 入口收敛 / AI Bot 与 Bot Fight Mode 核验 / Rate limiting 方案 / CloudBase 源站隔离设计

任务 ID：`ISSUE-0020-ZONE-HARDENING-BATCH-2`

执行时间：`2026-07-18 17:03:26 +08:00`

目标 zone / Worker：

- zone：`ungradeedu.eu.cc`（Free）。
- Worker：`ungradu-edu-proxy`。
- 本轮未改 DNS、Custom Domains、Worker 代码、Worker 部署、CloudBase 生产配置或 SSL/TLS mode。

#### A. workers.dev 公开入口

- 变更前 Cloudflare `Workers & Pages -> ungradu-edu-proxy -> Domains` 页面显示 Production Worker URL `ungradu-edu-proxy.vangewang0919.workers.dev` 开关为 On；`ungradeedu.eu.cc` 与 `www.ungradeedu.eu.cc` 两个 Custom Domain 均存在。
- 页面只对唯一可见控件执行过一次关闭点击；随后浏览器控制通道超时，按门禁停止，不重复点击、不触碰两个 Custom Domain。
- 独立生产证据：变更前 workers.dev `/` 为 200；点击后变为 404，响应体不再是业务页面。正式根域 `/` 仍为 200，`www.ungradeedu.eu.cc/feedback?from=www&keep=A` 仍为 308，Location 精确保留 path/query。
- 状态结论：公网行为已经证明 workers.dev 不再提供业务页面；但 Cloudflare 页面未能再次读出开关新状态，保留该页面作为 handoff，需人工只读确认开关显示 Off，不得再次点击。

变更后生产回归：

- `https://ungradeedu.eu.cc/`、`/rules`、`/feedback`：200。
- 匿名 `https://ungradeedu.eu.cc/api/feedback`：401。
- `https://www.ungradeedu.eu.cc/feedback?from=www&keep=A`：308，Location 为 `https://ungradeedu.eu.cc/feedback?from=www&keep=A`。
- `http://ungradeedu.eu.cc/`：301，Location 为 `https://ungradeedu.eu.cc/`。
- `https://ungradu-edu-proxy.vangewang0919.workers.dev/`：404，不再提供业务页面。
- 根域与匿名 API 保留 `CF-Ray`、HSTS、CSP、Permissions-Policy、Referrer-Policy、`X-Content-Type-Options`、`X-Frame-Options`；未出现 `x-nextjs-*`、`x-request-id`、`x-upstream-*` 或 `x-cloudbase*`。
- `openssl s_client -tls1` 仍返回 `tlsv1 alert protocol version` / alert 70，TLS 1.0 拒绝未回归。

#### B. AI bots

- 目标是把 Search / Agent / Training 三类 AI bot 从 Allow 改为 Block。
- Cloudflare Security Settings 页面可打开，但进入 Bot traffic 与继续定位控件时浏览器交互连续超时，无法形成逐项保存及页面新状态证据。
- 按门禁停止操作；本轮没有可确认的 AI bot 外部配置变更，不宣称三类已 Block。
- 最小人工动作：在保留的 `Security -> Settings -> Bot traffic` 页面逐项把 Search、Agent、Training 改为 Block，逐项确认页面显示 Block；完成后触发代码开发员重新执行同一套生产回归。

#### C. Bot Fight Mode

- 当前 zone 明确为 Free；当前 Security Settings / Bot 页面没有证明一个可单独启用且不要求升级的 Bot Fight Mode 开关。
- 结论：`N/A / 当前页面未能证明`。未启用、未购买、未升级，也未变更 AI Labyrinth 或其他 Bot / AI 功能。

#### D. Rate limiting

- 仓库与路由核验显示，匿名高风险写入口是 `POST /api/feedback`；匿名 `GET /api/feedback` 返回 401。
- `POST /api/auth/email/send-code` 已在应用层按邮箱哈希执行 60 秒冷却并有 429 测试覆盖，不应在没有业务依据时再凭空选择登录路径作为唯一 Cloudflare 限流规则。
- 仓库没有投诉/反馈提交频率、允许突发量、共享 NAT 误伤容忍度或封禁时长的正式依据，因此本轮没有创建 Free 0/1 rate limiting 规则，状态为 `WAITING_DECISION`。
- 待决方案：规则只精确匹配主域名、HTTP `POST`、URI path `/api/feedback`；由业务方先确认每 IP 阈值、统计窗口、阻断时长和误伤容忍度。候选值如 `5 次 / 10 分钟 / IP，阻断 10 分钟` 仅用于决策讨论，未应用到生产。
- 回滚设计：删除/禁用这一条唯一规则即可；创建后必须立即复测普通页面、匿名 GET 401、一次正常反馈写入，以及阈值触发后的 429/阻断行为。

#### E. CloudBase 源站绕过验证与最小隔离设计

当前公开源站：

- `https://ungradu-edu-prod-275285-6-1445807473.sh.run.tcloudbase.com`
- 直接访问 `/`、`/rules`、`/feedback` 均为 200，匿名 `/api/feedback` 为 401。
- 直接源站响应泄露 `server: cbrgw`、`x-cloudbase-request-id`、`x-cloudbase-session-id`、`x-cloudbase-upstream-*`、`x-nextjs-*`、`x-powered-by: Next.js`、`x-request-id` 与 `x-upstream-status-code`。

风险结论：

- 默认 CloudBase 公网域名可绕过 Cloudflare 的 Always Use HTTPS、AI Bot / Bot Fight / WAF / rate-limit（若后续启用）、canonical host 处理和 Worker 响应头去指纹；攻击者仍可直接访问页面和匿名写接口。
- CloudBase 官方文档说明云托管默认公网域名不带鉴权，可在服务设置关闭公网访问；也支持由服务自身实现鉴权，或通过 HTTP 网关自定义域名开启鉴权。但直接关闭公网访问会同时切断当前公网 Cloudflare Worker 对该源站的回源，不能在本轮直接执行。
- 参考：`https://docs.cloudbase.net/run/develop/access/client`、`https://docs.cloudbase.net/run/deploy/networking/public`、`https://docs.cloudbase.net/run/deploy/service-setting`。

推荐最小安全改造（未执行）：

1. 在应用服务端增加独立的源站访问校验，使用 Cloudflare Worker Secret 保存随机密钥；Worker 丢弃客户端同名头，再向 CloudBase 回源时注入专用鉴权头。源站使用恒定时间比较，缺失或错误返回 403；密钥不得写入仓库或日志。
2. 先部署“仅记录、不阻断”或同时接受旧流量的源站版本，再配置 Worker Secret 并部署 Worker，验证根域与 www；最后开启源站强制校验并验证直接 CloudBase URL 返回 403。
3. 待架构允许 Worker 通过 CloudBase 支持的鉴权网关或私网链路回源后，再评估关闭 CloudBase 公网开关；不要在公共 Worker 仍依赖公网域名时直接关闭。
4. 回滚顺序：先关闭源站强制校验恢复公开访问，再回滚 Worker 到上一生产版本；保留前一密钥一段短轮换窗口，确认恢复后再撤销新密钥。

本轮边界与唯一恢复动作：

- 实际外部行为变化仅有 workers.dev 从业务 200 收敛为 404；AI bots、Bot Fight Mode、Rate limiting、CloudBase、DNS、Custom Domains、Worker 代码与部署均未确认发生变更。
- Cloudflare 相关 tab 均以 handoff 保留，不自动关闭。
- 唯一恢复动作：项目总负责人先在已保留页面只读确认 workers.dev 开关为 Off，并人工完成 Search / Agent / Training 三项 Block；完成后通知本代码开发员线程重新执行生产回归。Rate limiting 和源站隔离分别等待业务阈值决策与新的实现/生产授权。
- 本记录只确认代码开发员 / 技术验证范围，不作产品验收，不修改或关闭 `ISSUE-0020`。

### ISSUE-0020 Cloudflare zone 第三批安全修复与源站观察模式准备

类型：生产安全配置能力核验 / Worker 源站鉴权头 / CloudBase 观察与强制模式 / 回滚准备

任务 ID：`ISSUE-0020-ZONE-HARDENING-BATCH-3`

执行时间：`2026-07-18 17:32:23 +08:00`

产品依据：

- 产品参数门禁 commit：`705db43e9ff02fc16210f7354824b25a391a82e3`。
- Rate limit 固定参数：仅 `ungradeedu.eu.cc` + `POST` + 精确 `/api/feedback`，每来源 IP 10 分钟最多 5 次，超限阻断 10 分钟。
- AI bots 固定参数：Search、Agent、Training 全部 Block。
- 源站隔离固定节奏：00:00-01:00 窗口、观察 24 小时、Worker 注入后灰度 30 分钟、强制 403 后监控 30 分钟，按产品定义触发回滚。

#### A. workers.dev 只读确认

- Cloudflare `Workers & Pages -> ungradu-edu-proxy -> Domains` 页面只读截图显示 Production Worker URL 为 Off，Preview URL 也为 Off。
- `ungradeedu.eu.cc` 与 `www.ungradeedu.eu.cc` 两个 Custom Domain 仍存在，未点击或修改任何域名控件。
- 独立线上请求再次确认 `https://ungradu-edu-proxy.vangewang0919.workers.dev/` 返回 404，不再提供业务页面。

#### B. 唯一 Rate limiting 规则

- Free zone 的 Security rules 页面显示 Rate limiting rules `0/1`，可进入唯一规则创建页。
- 创建页明确提供 Requests、Period、Action、Duration 字段；在只读读取 Period / Duration 可选值时，浏览器控制通道唯一一次尝试超时。
- 因无法证明 Free UI 同时支持 10 分钟统计周期和 10 分钟阻断，按产品门禁停止；没有填写、保存、草稿或部署任何限流规则，也没有擅自替换为其他周期。
- 状态：`WAITING_CAPABILITY_CONFIRMATION`。最小人工动作是在保留的创建页分别展开 Period 与 Duration，确认两者是否都有 10 minutes；确认后恢复本线程继续严格按唯一参数部署和回归。

#### C. AI bots

- Security Settings 页面可打开并显示 Bot traffic 入口。
- 对唯一 Bot traffic 按钮执行一次进入操作时超时，按门禁停止，不重复点击。
- 本轮没有可确认的 Search / Agent / Training 配置变更，不宣称已 Block。
- 最小人工动作：在保留页面进入 Bot traffic，把 Search、Agent、Training 逐项设为 Block 并确认页面状态；随后恢复本线程执行生产回归。

#### D. CloudBase 源站观察模式代码准备

涉及文件：

- `Code文档/cloudflare/worker.ts`
- `Code文档/cloudflare/worker.js`
- `Code文档/cloudflare/wrangler.example.toml`
- `Code文档/cloudflare/README.md`
- `Code文档/cloudflare/origin-isolation-runbook.md`
- `Code文档/server/origin-request-verification.ts`
- `Code文档/middleware.ts`
- `Code文档/.env.example`
- `Code文档/tests/cloudflare-worker-proxy.test.ts`
- `Code文档/tests/origin-request-verification.test.ts`
- `Code文档/tests/origin-verification-middleware.test.ts`

实现摘要：

- 私有请求头固定为 `x-ungrade-origin-verify`；真实值只允许存入 Cloudflare Worker 与 CloudBase 平台 Secret `ORIGIN_VERIFY_SECRET`，不写仓库、日志或回复。
- Worker 先删除客户端提供的同名头，再仅从平台 Secret 注入；如果 Secret 尚未配置，则不转发客户端伪造值。
- Worker 同时删除上游响应中的私有验证头，避免 Secret 被反射到浏览器。
- CloudBase 应用中间件支持 `ORIGIN_VERIFY_MODE=off|observe|enforce`：observe 对 missing / invalid / misconfigured 只记录不拒绝，enforce 对非 valid 请求返回 403。
- 观察日志只包含 event、method、pathname、mode、status、shouldReject；不记录 Secret、Cookie、query、请求正文或来源 IP。
- Wrangler 示例改为 `workers_dev = false`，与当前生产入口状态一致；两个 Custom Domain 保留。
- 新增运行手册，冻结 24 小时观察、30 分钟 Worker 灰度、30 分钟强制监控、验收账号检查和“先取消源站 403，再回滚 Worker”的固定回滚顺序。

TDD 证据：

- RED：`npm test -- cloudflare-worker-proxy origin-request-verification` 返回 exit 1；2 个测试文件中 9 个失败、5 个通过，准确命中 workers.dev 配置仍为 true、客户端伪造头未剥离、平台 Secret 未注入、源站验证模块缺失。
- RED：`npm test -- origin-verification-middleware` 返回 exit 1；2 个测试均因中间件缺失失败。
- GREEN：`npm test -- cloudflare-worker-proxy origin-request-verification origin-verification-middleware` 返回 exit 0；3 个测试文件、16 个测试全部通过。

完整工程验证：

- `node --check cloudflare/worker.js`：通过。
- `npm run typecheck`：通过。
- `npm run lint`：首轮因测试变量名 `module` 命中 Next.js lint 规则失败；改为 `middlewareModule` 后重跑通过，0 warning。
- `npm test`：47 个测试文件、189 个测试全部通过。
- `npm run build`：通过；Next.js 15.5.19 生产构建完成，Middleware 34.4 kB，31 个静态页面生成完成。

生产只读回归（代码未部署）：

- 根域 `/`、`/rules`、`/feedback`：200。
- 匿名 `GET /api/feedback`：401。
- `www.ungradeedu.eu.cc/feedback?from=www&keep=C`：308，Location 精确为 `https://ungradeedu.eu.cc/feedback?from=www&keep=C`。
- `http://ungradeedu.eu.cc/`：301，Location 为 `https://ungradeedu.eu.cc/`。
- workers.dev：404。
- 正式根域核心页面和匿名 API 保留 `CF-Ray`、HSTS、CSP、Permissions-Policy、Referrer-Policy、`X-Content-Type-Options`、`X-Frame-Options`；未出现 `x-nextjs-*`、`x-request-id`、`x-upstream-*`、`x-cloudbase*` 或 `x-ungrade-origin-verify`。
- `openssl s_client -tls1` 仍返回 `alert protocol version` / alert 70，TLS 1.0 继续被拒绝。
- CloudBase 默认源站 `/` 仍为 200 并泄露 CloudBase / Next.js 指纹，证明观察模式尚未部署，当前绕过风险仍存在。
- 未找到可安全使用的生产验收账号，因此未执行登录后反馈提交；未访问浏览器 Cookie 或用户隐私状态。

部署与 Secret 状态：

- 执行时为北京时间 17:32，不在批准的 00:00-01:00 窗口。
- 当前没有同时写入 Cloudflare 与 CloudBase 平台 Secret 并完成回滚的可靠通道，因此未生成孤立 Secret、未保存 Secret、未部署 Worker、未部署 CloudBase 观察模式、未提前启用强制 403。
- 状态：`WAITING_WINDOW + WAITING_ACCESS`。恢复条件是进入批准窗口，并由配置执行侧准备 Cloudflare / CloudBase 两端 Secret 写权限、上一版本回滚入口和专用非敏感验收账号。

边界与下一步：

- 本轮不修改 DNS、Custom Domains、SSL/TLS mode、Issue 状态或其他 Cloudflare 功能，不作产品验收。
- 唯一下一步：配置执行侧先在保留页面确认 Rate limit 的 Period / Duration 是否均支持 10 minutes，并人工完成三类 AI bots Block；同时为 00:00-01:00 窗口准备两端 Secret 权限和专用验收账号，满足后恢复本代码开发员线程执行观察模式部署与完整回归。

### ISSUE-0020 Cloudflare feedback POST 限流生产复核

类型：生产只读配置核验 / 精确路径限流行为验证 / 安全基线回归

执行时间：`2026-07-18 23:42:15 +08:00`

预期规则：

- 名称：`feedback-post-rate-limit`。
- 表达式：`(http.host eq "ungradeedu.eu.cc" and http.request.method eq "POST" and http.request.uri.path eq "/api/feedback")`。
- 计数特征：来源 IP；3 requests / 10 seconds。
- 动作：Block；阻断 10 seconds。

Cloudflare 控制台只读证据：

- zone 明确为 `ungradeedu.eu.cc`，Security rules 列表显示 Rate limiting rules `1/1 rules`。
- 唯一规则 `feedback-post-rate-limit` 状态为 `Active`，顺序为 1。
- 列表展示的表达式与预期 host、POST、精确 `/api/feedback` 完全一致，Action 为 `Block`。
- 规则 ID：`5f7a1c8ea176415080380e366b2094b8`。本轮未点击编辑、未保存或再次部署规则。
- 进入详情页读取来源 IP、3/10 秒和 10 秒阻断字段时，Chrome 被关闭；现有列表页面和随后生产行为可证明规则存在、启用、表达式、Block 动作及实际 3 次后阻断/10 秒恢复，但当前控制台页面未能单独证明计数特征字段为来源 IP。

正常基线：

- `GET https://ungradeedu.eu.cc/`：200，CF-Ray `a1d29666f80cd2f5-NRT`。
- `GET /rules`：200，CF-Ray `a1d2966d9ed44324-NRT`。
- `GET /feedback`：200，CF-Ray `a1d296732cfdb1de-NRT`。
- `www.ungradeedu.eu.cc/feedback?from=rate-review&keep=1`：308，Location 精确为 `https://ungradeedu.eu.cc/feedback?from=rate-review&keep=1`。
- `http://ungradeedu.eu.cc/`：301，Location 为 `https://ungradeedu.eu.cc/`。
- HSTS、CSP、Permissions-Policy、Referrer-Policy、`X-Content-Type-Options`、`X-Frame-Options` 与 CF-Ray 均存在；未发现 `x-nextjs-*`、`x-request-id`、`x-upstream-*`、`x-cloudbase*` 指纹头。
- `openssl s_client -tls1` 返回 `Cipher is (NONE)`、`tlsv1 alert protocol version`、alert 70，TLS 1.0 继续被拒绝。

限流行为：

- 为避免写入真实生产反馈记录，测试使用空 JSON；该载荷在应用输入校验层返回 400，而不是任务预期的 401。仓库现有接口约定允许合法匿名 POST 创建反馈，因此未发送可能产生真实记录的合法匿名载荷。
- 基线空 JSON POST：400，CF-Ray `a1d296756bc68e04-NRT`，应用 JSON 校验错误。
- 清空窗口后同一测试源在 10 秒内连续请求：第 1 次 400 / `a1d296c08f48915d-NRT`，第 2 次 400 / `a1d296c29bb6d769-NRT`，第 3 次 400 / `a1d296c898876468-NRT`；三次均进入应用输入校验。
- 第 4 次：429，CF-Ray `a1d296ce0ef3eff8-NRT`，`content-type: text/plain; charset=UTF-8`，响应特征 `error code: 1015`，确认由 Cloudflare 在第四次拦截。
- 等待 11 秒后，同路径 POST 恢复为应用 400，CF-Ray `a1d29714be7c51f3-NRT`；`GET /feedback` 仍为 200，CF-Ray `a1d29717ee75dfaa-NRT`，未发现持续误伤。

技术结论与边界：

- `RATE_LIMIT_BEHAVIOR_PASS`：规则精确命中根域 POST `/api/feedback`，前三次放行至应用、第四次 Cloudflare 429，约 10 秒后恢复；其他页面及 HTTPS/TLS/安全头/去指纹无回归。
- `BASELINE_EXPECTATION_MISMATCH`：任务预期“匿名 POST = 401”与当前生产及仓库接口约定不一致。空 JSON 实际为 400；合法匿名反馈在现有代码中允许保存。未为追求 401 而制造生产反馈数据，也未修改产品决策或 Issue。
- 控制台来源 IP 字段因 Chrome 已关闭而未能从详情页独立证明；浏览器无法执行 finalize/handoff。Chrome 安装、扩展和 native host 均正常，唯一最小恢复动作是用户重新打开 Chrome 后恢复本线程，只读读取该规则详情页。
- 本轮只更新开发员工作记录，不修改规则、业务代码、产品文档或 Issue 状态，不作产品验收。

### Cloudflare AI Crawl Control 生产技术复测

类型：外部安全配置证据复核 / User-Agent 对照请求 / 核心生产回归

执行时间：`2026-07-19 00:30:53 +08:00`（UTC `2026-07-18T16:30:53Z`）

Dashboard 外部配置证据（由项目总负责人只读页面核对提供）：

- `AI Crawl Control -> Security` 页面共加载 32 个 crawler。
- Category 为 AI Assistant / AI Crawler / AI Search 的 26 个 crawler 中，`Block Crawler=true` 为 26/26，未阻止为 0。
- Googlebot 属于 Search Engine Crawler，`Block Crawler=false`，保持允许；Arquivo Web Crawler 属于 Archiver，`Block Crawler=true`。
- 以上是 Cloudflare Dashboard 配置状态证据；本代码开发员线程未修改页面配置，也不把 26/26 配置状态直接伪装成 26 个真实爬虫均已通过 HTTP 阻断实测。

核心生产回归：

- 根域 `/`：200，CF-Ray `a1d2dda2f921e359-NRT`。
- `/rules`：200，CF-Ray `a1d2dda31db8c8d8-NRT`。
- `/feedback`：200，CF-Ray `a1d2dda57c35a88f-NRT`。
- 匿名 `GET /api/feedback`：401，CF-Ray `a1d2dda5c99c612f-NRT`。
- `https://www.ungradeedu.eu.cc/feedback?from=ai-crawl-review&keep=1`：308，Location 精确为 `https://ungradeedu.eu.cc/feedback?from=ai-crawl-review&keep=1`，CF-Ray `a1d2dda8a994e3b7-NRT`。
- `http://ungradeedu.eu.cc/`：301，Location 为 `https://ungradeedu.eu.cc/`，CF-Ray `a1d2dda7f8dfafd3-NRT`。
- 根域、`/rules`、`/feedback` 均保留 CF-Ray、HSTS、CSP、Permissions-Policy、Referrer-Policy、`X-Content-Type-Options`、`X-Frame-Options`；未发现 `x-nextjs-*`、`x-request-id`、`x-upstream-*`、`x-cloudbase*`、`x-powered-by` 指纹头。
- `openssl s_client -tls1` 返回 exit 1、`Cipher is (NONE)`、`tlsv1 alert protocol version`、alert 70，TLS 1.0 继续被拒绝。

User-Agent 对照 HTTP 证据（同一普通测试源，仅改变 UA 字符串）：

- OAI-SearchBot/1.0：403，CF-Ray `a1d2ddaa4ba0ccca-NRT`，`text/html; charset=UTF-8`，响应正文包含 `Attention Required! | Cloudflare`、`Sorry, you have been blocked` 与 Cloudflare Ray ID。
- ChatGPT-User/1.0：403，CF-Ray `a1d2ddaa0891afd4-NRT`，`text/html; charset=UTF-8`。
- Googlebot/2.1：200，CF-Ray `a1d2ddab2a30262f-NRT`，与 Dashboard 保持允许的配置方向一致。
- GPTBot/1.0：200，CF-Ray `a1d2dfa6bcb57d99-NRT`；GPTBot/1.2：200，CF-Ray `a1d2dda9ecc02821-NRT`。
- ClaudeBot/1.0：200，CF-Ray `a1d2dfa8ab78bea7-NRT`。

证据边界与技术结论：

- `CORE_PRODUCTION_REGRESSION_PASS`：正式根域、www canonical、核心路由、匿名读取保护、HTTP 到 HTTPS、TLS 1.0 拒绝、安全头和去指纹均无回归。
- `AI_CRAWL_HTTP_PARTIAL_EVIDENCE`：从同一普通测试源伪造 UA 时，OAI-SearchBot 与 ChatGPT-User 可复现 Cloudflare 403，Googlebot 可复现 200；这证明部分请求路径上的差异化拦截行为，但不能覆盖 Dashboard 中全部 26 个 crawler。
- GPTBot/1.0、GPTBot/1.2 与 ClaudeBot 的伪造 UA 返回 200，不足以证明真实、经 Cloudflare 验证的对应爬虫能够绕过，也不能证明 Dashboard 的 26/26 均已获得真实 HTTP 阻断；Cloudflare 可能结合来源 IP、已验证 bot 身份或其他分类信号。不得仅据普通来源伪造 UA 的结果修改 Cloudflare 配置。
- 本轮不作产品验收，不维护 Issue 状态，不修改 Cloudflare、DNS、Custom Domains、Worker 或业务代码。

### CloudBase 源站隔离 rollout 现场预检与下一窗口准备

类型：生产变更前置门禁 / 回滚条件核验 / 窗口状态记录

现场预检时间：`2026-07-19 00:53 +08:00`

已确认环境（由项目总负责人在已登录页面只读核对）：

- Cloudflare 当前目标为 Worker `ungradu-edu-proxy` production。
- CloudBase 生产环境 ID 为 `ungradu-edu-prod-d3efys1f5970e3f`。
- 当前 Worker 上游仍为 `ungradu-edu-prod-275285-6-1445807473.sh.run.tcloudbase.com`。
- CloudBase 套餐到期时间为 `2026-07-27 23:59:59`；24 小时观察、后续灰度和 enforce 阶段必须在服务有效期及回滚能力可用时执行。
- Cloudflare 与腾讯云页面已登录并由总负责人以 handoff 保留；本代码开发员线程未重新接管或操作页面。

未满足的强制前置条件：

1. 专用非敏感验收账号尚未核验，不得使用真实未成年人、联系方式或投诉内容代替。
2. 上一 Worker deployment 尚未完整核验为可用回滚目标。
3. 上一 CloudBase revision 及其回滚入口尚未完整核验。

现场决策与禁止动作：

- 当前距批准窗口 `00:00-01:00` 结束不足 7 分钟，无法在保证测试、观察和五分钟内启动回滚的条件下安全开始 Phase 1。
- 状态保持 `WAITING_WINDOW + WAITING_PRECONDITIONS`；这不是部署完成或产品验收。
- 本轮未生成 Secret，未把 Secret 写入仓库、命令历史、日志或回复，未保存 Cloudflare / CloudBase 变量，未部署 CloudBase，未部署 Worker，未启用 `ORIGIN_VERIFY_MODE=observe|enforce`。
- 未修改 DNS、Custom Domains、Worker 路由、CloudBase 公网入口、产品文档或 Issue 状态。

下一完整窗口执行入口：

- 恢复触发条件：北京时间下一次完整 `00:00-01:00` 窗口到来，且专用非敏感验收账号、上一 Worker deployment、上一 CloudBase revision/回滚入口三项均已逐项核验。
- 恢复后严格从 `origin-isolation-runbook.md` Phase 1 开始：先在批准的 Secret 管理位置生成强随机 Secret，将相同值作为两端掩码 Secret 保存；随后仅先部署 CloudBase `ORIGIN_VERIFY_MODE=observe`，不改 Worker。
- Phase 1 首个验证点：直接 CloudBase 请求行为保持不变，观察日志只记录 `missing` 等分类且不包含 Secret、Cookie、query、请求正文或 IP；完成 24 小时观察后才能进入 Worker 注入的 Phase 2。
- 唯一下一步：总负责人或配置执行侧在下一完整窗口开始前补齐并记录上述三项前置证据，满足后恢复本代码开发员线程执行 Phase 1。

### 聊天消息区域固定高度与内部滚动回归修复

类型：本地 UI 缺陷修复 / 真实浏览器几何回归 / 禁止生产部署

执行时间：`2026-07-23 23:52 +08:00` 至 `2026-07-24 00:08 +08:00`

范围与边界：

- 用户再次确认 `/chats/[id]` 消息历史区域必须保持固定、受限且合理的可视高度；消息增加只能增加该区域的 `scrollHeight`，不得继续撑高页面；输入区必须保持可见。
- 本轮仅修改聊天页源码、聊天消息面板、全局聊天样式、直接回归测试和本开发员工作记录；未修改产品、UI、Issue 文档或状态。
- 生产仍为 CloudBase revision `024`、`ORIGIN_VERIFY_MODE=observe` 的源站隔离观察状态。本轮未打开或操作 Cloudflare/CloudBase 控制台，未修改 Worker、DNS、Secret、生产变量或任何生产部署。

真实红色反馈环与根因证据：

- 将聊天页真实消息区抽取为 `ChatMessagePanel`，生产 `/chats/[id]` 直接使用该组件；Vitest 通过 React 服务端渲染真实组件，再由本机 Chrome headless 加载项目真实 `globals.css` 并测量 DOM 几何，而不是仅检查 CSS 字符串。
- 负向基线临时移除既有固定网格/高度/内部 overflow 约束后，同一测试稳定复现无限增长：桌面 3 条消息主面板 `clientHeight=1057`、80 条消息 `12715`；移动端分别为 `1039` 与 `11826`，测试红灯。负向注入只用于验证反馈环，未保留在代码中。
- 恢复既有固定约束后，消息量本身不再改变主面板高度；但新增“消息区必须位于输入区之前”的真实几何断言发现旧 `min-height:360px/260px` 会令消息列表底部越过 compose 顶部，桌面、390px 移动端和软键盘压缩视口均红灯。根因是固定 `min-height` 抵消了 `minmax(0, 1fr)` 的可收缩中间轨道。
- 自动滚底测试先因 `scrollMessageListToLatest is not a function` 红灯，证明此前组件没有可验证的首次进入/新消息滚底实现。

修复内容：

- `conversation-main` 保持三行网格，并增加 `overflow:hidden`，将消息历史严格限制在聊天主面板内部；桌面高度调整为 `clamp(640px, calc(100vh - 120px), 760px)`。
- 移动端使用 `clamp(640px, calc(100dvh - 60px), 760px)`；软键盘压缩视口下优先保留输入区并允许消息轨道收缩。
- 消息列表改为 `min-height:0`、`overflow-y:auto`、`overflow-x:hidden`、`align-content:start`、`scrollbar-gutter:stable`、`overscroll-behavior:contain` 和 `touch-action:pan-y`，避免固定最小高度覆盖输入区，同时支持键盘与触控滚动。
- 消息列表增加可访问名称和 `tabIndex=0`；首次进入或最新消息 ID 变化时滚动到底部。轮询返回同一末条消息 ID 时 effect 不会重复触发，避免用户查看历史时被无新消息轮询拉回底部。
- 长消息继续使用 `overflow-wrap:anywhere`，并由真实 390px 内容宽度回归验证无横向溢出。

绿灯证据：

- `npm test -- chat-message-panel-scroll chat-layout-browser --reporter=verbose`：2 个测试文件、4 项测试全部通过。
- 桌面：3 条与 80 条消息主面板均为 `674px`，消息区均为 `275px`；80 条消息 `scrollHeight=12332px`。
- 390px 移动端：主面板均为 `678px`，消息区均为 `184px`；80 条消息 `scrollHeight=11955px`。
- 390px 软键盘压缩视口：主面板均为 `634px`，消息区均为 `140px`；80 条消息 `scrollHeight=11955px`。
- 三种视口均满足：消息区可实际滚动、`tabIndex=0`、消息区不覆盖输入区、输入区位于主面板内、页面自身无横向溢出。
- `npm run typecheck`：exit 0，24.3 秒。
- `npm run lint`：exit 0，79.3 秒，0 warnings。
- `npm test`：49 个测试文件、193 项测试全部通过。
- `npm run build`：Next.js 15.5.19 生产构建成功，31 个静态页面生成完成，`/chats/[id]` 动态路由产物生成成功。

待部署状态与风险：

- 本轮构建产物仅用于本地验证，未部署。待部署包为本轮聊天页源码、消息面板、样式与回归测试对应提交。
- 真实双账号登录、发送消息与触控手势的人工作业未在生产执行；自动化已覆盖真实组件几何、滚动能力、390px 内容宽度、软键盘压缩视口、自动滚底纯逻辑及全量工程门禁。
- 唯一下一步：由项目总负责人等待当前 024 observe 观察门禁允许后，另行安排该代码提交的部署与独立产品/UI 验收；本代码开发员不得自行部署或关闭 Issue。

### 聊天软键盘压缩视口 UI SERIOUS 返工

类型：UI 独立复核 SERIOUS 返工 / 真实 visual viewport 与焦点回归 / 禁止生产部署

执行时间：`2026-07-24 00:24 +08:00` 至 `2026-07-24 00:39 +08:00`

返工输入与边界：

- UI 独立复核指出提交 `9fedf548dfb72d586ce58abdc169efffcf792056` 的移动规则使用 `640px` 最小高度，而旧“390×600”测试只证明输入区在约 634px 高主面板内，没有证明主面板和输入区仍位于 visual viewport。
- 本轮仅修改聊天移动样式、真实 Chrome 回归测试和本开发员工作记录；未修改产品、UI、Issue 文档或状态。
- 生产 CloudBase revision `024`、`ORIGIN_VERIFY_MODE=observe` 未触碰；未打开或操作 Cloudflare/CloudBase 控制台，未修改 Worker、DNS、Secret、生产变量或执行部署。

红色反馈环：

- 浏览器测试由 `--window-size`/`--dump-dom` 升级为 Chrome DevTools Protocol `Emulation.setDeviceMetricsOverride`，直接创建精确 `visualViewport=390×600`，避免 Chrome headless 最小外窗宽度把约 526px viewport 冒充为 390px。
- 测试 DOM 使用真实 `ChatMessagePanel`、真实 `globals.css`，并加入 RootLayout 的 sticky `site-header`（390px 下实际高度约 `114.8px`）、页面 workspace header 和 context 占位；测试真实聚焦 `#message-text` 后等待两帧再读取几何。
- 受控恢复旧 `clamp(640px, calc(100dvh - 60px), 760px)` 规则时，同一精确 390×600 测试稳定红灯：主面板 `clientHeight=634`、`top=-165.125`、`bottom=474.875`。虽然 compose bottom `453.875` 被浏览器滚入视野，但整个聊天主面板上部已离开 visual viewport，准确复现 UI SERIOUS。
- 该负向恢复只用于红灯证明，未保留在最终代码。

假设验证与根因：

1. 仅去掉 640px 下限：主面板降至约 578px，但消息区仅 84px，且原生焦点滚动仍令主面板 top 约 `-109.9px`，不足以修复。
2. 小高度视口收紧 header、消息区和 compose 垂直间距：消息区恢复到 122px，但主面板 top 仍约 `-113.9px`，证明空间与焦点定位是两个独立问题。
3. textarea `scroll-margin-top`：Chrome 原生焦点滚动不采用该值，几何不变，已移除。
4. `position:sticky` focused mode：被 `.wide-panel { overflow:hidden }` 的既有滚动边界阻断，几何不变，已移除。
5. 最终根因修复：移动高度必须由动态 viewport 控制；在高度不超过 700px 且聊天区获得焦点时，需要让主面板进入 visual viewport 定位上下文，而不能依赖浏览器只把 textarea 自身滚入视野。

最终修复：

- 移动常规高度改为 `clamp(480px, calc(100dvh - 16px), 760px)`，删除会超过压缩 viewport 的 640px 下限；390×844 正常场景仍由 760px 上限保持原体验。
- `max-width:720px` 且 `max-height:700px` 时收紧 header、消息区和 compose 的垂直 padding/margin，给消息历史保留可用高度。
- 同一压缩条件下，聊天区 `:focus-within` 进入可逆 focused mode：固定于 visual viewport `top:8px`，高度 `calc(100dvh - 16px)`，宽度 `min(calc(100vw - 16px), 390px)`，层级高于 sticky 顶栏；焦点离开聊天区即恢复正常文档流。
- 消息列表既有 `overflow-y:auto`、`touch-action:pan-y`、`tabIndex=0`、长消息换行和仅最新消息 ID 变化时滚底逻辑均未修改。

绿灯证据：

- 精确 390×600：`viewportWidth=visualViewportWidth=390`、`viewportHeight=visualViewportHeight=600`，输入框真实获得焦点。
- 主面板 3 条和 80 条消息均为 `clientHeight=578`、`top=8`、`bottom=592`；compose `top=348.21875`、`bottom=571`，全部位于 visual viewport。
- 消息区 3 条和 80 条均为 `148px`；80 条长消息仅令内部 `scrollHeight=11366px`，可滚动、输入区不重叠、页面横向宽度仍为 390px。
- 390×844 正常移动：主面板 754px、消息区 260px，80 条消息内部 `scrollHeight=11691px`，无横向溢出。
- 桌面 1280×800：主面板 674px、消息区 275px，80 条消息内部 `scrollHeight=12332px`。
- `npm test -- chat-layout-browser chat-message-panel-scroll --reporter=verbose`：2 个文件、4 项测试通过；滚底测试保持通过。
- `npm run typecheck`：exit 0。
- `npm run lint`：exit 0，0 warnings。
- `npm test`：49 个测试文件、193 项测试全部通过。
- `npm run build`：Next.js 15.5.19 构建成功，31 个静态页面生成完成，`/chats/[id]` 构建产物生成成功。

技术结论与下一步：

- `UI_SERIOUS_TECHNICAL_CONDITION_FIXED_LOCALLY`：UI 复核指出的 390×600 visual viewport 条件已由真实焦点和精确视口几何证明消除；这不是 UI 独立复核最终验收。
- 本轮未部署，待部署包为本次返工提交；生产仍保持 024 observe。
- 唯一下一步：项目总负责人将新提交返回原 UI 独立复核线程复验；复验通过且生产观察/部署门禁另行满足后，再安排部署，代码开发员不自行部署或修改 Issue。

### 智能客服消息历史固定高度与内部滚动修复

类型：同一聊天布局任务返工 / 智能客服独立布局根因修复 / 禁止生产部署

执行时间：`2026-07-24`

任务范围与边界：

- 项目总负责人确认提交 `595a692335ba2ca56e2ff1e78bb553d8e8607dbf` 只处理人际聊天 `.conversation-main` / `.message-list`，智能客服独立使用 `.customer-service-chat` / `.customer-service-messages`。
- 本轮仅修改 `app/globals.css`、智能客服布局回归测试和本开发员工作记录；未修改智能客服业务逻辑、人际聊天组件、产品 / UI / Issue 文档或状态。
- 未打开或操作 Cloudflare / CloudBase 控制台，未修改 Worker、DNS、Secret、生产变量或执行生产部署；生产继续保持 CloudBase revision `024`、`ORIGIN_VERIFY_MODE=observe`。

重复与冗余审计：

- 审计 `595a6923` 与当前 CSS 后确认，人际聊天基础规则、`max-width:720px` 移动覆盖和 `max-height:700px` 焦点压缩覆盖分别服务桌面、常规移动和软键盘压缩场景；它们不是同一条件下的重复规则。
- 智能客服的基础规则与移动媒体查询也分别负责通用布局和移动高度覆盖；未发现可证明无效或被完全覆盖的重复规则。
- 因此本轮没有删除既有人际聊天规则。只把智能客服消息区原先仅有的 `min-height:340px` / 移动 `min-height:160px` 改为受限高度策略；这些旧声明是缺陷根因，不作为“冗余清理”扩大删除范围。

红色反馈环与根因证据：

- 新增真实 Chrome 几何回归：由 React 服务端渲染真实 `CustomerServiceChat`，加载项目真实 `globals.css`，再在同一真实消息 DOM 中扩展到 80 条长短消息。
- 修复前桌面 3 条消息时 `.customer-service-chat clientHeight=551px`，80 条时增长到 `7767px`；消息区 `clientHeight=scrollHeight=7550px`、`overflowY=visible`，无法内部滚动。
- 修复前 390×844 移动端 3 条消息时客服容器 `789px`，80 条时增长到 `13287px`；消息区 `clientHeight=scrollHeight=12881px`、`overflowY=visible`。
- 证据排除人际聊天规则回归与页面宽度问题，根因确定为 `.customer-service-messages` 只有最小高度，没有固定 / 最大高度和纵向 overflow 约束；父级自动网格因此随内容持续增长。

修复内容：

- 智能客服消息区使用 `height:clamp(300px, 42vh, 420px)`；移动端使用 `height:clamp(180px, 32dvh, 260px)`，建立桌面与移动端明确受限高度。
- 增加 `align-content:start`、`min-height:0`、`overflow-y:auto`、`overflow-x:hidden`、`overscroll-behavior:contain`、`scrollbar-gutter:stable` 和 `touch-action:pan-y`。
- 快捷问题与输入表单保持在消息区之后的独立网格行，不被消息覆盖；长消息继续使用既有断行规则。

绿灯与完整验证：

- `npm test -- chat-layout-css customer-service-layout-browser chat-layout-browser chat-message-panel-scroll --reporter=verbose`：exit 0，4 个测试文件、9 项测试全部通过。
- 桌面 3 / 80 条消息的客服容器均为 `547px`、消息区均为 `330px`；80 条消息内部 `scrollHeight=7550px`，`overflowY=auto` 且可滚动。
- 390×844 移动端 3 / 80 条消息的客服容器均为 `660px`、消息区均为 `254px`；80 条消息内部 `scrollHeight=12881px`，`overflowY=auto` 且可滚动。
- 桌面与移动端均满足消息区在快捷问题之前、快捷问题在输入区之前、输入区包含于客服容器、页面无横向溢出。
- 既有人际聊天桌面、390×844 和精确 390×600 焦点压缩视口几何回归继续通过，自动滚底测试继续通过。
- `npm run typecheck`：exit 0。
- `npm run lint`：exit 0，0 warnings。
- `npm test`：exit 0，50 个测试文件、196 项测试全部通过。
- `npm run build`：exit 0；Next.js 15.5.19 生产构建成功，生成 31 个静态页面，`/customer-service` 保持静态预渲染。

待部署状态与唯一下一步：

- 本轮只形成待部署代码包和测试证据，没有触碰生产 024 observe。
- 唯一下一步：项目总负责人将本轮提交交回独立 UI / 产品验收；待现有生产观察门禁和部署授权另行通过后安排部署，不由代码开发员自行部署或修改 Issue。

### 智能客服下方工作区信息密度与单行快捷问题调整

类型：`CS-UI-LAYOUT-20260727-001` 同一代码任务续作 / 用户确认预览实现 / 禁止生产部署

执行时间：`2026-07-27`

最终冻结范围：

- 业务方即时更正后，客服页现有 Hero 的标题、说明、装饰和“查看规则 / 风险反馈”按钮全部保持原文与原视觉，不压缩、不重排、不强制单行；全站导航未修改。
- 仅调整 Hero 下方客服工作区：缩小左右外留白，左侧咨询卡约占 24%，右侧聊天卡约占 74%，两卡桌面顶底对齐。
- 消息历史区域加高但继续使用受限高度和内部纵向滚动；消息正文从 16px 降为 14px，输入框、发送按钮和业务逻辑未缩放或修改。
- 5 个既有快捷问题不删除、不改文案、不改点击逻辑；桌面全部单排可见，移动端保持单排并只在快捷问题容器内横向触控滚动。

涉及文件：

- `Code文档/app/customer-service/page.tsx`
- `Code文档/app/globals.css`
- `Code文档/tests/customer-service-layout-browser.test.ts`
- `Code文档/tests/chat-layout-css.test.ts`
- `Code文档/tests/customer-service-page-copy.test.ts`
- `Code文档/开发员工作记录.md`

红色反馈环：

- 真实 Chrome 回归继续服务端渲染生产 `CustomerServiceChat`、加载真实 `globals.css`，并使用 3 / 80 条消息测量桌面 1280×800 与移动 390×844 几何。
- 修复前桌面下方工作区宽 `912px`、消息区 `330px`、消息正文 `16px`，5 个快捷问题发生换行；移动工作区宽 `274px`、消息区 `254px`，5 个快捷问题同样多行。
- 收紧后的测试先以 exit 1 稳定命中上述宽度、字号、高度和单排要求，证明反馈环不是无关浅层样式断言。

实现摘要：

- 保持原 Hero 内部 JSX 不变，仅给客服页外层增加专用 shell，并将下方工作区作为相邻区块独立扩宽，避免扩大 Hero 或影响其他 `.dplus-chat-page`。
- 下方工作区最大宽度 `1240px`，桌面列关系使用 `24% / 剩余宽度`；聊天卡内边距小幅收紧，消息区使用 `height:clamp(390px, 50dvh, 500px)`。
- 移动端消息区使用 `height:clamp(306px, 38dvh, 340px)`；工作区按既有断点堆叠，聊天主体仍优先于说明卡。
- 快捷问题容器改为 `flex-wrap:nowrap`、`overflow-x:auto`、`touch-action:pan-x`；按钮保持最小 40px 点击高度和 D+ 粗边框 / 圆角 / 阴影。

绿灯与工程门禁：

- `npm test -- chat-layout-css customer-service-layout-browser customer-service-page-copy chat-layout-browser chat-message-panel-scroll --reporter=verbose`：exit 0，5 个文件、14 项测试全部通过。
- 真实回归桌面：工作区 `1224px`，左 / 右卡实际约 `24.0% / 74.0%`，消息区 `394px`；80 条消息只把内部 `scrollHeight` 增至 `6091px`；5 个快捷问题单排且无需横滚。
- 真实回归移动：工作区 `358px`，消息区 `315px`；80 条消息只把内部 `scrollHeight` 增至 `7544px`；快捷问题单排，内部 `scrollWidth=829px > clientWidth=326px` 且可横滚，文档宽度保持 `390px`。
- 既有人际聊天桌面、390×844 和 390×600 软键盘压缩视口回归继续通过。
- `npm run typecheck`：exit 0。
- `npm run lint`：exit 0，0 warnings。
- `npm test`：exit 0，50 个测试文件、197 项测试全部通过。
- `npm run build`：exit 0；Next.js 15.5.19 构建成功，31 个静态页面生成完成，`/customer-service` 继续静态预渲染。

本地可视复核：

- 本地 `/customer-service` 桌面实际页面：下方工作区约 `1209px`，左 / 右卡约 `24.0% / 74.0%`，消息区 `394px`，5 个快捷问题单排，输入和发送可用。
- 390×844：消息区 `315px`，快捷问题容器 `scrollWidth=829px`、实际横滚至 `scrollLeft=280`；页面 `scrollWidth=375px <= viewport 390px`，无整页横向溢出。
- 点击首个快捷问题后消息数从 1 增至 3，输入和发送按钮保持启用；console error / warn 为 0。
- 截图：`customer-service-layout-desktop-workspace-20260727.png`、`customer-service-layout-mobile-chat-20260727.png`。

边界、风险与唯一下一步：

- 未修改 `CustomerServiceChat` 的问题数组、回答逻辑、点击处理或 API；未修改产品 / UI / Issue 文档和状态。
- 未打开或操作 Cloudflare / CloudBase 控制台，未修改 Worker、DNS、Secret 或生产环境；生产继续保持 revision `024`、`ORIGIN_VERIFY_MODE=observe`，本轮代码未部署。
- 移动端快捷问题需要用户横向滑动查看后续问题，已由明确滚动条、内部滚动几何和触控属性覆盖；真实设备手势仍由后续独立 UI 复核确认。
- 唯一下一步：项目总负责人把本轮待部署提交交回独立 UI / 产品验收；仅在 024 observe 门禁和部署授权另行通过后安排生产部署。

### 六个公开页面 D+ 视觉刷新（独立 worktree 待部署包）

类型：`SITEWIDE-UI-REFRESH-20260727-001` / 六页最终预览正式实现 / 禁止生产部署

执行时间：`2026-07-27`

隔离与边界：

- 以已推送基线 `035304e85a7250ec90dd62fdfb08cd5489697ee9` 创建独立 worktree `D:\codex_project\家教对接website-ui-refresh-20260727` 和分支 `codex/sitewide-ui-refresh-20260727`；没有在原脏工作树切分支、清理、重置或带入未提交内容。
- 仅实现业务方确认的六张最终预览：首页、登录 / 注册、规则、智能客服、家教信息广场、需求广场；没有重新发散设计。
- 保留现有路由、登录态、邮箱验证码 / 密码登录、广场筛选 / 发布 / 数据加载和客服回答逻辑；没有修改 Cloudflare、CloudBase、DNS、Secret、生产配置、Issue 状态或知识库。
- 生产继续保持 CloudBase revision `024`、`ORIGIN_VERIFY_MODE=observe`；本轮未部署。

实现摘要：

- 建立统一 D+ 低饱和黄绿渐变、暖白 / 浅蓝 / 浅紫卡面、粗黑描边、圆角和硬阴影；公共导航保留未登录三入口与现有登录态切换，仅升级品牌标识。
- 首页实现确认的标题、胶囊、三项利益点、两张发布主卡、四张入口卡及右侧沟通原则 / 卡通人物；功能反馈仍只位于下方入口。
- 登录页使用双栏 D+ 入口与真实表单；规则页使用左侧说明、右侧四条业务规则，并按冻结要求移除用户可见的 HTTPS / Worker 技术说明。
- 智能客服使用紧凑横向信息条、桌面约 `24% / 74%` 工作区；删除左侧 Dify 延后入口视觉块，但未删除后端 / 环境变量集成能力。消息正文为 14px，消息区固定高度并内部滚动；5 个快捷问题桌面 1 行、390px 移动端精确 2 行。
- 两个广场使用左侧纵向筛选、右上低矮介绍 / 发布横条、右下高结果框的三框结构；保留真实数据与筛选函数，没有硬编码预览假数据。
- 登录、规则和两个广场统一使用左上角标准返回首页箭头；移动端自然堆叠且不产生整页横向溢出。

红 / 绿反馈环：

- 新增 `tests/sitewide-ui-refresh.test.ts`，首次执行 8 项全部红灯，稳定命中公共品牌 / 导航、首页入口、标准返回箭头、登录双栏、规则技术文案删除、客服 Dify 视觉删除、两广场三框布局和响应式 CSS 契约缺口。
- 最终针对性命令 `npm test -- sitewide-ui-refresh chat-layout-css customer-service-page-copy customer-service-layout-browser security-access-copy --reporter=verbose`：exit 0，5 个文件、19 项测试全部通过。
- 真实 Chrome 客服几何：桌面 1280×800 左 / 右约 `24.0% / 74.0%`、消息区 424px，80 条消息只增加内部 `scrollHeight` 到 6091px；5 chips 为 1 行。390×844 消息区 315px，80 条消息内部 `scrollHeight=7544px`，5 chips 为 2 行；输入可用且页面宽度保持 390px。

工程与浏览器验证：

- `npm run typecheck`：exit 0。
- `npm run lint`：exit 0，0 warnings。
- `npm test -- --exclude tests/production-ops-baseline-script.test.ts`：exit 0，50 个文件、203 项测试全部通过。
- 完整 `npm test`：50 个文件、204 项通过，1 项失败；唯一失败为隔离基线未包含 5 份 S2 运维文档，`production-ops-baseline-script.test.ts` 因文档缺件失败。该失败在本轮红灯前已存在，且这些文档只存在于原共享脏工作树，未越权带入或修改测试掩盖。
- 使用仓库公开测试占位变量执行 `npm run build`：exit 0；Next.js 15.5.19 编译、类型 / lint、31 个静态页面生成完成。因依赖目录为指向原工作树 `node_modules` 的隔离 junction，standalone traced-files 复制出现一次 Windows `EPERM` symlink 警告，但 build 保持 exit 0；未读取或复制真实密钥。
- 本地 Browser 复验六页桌面 1536×1024 与移动 390×844，共保存 12 张完整截图到线程 visualization 目录；各页 `document.scrollWidth <= viewport clientWidth`。
- 关键交互：登录“密码登录”切换后密码框唯一可见，再切回验证码；家教广场科目切换到“数学”后重置回“全部”；客服点击首个快捷问题后消息数 `1 -> 3` 且回答可见。浏览器 console error / warn 为 0。
- 客服实际 1536px 几何为左 / 右约 `24.0% / 74.3%`、顶底对齐、消息区 `overflow-y:auto`；390px 实际内容宽 375px，无整页横向溢出，chips 精确 2 行且输入区存在。

涉及文件：

- `Code文档/app/layout.tsx`
- `Code文档/app/page.tsx`
- `Code文档/app/login/page.tsx`
- `Code文档/app/rules/page.tsx`
- `Code文档/app/customer-service/page.tsx`
- `Code文档/app/tutor-profiles/page.tsx`
- `Code文档/app/parent-needs/page.tsx`
- `Code文档/app/globals.css`
- `Code文档/tests/sitewide-ui-refresh.test.ts`
- `Code文档/tests/security-access-copy.test.ts`
- `Code文档/tests/chat-layout-css.test.ts`
- `Code文档/tests/customer-service-layout-browser.test.ts`
- `Code文档/tests/customer-service-page-copy.test.ts`
- `Code文档/开发员工作记录.md`

剩余门禁与唯一下一步：

- 唯一非本轮阻塞为基线 S2 运维文档缺件导致完整测试 1 项失败；本轮 UI 代码、针对性测试、其余全量测试、类型、lint、build 和浏览器门禁均已通过。
- 唯一下一步：项目总负责人将新分支提交交给独立 UI / 产品复核；只有生产观察门禁与另行部署授权通过后才安排部署，代码开发员不自行合并、创建 PR、部署或修改 Issue。

### 六页冻结稿像素级返工与全站收口

类型：`SITEWIDE-UI-REFRESH-20260727-001` 同一分支严重返工 / 当前代码一致性复跑 / 禁止生产部署

执行时间：`2026-07-28`

实现与视觉结论：

- 继续使用隔离 worktree `D:\codex_project\家教对接website-ui-refresh-20260727` 和分支 `codex/sitewide-ui-refresh-20260727`，以失败基线 `b2b549888d98ffd68e7ae8c620b0ea90b393d8ae` 返工；原脏工作树未切分支、未清理、未复制未提交文件。
- 六页分别按冻结参考原生尺寸重拍并生成 `reference / actual / 50% overlay / diff / geometry`；家教信息广场和需求广场的冻结 fixture 只用于截图初始态，真实 API 或任一筛选变化立即切换 live 结果层。
- 智能客服 pristine 视觉与真实交互层分离；点击快捷问题或输入内容后进入动态消息层，固定高度消息区、内部滚动、输入和发送行为保留。
- 最终一致性复跑的 MAE：主页 `4.5555`、登录 `4.9851`、规则 `5.2593`、智能客服 `1.2356`、家教信息广场 `0.2551`、需求广场 `0.1993`；六页 overlay 均沿用业务方逐页人工候选通过结论，当前复跑未发现后续 CSS 污染。
- 390×844 六页均为 `innerWidth=clientWidth=scrollWidth=390`、单一 `body > main`，没有整页横向溢出；另补 1536×1024 响应式截图。收口检查发现并修复登录人物中间说明、规则试运行说明在移动端继承白字的问题，均先红后绿，原生桌面差异值未变化。

浏览器交互与网络证据：

- 首页“我要找家教”真实 button 可聚焦且不是 disabled；登录页可切换到密码模式并聚焦邮箱输入；规则返回首页链接可聚焦。
- 客服快捷问题可通过键盘触发并进入 active 动态消息态，输入框可录入；两广场筛选后 URL 写入 `subject=数学` 且结果层切换为 live，详情与发布入口保留真实 href 和焦点命中区。
- 最初 404 的确切 URL 为 `/favicon.ico`，来源为浏览器默认图标请求；Root Metadata 改用既有本地 `brand-mark.png` 后，六页及交互复测不再出现 404。
- 剩余 401 的确切 URL 均为匿名 `GET /api/auth/session`，来源为既有 `useTestSession` 登录态探测（公共导航、登录态内容和首页受保护入口会分别调用）；这是匿名鉴权边界，不是本轮资源缺失或交互失败。最终证据中未出现除该 401 外的 HTTP 4xx/5xx，也未出现 Runtime exception。

测试、构建与隔离门禁：

- 视觉针对性测试：exit 0，7 个文件、19 项全部通过；新增的登录 / 规则移动文字可读性测试均保留红灯后绿灯证据。
- `npm run typecheck`：exit 0。
- `npm run lint`：exit 0，0 warning。
- 隔离 worktree 完整测试首次为 223 通过 / 1 失败；唯一失败是 `production-ops-baseline-script.test.ts` 读取不到 5 份未纳入 Git 基线的 S2 运维文档。按总负责人明确拆分门禁，隔离 worktree 运行其余测试为 222/222，通过；同文件不依赖外部文档的生产测试开关用例为 1/1，通过。
- 原脏工作树只读确认 5 份文档全部存在且均未被 Git 跟踪；在原工作树只读运行 `production-ops-baseline-script.test.ts` 为 2/2 通过。因此该项记录为隔离 worktree 与未入 Git 基线夹具分离，不阻止 UI 提交；没有复制、暂存或放宽文档 / 测试。
- 使用仅存在于单次构建进程的非敏感占位环境变量执行 `npm run build`：exit 0；Next.js 15.5.19 编译、类型 / lint、31 个静态页面生成完成。standalone traced-files 因隔离 worktree 的共享 `node_modules` junction 出现 Windows `EPERM` symlink warning，但构建退出码为 0，未生成待提交构建产物。
- `git diff --check`：exit 0。80 个职责内变更 / 新增文件中，Secret 模式候选为 0，`.next` / `node_modules` / cache / coverage 等待提交产物为 0。
- 项目资产共 60 个，和六张整页冻结参考的 SHA-256 精确复制数为 0；三个 1536×1024 背景文件由脚本删除内容区后重建为无文字、无按钮、无卡片的纯底层画布，其他均为人物 / 图标 / 装饰或局部视觉切片，没有使用整页参考图作为页面背景。
- 原工作树仍在 `codex/vnext-feedback-status-security-20260717`、HEAD `035304e85a7250ec90dd62fdfb08cd5489697ee9`；隔离分支提交前仍以 `b2b5498...` 为 HEAD。生产 CloudBase revision `024` 与 observe 状态未触碰。

证据位置与唯一下一步：

- 最终当前代码证据根目录：`C:\Users\86166\.codex\visualizations\2026\07\17\019f70ec-6331-7083-aecb-bb8484511518\sitewide-pixel-rework\final-current-20260728`；每页目录包含 `native`、`mobile`、`interaction`，主页 / 登录 / 规则另含 `responsive-1536x1024`。
- 唯一下一步：精确暂存本任务源码、资产、脚本、测试和本工作记录，提交并推送 `codex/sitewide-ui-refresh-20260727`；随后等待独立 UI 复核，不合并、不创建 PR、不部署、不修改 Issue。

### 六页视觉跟进阻塞项返工与交互门禁补齐

类型：`SITEWIDE-UI-REFRESH-20260727-001` 同一分支跟进返工 / 交互与无障碍证据 / 禁止生产部署

执行时间：`2026-07-28`

本轮修复：

- 登录页为 code/password 两种真实表单状态补齐明确前景色；1536×1024 和 390×844 实机证据均未出现白字白底。
- 规则页左侧试运行说明恢复可读前景色，返回首页链接通过真实 Tab 路径取得可见焦点，不改变冻结布局。
- 首页所有真实链接/按钮统一增加 D+ 蓝色 `:focus-visible` 环；默认冻结截图不受影响。
- 智能客服不再在快捷问题、输入和发送后切换到另一套框架。冻结 chat frame 与欢迎消息持续存在，动态消息在同一固定高度滚动区内渲染；新消息自动滚底，历史滚动证据为桌面 `clientHeight=481 / scrollHeight=1668`、390 视口 `clientHeight=315 / scrollHeight=2268`。
- 家教信息广场移除 live “当前结果”标签右侧的多余阴影竖线；家教/需求两广场均用公开生产 API 的普通真实数据完成筛选 live 层截图，capture 记录只保留来源、状态码和数量。
- 客服左侧被要求删除的三项 Dify/离线说明继续保持零渲染。

视觉与交互证据：

- 本地 production-mode 六页重新拍摄原生参考尺寸、1536×1024 和 390×844；原生 MAE 保持：首页 `4.5555`、登录 `4.9851`、规则 `5.2593`、客服 `1.2356`、家教信息广场 `0.2551`、需求广场 `0.1993`。
- 原生 `reference / actual / overlay / diff / metrics` 位于 `sitewide-followup-rework\final-production-20260728\comparisons-native`。由于冻结稿没有 390 移动版，响应式目录另提供带明确 `center-cover derived reference` 说明的 overlay/diff/MAE，仅用于追踪，不把变形后的桌面稿冒充移动像素验收基准。
- 首页与规则焦点证据由真实 Tab 遍历产生；客服同一快捷按钮分别用 Enter 与 Space 触发，另覆盖手工输入发送和长历史滚动。
- 六页 1536×1024 与 390×844 共遍历 `124` 个 Tab 焦点：全部正尺寸、`opacity=1`，且具有 outline 或 shadow。所有视口均 `DPR=1`、`innerWidth=clientWidth=scrollWidth`、单一主页面节点。
- production-mode 网络记录只有匿名 `/api/auth/session` 的预期 401；没有意外 404/500、资源缺失、运行时异常或整页横向溢出。
- 静态切片与真实交互覆盖关系、六页路径和交互截图索引已写入 `design-qa.md` 与 `Code文档/docs/sitewide-ui-refresh-follow-up-evidence-20260728.md`。完整冻结页没有被用作页面背景；真实按钮、输入、筛选、消息滚动和焦点层继续为可操作 DOM。

最终工程门禁：

- `npm test -- --run tests/sitewide-followup-rework-contract.test.ts tests/sitewide-ui-refresh.test.ts tests/sitewide-visual-tooling.test.ts tests/customer-service-layout-browser.test.ts tests/chat-layout-css.test.ts`：exit 0，`5` 个文件、`22` 项通过。
- `npm run typecheck`：exit 0。
- `npm run lint`：exit 0，0 warning。
- 隔离 worktree 全量测试先得到 `229/230`；唯一失败仍是隔离基线缺少原工作树未入 Git 的 5 份 S2 运维文档。隔离 worktree 排除该外部夹具后 `228/228` 通过；原工作树只读定向运维测试 `2/2` 通过，未复制文档、未放宽测试。
- `npm run build`：exit 0，Next.js 15.5.19 生成 31 个静态页面；standalone 收集阶段仅出现共享 `node_modules` junction 的既知 Windows symlink `EPERM` warning。
- 原脏工作树保持分支 `codex/vnext-feedback-status-security-20260717`、HEAD `035304e85a7250ec90dd62fdfb08cd5489697ee9`；本轮任务开始与收口检查均为 `110` 条既有状态项，未在原路径新增或修改任务文件。
- 未打开或修改 Cloudflare / CloudBase / DNS / Secret / Issue；未部署。生产继续保持已知 CloudBase revision `024` 与 `ORIGIN_VERIFY_MODE=observe`。

门禁状态与唯一下一步：

- 开发侧返工证据已形成，但不代替独立 UI 复核、合并授权、生产部署或业务方验收。
- 唯一下一步：精确暂存本轮 7 个职责内文件，提交并推送现有 `codex/sitewide-ui-refresh-20260727`，随后进入独立 UI 复核等待态。

### 独立 UI 复核双 SERIOUS 聚焦返工

类型：`SITEWIDE-UI-REFRESH-20260727-001` 同一分支聚焦返工 / 禁止生产部署

执行时间：`2026-07-28 07:23:54 +08:00`

失败复现与根因：

- 独立 UI 对 `97ad27b3ff3268a0d25603db2e7f3ff3eae8fca9` 的复核仅保留两个
  SERIOUS：客服 21 条长历史滚动时出现浅绿色孔洞 / 气泡重叠，以及家教信息广场
  desktop live 结果的“老师资料”标签收缩。
- 客服根因是 active 状态仍使用带透明欢迎区的整框静态皮肤，同时真实欢迎消息被压成
  `height: 0 / opacity: 0`，滚动后透明区暴露页面渐变，并与真实滚动消息形成两套坐标系。
- 家教广场根因是 live 卡片中的 `.listing-card-meta` 仍可参与 flex 收缩，缺少 live
  状态下的固定最小宽度、前景色和不换行约束。
- 定向测试先红：真实 Chrome 长历史测试证明欢迎消息不占流空间；CSS contract
  证明 live 标签缺少 `min-width: 106px` 与 `white-space: nowrap`。

聚焦修复：

- 客服 pristine 冻结视觉继续使用原静态 frame，默认 1536×1024 与 390×844 截图
  和上一批准证据 SHA-256 完全一致。
- active 状态改为真实白色有边框聊天容器；欢迎消息成为消息列表中的真实
  `101px` flow item，自身承载已批准欢迎视觉，不再依赖绝对定位伪元素或透明孔洞。
  真实动态消息、输入、发送、自动滚底和历史内部滚动保持原组件行为。
- 家教广场仅为 desktop live 结果的“老师资料”标签补齐 D+ 胶囊外观、
  `flex: 0 0 106px`、`min-width: 106px` 与 `white-space: nowrap`；fixture
  初始态和移动布局不改。
- 视觉证据脚本改为先打开 `about:blank`，在首个业务页面请求前安装 public-data
  override，再导航；新增欢迎消息 flow / overlap 和老师标签几何字段，消除测试工具
  首次加载产生的本地 API 500 噪声。

定向浏览器证据：

- 证据根目录：
  `C:\Users\86166\.codex\visualizations\2026\07\17\019f70ec-6331-7083-aecb-bb8484511518\sitewide-followup-rework\serious-01-02-fix-20260728`。
- 客服 desktop 21 条消息：`clientHeight=473`、`scrollHeight=1637`、
  `scrollTop=160`；欢迎消息高度 `101px`、真实占据 flow、伪元素为 `none`、
  相邻消息 overlap `0`，浅绿色孔洞与覆盖均消失。
- 客服 390×844：`clientHeight=315`、`scrollHeight=2268`、
  `scrollTop=160`；欢迎消息 flow 高度 `95.0625px`、overlap `0`，
  `scrollWidth=390`。
- 家教广场 desktop public-real-data：公开 API HTTP `200`、1 条真实结果、
  保留真实不透明详情 ID；“老师资料”完整显示，宽 `106px`、不换行。
- 家教广场 390×844：公开 API HTTP `200`、“老师资料”完整显示，
  页面 `innerWidth=clientWidth=scrollWidth=390`。
- 新增 22 组全站回归：六页 desktop / mobile 默认态、首页焦点、登录密码态、
  规则焦点、客服 Enter / Space / 手工发送、需求广场 public-real-data live
  筛选全部为单一主节点、横向溢出 `0`；除匿名 `/api/auth/session` 预期 `401`
  外无 4xx/5xx 或 runtime exception。

工程门禁：

- 定向红绿测试：
  `npm test -- --run tests/customer-service-layout-browser.test.ts tests/sitewide-followup-rework-contract.test.ts`
  为 `2` 文件、`9` 项通过。
- `npm run typecheck`：exit `0`。
- `npm run lint`：exit `0`，0 warning。
- 完整 `npm test` 为 `230/231`；唯一失败仍是隔离 worktree 缺少原脏工作树中
  5 份未入 Git 的 S2 运维文档。隔离 worktree 排除该外部夹具后 `229/229`
  通过；原工作树只读定向运维测试 `2/2` 通过。没有复制文档或放宽测试。
- 首次无占位环境变量 build 按预期 fail-fast 于缺少 `CLOUDBASE_ENV_ID`；
  随后使用 Dockerfile 已公开、仅存在于单次构建进程的非敏感 build 占位变量执行
  `npm run build`：exit `0`，生成 31 个静态页面。standalone 阶段仅保留共享
  `node_modules` junction 的既知 Windows `EPERM` symlink warning。
- `git diff --check`：exit `0`。未加入 `.next`、截图、日志、缓存、依赖目录或
  Secret；没有修改 Cloudflare、CloudBase、DNS、生产 revision、Issue 或其他角色文件。
- 原脏工作树仅执行获授权的运维基线只读测试，未切换分支、未清理、未复制或暂存
  其任何文件；生产未部署。

门禁状态与唯一下一步：

- 两个 SERIOUS 的开发侧可见缺陷和自动化反馈环已修复，但不自行宣称独立 UI 或
  用户验收通过。
- 唯一下一步：精确暂存本轮源码、测试、证据索引与本工作记录，提交并推送同一
  `codex/sitewide-ui-refresh-20260727`；随后等待独立 UI 对两张失败状态重新复核。

### 两广场筛选框浅色外缘单点像素返工

类型：`SITEWIDE-UI-REFRESH-20260727-001` 同一分支单点像素返工 / 禁止生产部署

执行时间：`2026-07-28 09:24:35 +08:00`

失败复现与根因：

- 基线保持 `cd086f3a0d60e24b836906de72989f843afa705c`，分支保持
  `codex/sitewide-ui-refresh-20260727`，仅在隔离 worktree 执行。
- 新增 `tests/sitewide-filter-asset-edge.test.ts`。第一阶段红灯证明两个
  415×882 静态筛选切片在圆角黑框之外仍为全不透明，外缘透明像素数均为 `0`，
  与要求的家教 `403` / 需求 `390` 不符。
- 仅清除切片黑框之外像素的 alpha 后，复拍又证明两页 1536×1024 背景资产在
  `x=137..559, y=83..972` 保留同一浅色面板底层，比筛选切片四周多 4px。
  该截图坐标位于切片之外，排除了 CSS / 切片为完整根因；因此按“证据证明才允许”
  的例外，仅重建两个背景 PNG 中会在黑框外露出的像素。
- 未修改 CSS、TSX、组件、布局、几何、文案、表单、结果卡或其他页面资产。

最小像素修复：

- `tutor-profiles-static-filter.png`：403 个黑框外像素 alpha 改为 0，全部 RGB
  字节保持原值，RGB SHA-256 为
  `587746b19ba9e5ad8b066325ebeb83559b4c04e2de7509ce54f49bc252d1c807`。
- `parent-needs-static-filter.png`：390 个黑框外像素 alpha 改为 0，全部 RGB
  字节保持原值，RGB SHA-256 为
  `3b4a3f02cb76c5491dfe8384a77c4885cea0395310451dc9eca876ee93255857`。
- 两个背景资产只修改“外侧 4px 环 + 切片透明圆角”对应的批准掩码：
  家教 10,843 像素、需求 10,830 像素。背景其他像素哈希由测试锁定。
- 与 `cd086f3a` 桌面截图逐像素比较：家教实际变化 10,837 像素、需求实际变化
  10,818 像素；两页变化边界均严格为 `x=137..559, y=83..972`，批准掩码外
  差异像素均为 `0`。因此黑色主边框和框内内容逐像素不变。

浏览器证据：

- 证据根目录：
  `C:\Users\86166\.codex\visualizations\2026\07\17\019f70ec-6331-7083-aecb-bb8484511518\sitewide-followup-rework\filter-edge-pixel-fix-20260728`。
- 最终桌面图：
  `final-captures\tutor-default-1536x1024\tutor-profiles-1536x1024-viewport.png`
  与
  `final-captures\parent-default-1536x1024\parent-needs-1536x1024-viewport.png`。
  `*-filter-before/after/diff-*` 提供局部原图、10× 放大与差异图；
  `pixel-diff-metrics.json` 提供机器可读边界。
- 两页 1536×1024 / 390×844 fixture 与 public-real-data 筛选均为单一主节点、
  DPR 1、横向溢出 0；公开 API HTTP 200，真实详情 ID 保留。
- 独立 Reset 浏览器证据记录两页先应用 `数学`，再重置为 query 为空、select
  为空、`live` 结果状态，公开数据 HTTP 200，横向溢出 0。
- 首页、登录、规则、客服四页重新截图与 `cd086f3a` 证据 SHA-256 完全一致。
- 除匿名 `GET /api/auth/session` 的预期 401 外，无其他 4xx/5xx 或 console
  error；本轮没有资源 404。

工程门禁：

- 红灯：定向资产测试 4 项中 2 项失败，最大通道突变实测 `14`。
- 绿灯：同一命令 1 文件 / 4 项通过；`npm run typecheck` exit 0；
  `npm run lint` exit 0、0 warning。
- 完整 `npm test` 为 `234/235`；唯一失败是隔离 worktree 缺少原工作树未入 Git
  的 5 份 S2 运维文档。隔离 worktree 排除该单项后 59 文件 / 233 项通过；
  原工作树只读运行该单项 2/2 通过。未复制文档、未放宽测试。
- 使用 Dockerfile 已公开且仅存在于单次进程的非敏感占位变量执行
  `npm run build`：exit 0，Next.js 15.5.19 生成 31 个静态页面。standalone
  阶段仅有共享 `node_modules` junction 的既知 Windows `EPERM` warning。
- 原工作树仅执行获授权的只读运维基线测试；未切分支、清理、复制、暂存或修改其
  任务文件。未修改生产、Cloudflare、CloudBase、DNS、Secret 或 Issue。

门禁状态与唯一下一步：

- 开发侧最小像素修复和回归证据已完成，但不代替用户验收或独立 UI 复核。
- 唯一下一步：精确暂存上述 4 个 PNG、1 个资产契约测试、证据索引和本工作记录，
  提交并推送同一分支；禁止部署。
