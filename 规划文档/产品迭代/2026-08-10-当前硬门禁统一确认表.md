# 当前硬门禁统一确认表（2026-08-10）

> 文档状态：`HARD_GATE_SHEET_READY / NON_CANONICAL`。唯一作者：产品经理 v2.3.0 / 019fad1b-0006-74f3-9b38-ae71e6464ad4。
> 本表把既有来源压缩为可逐行确认的产品输入，不改写来源、不替代控制台/账号/采购/人工 owner 证据，
> 不构成密钥取得、供应商采购、生产写入、部署或 Issue 关闭授权。

## 1. 来源与当前状态

| 来源 | 当前状态 | 只读核对 SHA-256 |
| --- | --- | --- |
| ISSUE-0020 临时 Cloudflare Worker 反代与安全基线加固 | `open / EXTERNAL_BLOCKED`；仅剩 4 项关闭门禁 | `77266823DABFB74EBF85D9A8CC90E45925238EF33C7763BB0AFD9673BE6F6E0B` |
| D4/D6/D7 决策冻结表 | D4=`PARTIAL_FREEZE / EXTERNAL_INPUT_PENDING`；D6/D7 技术默认冻结 | `86FC7F6A2009947ED1EC577830B1351224C6E03DD70EADFC6C7F84D76954FCEB` |
| ISSUE-0036 业务门禁决策包 | 14 门待逐项业务确认；人工审核运营 owner 未登记 | `AFFA051D5A78D91DE09D23276DB4452AAB874575907883F5257C189516305C73` |
| ISSUE-0036 已审查 Spec / Hermes Round 3/3 | Spec `005EA5F2490DC2E43A134BA0421EFBD357179C90E29A6F2AB560F6F61A97B437`；报告 `B3749F4C713C743FCF2510B1F7BE0F917B92EE4265D916947FBA8AFB178AE470`；`0 SERIOUS`，禁止第四轮 | 只读溯源，不改变门禁状态 |

确认词只使用：`接受推荐`、`改为<明确值>`、`暂不确认`。涉及账号、控制台、采购、供应商、人工 owner 的行，用户回复只能表达产品决定，不能替代外部证据；严禁贴出任何 Secret、Token、密码或联系方式正文。

## 2. ISSUE-0020：剩余 4 项

| ID | 推荐安全默认（可先做） | 为何必须确认 / 外部证据 | 确认后授权边界 | 未确认时 fail-closed |
| --- | --- | --- | --- | --- |
| 0020-A1 凭据轮换 | 立即轮换此前暴露的生产凭据；只归档轮换时间、对象引用、旧凭据失效证明，不留明文。 | 必须由项目总负责人/凭据持有方给出脱敏轮换证据，证明旧值不可用。 | 仅允许继续 ISSUE-0020 技术复核和回滚准备；不授权泄露凭据再次使用。 | 保持 `EXTERNAL_BLOCKED`，不得把“计划轮换”当通过。 |
| 0020-A2 专用登录回归 | 使用专用、非敏感、可清理的测试账号；禁止真实未成年人、联系方式或投诉隐私数据。 | 需要账号实际登录并成功提交一次 feedback 的可归档结果，证明源站隔离不破坏业务写入。 | 仅授权该账号在限定窗口做一次合成回归与清理；不授权生产业务数据写入。 | 未取得成功回归前，保持登录态/业务门阻塞。 |
| 0020-A3 回滚入口 | 保留 CloudBase 先恢复 observe/off、确认直访恢复，再回滚 Worker 的顺序；先做只读现场确认。 | 需要 Cloudflare/CloudBase 配置执行侧现场证明当前回滚入口真实可用。 | 仅授权按既定顺序执行受控回滚演练/窗口；不得倒置。 | 无现场证据即不执行切换或回滚，维持当前保护状态。 |
| 0020-A4 残余风险接受 | 推荐按当前实际状态逐项接受：单一 CloudBase 上游、监控覆盖缺口、SSL Full 非 strict、Free 计划能力边界等。 | 这是业务方风险偏好与责任确认，技术证据不能代替。 | 仅在 A1–A3 技术证据齐备后，允许业务方决定是否接受剩余风险并路由 Issue 管理员复核。 | 业务沉默或拒绝时保持 `open / EXTERNAL_BLOCKED`，不关闭 Issue。 |

## 3. D4 / ISSUE-0031：必须先补外部事实再决定

| ID | 推荐安全默认（可先做） | 为何必须确认 / 外部证据 | 确认后授权边界 | 未确认时 fail-closed |
| --- | --- | --- | --- | --- |
| D4-1 资源/地域/候选 | 仅把现有环境内 CloudBase Serverless MySQL 作为候选；canonical 环境 `ungradu-edu-prod-d3efys1f5970e3f`；provider-reported 精确 region 必须与生产环境一致。 | region、现有资源状态、备份能力和适用地域只能由控制台/provider 只读证据证明，不能从域名或套餐猜测。 | 证据与业务确认齐备后，才可进入 0031 选型矩阵、迁移演练和实施评审；仍不默认采购独立腾讯云 MySQL。 | region/资源状态未证实：`EXTERNAL_INPUT_PENDING`，不实现、不采购、不迁移、不生产写入。 |
| D4-2 增量预算 | 推荐上限 `20 CNY/月`；这是待批准上限，不是已批准支出。 | 控制台必须显示实际按量价格/计费说明，业务方须明确是否承担该费用。 | 仅在价格证据和带日期批准后，允许按批准范围评估/开通；不扩展预算或地域。 | 无价格或未批准：禁止采购/开通，保持当前文档存储单源。 |
| D4-3 可靠性与切换目标 | `RPO≤15m`、`RTO≤60m`、短写目标 `≤5m`/硬上限 `15m`、备份 `30d`、对账 `100%`、数据丢失 `0`、观察 `72h`；经演练短写冻结+单源切换，不双写。 | 这些目标影响停机、成本、迁移风险和业务连续性，必须业务方带日期确认并由演练证据证明。 | 仅授权完成合成迁移、备份恢复、对账、回滚演练；生产切换须另有批准窗口和 receipt。 | 任一目标/演练失败：停止切换，保留现有单源并回滚，不用双写“临时通过”。 |

## 4. ISSUE-0036：14 项业务门禁逐行确认

以下组合为最保守整体推荐：窄字段、确定性检测优先、未知即人工、AI 仅辅助路由且默认不出域、首期不自动拒绝、OCR 关闭、零公开漏放、无人工 owner 不进生产。每行仍须按业务门确认，不能把推荐误报为供应商/生产接受。

| ID | 推荐安全默认（可先做） | 为何必须确认 / 外部证据 | 确认后授权边界 | 未确认时 fail-closed |
| --- | --- | --- | --- | --- |
| 0036-01 公开字段 | 只审核现有 `childIntro`、`abilityDescription`；未盘点字段不进入公开审核。 | 字段清单决定儿童数据出域与公开范围，生产页面/API 映射需证据。 | 仅授权已确认字段进入实现与合成验收；扩展字段须新确认。 | 未列字段保持 `pending/manual`，不公开新版本。 |
| 0036-02 类别/国家/语言 | 确定性覆盖邮箱、手机、座机、微信、QQ、URL、长数字、中文数字及混淆；未知国家/语言转人工。 | 误杀/漏放和本地化责任由业务决定，生产白名单与文案需确认。 | 仅授权确认范围内的规则和 golden cases。 | 不确定类别不自动 allow/reject，保持 `manual/pending`。 |
| 0036-03 自动拒绝动作 | 默认高置信命中也先不公开并转人工；AI 不得单独拒绝/发布。 | 自动拒绝会改变用户内容、申诉和合规责任，阈值/reasonCode 必须业务确认。 | 仅在明确确认动作、阈值、申诉文案及观察方案后，才可评估受限生产开关。 | 不自动拒绝、不自动放行，异常进入人工。 |
| 0036-04 AI 角色 | provider-neutral stub；若启用仅结构化分类/路由辅助，不作唯一证据，默认不外发。 | 模型、地域、DPA、训练用途和预算涉及隐私/供应商责任。 | 仅在外部输入齐备并通过集成/安全验证后授权最小字段调用。 | 不调用生产 AI；超时、注入、schema/预算异常均 `pending/manual`。 |
| 0036-05 SLO/误杀漏放 | 提交 p95≤1s；确定性≤200ms；自动路径≤10s、硬15s；入队 p95≤5m、裁决≤30m、99%≤2h；公开漏放0，黄金集误杀建议≤2%。 | 阈值影响成本、人工容量和用户承诺，需业务带日期确认并用生产观察验证。 | 仅授权本地/集成测量和经确认阈值的阶段性验证。 | 超时/容量/阈值失败保持不公开并告警。 |
| 0036-06 人工工作时间 | 未声明工作时段按无人值守处理，不自动发布；先用虚拟时钟/合成队列。 | 业务必须确认工作日、时区、节假日、值班和升级 owner。 | 仅在排班/升级链登记后授权生产人工队列。 | 无值班覆盖则 `pending/manual` 并告警。 |
| 0036-07 申诉 SLA | 入队 p95≤30m、裁决 p95≤4h、99%≤1 工作日；超时保持 `appeal_pending/needs_manual_review`。 | 工作日定义、人工容量和文案是业务承诺，不可由测试推定。 | 仅授权确认后的申诉观察和告警。 | 超时不公开、不转 published、不绕过审核。 |
| 0036-08 vendor/model/region/DPA/budget | 供应商中立、默认零外部调用；请求/响应不留存、不训练、不广告画像，设硬预算熔断。 | 供应商、模型、地域、DPA、生产 secret owner 和预算必须由业务/采购/控制台证据确认。 | 仅在采购/DPA/密钥托管/地域和生产 key 均证实后授权最小字段调用。 | 不采购、不出域、不写生产 secret，保持本地/人工路径。 |
| 0036-09 原文/审计保留 | 供应商原始请求/响应保留0；审核副本≤24h；只保留 hash、类别、版本、耗时、错误和 actor；审计≤180d或更短必要期。 | 保留期涉及隐私、法律和删除 receipt，需业务/合规与生产证据确认。 | 仅授权 TTL、脱敏、删除和访问审计实现。 | 删除/审计失败即 fail-closed，不延长无限期保留。 |
| 0036-10 未成年人/OCR | 仅处理必要文本；OCR 首期关闭，不采集/上传图片文字。 | OCR 例外会扩大敏感数据出域和供应商责任。 | 仅在业务/隐私确认供应商、地域、DPA、告知和保留后评估。 | 保持 OCR off，误入图片审核则不外发并转人工。 |
| 0036-11 旧 published 快照 | 旧 published 保持可见；新编辑版本先 `pending_review`；删除恢复走 `deleted→pending_review`。 | 可见性和风险停牌是业务体验/安全决定，生产文案需确认。 | 仅授权版本并发、旧快照、恢复重审的本地/集成实现。 | 新版不公开、不替换旧快照；stale/失败告警。 |
| 0036-12 人工运营 owner | 不虚构人员；无已登记实名/角色、备援、最小权限、值班、访问审批和升级链则不进生产。 | 这是儿童/联系方式数据的实际访问责任，必须真实人工 owner 提供证据。 | 仅在 owner 及备援、权限和 SLA 归档后授权生产人工审核。 | 队列保持阻塞或 `pending/manual`，不能自动发布。 |
| 0036-13 二审规则 | 联系方式高风险命中、模型/规则冲突、申诉和未成年人高风险样本由独立二审；同一 actor 不自批。 | 触发阈值、权限、替补和审计由业务决定。 | 仅授权角色分离、双签和负测后的受限生产观察。 | 无独立二审则不公开、不自动降级为单人通过。 |
| 0036-14 申诉权限 | 仅原 owner；`reasonCode` 必填；24h最多1次；同 hash 维持原判第3次锁7d；服务端幂等/防绕过。 | owner/管理员例外、文案、身份和观察需业务确认。 | 仅授权确认角色范围内的申诉状态机与审计。 | 非 owner、删除态、stale、冲突 key、锁定期请求拒绝或幂等且不入队。 |

## 5. 当前可确认项与必须外部取证项

### 用户现在可逐行确认（不替代证据）

- 0020-A4 的残余风险是否接受；A1–A3 可确认执行方向，但不能代替实际证据。
- D4-2 的 `≤20 CNY/月` 是否作为预算上限；D4-3 的 RPO/RTO、短写、备份、对账和观察目标。
- ISSUE-0036 的 01–07、09–11、13–14 产品默认；推荐整体组合即“接受推荐”，任何放宽需明确写值。
- ISSUE-0036-03 是否继续首期不自动拒绝、0036-04 是否坚持 AI 仅辅助、0036-10 是否保持 OCR 关闭。

### 必须由外部角色/系统提供的证据

- 0020-A1：凭据持有方的脱敏轮换与旧值失效证明；A2：专用账号真实登录和 feedback 成功；A3：Cloudflare/CloudBase 回滚入口现场确认。
- D4-1：控制台/provider-reported 精确 region、现有资源、备份和适用地域；D4-2：控制台价格/计费说明。没有这些事实，用户不能用“接受推荐”替代。
- D6/D7 相关外部输入若进入 0032/0034：widget/secret/provider/region、告警/备份/恢复演练、控制差距和生产观察。
- 0036-08：采购/供应商、模型、region、DPA、训练禁用、预算、生产 key 的安全托管；0036-12/13：真实人工审核 owner、备援、权限、值班、二审和升级链。

## 6. 确认顺序与唯一实施顺序

1. 先取得 0020-A1/A2/A3、D4-1/D4-2 的外部只读/账号证据；任何缺失继续 `EXTERNAL_BLOCKED` 或 `EXTERNAL_INPUT_PENDING`。
2. 业务方逐行确认 0020-A4、D4-2/D4-3 与 0036 14 门；业务沉默保持 `USER_CONFIRMATION_PENDING / ACCEPTANCE_PENDING`。
3. 仅在 0036 14 门和人工 owner/外部供应商门满足后，由登记代码开发员先做本地/集成安全默认实现，随后独立复核、预生产和生产验收。
4. 0036 业务闭环完成后，按既有依赖实施 `0031 → 0034 → 0032`；D4 未解除不得启动 0031，D7/D6 外部门未解除不得进入对应生产窗口。任何阶段不得并行产生两个有副作用写入窗口。
5. 每阶段分别取得 TECH_VERIFIED、独立验收、生产证据和 BUSINESS_ACCEPTED；不得把用户回复、测试通过或技术默认当作生产/Issue 关闭。

## 7. 最短回复格式

用户可直接回复一行（未列项保持未确认）：

`0020-A1=接受推荐; A2=接受推荐; A3=接受推荐; A4=接受推荐; D4-1=接受推荐; D4-2=接受推荐(上限20 CNY/月); D4-3=接受推荐; 0036-01..14=接受推荐`

如需修改，把对应项替换为 `改为<明确值>`；不要粘贴 Secret、Token、密码、真实联系方式或人工账号。`接受推荐` 只冻结产品方向，外部证据项仍须由指定责任角色补齐。

## 8. 未通过门禁与本轮边界

当前仍未通过：ISSUE-0020 四项、D4 region/资源/价格与实际开通、D6 widget/secret/provider 外部输入、D7 控制差距/演练/业务签收、0036 14 门与真实人工审核运营 owner。ISSUE-0020 继续 `open / EXTERNAL_BLOCKED`；0031/0032/0034/0036 不因本表获得实现或生产授权。

本轮只允许新建本表并追加产品经理工作记录；未修改任何 Spec、Issue、Hermes、metadata、Document QA ledger、代码、UI、总负责人文件或配置；未运行 npm、Git mutation、Hermes、部署、Cloudflare/CloudBase 操作或生产写入。

**唯一下一步：** 总负责人将本表按最短格式路由给业务方；同时由对应责任角色补齐 A1–A3、D4-1/D4-2 及 0036-08/12 的外部证据，未齐备前保持 fail-closed。
